//! Auto-generated by `datalink_shim_duckdb_dynlink_emit::emit_dynlink`
//! (Phase A, opaque-blob scalar dispatch). Do NOT edit by hand — regenerate.
#![allow(unused_imports, dead_code)]
#![allow(unsafe_op_in_unsafe_fn)]

use std::sync::atomic::AtomicU32;

mod bindings {
    wit_bindgen::generate!({
        path: "wit",
        world: "bridge",
        generate_all,
    });
}

use bindings::duckdb::extension::types::{
    Capabilitykind, Duckerror, Duckvalue, Funcflags, Invokeinfo, Logicaltype, Resultset,
};
use bindings::duckdb::extension::runtime;
use bindings::duckdb::extension::runtime_ext;
use bindings::duckdb::extension::catalog;
use bindings::exports::duckdb::extension::guest::{self as guest_export, Guest as GuestGuest, Loadresult};
use bindings::exports::duckdb::extension::callback_dispatch::{
    self as cb_export, Guest as CallbackGuest,
};

use bindings::compose::dynlink::linker;

const PROVIDER_ID: &str = "postgis-composed";
const EXTENSION_ROOT: &str = "postgis";
const CATALOG_EXTENSION: &str = "postgis";
const CATALOG_VERSION: &str = "0.1.0";

fn resolve() -> Result<linker::Instance, Duckerror> {
    linker::resolve_by_id(&PROVIDER_ID.to_string())
        .map_err(|e| Duckerror::Internal(format!("dynlink resolve('{}'): {:?}", PROVIDER_ID, e)))
}

// -----------------------------------------------------------
// CBOR envelope (mirrors provider crate's Request/Response).
// -----------------------------------------------------------

#[derive(Debug, Clone)]
enum CborValue {
    Null,
    Bool(bool),
    Int(i64),
    Uint(u64),
    Float(f64),
    Text(String),
    Bytes(Vec<u8>),
    List(Vec<CborValue>),
}

impl serde::Serialize for CborValue {
    fn serialize<S: serde::Serializer>(&self, s: S) -> Result<S::Ok, S::Error> {
        match self {
            CborValue::Null => s.serialize_unit(),
            CborValue::Bool(b) => s.serialize_bool(*b),
            CborValue::Int(i) => s.serialize_i64(*i),
            CborValue::Uint(u) => s.serialize_u64(*u),
            CborValue::Float(f) => s.serialize_f64(*f),
            CborValue::Text(t) => s.serialize_str(t),
            CborValue::Bytes(b) => s.serialize_bytes(b),
            CborValue::List(items) => {
                use serde::ser::SerializeSeq;
                let mut seq = s.serialize_seq(Some(items.len()))?;
                for item in items {
                    seq.serialize_element(item)?;
                }
                seq.end()
            }
        }
    }
}

#[derive(Debug, Clone, serde::Serialize)]
struct Request {
    #[serde(rename = "v")]
    version: u32,
    args: Vec<CborValue>,
}

#[derive(Debug, Clone, serde::Deserialize)]
struct Response {
    #[serde(default)]
    ok: Option<ResponseValue>,
    #[serde(default)]
    err: Option<String>,
}

#[derive(Debug, Clone)]
enum ResponseValue {
    Null,
    Bool(bool),
    Int(i64),
    Uint(u64),
    Float(f64),
    Text(String),
    Bytes(Vec<u8>),
    List(Vec<ResponseValue>),
}

impl<'de> serde::Deserialize<'de> for ResponseValue {
    fn deserialize<D: serde::Deserializer<'de>>(d: D) -> Result<Self, D::Error> {
        use serde::de::{Error, MapAccess, SeqAccess, Visitor};
        struct V;
        impl<'de> Visitor<'de> for V {
            type Value = ResponseValue;
            fn expecting(&self, f: &mut core::fmt::Formatter) -> core::fmt::Result {
                f.write_str("a CBOR value")
            }
            fn visit_unit<E: Error>(self) -> Result<ResponseValue, E> { Ok(ResponseValue::Null) }
            fn visit_none<E: Error>(self) -> Result<ResponseValue, E> { Ok(ResponseValue::Null) }
            fn visit_bool<E: Error>(self, v: bool) -> Result<ResponseValue, E> { Ok(ResponseValue::Bool(v)) }
            fn visit_i64<E: Error>(self, v: i64) -> Result<ResponseValue, E> { Ok(ResponseValue::Int(v)) }
            fn visit_u64<E: Error>(self, v: u64) -> Result<ResponseValue, E> {
                if v <= i64::MAX as u64 { Ok(ResponseValue::Int(v as i64)) }
                else { Ok(ResponseValue::Uint(v)) }
            }
            fn visit_f64<E: Error>(self, v: f64) -> Result<ResponseValue, E> { Ok(ResponseValue::Float(v)) }
            fn visit_str<E: Error>(self, v: &str) -> Result<ResponseValue, E> { Ok(ResponseValue::Text(v.to_string())) }
            fn visit_string<E: Error>(self, v: String) -> Result<ResponseValue, E> { Ok(ResponseValue::Text(v)) }
            fn visit_bytes<E: Error>(self, v: &[u8]) -> Result<ResponseValue, E> { Ok(ResponseValue::Bytes(v.to_vec())) }
            fn visit_byte_buf<E: Error>(self, v: Vec<u8>) -> Result<ResponseValue, E> { Ok(ResponseValue::Bytes(v)) }
            fn visit_seq<A: SeqAccess<'de>>(self, mut s: A) -> Result<ResponseValue, A::Error> {
                let mut items = Vec::new();
                while let Some(v) = s.next_element::<ResponseValue>()? { items.push(v); }
                Ok(ResponseValue::List(items))
            }
            fn visit_map<A: MapAccess<'de>>(self, mut m: A) -> Result<ResponseValue, A::Error> {
                let k: Option<String> = m.next_key()?;
                let k = k.ok_or_else(|| A::Error::custom("empty map"))?;
                let v = match k.as_str() {
                    "Null" => { let _: serde::de::IgnoredAny = m.next_value()?; ResponseValue::Null }
                    "Bool" => ResponseValue::Bool(m.next_value()?),
                    "Int"  => ResponseValue::Int(m.next_value()?),
                    "Uint" => ResponseValue::Uint(m.next_value()?),
                    "Float" => ResponseValue::Float(m.next_value()?),
                    "Text" => ResponseValue::Text(m.next_value()?),
                    "Bytes" => {
                        let b: serde_bytes::ByteBuf = m.next_value()?;
                        ResponseValue::Bytes(b.into_vec())
                    }
                    "List" => ResponseValue::List(m.next_value()?),
                    other => return Err(A::Error::custom(format!("unknown tag: {}", other))),
                };
                Ok(v)
            }
        }
        d.deserialize_any(V)
    }
}

fn encode_request(args: Vec<CborValue>) -> Result<Vec<u8>, Duckerror> {
    let mut out = Vec::new();
    ciborium::into_writer(&Request { version: 1, args }, &mut out)
        .map_err(|e| Duckerror::Internal(format!("cbor encode: {}", e)))?;
    Ok(out)
}

fn decode_response(bytes: &[u8]) -> Result<Response, Duckerror> {
    ciborium::from_reader(bytes)
        .map_err(|e| Duckerror::Internal(format!("cbor decode: {}", e)))
}

fn call(method: &str, args: Vec<CborValue>) -> Result<ResponseValue, Duckerror> {
    let inst = resolve()?;
    let payload = encode_request(args)?;
    let bytes = inst
        .invoke(&method.to_string(), &payload)
        .map_err(|e| Duckerror::Internal(format!("{}: invoke: {:?}", method, e)))?;
    let resp = decode_response(&bytes)?;
    if let Some(err) = resp.err {
        return Err(Duckerror::Internal(format!("{}: {}", method, err)));
    }
    Ok(resp.ok.unwrap_or(ResponseValue::Null))
}

// -----------------------------------------------------------
// duckvalue / ResponseValue marshalling.
// -----------------------------------------------------------

fn duckv_to_cbor(v: &Duckvalue) -> CborValue {
    // Arm names mirror the WIT-bindgen output for the
    // `duckdb:extension@4.0.0/types/duckvalue` variant. See
    // ~/git/ducklink/wit/duckdb-extension/types.wit:97-120.
    match v {
        Duckvalue::Null => CborValue::Null,
        Duckvalue::Boolean(b) => CborValue::Bool(*b),
        Duckvalue::Int8(i) => CborValue::Int(*i as i64),
        Duckvalue::Int16(i) => CborValue::Int(*i as i64),
        Duckvalue::Int32(i) => CborValue::Int(*i as i64),
        Duckvalue::Int64(i) => CborValue::Int(*i),
        Duckvalue::Uint8(u) => CborValue::Int(*u as i64),
        Duckvalue::Uint16(u) => CborValue::Int(*u as i64),
        Duckvalue::Uint32(u) => CborValue::Int(*u as i64),
        Duckvalue::Uint64(u) => CborValue::Uint(*u),
        Duckvalue::Float32(f) => CborValue::Float(*f as f64),
        Duckvalue::Float64(f) => CborValue::Float(*f),
        Duckvalue::Text(s) => CborValue::Text(s.clone()),
        Duckvalue::Blob(b) => CborValue::Bytes(b.clone()),
        // Date / Time / Timestamp / Timestamptz ferry as int64
        // instants (canonical DuckDB storage).
        Duckvalue::Timestamp(i) => CborValue::Int(*i),
        Duckvalue::Timestamptz(i) => CborValue::Int(*i),
        Duckvalue::Date(i) => CborValue::Int(*i as i64),
        Duckvalue::Time(i) => CborValue::Int(*i),
        // Structured arms (Decimal / Interval / Uuid / Complex)
        // ferry as null at Phase A. The provider owns their
        // encoding when Phase B threads structured logical types.
        Duckvalue::Decimal(_)
        | Duckvalue::Interval(_)
        | Duckvalue::Uuid(_)
        | Duckvalue::Complex(_) => CborValue::Null,
    }
}

fn response_to_duckv(v: ResponseValue) -> Duckvalue {
    match v {
        ResponseValue::Null => Duckvalue::Null,
        ResponseValue::Bool(b) => Duckvalue::Boolean(b),
        ResponseValue::Int(i) => Duckvalue::Int64(i),
        ResponseValue::Uint(u) => Duckvalue::Uint64(u),
        ResponseValue::Float(f) => Duckvalue::Float64(f),
        ResponseValue::Text(t) => Duckvalue::Text(t),
        ResponseValue::Bytes(b) => Duckvalue::Blob(b),
        ResponseValue::List(_) => Duckvalue::Null,
    }
}

// -----------------------------------------------------------
// Columnar hot-path helpers (call_scalar_batch_col).
//
// Simplified vs. the fully-typed postgis-ducklink-bridge
// reference: since dynlink Phase A dispatch is opaque-blob,
// we always lift/lower through Duckvalue::Blob columns. That
// keeps the per-row hot path scalar-Blob-only; multi-shape
// dispatch is a follow-up when the catalog carries logical
// types.
// -----------------------------------------------------------

use bindings::duckdb::extension::column_types;

fn cv_is_valid(validity: &[u8], i: usize) -> bool {
    if validity.is_empty() { return true; }
    let byte = i / 8;
    let bit = i % 8;
    (validity.get(byte).copied().unwrap_or(0) >> bit) & 1 == 1
}

fn colvec_get(cv: &column_types::Colvec, i: usize) -> Duckvalue {
    if !cv_is_valid(cv.validity.as_slice(), i) {
        return Duckvalue::Null;
    }
    match &cv.data {
        column_types::Column::Boolean(xs) => xs.get(i).copied().map(Duckvalue::Boolean).unwrap_or(Duckvalue::Null),
        column_types::Column::Int64(xs) => xs.get(i).copied().map(Duckvalue::Int64).unwrap_or(Duckvalue::Null),
        column_types::Column::Uint64(xs) => xs.get(i).copied().map(Duckvalue::Uint64).unwrap_or(Duckvalue::Null),
        column_types::Column::Float64(xs) => xs.get(i).copied().map(Duckvalue::Float64).unwrap_or(Duckvalue::Null),
        column_types::Column::Int32(xs) => xs.get(i).copied().map(Duckvalue::Int32).unwrap_or(Duckvalue::Null),
        column_types::Column::Int8(xs) => xs.get(i).copied().map(Duckvalue::Int8).unwrap_or(Duckvalue::Null),
        column_types::Column::Int16(xs) => xs.get(i).copied().map(Duckvalue::Int16).unwrap_or(Duckvalue::Null),
        column_types::Column::Uint8(xs) => xs.get(i).copied().map(Duckvalue::Uint8).unwrap_or(Duckvalue::Null),
        column_types::Column::Uint16(xs) => xs.get(i).copied().map(Duckvalue::Uint16).unwrap_or(Duckvalue::Null),
        column_types::Column::Uint32(xs) => xs.get(i).copied().map(Duckvalue::Uint32).unwrap_or(Duckvalue::Null),
        column_types::Column::Float32(xs) => xs.get(i).copied().map(Duckvalue::Float32).unwrap_or(Duckvalue::Null),
        column_types::Column::Timestamp(xs) => xs.get(i).copied().map(Duckvalue::Timestamp).unwrap_or(Duckvalue::Null),
        column_types::Column::Date(xs) => xs.get(i).copied().map(Duckvalue::Date).unwrap_or(Duckvalue::Null),
        column_types::Column::Time(xs) => xs.get(i).copied().map(Duckvalue::Time).unwrap_or(Duckvalue::Null),
        column_types::Column::Timestamptz(xs) => xs.get(i).copied().map(Duckvalue::Timestamptz).unwrap_or(Duckvalue::Null),
        column_types::Column::Text(xs) => xs.get(i).cloned().map(Duckvalue::Text).unwrap_or(Duckvalue::Null),
        column_types::Column::Blob(xs) => xs.get(i).cloned().map(Duckvalue::Blob).unwrap_or(Duckvalue::Null),
        // Decimal / Interval / Uuid / Complex column arms are
        // rendered as Null in the Phase A opaque-blob path.
        _ => Duckvalue::Null,
    }
}

fn validate_colvec_rows(args: &[column_types::Colvec]) -> Result<usize, Duckerror> {
    let n_rows = if args.is_empty() { 0 } else { args[0].rows as usize };
    for (j, cv) in args.iter().enumerate() {
        if cv.rows as usize != n_rows {
            return Err(Duckerror::Internal(format!(
                "columnar dispatch: arg-column {} has rows={} but expected {}",
                j, cv.rows, n_rows
            )));
        }
    }
    Ok(n_rows)
}

fn materialize_row(args: &[column_types::Colvec], i: usize, out: &mut Vec<Duckvalue>) {
    out.clear();
    if out.capacity() < args.len() {
        out.reserve(args.len() - out.capacity());
    }
    for cv in args {
        out.push(colvec_get(cv, i));
    }
}

/// Lower a `Vec<Duckvalue>` (per-row scalar returns) into a
/// typed `Colvec`. Uses a TWO-PASS approach so that NULL rows
/// preceding the first non-NULL row still land in the eventual
/// arm's buffer as zero placeholders (single-pass "pick on first
/// non-NULL" left the pre-arm nulls in `blobs`, giving a
/// buffer.len() < rows off-by-one when the picked arm was not
/// Blob — every subsequent lookup was misaligned).
///
/// Pass 1 walks `&values` to determine the arm (and rejects
/// heterogeneous batches / unsupported Decimal/Interval/Uuid/
/// Complex arms). Pass 2 consumes `values` and pushes real
/// values or zero placeholders into that arm's buffer so the
/// buffer index matches the row index; the validity bitmap
/// carries the actual NULL rows. A colvec of all-NULLs is
/// lowered as Blob (chosen arbitrarily: no data buffer is ever
/// addressed since the validity bitmap zeros every row).
fn values_to_colvec(values: Vec<Duckvalue>) -> Result<column_types::Colvec, Duckerror> {
    let n = values.len();
    let rows = n as u32;
    let mut bits: Vec<u8> = vec![0u8; (n + 7) / 8];
    let mut any_null = false;

    // Discriminator: which arm we picked, populated on first
    // non-NULL row. Repeated per-arm buffers avoid an enum tag
    // in the hot path and let the compiler prove exhaustiveness.
    #[derive(Clone, Copy, PartialEq, Eq, Debug)]
    enum Arm {
        Unknown,
        Boolean,
        Int8, Int16, Int32, Int64,
        Uint8, Uint16, Uint32, Uint64,
        Float32, Float64,
        Text, Blob,
        Date, Time, Timestamp, Timestamptz,
    }

    fn arm_of(v: &Duckvalue) -> Option<Arm> {
        Some(match v {
            Duckvalue::Boolean(_) => Arm::Boolean,
            Duckvalue::Int8(_) => Arm::Int8,
            Duckvalue::Int16(_) => Arm::Int16,
            Duckvalue::Int32(_) => Arm::Int32,
            Duckvalue::Int64(_) => Arm::Int64,
            Duckvalue::Uint8(_) => Arm::Uint8,
            Duckvalue::Uint16(_) => Arm::Uint16,
            Duckvalue::Uint32(_) => Arm::Uint32,
            Duckvalue::Uint64(_) => Arm::Uint64,
            Duckvalue::Float32(_) => Arm::Float32,
            Duckvalue::Float64(_) => Arm::Float64,
            Duckvalue::Text(_) => Arm::Text,
            Duckvalue::Blob(_) => Arm::Blob,
            Duckvalue::Date(_) => Arm::Date,
            Duckvalue::Time(_) => Arm::Time,
            Duckvalue::Timestamp(_) => Arm::Timestamp,
            Duckvalue::Timestamptz(_) => Arm::Timestamptz,
            Duckvalue::Null
            | Duckvalue::Decimal(_)
            | Duckvalue::Interval(_)
            | Duckvalue::Uuid(_)
            | Duckvalue::Complex(_) => return None,
        })
    }

    fn mismatch(expected: &Arm, actual: &Duckvalue) -> Duckerror {
        Duckerror::Internal(format!(
            "values_to_colvec: column arm mismatch (expected={:?}, saw variant with tag {:?}); \
             heterogeneous rows in a single columnar batch are unsupported",
            expected, core::mem::discriminant(actual),
        ))
    }

    // ---- Pass 1: pick the arm across every non-NULL row. ----
    let mut arm = Arm::Unknown;
    for (i, v) in values.iter().enumerate() {
        if matches!(v, Duckvalue::Null) {
            any_null = true;
            continue;
        }
        // Refuse unsupported (Decimal/Interval/Uuid/Complex)
        // returns explicitly — the old code silently NULLed them.
        let this_arm = arm_of(v).ok_or_else(|| Duckerror::Internal(format!(
            "values_to_colvec: unsupported column arm for row {} (Decimal/Interval/Uuid/Complex \
             lowering is not yet implemented in the dynlink emit)",
            i,
        )))?;
        if arm == Arm::Unknown {
            arm = this_arm;
        } else if arm != this_arm {
            return Err(mismatch(&arm, v));
        }
    }

    // ---- Pass 2: allocate the chosen arm's buffer at exact
    // length and materialize (real values for non-NULL rows,
    // zero placeholders for NULL rows).
    let mut booleans: Vec<bool> = Vec::new();
    let mut int8s: Vec<i8> = Vec::new();
    let mut int16s: Vec<i16> = Vec::new();
    let mut int32s: Vec<i32> = Vec::new();
    let mut int64s: Vec<i64> = Vec::new();
    let mut uint8s: Vec<u8> = Vec::new();
    let mut uint16s: Vec<u16> = Vec::new();
    let mut uint32s: Vec<u32> = Vec::new();
    let mut uint64s: Vec<u64> = Vec::new();
    let mut float32s: Vec<f32> = Vec::new();
    let mut float64s: Vec<f64> = Vec::new();
    let mut texts: Vec<String> = Vec::new();
    let mut blobs: Vec<Vec<u8>> = Vec::new();
    let mut dates: Vec<i32> = Vec::new();
    let mut times: Vec<i64> = Vec::new();
    let mut timestamps: Vec<i64> = Vec::new();
    let mut timestamptzs: Vec<i64> = Vec::new();

    for (i, v) in values.into_iter().enumerate() {
        if matches!(v, Duckvalue::Null) {
            // Placeholder so the buffer index tracks the row
            // index; the validity bit stays 0.
            match arm {
                Arm::Unknown | Arm::Blob => blobs.push(Vec::new()),
                Arm::Boolean => booleans.push(false),
                Arm::Int8 => int8s.push(0),
                Arm::Int16 => int16s.push(0),
                Arm::Int32 => int32s.push(0),
                Arm::Int64 => int64s.push(0),
                Arm::Uint8 => uint8s.push(0),
                Arm::Uint16 => uint16s.push(0),
                Arm::Uint32 => uint32s.push(0),
                Arm::Uint64 => uint64s.push(0),
                Arm::Float32 => float32s.push(0.0),
                Arm::Float64 => float64s.push(0.0),
                Arm::Text => texts.push(String::new()),
                Arm::Date => dates.push(0),
                Arm::Time => times.push(0),
                Arm::Timestamp => timestamps.push(0),
                Arm::Timestamptz => timestamptzs.push(0),
            }
            continue;
        }
        bits[i / 8] |= 1u8 << (i % 8);
        match v {
            Duckvalue::Boolean(b) => booleans.push(b),
            Duckvalue::Int8(x) => int8s.push(x),
            Duckvalue::Int16(x) => int16s.push(x),
            Duckvalue::Int32(x) => int32s.push(x),
            Duckvalue::Int64(x) => int64s.push(x),
            Duckvalue::Uint8(x) => uint8s.push(x),
            Duckvalue::Uint16(x) => uint16s.push(x),
            Duckvalue::Uint32(x) => uint32s.push(x),
            Duckvalue::Uint64(x) => uint64s.push(x),
            Duckvalue::Float32(x) => float32s.push(x),
            Duckvalue::Float64(x) => float64s.push(x),
            Duckvalue::Text(s) => texts.push(s),
            Duckvalue::Blob(b) => blobs.push(b),
            Duckvalue::Date(x) => dates.push(x),
            Duckvalue::Time(x) => times.push(x),
            Duckvalue::Timestamp(x) => timestamps.push(x),
            Duckvalue::Timestamptz(x) => timestamptzs.push(x),
            // Null / unsupported: already handled above (or
            // rejected in pass 1).
            _ => unreachable!("arm_of admitted a variant we didn't push"),
        }
    }
    let validity = if any_null { bits } else { Vec::new() };
    let data = match arm {
        Arm::Unknown | Arm::Blob => column_types::Column::Blob(blobs),
        Arm::Boolean => column_types::Column::Boolean(booleans),
        Arm::Int8 => column_types::Column::Int8(int8s),
        Arm::Int16 => column_types::Column::Int16(int16s),
        Arm::Int32 => column_types::Column::Int32(int32s),
        Arm::Int64 => column_types::Column::Int64(int64s),
        Arm::Uint8 => column_types::Column::Uint8(uint8s),
        Arm::Uint16 => column_types::Column::Uint16(uint16s),
        Arm::Uint32 => column_types::Column::Uint32(uint32s),
        Arm::Uint64 => column_types::Column::Uint64(uint64s),
        Arm::Float32 => column_types::Column::Float32(float32s),
        Arm::Float64 => column_types::Column::Float64(float64s),
        Arm::Text => column_types::Column::Text(texts),
        Arm::Date => column_types::Column::Date(dates),
        Arm::Time => column_types::Column::Time(times),
        Arm::Timestamp => column_types::Column::Timestamp(timestamps),
        Arm::Timestamptz => column_types::Column::Timestamptz(timestamptzs),
    };
    Ok(column_types::Colvec { rows, validity, data })
}

fn scalar_name_by_arm_idx(arm: usize) -> Option<&'static str> {
    match arm as u32 {
        0 => Some("add_edge"),
        1 => Some("add_edge_mod_face"),
        2 => Some("add_edge_new_faces"),
        3 => Some("add_face"),
        4 => Some("add_iso_edge"),
        5 => Some("add_iso_node"),
        6 => Some("add_node"),
        7 => Some("add_topogeom_column"),
        8 => Some("as_gml"),
        9 => Some("as_topojson"),
        10 => Some("as_topojson_with_edge_map"),
        11 => Some("build"),
        12 => Some("change_edge_geom"),
        13 => Some("copy_topology"),
        14 => Some("create_index"),
        15 => Some("create_raster"),
        16 => Some("create_topo_geom"),
        17 => Some("create_topology"),
        18 => Some("destroy_index"),
        19 => Some("drop_topogeom_column"),
        20 => Some("drop_topology"),
        21 => Some("from_binary"),
        22 => Some("from_bytes"),
        23 => Some("get_edge_by_point"),
        24 => Some("get_edge_geometry"),
        25 => Some("get_face_by_point"),
        26 => Some("get_face_containing_point"),
        27 => Some("get_face_edges"),
        28 => Some("get_face_geometry"),
        29 => Some("get_node_by_point"),
        30 => Some("get_node_edges"),
        31 => Some("get_node_geometry"),
        32 => Some("get_ring_edges"),
        33 => Some("get_topology_id"),
        34 => Some("get_topology_name"),
        35 => Some("get_topology_srid"),
        36 => Some("insert_wkb"),
        37 => Some("list_cast_rewrites"),
        38 => Some("list_operator_rewrites"),
        39 => Some("list_preprocessor_patterns"),
        40 => Some("make_topo_element"),
        41 => Some("make_topology_precise"),
        42 => Some("mod_edge_heal"),
        43 => Some("mod_edge_split"),
        44 => Some("move_iso_node"),
        45 => Some("nearest"),
        46 => Some("new_edge_heal"),
        47 => Some("new_edges_split"),
        48 => Some("op_bbox_distance"),
        49 => Some("op_bbox_intersects_nd"),
        50 => Some("op_bbox_intersects_twod"),
        51 => Some("op_equals_spatially"),
        52 => Some("op_knn_distance"),
        53 => Some("polygonize"),
        54 => Some("postgis_addbbox"),
        55 => Some("postgis_dropbbox"),
        56 => Some("postgis_full_version"),
        57 => Some("postgis_geos_compiled_version"),
        58 => Some("postgis_geos_noop"),
        59 => Some("postgis_geos_version"),
        60 => Some("postgis_getbbox"),
        61 => Some("postgis_hasbbox"),
        62 => Some("postgis_lib_build_date"),
        63 => Some("postgis_lib_revision"),
        64 => Some("postgis_lib_version"),
        65 => Some("postgis_libjson_version"),
        66 => Some("postgis_liblwgeom_version"),
        67 => Some("postgis_libprotobuf_version"),
        68 => Some("postgis_libxml_version"),
        69 => Some("postgis_noop"),
        70 => Some("postgis_proj_compiled_version"),
        71 => Some("postgis_proj_version"),
        72 => Some("postgis_scripts_build_date"),
        73 => Some("postgis_scripts_installed"),
        74 => Some("postgis_scripts_released"),
        75 => Some("postgis_svn_version"),
        76 => Some("postgis_topology_scripts_installed"),
        77 => Some("postgis_type_name"),
        78 => Some("postgis_version"),
        79 => Some("postgis_wagyu_version"),
        80 => Some("query_envelope"),
        81 => Some("query_knn"),
        82 => Some("query_within_distance"),
        83 => Some("rem_edge_mod_face"),
        84 => Some("rem_edge_new_face"),
        85 => Some("remove_face"),
        86 => Some("remove_iso_edge"),
        87 => Some("remove_iso_node"),
        88 => Some("remove_unused_primitives"),
        89 => Some("rename_topology"),
        90 => Some("st_3d_closest_point"),
        91 => Some("st_3d_contains"),
        92 => Some("st_3d_d_within"),
        93 => Some("st_3d_dwithin"),
        94 => Some("st_3d_intersects"),
        95 => Some("st_3d_line_interpolate_point"),
        96 => Some("st_3d_longest_line"),
        97 => Some("st_3d_make_box"),
        98 => Some("st_3d_max_distance"),
        99 => Some("st_3d_shortest_line"),
        100 => Some("st_3dboundary"),
        101 => Some("st_3dcentroid"),
        102 => Some("st_3dclosestpoint"),
        103 => Some("st_3dcontains"),
        104 => Some("st_3dconvexhull"),
        105 => Some("st_3ddifference"),
        106 => Some("st_3ddistance"),
        107 => Some("st_3ddwithin"),
        108 => Some("st_3denvelope"),
        109 => Some("st_3dintersection"),
        110 => Some("st_3dintersects"),
        111 => Some("st_3dlength"),
        112 => Some("st_3dlineinterpolatepoint"),
        113 => Some("st_3dlongestline"),
        114 => Some("st_3dmakebox"),
        115 => Some("st_3dmaxdistance"),
        116 => Some("st_3dperimeter"),
        117 => Some("st_3dreverse"),
        118 => Some("st_3drotate"),
        119 => Some("st_3dscale"),
        120 => Some("st_3dshortestline"),
        121 => Some("st_3dtranslate"),
        122 => Some("st_3dunion"),
        123 => Some("st_add_band"),
        124 => Some("st_add_face"),
        125 => Some("st_add_iso_edge"),
        126 => Some("st_add_iso_node"),
        127 => Some("st_add_measure"),
        128 => Some("st_add_point"),
        129 => Some("st_addband"),
        130 => Some("st_addface"),
        131 => Some("st_addisoedge"),
        132 => Some("st_addisonode"),
        133 => Some("st_addmeasure"),
        134 => Some("st_addpoint"),
        135 => Some("st_affine"),
        136 => Some("st_angle"),
        137 => Some("st_approx_histogram"),
        138 => Some("st_approx_quantile"),
        139 => Some("st_approx_quantiles"),
        140 => Some("st_approx_summary_stats"),
        141 => Some("st_approx_value_count"),
        142 => Some("st_approx_value_counts"),
        143 => Some("st_approx_value_percent"),
        144 => Some("st_approx_value_percents"),
        145 => Some("st_approximate_medial_axis"),
        146 => Some("st_approximatemedialaxis"),
        147 => Some("st_area"),
        148 => Some("st_area_threed"),
        149 => Some("st_area_twod"),
        150 => Some("st_areathreed"),
        151 => Some("st_as_binary"),
        152 => Some("st_as_encoded_polyline"),
        153 => Some("st_as_ewkb"),
        154 => Some("st_as_ewkt"),
        155 => Some("st_as_ewkt_precision"),
        156 => Some("st_as_flatgeobuf"),
        157 => Some("st_as_gdal_raster"),
        158 => Some("st_as_geobuf"),
        159 => Some("st_as_geojson"),
        160 => Some("st_as_geojson_full"),
        161 => Some("st_as_gml"),
        162 => Some("st_as_gml_full"),
        163 => Some("st_as_hexewkb"),
        164 => Some("st_as_jpeg"),
        165 => Some("st_as_kml"),
        166 => Some("st_as_kml_full"),
        167 => Some("st_as_lat_lon_text"),
        168 => Some("st_as_marc21"),
        169 => Some("st_as_marc21_fmt"),
        170 => Some("st_as_mvt"),
        171 => Some("st_as_mvt_geom"),
        172 => Some("st_as_png"),
        173 => Some("st_as_svg"),
        174 => Some("st_as_svg_full"),
        175 => Some("st_as_text"),
        176 => Some("st_as_text_precision"),
        177 => Some("st_as_tiff"),
        178 => Some("st_as_topo_json"),
        179 => Some("st_as_twkb"),
        180 => Some("st_as_x3d"),
        181 => Some("st_as_x3d_full"),
        182 => Some("st_asbinary"),
        183 => Some("st_asencodedpolyline"),
        184 => Some("st_asewkb"),
        185 => Some("st_asewkt"),
        186 => Some("st_asgeojson"),
        187 => Some("st_asgml"),
        188 => Some("st_ashexewkb"),
        189 => Some("st_askml"),
        190 => Some("st_aslatlontext"),
        191 => Some("st_asmarc21"),
        192 => Some("st_asmvt"),
        193 => Some("st_asmvtgeom"),
        194 => Some("st_aspect"),
        195 => Some("st_aspng"),
        196 => Some("st_assvg"),
        197 => Some("st_astext"),
        198 => Some("st_astiff"),
        199 => Some("st_astopojson"),
        200 => Some("st_astwkb"),
        201 => Some("st_asx3d"),
        202 => Some("st_azimuth"),
        203 => Some("st_band_count"),
        204 => Some("st_band_max"),
        205 => Some("st_band_mean"),
        206 => Some("st_band_metadata"),
        207 => Some("st_band_min"),
        208 => Some("st_band_nodata_value"),
        209 => Some("st_band_pixel_type"),
        210 => Some("st_band_stddev"),
        211 => Some("st_band_sum"),
        212 => Some("st_bandnodatavalue"),
        213 => Some("st_bandpixeltype"),
        214 => Some("st_bands_metadata"),
        215 => Some("st_bboxdistance"),
        216 => Some("st_bboxintersects"),
        217 => Some("st_bboxintersectsnd"),
        218 => Some("st_bd_mpoly_from_text"),
        219 => Some("st_bd_poly_from_text"),
        220 => Some("st_bdmpolyfromtext"),
        221 => Some("st_bdpolyfromtext"),
        222 => Some("st_boundary"),
        223 => Some("st_boundary_threed"),
        224 => Some("st_boundarythreed"),
        225 => Some("st_bounding_diagonal"),
        226 => Some("st_boundingdiagonal"),
        227 => Some("st_box2d_from_geohash"),
        228 => Some("st_box2dfromgeohash"),
        229 => Some("st_box_from_geohash"),
        230 => Some("st_boxfromgeohash"),
        231 => Some("st_buffer"),
        232 => Some("st_buffer_quad_segs"),
        233 => Some("st_buffer_with_style"),
        234 => Some("st_build_area"),
        235 => Some("st_buildarea"),
        236 => Some("st_centroid"),
        237 => Some("st_centroid_threed"),
        238 => Some("st_centroidthreed"),
        239 => Some("st_chaikin_smoothing"),
        240 => Some("st_chaikinsmoothing"),
        241 => Some("st_clip_by_box2d"),
        242 => Some("st_clipbybox2d"),
        243 => Some("st_closest_point"),
        244 => Some("st_closest_point_3d"),
        245 => Some("st_closest_point_of_approach"),
        246 => Some("st_closest_point_threed"),
        247 => Some("st_closestpoint"),
        248 => Some("st_collect"),
        249 => Some("st_collection_extract"),
        250 => Some("st_collection_homogenize"),
        251 => Some("st_collectionextract"),
        252 => Some("st_collectionhomogenize"),
        253 => Some("st_concave_hull"),
        254 => Some("st_concave_hull_holes"),
        255 => Some("st_concavehull"),
        256 => Some("st_constrained_delaunay_triangles"),
        257 => Some("st_constraineddelaunaytriangles"),
        258 => Some("st_constrainedelaunaytriangles"),
        259 => Some("st_contains"),
        260 => Some("st_contains_3d"),
        261 => Some("st_contains_properly"),
        262 => Some("st_contains_threed"),
        263 => Some("st_containsproperly"),
        264 => Some("st_convex_hull"),
        265 => Some("st_convexhull"),
        266 => Some("st_coord_dim"),
        267 => Some("st_coorddim"),
        268 => Some("st_count"),
        269 => Some("st_coverage_clean"),
        270 => Some("st_coverage_invalid_edges"),
        271 => Some("st_coverage_simplify"),
        272 => Some("st_coverage_union"),
        273 => Some("st_coverageclean"),
        274 => Some("st_coverageinvalidedges"),
        275 => Some("st_coveragesimplify"),
        276 => Some("st_covered_by"),
        277 => Some("st_coveredby"),
        278 => Some("st_covers"),
        279 => Some("st_covers_threed"),
        280 => Some("st_cpa_within"),
        281 => Some("st_cpawithin"),
        282 => Some("st_create_topo_geo"),
        283 => Some("st_create_topology"),
        284 => Some("st_createtopology"),
        285 => Some("st_crosses"),
        286 => Some("st_curve_n"),
        287 => Some("st_curve_to_line"),
        288 => Some("st_curven"),
        289 => Some("st_curvetoline"),
        290 => Some("st_d_within"),
        291 => Some("st_delaunay_triangles"),
        292 => Some("st_delaunaytriangles"),
        293 => Some("st_dfully_within"),
        294 => Some("st_dfully_within_threed"),
        295 => Some("st_dfullywithin"),
        296 => Some("st_difference"),
        297 => Some("st_difference_gridsize"),
        298 => Some("st_dimension"),
        299 => Some("st_disjoint"),
        300 => Some("st_distance"),
        301 => Some("st_distance_cpa"),
        302 => Some("st_distance_sphere"),
        303 => Some("st_distance_sphere_radius"),
        304 => Some("st_distance_spheroid"),
        305 => Some("st_distance_threed"),
        306 => Some("st_distancecpa"),
        307 => Some("st_distancesphere"),
        308 => Some("st_distancespheroid"),
        309 => Some("st_distancethreed"),
        310 => Some("st_dump_as_polygons"),
        311 => Some("st_dump_values"),
        312 => Some("st_dump_values_multi"),
        313 => Some("st_dumpaspolygons"),
        314 => Some("st_dumpvalues"),
        315 => Some("st_dwithin"),
        316 => Some("st_dwithin_3d"),
        317 => Some("st_dwithin_threed"),
        318 => Some("st_end_point"),
        319 => Some("st_endpoint"),
        320 => Some("st_envelope"),
        321 => Some("st_envelope_threed"),
        322 => Some("st_envelopethreed"),
        323 => Some("st_equals"),
        324 => Some("st_equals_exact"),
        325 => Some("st_equalsexact"),
        326 => Some("st_expand"),
        327 => Some("st_expand_xy"),
        328 => Some("st_expand_xyz"),
        329 => Some("st_expand_xyzm"),
        330 => Some("st_exterior_ring"),
        331 => Some("st_exteriorring"),
        332 => Some("st_filter_by_m"),
        333 => Some("st_filterbym"),
        334 => Some("st_flip_coordinates"),
        335 => Some("st_flipcoordinates"),
        336 => Some("st_force2d"),
        337 => Some("st_force3d"),
        338 => Some("st_force3dm"),
        339 => Some("st_force3dz"),
        340 => Some("st_force4d"),
        341 => Some("st_force_2d"),
        342 => Some("st_force_3d"),
        343 => Some("st_force_3dm"),
        344 => Some("st_force_4d"),
        345 => Some("st_force_collection"),
        346 => Some("st_force_curve"),
        347 => Some("st_force_fourd"),
        348 => Some("st_force_polygon_ccw"),
        349 => Some("st_force_polygon_cw"),
        350 => Some("st_force_rhr"),
        351 => Some("st_force_sfs"),
        352 => Some("st_force_threed"),
        353 => Some("st_force_threedm"),
        354 => Some("st_force_threedz"),
        355 => Some("st_force_twod"),
        356 => Some("st_forcecollection"),
        357 => Some("st_forcecurve"),
        358 => Some("st_forcefourd"),
        359 => Some("st_forcepolygonccw"),
        360 => Some("st_forcepolygoncw"),
        361 => Some("st_forcerhr"),
        362 => Some("st_forcesfs"),
        363 => Some("st_forcethreed"),
        364 => Some("st_forcethreedm"),
        365 => Some("st_forcethreedz"),
        366 => Some("st_forcetwod"),
        367 => Some("st_frechet_distance"),
        368 => Some("st_frechetdistance"),
        369 => Some("st_from_flatgeobuf"),
        370 => Some("st_fromflatgeobuf"),
        371 => Some("st_generate_points"),
        372 => Some("st_generatepoints"),
        373 => Some("st_geo_reference"),
        374 => Some("st_geog_area"),
        375 => Some("st_geog_azimuth"),
        376 => Some("st_geog_buffer"),
        377 => Some("st_geog_buffer_with_segs"),
        378 => Some("st_geog_centroid"),
        379 => Some("st_geog_closest_point"),
        380 => Some("st_geog_convex_hull"),
        381 => Some("st_geog_covered_by"),
        382 => Some("st_geog_covers"),
        383 => Some("st_geog_difference"),
        384 => Some("st_geog_distance"),
        385 => Some("st_geog_dwithin"),
        386 => Some("st_geog_expand"),
        387 => Some("st_geog_from_ewkt"),
        388 => Some("st_geog_from_text"),
        389 => Some("st_geog_from_wkb"),
        390 => Some("st_geog_geometry_type"),
        391 => Some("st_geog_intersection"),
        392 => Some("st_geog_intersects"),
        393 => Some("st_geog_is_closed"),
        394 => Some("st_geog_is_empty"),
        395 => Some("st_geog_is_simple"),
        396 => Some("st_geog_length"),
        397 => Some("st_geog_line_substring"),
        398 => Some("st_geog_npoints"),
        399 => Some("st_geog_perimeter"),
        400 => Some("st_geog_point"),
        401 => Some("st_geog_project"),
        402 => Some("st_geog_segmentize"),
        403 => Some("st_geog_summary"),
        404 => Some("st_geog_sym_difference"),
        405 => Some("st_geog_to_geom"),
        406 => Some("st_geog_union"),
        407 => Some("st_geogarea"),
        408 => Some("st_geogazimuth"),
        409 => Some("st_geogbuffer"),
        410 => Some("st_geogbufferwithsegs"),
        411 => Some("st_geogcentroid"),
        412 => Some("st_geogcoveredby"),
        413 => Some("st_geogcovers"),
        414 => Some("st_geogdistance"),
        415 => Some("st_geogdwithin"),
        416 => Some("st_geogexpand"),
        417 => Some("st_geogfromtext"),
        418 => Some("st_geogfromwkb"),
        419 => Some("st_geoggeometrytype"),
        420 => Some("st_geogintersects"),
        421 => Some("st_geogisclosed"),
        422 => Some("st_geogisempty"),
        423 => Some("st_geogissimple"),
        424 => Some("st_geoglength"),
        425 => Some("st_geognpoints"),
        426 => Some("st_geogperimeter"),
        427 => Some("st_geogpoint"),
        428 => Some("st_geography_from_text"),
        429 => Some("st_geographyfromtext"),
        430 => Some("st_geogsummary"),
        431 => Some("st_geohash"),
        432 => Some("st_geom_from_ewkb"),
        433 => Some("st_geom_from_ewkt"),
        434 => Some("st_geom_from_geobuf"),
        435 => Some("st_geom_from_geohash"),
        436 => Some("st_geom_from_geojson"),
        437 => Some("st_geom_from_gml"),
        438 => Some("st_geom_from_gml_srid"),
        439 => Some("st_geom_from_hexewkb"),
        440 => Some("st_geom_from_kml"),
        441 => Some("st_geom_from_marc21"),
        442 => Some("st_geom_from_text"),
        443 => Some("st_geom_from_text_srid"),
        444 => Some("st_geom_from_twkb"),
        445 => Some("st_geom_from_wkb"),
        446 => Some("st_geomcoll_from_text"),
        447 => Some("st_geomcoll_from_text_srid"),
        448 => Some("st_geomcoll_from_wkb"),
        449 => Some("st_geomcoll_from_wkb_srid"),
        450 => Some("st_geomcollfromtext"),
        451 => Some("st_geomcollfromwkb"),
        452 => Some("st_geomequal"),
        453 => Some("st_geometric_median"),
        454 => Some("st_geometricmedian"),
        455 => Some("st_geometry_from_text"),
        456 => Some("st_geometry_n"),
        457 => Some("st_geometry_type"),
        458 => Some("st_geometryfromtext"),
        459 => Some("st_geometryn"),
        460 => Some("st_geometrytype"),
        461 => Some("st_geomfromewkb"),
        462 => Some("st_geomfromewkt"),
        463 => Some("st_geomfromgeobuf"),
        464 => Some("st_geomfromgeohash"),
        465 => Some("st_geomfromgeojson"),
        466 => Some("st_geomfromgml"),
        467 => Some("st_geomfromgmlsrid"),
        468 => Some("st_geomfromhexewkb"),
        469 => Some("st_geomfromkml"),
        470 => Some("st_geomfrommarc21"),
        471 => Some("st_geomfromtext"),
        472 => Some("st_geomfromtextsrid"),
        473 => Some("st_geomfromtwkb"),
        474 => Some("st_geomfromwkb"),
        475 => Some("st_get_edge_by_point"),
        476 => Some("st_get_edge_geometry"),
        477 => Some("st_get_face_geometry"),
        478 => Some("st_get_node_by_point"),
        479 => Some("st_get_node_geometry"),
        480 => Some("st_getedgebypoint"),
        481 => Some("st_getedgegeometry"),
        482 => Some("st_getfacegeometry"),
        483 => Some("st_getnodebypoint"),
        484 => Some("st_getnodegeometry"),
        485 => Some("st_gml_to_sql"),
        486 => Some("st_gmltosql"),
        487 => Some("st_has_arc"),
        488 => Some("st_has_m"),
        489 => Some("st_has_no_band"),
        490 => Some("st_has_z"),
        491 => Some("st_hasarc"),
        492 => Some("st_hasm"),
        493 => Some("st_hasnoband"),
        494 => Some("st_hasz"),
        495 => Some("st_hausdorff_distance"),
        496 => Some("st_hausdorffdistance"),
        497 => Some("st_height"),
        498 => Some("st_hexagon"),
        499 => Some("st_hill_shade"),
        500 => Some("st_hillshade"),
        501 => Some("st_histogram"),
        502 => Some("st_init_topo_geo"),
        503 => Some("st_interior_ring_n"),
        504 => Some("st_interiorringn"),
        505 => Some("st_interpolate_point"),
        506 => Some("st_intersection"),
        507 => Some("st_intersection_gridsize"),
        508 => Some("st_intersects"),
        509 => Some("st_intersects_3d"),
        510 => Some("st_intersects_threed"),
        511 => Some("st_inverse_transform_pipeline"),
        512 => Some("st_inversetransformpipeline"),
        513 => Some("st_is_closed"),
        514 => Some("st_is_collection"),
        515 => Some("st_is_empty"),
        516 => Some("st_is_measured"),
        517 => Some("st_is_polygon_ccw"),
        518 => Some("st_is_polygon_cw"),
        519 => Some("st_is_ring"),
        520 => Some("st_is_simple"),
        521 => Some("st_is_valid"),
        522 => Some("st_is_valid_detail"),
        523 => Some("st_is_valid_detail_flags"),
        524 => Some("st_is_valid_flags"),
        525 => Some("st_is_valid_reason"),
        526 => Some("st_is_valid_reason_flags"),
        527 => Some("st_is_valid_trajectory"),
        528 => Some("st_isclosed"),
        529 => Some("st_iscollection"),
        530 => Some("st_isempty"),
        531 => Some("st_ispolygonccw"),
        532 => Some("st_ispolygoncw"),
        533 => Some("st_isring"),
        534 => Some("st_issimple"),
        535 => Some("st_isvalid"),
        536 => Some("st_isvaliddetail"),
        537 => Some("st_isvalidreason"),
        538 => Some("st_isvalidtrajectory"),
        539 => Some("st_knndistance"),
        540 => Some("st_largest_empty_circle"),
        541 => Some("st_largestemptycircle"),
        542 => Some("st_length"),
        543 => Some("st_length2d"),
        544 => Some("st_length_2d"),
        545 => Some("st_length_spheroid"),
        546 => Some("st_length_threed"),
        547 => Some("st_length_twod"),
        548 => Some("st_length_twod_spheroid"),
        549 => Some("st_lengthspheroid"),
        550 => Some("st_lengththreed"),
        551 => Some("st_letters"),
        552 => Some("st_line_crossing_direction"),
        553 => Some("st_line_extend"),
        554 => Some("st_line_from_encoded_polyline"),
        555 => Some("st_line_from_multi_point"),
        556 => Some("st_line_from_text"),
        557 => Some("st_line_from_text_srid"),
        558 => Some("st_line_from_wkb"),
        559 => Some("st_line_from_wkb_srid"),
        560 => Some("st_line_interpolate_point"),
        561 => Some("st_line_interpolate_point_3d"),
        562 => Some("st_line_interpolate_point_threed"),
        563 => Some("st_line_interpolate_points"),
        564 => Some("st_line_locate_point"),
        565 => Some("st_line_merge"),
        566 => Some("st_line_merge_directed"),
        567 => Some("st_line_to_curve"),
        568 => Some("st_linecrossingdirection"),
        569 => Some("st_lineextend"),
        570 => Some("st_linefromencodedpolyline"),
        571 => Some("st_linefrommultipoint"),
        572 => Some("st_linefromtext"),
        573 => Some("st_linefromwkb"),
        574 => Some("st_lineinterpolatepoint"),
        575 => Some("st_lineinterpolatepoints"),
        576 => Some("st_linelocatepoint"),
        577 => Some("st_linemerge"),
        578 => Some("st_linetocurve"),
        579 => Some("st_locate_along"),
        580 => Some("st_locate_between"),
        581 => Some("st_locate_between_elevations"),
        582 => Some("st_locatealong"),
        583 => Some("st_locatebetween"),
        584 => Some("st_locatebetweenelevations"),
        585 => Some("st_longest_line"),
        586 => Some("st_longest_line_3d"),
        587 => Some("st_longest_line_threed"),
        588 => Some("st_longestline"),
        589 => Some("st_m"),
        590 => Some("st_make_box2d"),
        591 => Some("st_make_box3d"),
        592 => Some("st_make_box_threed"),
        593 => Some("st_make_empty_coverage"),
        594 => Some("st_make_empty_raster"),
        595 => Some("st_make_envelope"),
        596 => Some("st_make_envelope_srid"),
        597 => Some("st_make_line"),
        598 => Some("st_make_line_threed"),
        599 => Some("st_make_line_two"),
        600 => Some("st_make_point"),
        601 => Some("st_make_point_m"),
        602 => Some("st_make_point_z"),
        603 => Some("st_make_point_zm"),
        604 => Some("st_make_polygon"),
        605 => Some("st_make_valid"),
        606 => Some("st_makebox"),
        607 => Some("st_makebox2d"),
        608 => Some("st_makebox3d"),
        609 => Some("st_makeemptyraster"),
        610 => Some("st_makeenvelope"),
        611 => Some("st_makeenvelopesrid"),
        612 => Some("st_makeline"),
        613 => Some("st_makepoint"),
        614 => Some("st_makepointm"),
        615 => Some("st_makepointz"),
        616 => Some("st_makepointzm"),
        617 => Some("st_makepolygon"),
        618 => Some("st_makevalid"),
        619 => Some("st_map_algebra"),
        620 => Some("st_map_algebra2"),
        621 => Some("st_mapalgebra"),
        622 => Some("st_mapalgebra2"),
        623 => Some("st_max_distance"),
        624 => Some("st_max_distance_3d"),
        625 => Some("st_max_distance_threed"),
        626 => Some("st_max_x"),
        627 => Some("st_max_y"),
        628 => Some("st_maxdistance"),
        629 => Some("st_maximum_inscribed_circle"),
        630 => Some("st_maximuminscribedcircle"),
        631 => Some("st_mem_size"),
        632 => Some("st_mem_union"),
        633 => Some("st_memsize"),
        634 => Some("st_metadata"),
        635 => Some("st_min_convex_hull"),
        636 => Some("st_min_x"),
        637 => Some("st_min_y"),
        638 => Some("st_minimum_bounding_circle"),
        639 => Some("st_minimum_bounding_circle_segs"),
        640 => Some("st_minimum_bounding_radius"),
        641 => Some("st_minimum_clearance"),
        642 => Some("st_minimum_clearance_line"),
        643 => Some("st_minimumboundingcircle"),
        644 => Some("st_minimumboundingradius"),
        645 => Some("st_minimumclearance"),
        646 => Some("st_minimumclearanceline"),
        647 => Some("st_mline_from_text"),
        648 => Some("st_mline_from_text_srid"),
        649 => Some("st_mline_from_wkb"),
        650 => Some("st_mline_from_wkb_srid"),
        651 => Some("st_mlinefromtext"),
        652 => Some("st_mlinefromwkb"),
        653 => Some("st_mmax"),
        654 => Some("st_mmin"),
        655 => Some("st_mod_edge_heal"),
        656 => Some("st_mod_edge_split"),
        657 => Some("st_modedgeheal"),
        658 => Some("st_modedgesplit"),
        659 => Some("st_mpoint_from_text"),
        660 => Some("st_mpoint_from_text_srid"),
        661 => Some("st_mpoint_from_wkb"),
        662 => Some("st_mpoint_from_wkb_srid"),
        663 => Some("st_mpointfromtext"),
        664 => Some("st_mpointfromwkb"),
        665 => Some("st_mpoly_from_text"),
        666 => Some("st_mpoly_from_text_srid"),
        667 => Some("st_mpoly_from_wkb"),
        668 => Some("st_mpoly_from_wkb_srid"),
        669 => Some("st_mpolyfromtext"),
        670 => Some("st_mpolyfromwkb"),
        671 => Some("st_multi"),
        672 => Some("st_multi_line_from_wkb"),
        673 => Some("st_multi_line_from_wkb_srid"),
        674 => Some("st_multi_line_string_from_text"),
        675 => Some("st_multi_line_string_from_text_srid"),
        676 => Some("st_multi_point_from_text"),
        677 => Some("st_multi_point_from_text_srid"),
        678 => Some("st_multi_point_from_wkb"),
        679 => Some("st_multi_point_from_wkb_srid"),
        680 => Some("st_multi_poly_from_wkb"),
        681 => Some("st_multi_poly_from_wkb_srid"),
        682 => Some("st_multi_polygon_from_text"),
        683 => Some("st_multi_polygon_from_text_srid"),
        684 => Some("st_n_dims"),
        685 => Some("st_ndims"),
        686 => Some("st_nearest_value"),
        687 => Some("st_nearestvalue"),
        688 => Some("st_new_edges_split"),
        689 => Some("st_newedgessplit"),
        690 => Some("st_node"),
        691 => Some("st_normalize"),
        692 => Some("st_npoints"),
        693 => Some("st_nrings"),
        694 => Some("st_num_bands"),
        695 => Some("st_num_curves"),
        696 => Some("st_num_geometries"),
        697 => Some("st_num_interior_ring"),
        698 => Some("st_num_interior_rings"),
        699 => Some("st_num_points"),
        700 => Some("st_numbands"),
        701 => Some("st_numcurves"),
        702 => Some("st_numgeometries"),
        703 => Some("st_numinteriorring"),
        704 => Some("st_numinteriorrings"),
        705 => Some("st_numpoints"),
        706 => Some("st_offset_curve"),
        707 => Some("st_offsetcurve"),
        708 => Some("st_ordering_equals"),
        709 => Some("st_orderingequals"),
        710 => Some("st_oriented_envelope"),
        711 => Some("st_orientedenvelope"),
        712 => Some("st_overlaps"),
        713 => Some("st_perimeter"),
        714 => Some("st_perimeter2d"),
        715 => Some("st_perimeter_2d"),
        716 => Some("st_perimeter_threed"),
        717 => Some("st_perimeter_twod"),
        718 => Some("st_perimeterthreed"),
        719 => Some("st_pixel_as_centroid"),
        720 => Some("st_pixel_as_centroids"),
        721 => Some("st_pixel_as_point"),
        722 => Some("st_pixel_as_points"),
        723 => Some("st_pixel_as_polygon"),
        724 => Some("st_pixel_as_polygons"),
        725 => Some("st_pixel_height"),
        726 => Some("st_pixel_of_value"),
        727 => Some("st_pixel_width"),
        728 => Some("st_pixelascentroid"),
        729 => Some("st_pixelaspoint"),
        730 => Some("st_pixelaspolygon"),
        731 => Some("st_pixelaspolygons"),
        732 => Some("st_pixelofvalue"),
        733 => Some("st_pixels_of_values"),
        734 => Some("st_point"),
        735 => Some("st_point_from_geohash"),
        736 => Some("st_point_from_text"),
        737 => Some("st_point_from_text_srid"),
        738 => Some("st_point_from_wkb"),
        739 => Some("st_point_from_wkb_srid"),
        740 => Some("st_point_inside_circle"),
        741 => Some("st_point_m"),
        742 => Some("st_point_m_srid"),
        743 => Some("st_point_n"),
        744 => Some("st_point_on_surface"),
        745 => Some("st_point_srid"),
        746 => Some("st_point_z"),
        747 => Some("st_point_z_srid"),
        748 => Some("st_point_zm"),
        749 => Some("st_point_zm_srid"),
        750 => Some("st_pointfromgeohash"),
        751 => Some("st_pointfromtext"),
        752 => Some("st_pointfromwkb"),
        753 => Some("st_pointinsidecircle"),
        754 => Some("st_pointm"),
        755 => Some("st_pointn"),
        756 => Some("st_pointonsurface"),
        757 => Some("st_points"),
        758 => Some("st_pointz"),
        759 => Some("st_pointzm"),
        760 => Some("st_polygon"),
        761 => Some("st_polygon_from_text"),
        762 => Some("st_polygon_from_text_srid"),
        763 => Some("st_polygon_from_wkb"),
        764 => Some("st_polygon_from_wkb_srid"),
        765 => Some("st_polygonfromtext"),
        766 => Some("st_polygonfromwkb"),
        767 => Some("st_polygonize"),
        768 => Some("st_polygonize_as_raster"),
        769 => Some("st_polygonize_topology"),
        770 => Some("st_polyhedralsurface_from_text"),
        771 => Some("st_polyhedralsurface_from_wkb"),
        772 => Some("st_project"),
        773 => Some("st_quantile"),
        774 => Some("st_quantiles"),
        775 => Some("st_quantize_coordinates"),
        776 => Some("st_quantizecoordinates"),
        777 => Some("st_rastenvelope"),
        778 => Some("st_raster_contains"),
        779 => Some("st_raster_contains_geom"),
        780 => Some("st_raster_convex_hull"),
        781 => Some("st_raster_covers"),
        782 => Some("st_raster_intersection_geom"),
        783 => Some("st_raster_intersects"),
        784 => Some("st_raster_intersects_geom"),
        785 => Some("st_raster_overlaps"),
        786 => Some("st_raster_to_world_coord_x"),
        787 => Some("st_raster_to_world_coord_y"),
        788 => Some("st_raster_within"),
        789 => Some("st_rastercontains"),
        790 => Some("st_rastercontainsgeom"),
        791 => Some("st_rasterconvexhull"),
        792 => Some("st_rastercovers"),
        793 => Some("st_rasterintersects"),
        794 => Some("st_rasterintersectsgeom"),
        795 => Some("st_rasteroverlaps"),
        796 => Some("st_rastertoworldcoordx"),
        797 => Some("st_rastertoworldcoordy"),
        798 => Some("st_rasterwithin"),
        799 => Some("st_rastheight"),
        800 => Some("st_rastnumbands"),
        801 => Some("st_rastwidth"),
        802 => Some("st_reclass"),
        803 => Some("st_reclass_exact"),
        804 => Some("st_reduce_precision"),
        805 => Some("st_reduce_precision_flags"),
        806 => Some("st_reduceprecision"),
        807 => Some("st_relate"),
        808 => Some("st_relate_bnr"),
        809 => Some("st_relate_match"),
        810 => Some("st_relate_with_pattern"),
        811 => Some("st_relatematch"),
        812 => Some("st_remove_point"),
        813 => Some("st_remove_repeated_points"),
        814 => Some("st_remove_small_parts"),
        815 => Some("st_removepoint"),
        816 => Some("st_removerepeatedpoints"),
        817 => Some("st_removesmallparts"),
        818 => Some("st_rescale"),
        819 => Some("st_resize"),
        820 => Some("st_retile"),
        821 => Some("st_reverse"),
        822 => Some("st_reverse_threed"),
        823 => Some("st_reversethreed"),
        824 => Some("st_rotate"),
        825 => Some("st_rotate_around_point"),
        826 => Some("st_rotate_around_xy"),
        827 => Some("st_rotate_threed"),
        828 => Some("st_rotate_threed_around_center"),
        829 => Some("st_rotate_twod_around_center"),
        830 => Some("st_rotatethreed"),
        831 => Some("st_rotation"),
        832 => Some("st_roughness"),
        833 => Some("st_scale"),
        834 => Some("st_scale_threed"),
        835 => Some("st_scale_with_origin"),
        836 => Some("st_scale_with_vector"),
        837 => Some("st_scale_x"),
        838 => Some("st_scale_y"),
        839 => Some("st_scalethreed"),
        840 => Some("st_scalex"),
        841 => Some("st_scaley"),
        842 => Some("st_scroll"),
        843 => Some("st_segmentize"),
        844 => Some("st_set_band_is_nodata"),
        845 => Some("st_set_band_nodata_value"),
        846 => Some("st_set_effective_area"),
        847 => Some("st_set_geo_reference"),
        848 => Some("st_set_geo_reference_string"),
        849 => Some("st_set_point"),
        850 => Some("st_set_rotation"),
        851 => Some("st_set_scale"),
        852 => Some("st_set_scale_xy"),
        853 => Some("st_set_skew"),
        854 => Some("st_set_skew_xy"),
        855 => Some("st_set_srid"),
        856 => Some("st_set_upper_left"),
        857 => Some("st_set_value"),
        858 => Some("st_set_value_at_point"),
        859 => Some("st_set_values"),
        860 => Some("st_set_values_rect"),
        861 => Some("st_seteffectivearea"),
        862 => Some("st_setendm"),
        863 => Some("st_setendz"),
        864 => Some("st_setm"),
        865 => Some("st_setpoint"),
        866 => Some("st_setsrid"),
        867 => Some("st_setstartm"),
        868 => Some("st_setstartz"),
        869 => Some("st_setvalue"),
        870 => Some("st_setx"),
        871 => Some("st_sety"),
        872 => Some("st_setz"),
        873 => Some("st_shared_paths"),
        874 => Some("st_sharedpaths"),
        875 => Some("st_shift_longitude"),
        876 => Some("st_shiftlongitude"),
        877 => Some("st_shortest_line"),
        878 => Some("st_shortest_line_3d"),
        879 => Some("st_shortest_line_threed"),
        880 => Some("st_shortestline"),
        881 => Some("st_simplify"),
        882 => Some("st_simplify_polygon_hull"),
        883 => Some("st_simplify_preserve_collapse"),
        884 => Some("st_simplify_preserve_topology"),
        885 => Some("st_simplify_vw"),
        886 => Some("st_simplify_vw_preserve_collapse"),
        887 => Some("st_simplifypolygonhull"),
        888 => Some("st_simplifypreservetopology"),
        889 => Some("st_simplifyvw"),
        890 => Some("st_skew_x"),
        891 => Some("st_skew_y"),
        892 => Some("st_skewx"),
        893 => Some("st_skewy"),
        894 => Some("st_slope"),
        895 => Some("st_snap"),
        896 => Some("st_snap_to_grid"),
        897 => Some("st_snap_to_grid_4d"),
        898 => Some("st_snap_to_grid_origin"),
        899 => Some("st_snap_to_grid_xy"),
        900 => Some("st_snaptogrid"),
        901 => Some("st_split"),
        902 => Some("st_square"),
        903 => Some("st_srid"),
        904 => Some("st_start_point"),
        905 => Some("st_startpoint"),
        906 => Some("st_subdivide_gridsize"),
        907 => Some("st_summary"),
        908 => Some("st_summary_stats"),
        909 => Some("st_summarystats"),
        910 => Some("st_swap_ordinates"),
        911 => Some("st_swapordinates"),
        912 => Some("st_sym_difference"),
        913 => Some("st_sym_difference_gridsize"),
        914 => Some("st_sym_difference_threed"),
        915 => Some("st_symdifference"),
        916 => Some("st_tile"),
        917 => Some("st_tile_envelope"),
        918 => Some("st_tileenvelope"),
        919 => Some("st_tin_from_text"),
        920 => Some("st_tin_from_wkb"),
        921 => Some("st_topology_edge_count"),
        922 => Some("st_topology_face_count"),
        923 => Some("st_topology_from_bytes"),
        924 => Some("st_topology_name"),
        925 => Some("st_topology_node_count"),
        926 => Some("st_topology_precision"),
        927 => Some("st_topology_srid"),
        928 => Some("st_topology_to_bytes"),
        929 => Some("st_topologyedgecount"),
        930 => Some("st_topologyfacecount"),
        931 => Some("st_topologyfrombytes"),
        932 => Some("st_topologyname"),
        933 => Some("st_topologynodecount"),
        934 => Some("st_topologyprecision"),
        935 => Some("st_topologysrid"),
        936 => Some("st_topologytobytes"),
        937 => Some("st_touches"),
        938 => Some("st_tpi"),
        939 => Some("st_transform"),
        940 => Some("st_transform_pipeline"),
        941 => Some("st_transform_pipeline_to_srid"),
        942 => Some("st_transformpipeline"),
        943 => Some("st_translate"),
        944 => Some("st_translate_threed"),
        945 => Some("st_translatethreed"),
        946 => Some("st_transscale"),
        947 => Some("st_tri"),
        948 => Some("st_triangle_from_text"),
        949 => Some("st_triangle_from_wkb"),
        950 => Some("st_triangulate_polygon"),
        951 => Some("st_triangulate_twod"),
        952 => Some("st_triangulatepolygon"),
        953 => Some("st_unary_union"),
        954 => Some("st_unaryunion"),
        955 => Some("st_union"),
        956 => Some("st_union_gridsize"),
        957 => Some("st_upper_left_x"),
        958 => Some("st_upper_left_y"),
        959 => Some("st_upperleftx"),
        960 => Some("st_upperlefty"),
        961 => Some("st_validate_topology"),
        962 => Some("st_validatetopology"),
        963 => Some("st_value"),
        964 => Some("st_value_count"),
        965 => Some("st_value_count_of_value"),
        966 => Some("st_value_counts"),
        967 => Some("st_value_percent"),
        968 => Some("st_value_percents"),
        969 => Some("st_valuecount"),
        970 => Some("st_voronoi_lines"),
        971 => Some("st_voronoi_polygons"),
        972 => Some("st_voronoilines"),
        973 => Some("st_voronoipolygons"),
        974 => Some("st_width"),
        975 => Some("st_within"),
        976 => Some("st_wkb_to_sql"),
        977 => Some("st_wkbtosql"),
        978 => Some("st_wkt_to_sql"),
        979 => Some("st_wkttosql"),
        980 => Some("st_world_to_raster_coord"),
        981 => Some("st_world_to_raster_coord_col"),
        982 => Some("st_world_to_raster_coord_row"),
        983 => Some("st_worldtorastercoord"),
        984 => Some("st_worldtorastercoordcol"),
        985 => Some("st_worldtorastercoordrow"),
        986 => Some("st_wrap_x"),
        987 => Some("st_wrapx"),
        988 => Some("st_x"),
        989 => Some("st_xmax"),
        990 => Some("st_xmin"),
        991 => Some("st_y"),
        992 => Some("st_ymax"),
        993 => Some("st_ymin"),
        994 => Some("st_z"),
        995 => Some("st_zmax"),
        996 => Some("st_zmflag"),
        997 => Some("st_zmin"),
        998 => Some("to_topogeom"),
        999 => Some("topo_element_array_append"),
        1000 => Some("topogeo_add_geometry"),
        1001 => Some("topogeo_add_linestring"),
        1002 => Some("topogeo_add_point"),
        1003 => Some("topogeo_add_polygon"),
        1004 => Some("topogeo_load_geometry"),
        1005 => Some("topology_add_edge"),
        1006 => Some("topology_add_edge_mod_face"),
        1007 => Some("topology_add_edge_new_faces"),
        1008 => Some("topology_add_face"),
        1009 => Some("topology_add_iso_edge"),
        1010 => Some("topology_add_iso_node"),
        1011 => Some("topology_add_node"),
        1012 => Some("topology_add_topogeom_column"),
        1013 => Some("topology_as_gml"),
        1014 => Some("topology_as_topojson"),
        1015 => Some("topology_as_topojson_with_edge_map"),
        1016 => Some("topology_change_edge_geom"),
        1017 => Some("topology_copy_topology"),
        1018 => Some("topology_create"),
        1019 => Some("topology_create_topo_geom"),
        1020 => Some("topology_drop_topogeom_column"),
        1021 => Some("topology_drop_topology"),
        1022 => Some("topology_edge_count"),
        1023 => Some("topology_face_count"),
        1024 => Some("topology_from_bytes"),
        1025 => Some("topology_get_edge_by_point"),
        1026 => Some("topology_get_edge_geometry"),
        1027 => Some("topology_get_face_by_point"),
        1028 => Some("topology_get_face_containing_point"),
        1029 => Some("topology_get_face_edges"),
        1030 => Some("topology_get_face_geometry"),
        1031 => Some("topology_get_node_by_point"),
        1032 => Some("topology_get_node_edges"),
        1033 => Some("topology_get_node_geometry"),
        1034 => Some("topology_get_ring_edges"),
        1035 => Some("topology_get_topology_id"),
        1036 => Some("topology_get_topology_name"),
        1037 => Some("topology_get_topology_srid"),
        1038 => Some("topology_make_topology_precise"),
        1039 => Some("topology_mod_edge_heal"),
        1040 => Some("topology_mod_edge_split"),
        1041 => Some("topology_move_iso_node"),
        1042 => Some("topology_name"),
        1043 => Some("topology_new_edge_heal"),
        1044 => Some("topology_new_edges_split"),
        1045 => Some("topology_node_count"),
        1046 => Some("topology_polygonize"),
        1047 => Some("topology_precision"),
        1048 => Some("topology_rem_edge_mod_face"),
        1049 => Some("topology_rem_edge_new_face"),
        1050 => Some("topology_remove_face"),
        1051 => Some("topology_remove_iso_edge"),
        1052 => Some("topology_remove_iso_node"),
        1053 => Some("topology_remove_unused_primitives"),
        1054 => Some("topology_rename_topology"),
        1055 => Some("topology_srid"),
        1056 => Some("topology_summary"),
        1057 => Some("topology_to_bytes"),
        1058 => Some("topology_to_topogeom"),
        1059 => Some("topology_topogeo_add_geometry"),
        1060 => Some("topology_topogeo_add_linestring"),
        1061 => Some("topology_topogeo_add_point"),
        1062 => Some("topology_topogeo_add_polygon"),
        1063 => Some("topology_topogeo_load_geometry"),
        1064 => Some("topology_total_topology_size"),
        1065 => Some("topology_upgrade_topology"),
        1066 => Some("topology_validate"),
        1067 => Some("topology_validate_topology"),
        1068 => Some("topology_validate_topology_precision"),
        1069 => Some("total_topology_size"),
        1070 => Some("upgrade_topology"),
        1071 => Some("validate_topology"),
        1072 => Some("validate_topology_precision"),
        _ => None,
    }
}

/// SQL-name aliasing: translate a compact-form SQL name (e.g.
/// `st_geomfromtext`) to its WIT-canonical (long) form
/// (`st_geom_from_text`) before kebab-casing to the provider
/// method name. The catalog carries both spellings as scalars in
/// `leaf.scalars` (both must register with DuckDB so callers can
/// use either), and as first-class `[[aliases]]` entries. The
/// provider dispatch only matches the WIT-canonical form; the
/// bridge's `dispatch_call_scalar` calls `canonical_for(name)` so
/// both spellings route to the same provider arm.
fn canonical_for(name: &str) -> &str {
    match name {
        "st_3dextent" => "st_3d_extent",
        "st_clusterintersectingaggregate" => "st_clusterintersecting",
        "st_clusterwithinaggregate" => "st_clusterwithin",
        "st_makeline" => "st_make_line_aggregate",
        "st_makelineagg" => "st_make_line_aggregate",
        "st_makelineaggregate" => "st_make_line_aggregate",
        "st_memunion" => "st_mem_union_aggregate",
        "st_polygonizeagg" => "st_polygonize_aggregate",
        "st_polygonizeaggregate" => "st_polygonize_aggregate",
        "st_rast_union_aggregate" => "st_rast_union",
        "st_unionagg" => "st_union_aggregate",
        "st_unionaggregate" => "st_union_aggregate",
        "st_3dclosestpoint" => "st_3d_closest_point",
        "st_3dcontains" => "st_3d_contains",
        "st_3d_dwithin" => "st_3d_d_within",
        "st_3ddwithin" => "st_3d_d_within",
        "st_3dintersects" => "st_3d_intersects",
        "st_3dlineinterpolatepoint" => "st_3d_line_interpolate_point",
        "st_3dlongestline" => "st_3d_longest_line",
        "st_3dmakebox" => "st_3d_make_box",
        "st_3dmaxdistance" => "st_3d_max_distance",
        "st_3dshortestline" => "st_3d_shortest_line",
        "st_addband" => "st_add_band",
        "st_addface" => "st_add_face",
        "st_addisoedge" => "st_add_iso_edge",
        "st_addisonode" => "st_add_iso_node",
        "st_addmeasure" => "st_add_measure",
        "st_addpoint" => "st_add_point",
        "st_approximatemedialaxis" => "st_approximate_medial_axis",
        "st_areathreed" => "st_area_threed",
        "st_asbinary" => "st_as_binary",
        "st_asencodedpolyline" => "st_as_encoded_polyline",
        "st_asewkb" => "st_as_ewkb",
        "st_asewkt" => "st_as_ewkt",
        "st_asgeojson" => "st_as_geojson",
        "st_asgml" => "st_as_gml",
        "st_ashexewkb" => "st_as_hexewkb",
        "st_askml" => "st_as_kml",
        "st_aslatlontext" => "st_as_lat_lon_text",
        "st_asmarc21" => "st_as_marc21",
        "st_asmvt" => "st_as_mvt",
        "st_asmvtgeom" => "st_as_mvt_geom",
        "st_aspng" => "st_as_png",
        "st_assvg" => "st_as_svg",
        "st_astext" => "st_as_text",
        "st_astiff" => "st_as_tiff",
        "st_astopojson" => "st_as_topo_json",
        "st_astwkb" => "st_as_twkb",
        "st_asx3d" => "st_as_x3d",
        "st_bandnodatavalue" => "st_band_nodata_value",
        "st_bandpixeltype" => "st_band_pixel_type",
        "st_bdmpolyfromtext" => "st_bd_mpoly_from_text",
        "st_bdpolyfromtext" => "st_bd_poly_from_text",
        "st_boundarythreed" => "st_boundary_threed",
        "st_boundingdiagonal" => "st_bounding_diagonal",
        "st_box2dfromgeohash" => "st_box2d_from_geohash",
        "st_boxfromgeohash" => "st_box_from_geohash",
        "st_buildarea" => "st_build_area",
        "st_centroidthreed" => "st_centroid_threed",
        "st_chaikinsmoothing" => "st_chaikin_smoothing",
        "st_clipbybox2d" => "st_clip_by_box2d",
        "st_closestpoint" => "st_closest_point",
        "st_collectionextract" => "st_collection_extract",
        "st_collectionhomogenize" => "st_collection_homogenize",
        "st_concavehull" => "st_concave_hull",
        "st_constraineddelaunaytriangles" => "st_constrained_delaunay_triangles",
        "st_containsproperly" => "st_contains_properly",
        "st_convexhull" => "st_convex_hull",
        "st_coorddim" => "st_coord_dim",
        "st_coverageclean" => "st_coverage_clean",
        "st_coverageinvalidedges" => "st_coverage_invalid_edges",
        "st_coveragesimplify" => "st_coverage_simplify",
        "st_coveredby" => "st_covered_by",
        "st_cpawithin" => "st_cpa_within",
        "st_createtopology" => "st_create_topology",
        "st_curven" => "st_curve_n",
        "st_curvetoline" => "st_curve_to_line",
        "st_dwithin" => "st_d_within",
        "st_delaunaytriangles" => "st_delaunay_triangles",
        "st_dfullywithin" => "st_dfully_within",
        "st_distancecpa" => "st_distance_cpa",
        "st_distancesphere" => "st_distance_sphere",
        "st_distancespheroid" => "st_distance_spheroid",
        "st_distancethreed" => "st_distance_threed",
        "st_dumpaspolygons" => "st_dump_as_polygons",
        "st_dumpvalues" => "st_dump_values",
        "st_endpoint" => "st_end_point",
        "st_envelopethreed" => "st_envelope_threed",
        "st_equalsexact" => "st_equals_exact",
        "st_exteriorring" => "st_exterior_ring",
        "st_filterbym" => "st_filter_by_m",
        "st_flipcoordinates" => "st_flip_coordinates",
        "st_force_2d" => "st_force2d",
        "st_force_3d" => "st_force3d",
        "st_force_3dm" => "st_force3dm",
        "st_force_4d" => "st_force4d",
        "st_forcecollection" => "st_force_collection",
        "st_forcecurve" => "st_force_curve",
        "st_forcefourd" => "st_force_fourd",
        "st_forcepolygonccw" => "st_force_polygon_ccw",
        "st_forcepolygoncw" => "st_force_polygon_cw",
        "st_forcerhr" => "st_force_rhr",
        "st_forcesfs" => "st_force_sfs",
        "st_forcethreed" => "st_force_threed",
        "st_forcethreedm" => "st_force_threedm",
        "st_forcethreedz" => "st_force_threedz",
        "st_forcetwod" => "st_force_twod",
        "st_frechetdistance" => "st_frechet_distance",
        "st_fromflatgeobuf" => "st_from_flatgeobuf",
        "st_generatepoints" => "st_generate_points",
        "st_geogarea" => "st_geog_area",
        "st_geogazimuth" => "st_geog_azimuth",
        "st_geogbuffer" => "st_geog_buffer",
        "st_geogbufferwithsegs" => "st_geog_buffer_with_segs",
        "st_geogcentroid" => "st_geog_centroid",
        "st_geogcoveredby" => "st_geog_covered_by",
        "st_geogcovers" => "st_geog_covers",
        "st_geogdistance" => "st_geog_distance",
        "st_geogdwithin" => "st_geog_dwithin",
        "st_geogexpand" => "st_geog_expand",
        "st_geogfromtext" => "st_geog_from_text",
        "st_geogfromwkb" => "st_geog_from_wkb",
        "st_geoggeometrytype" => "st_geog_geometry_type",
        "st_geogintersects" => "st_geog_intersects",
        "st_geogisclosed" => "st_geog_is_closed",
        "st_geogisempty" => "st_geog_is_empty",
        "st_geogissimple" => "st_geog_is_simple",
        "st_geoglength" => "st_geog_length",
        "st_geognpoints" => "st_geog_npoints",
        "st_geogperimeter" => "st_geog_perimeter",
        "st_geogpoint" => "st_geog_point",
        "st_geogsummary" => "st_geog_summary",
        "st_geographyfromtext" => "st_geography_from_text",
        "st_geomfromewkb" => "st_geom_from_ewkb",
        "st_geomfromewkt" => "st_geom_from_ewkt",
        "st_geomfromgeobuf" => "st_geom_from_geobuf",
        "st_geomfromgeohash" => "st_geom_from_geohash",
        "st_geomfromgeojson" => "st_geom_from_geojson",
        "st_geomfromgml" => "st_geom_from_gml",
        "st_geomfromgmlsrid" => "st_geom_from_gml_srid",
        "st_geomfromhexewkb" => "st_geom_from_hexewkb",
        "st_geomfromkml" => "st_geom_from_kml",
        "st_geomfrommarc21" => "st_geom_from_marc21",
        "st_geomfromtext" => "st_geom_from_text",
        "st_geomfromtextsrid" => "st_geom_from_text_srid",
        "st_geomfromtwkb" => "st_geom_from_twkb",
        "st_geomfromwkb" => "st_geom_from_wkb",
        "st_geomcollfromtext" => "st_geomcoll_from_text",
        "st_geomcollfromwkb" => "st_geomcoll_from_wkb",
        "st_geometricmedian" => "st_geometric_median",
        "st_geometryfromtext" => "st_geometry_from_text",
        "st_geometryn" => "st_geometry_n",
        "st_geometrytype" => "st_geometry_type",
        "st_getedgebypoint" => "st_get_edge_by_point",
        "st_getedgegeometry" => "st_get_edge_geometry",
        "st_getfacegeometry" => "st_get_face_geometry",
        "st_getnodebypoint" => "st_get_node_by_point",
        "st_getnodegeometry" => "st_get_node_geometry",
        "st_gmltosql" => "st_gml_to_sql",
        "st_hasarc" => "st_has_arc",
        "st_hasm" => "st_has_m",
        "st_hasnoband" => "st_has_no_band",
        "st_hasz" => "st_has_z",
        "st_hausdorffdistance" => "st_hausdorff_distance",
        "st_hillshade" => "st_hill_shade",
        "st_interiorringn" => "st_interior_ring_n",
        "st_inversetransformpipeline" => "st_inverse_transform_pipeline",
        "st_isclosed" => "st_is_closed",
        "st_iscollection" => "st_is_collection",
        "st_isempty" => "st_is_empty",
        "st_ispolygonccw" => "st_is_polygon_ccw",
        "st_ispolygoncw" => "st_is_polygon_cw",
        "st_isring" => "st_is_ring",
        "st_issimple" => "st_is_simple",
        "st_isvalid" => "st_is_valid",
        "st_isvaliddetail" => "st_is_valid_detail",
        "st_isvalidreason" => "st_is_valid_reason",
        "st_isvalidtrajectory" => "st_is_valid_trajectory",
        "st_largestemptycircle" => "st_largest_empty_circle",
        "st_length_2d" => "st_length2d",
        "st_lengthspheroid" => "st_length_spheroid",
        "st_lengththreed" => "st_length_threed",
        "st_linecrossingdirection" => "st_line_crossing_direction",
        "st_lineextend" => "st_line_extend",
        "st_linefromencodedpolyline" => "st_line_from_encoded_polyline",
        "st_linefrommultipoint" => "st_line_from_multi_point",
        "st_linefromtext" => "st_line_from_text",
        "st_linefromwkb" => "st_line_from_wkb",
        "st_lineinterpolatepoint" => "st_line_interpolate_point",
        "st_lineinterpolatepoints" => "st_line_interpolate_points",
        "st_linelocatepoint" => "st_line_locate_point",
        "st_linemerge" => "st_line_merge",
        "st_linetocurve" => "st_line_to_curve",
        "st_locatealong" => "st_locate_along",
        "st_locatebetween" => "st_locate_between",
        "st_locatebetweenelevations" => "st_locate_between_elevations",
        "st_longestline" => "st_longest_line",
        "st_makebox2d" => "st_make_box2d",
        "st_makebox3d" => "st_make_box3d",
        "st_makeemptyraster" => "st_make_empty_raster",
        "st_makeenvelope" => "st_make_envelope",
        "st_makeenvelopesrid" => "st_make_envelope_srid",
        "st_makeline" => "st_make_line",
        "st_makepoint" => "st_make_point",
        "st_makepointm" => "st_make_point_m",
        "st_makepointz" => "st_make_point_z",
        "st_makepointzm" => "st_make_point_zm",
        "st_makepolygon" => "st_make_polygon",
        "st_makevalid" => "st_make_valid",
        "st_mapalgebra" => "st_map_algebra",
        "st_mapalgebra2" => "st_map_algebra2",
        "st_maxdistance" => "st_max_distance",
        "st_maximuminscribedcircle" => "st_maximum_inscribed_circle",
        "st_memsize" => "st_mem_size",
        "st_minimumboundingcircle" => "st_minimum_bounding_circle",
        "st_minimumboundingradius" => "st_minimum_bounding_radius",
        "st_minimumclearance" => "st_minimum_clearance",
        "st_minimumclearanceline" => "st_minimum_clearance_line",
        "st_mlinefromtext" => "st_mline_from_text",
        "st_mlinefromwkb" => "st_mline_from_wkb",
        "st_modedgeheal" => "st_mod_edge_heal",
        "st_modedgesplit" => "st_mod_edge_split",
        "st_mpointfromtext" => "st_mpoint_from_text",
        "st_mpointfromwkb" => "st_mpoint_from_wkb",
        "st_mpolyfromtext" => "st_mpoly_from_text",
        "st_mpolyfromwkb" => "st_mpoly_from_wkb",
        "st_ndims" => "st_n_dims",
        "st_nearestvalue" => "st_nearest_value",
        "st_newedgessplit" => "st_new_edges_split",
        "st_numbands" => "st_num_bands",
        "st_numcurves" => "st_num_curves",
        "st_numgeometries" => "st_num_geometries",
        "st_numinteriorring" => "st_num_interior_ring",
        "st_numinteriorrings" => "st_num_interior_rings",
        "st_numpoints" => "st_num_points",
        "st_offsetcurve" => "st_offset_curve",
        "st_orderingequals" => "st_ordering_equals",
        "st_orientedenvelope" => "st_oriented_envelope",
        "st_perimeter_2d" => "st_perimeter2d",
        "st_perimeterthreed" => "st_perimeter_threed",
        "st_pixelascentroid" => "st_pixel_as_centroid",
        "st_pixelaspoint" => "st_pixel_as_point",
        "st_pixelaspolygon" => "st_pixel_as_polygon",
        "st_pixelaspolygons" => "st_pixel_as_polygons",
        "st_pixelofvalue" => "st_pixel_of_value",
        "st_pointfromgeohash" => "st_point_from_geohash",
        "st_pointfromtext" => "st_point_from_text",
        "st_pointfromwkb" => "st_point_from_wkb",
        "st_pointinsidecircle" => "st_point_inside_circle",
        "st_pointm" => "st_point_m",
        "st_pointn" => "st_point_n",
        "st_pointonsurface" => "st_point_on_surface",
        "st_pointz" => "st_point_z",
        "st_pointzm" => "st_point_zm",
        "st_polygonfromtext" => "st_polygon_from_text",
        "st_polygonfromwkb" => "st_polygon_from_wkb",
        "st_quantizecoordinates" => "st_quantize_coordinates",
        "st_rastercontains" => "st_raster_contains",
        "st_rastercontainsgeom" => "st_raster_contains_geom",
        "st_rasterconvexhull" => "st_raster_convex_hull",
        "st_rastercovers" => "st_raster_covers",
        "st_rasterintersects" => "st_raster_intersects",
        "st_rasterintersectsgeom" => "st_raster_intersects_geom",
        "st_rasteroverlaps" => "st_raster_overlaps",
        "st_rastertoworldcoordx" => "st_raster_to_world_coord_x",
        "st_rastertoworldcoordy" => "st_raster_to_world_coord_y",
        "st_rasterwithin" => "st_raster_within",
        "st_reduceprecision" => "st_reduce_precision",
        "st_relatematch" => "st_relate_match",
        "st_removepoint" => "st_remove_point",
        "st_removerepeatedpoints" => "st_remove_repeated_points",
        "st_removesmallparts" => "st_remove_small_parts",
        "st_reversethreed" => "st_reverse_threed",
        "st_rotatethreed" => "st_rotate_threed",
        "st_scalethreed" => "st_scale_threed",
        "st_scalex" => "st_scale_x",
        "st_scaley" => "st_scale_y",
        "st_seteffectivearea" => "st_set_effective_area",
        "st_setpoint" => "st_set_point",
        "st_setsrid" => "st_set_srid",
        "st_setvalue" => "st_set_value",
        "st_sharedpaths" => "st_shared_paths",
        "st_shiftlongitude" => "st_shift_longitude",
        "st_shortestline" => "st_shortest_line",
        "st_simplifypolygonhull" => "st_simplify_polygon_hull",
        "st_simplifypreservetopology" => "st_simplify_preserve_topology",
        "st_simplifyvw" => "st_simplify_vw",
        "st_skewx" => "st_skew_x",
        "st_skewy" => "st_skew_y",
        "st_snaptogrid" => "st_snap_to_grid",
        "st_startpoint" => "st_start_point",
        "st_summarystats" => "st_summary_stats",
        "st_swapordinates" => "st_swap_ordinates",
        "st_symdifference" => "st_sym_difference",
        "st_tileenvelope" => "st_tile_envelope",
        "st_topologyedgecount" => "st_topology_edge_count",
        "st_topologyfacecount" => "st_topology_face_count",
        "st_topologyfrombytes" => "st_topology_from_bytes",
        "st_topologyname" => "st_topology_name",
        "st_topologynodecount" => "st_topology_node_count",
        "st_topologyprecision" => "st_topology_precision",
        "st_topologysrid" => "st_topology_srid",
        "st_topologytobytes" => "st_topology_to_bytes",
        "st_transformpipeline" => "st_transform_pipeline",
        "st_translatethreed" => "st_translate_threed",
        "st_triangulatepolygon" => "st_triangulate_polygon",
        "st_unaryunion" => "st_unary_union",
        "st_upperleftx" => "st_upper_left_x",
        "st_upperlefty" => "st_upper_left_y",
        "st_validatetopology" => "st_validate_topology",
        "st_valuecount" => "st_value_count",
        "st_voronoilines" => "st_voronoi_lines",
        "st_voronoipolygons" => "st_voronoi_polygons",
        "st_wkbtosql" => "st_wkb_to_sql",
        "st_wkttosql" => "st_wkt_to_sql",
        "st_worldtorastercoord" => "st_world_to_raster_coord",
        "st_worldtorastercoordcol" => "st_world_to_raster_coord_col",
        "st_worldtorastercoordrow" => "st_world_to_raster_coord_row",
        "st_wrapx" => "st_wrap_x",
        "st_dumppoints" => "st_dump_points",
        "st_dumprings" => "st_dump_rings",
        "st_dumpsegments" => "st_dump_segments",
        "st_hexagongrid" => "st_hexagon_grid",
        "st_squaregrid" => "st_square_grid",
        other => other,
    }
}

// ────────────────────────────────────────────────────────────
// Handle table + register block.
//
// Every scalar the catalog names gets exactly one runtime
// handle (allocated by NEXT_HANDLE.fetch_add at register time)
// and one dense arm_idx (assigned by codegen). `handle_table`
// maps handle → arm_idx; the dispatch arms (call_scalar +
// call_scalar_batch_col) look up the arm and delegate to the
// per-arm provider method through the CBOR envelope.
// ────────────────────────────────────────────────────────────

fn handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

// Phase 9.3.next: aggregate + table dispatch handle tables.
// Kept structurally uniform with the scalar handle_table so a
// future consolidation into a single enum-tagged table is a
// one-liner. Empty when the catalog has no aggregates / tables.
fn aggregate_handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

fn table_handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

fn identity_cast_handles() -> &'static std::sync::Mutex<std::collections::HashSet<u32>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashSet<u32>>> = std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashSet::new()))
}

static NEXT_HANDLE: AtomicU32 = AtomicU32::new(1);

fn register_logical_types() -> Result<(), Duckerror> {
    // BOX2D — cast-only registration (see emit_dynlink.rs comment).
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "BOX2D".into(), to: "BLOB".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(BOX2D -> BLOB) skipped: {}", e);
        }
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "BLOB".into(), to: "BOX2D".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(BLOB -> BOX2D) skipped: {}", e);
        }
    }
    // BOX3D — cast-only registration (see emit_dynlink.rs comment).
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "BOX3D".into(), to: "BLOB".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(BOX3D -> BLOB) skipped: {}", e);
        }
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "BLOB".into(), to: "BOX3D".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(BLOB -> BOX3D) skipped: {}", e);
        }
    }
    // GEOGRAPHY — cast-only registration (see emit_dynlink.rs comment).
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "GEOGRAPHY".into(), to: "BLOB".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(GEOGRAPHY -> BLOB) skipped: {}", e);
        }
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "BLOB".into(), to: "GEOGRAPHY".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(BLOB -> GEOGRAPHY) skipped: {}", e);
        }
    }
    // GEOMETRY — cast-only registration (see emit_dynlink.rs comment).
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "GEOMETRY".into(), to: "BLOB".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(GEOMETRY -> BLOB) skipped: {}", e);
        }
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "BLOB".into(), to: "GEOMETRY".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(BLOB -> GEOMETRY) skipped: {}", e);
        }
    }
    // RASTER — cast-only registration (see emit_dynlink.rs comment).
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "RASTER".into(), to: "BLOB".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(RASTER -> BLOB) skipped: {}", e);
        }
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "BLOB".into(), to: "RASTER".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(BLOB -> RASTER) skipped: {}", e);
        }
    }
    // TOPOGEOMETRY — cast-only registration (see emit_dynlink.rs comment).
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "TOPOGEOMETRY".into(), to: "BLOB".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(TOPOGEOMETRY -> BLOB) skipped: {}", e);
        }
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        identity_cast_handles().lock().expect("identity cast handles mutex poisoned").insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec { from: "BLOB".into(), to: "TOPOGEOMETRY".into(), kind: catalog::CastKind::Explicit };
        if let Err(e) = catalog::register_cast(&spec, callback) {
            eprintln!("[dynlink-emit] register-cast(BLOB -> TOPOGEOMETRY) skipped: {}", e);
        }
    }
    Ok(())
}


fn register_scalars() -> Result<(), Duckerror> {
    // Acquire the base scalar-registry capability from the host.
    // Fix (#834 followup): the previous implementation used
    // `runtime-ext.register-scalar-ex` with an empty arg list
    // (`args: Vec::new()` + `varargs: Some(&Logicaltype::Blob)`)
    // — variadic-Blob for every scalar. That erased the per-fn
    // arity + arg logicaltypes DuckDB needs at bind time (e.g.
    // `st_distance(GEOMETRY, GEOMETRY) -> DOUBLE` bound as
    // `st_distance(BLOB...) -> BLOB`, so callers threading typed
    // arguments couldn't overload-resolve). The base
    // `runtime.scalar-registry.register` path emits the actual
    // arity + per-arg + return logicaltype from the sibling
    // shim-interface DB (see `emit_dynlink::load_scalar_sigs`).
    let capability = runtime::get_capability(Capabilitykind::Scalar)
        .ok_or_else(|| Duckerror::Internal("host did not expose scalar capability".into()))?;
    let registry = match capability {
        runtime::Capability::Scalar(r) => r,
        _ => {
            return Err(Duckerror::Internal(
                "scalar capability returned unexpected variant".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("add_edge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["add_edge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "add_edge",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("add_edge_mod_face (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["add_edge_mod_face".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "add_edge_mod_face",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("add_edge_new_faces (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["add_edge_new_faces".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "add_edge_new_faces",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("add_face (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["add_face".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "add_face",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 4usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("add_iso_edge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["add_iso_edge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "add_iso_edge",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 5usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("add_iso_node (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["add_iso_node".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "add_iso_node",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 6usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("add_node (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["add_node".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "add_node",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 7usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("add_topogeom_column (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["add_topogeom_column".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "add_topogeom_column",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("as_gml (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["as_gml".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "as_gml",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 9usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("as_topojson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["as_topojson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "as_topojson",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 10usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("as_topojson_with_edge_map (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["as_topojson_with_edge_map".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "as_topojson_with_edge_map",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 11usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("build (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["build".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "build",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 12usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("change_edge_geom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["change_edge_geom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "change_edge_geom",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 13usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("copy_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["copy_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "copy_topology",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 14usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("create_index (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["create_index".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "create_index",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 15usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("create_raster (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["create_raster".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "create_raster",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 16usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("create_topo_geom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["create_topo_geom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "create_topo_geom",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 17usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("create_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["create_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "create_topology",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 18usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("destroy_index (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["destroy_index".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "destroy_index",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 19usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("drop_topogeom_column (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["drop_topogeom_column".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "drop_topogeom_column",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 20usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("drop_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["drop_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "drop_topology",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 21usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("from_binary (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["from_binary".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "from_binary",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 22usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("from_bytes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["from_bytes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "from_bytes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 23usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_edge_by_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_edge_by_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_edge_by_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 24usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_edge_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_edge_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_edge_geometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 25usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_face_by_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_face_by_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_face_by_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 26usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_face_containing_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_face_containing_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_face_containing_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 27usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_face_edges (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_face_edges".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_face_edges",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 28usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_face_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_face_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_face_geometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 29usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_node_by_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_node_by_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_node_by_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 30usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_node_edges (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_node_edges".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_node_edges",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 31usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_node_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_node_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_node_geometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 32usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_ring_edges (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_ring_edges".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_ring_edges",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 33usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_topology_id (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_topology_id".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_topology_id",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 34usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_topology_name (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_topology_name".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_topology_name",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 35usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("get_topology_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["get_topology_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "get_topology_srid",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 36usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("insert_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["insert_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "insert_wkb",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 37usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("list_cast_rewrites (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["list_cast_rewrites".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "list_cast_rewrites",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 38usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("list_operator_rewrites (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["list_operator_rewrites".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "list_operator_rewrites",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 39usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("list_preprocessor_patterns (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["list_preprocessor_patterns".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "list_preprocessor_patterns",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 40usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("make_topo_element (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["make_topo_element".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "make_topo_element",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 41usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("make_topology_precise (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["make_topology_precise".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "make_topology_precise",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 42usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("mod_edge_heal (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["mod_edge_heal".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "mod_edge_heal",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 43usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("mod_edge_split (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["mod_edge_split".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "mod_edge_split",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 44usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("move_iso_node (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["move_iso_node".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "move_iso_node",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 45usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("nearest (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["nearest".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "nearest",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 46usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("new_edge_heal (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["new_edge_heal".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "new_edge_heal",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 47usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("new_edges_split (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["new_edges_split".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "new_edges_split",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 48usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("op_bbox_distance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["op_bbox_distance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "op_bbox_distance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 49usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("op_bbox_intersects_nd (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["op_bbox_intersects_nd".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "op_bbox_intersects_nd",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 50usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("op_bbox_intersects_twod (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["op_bbox_intersects_twod".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "op_bbox_intersects_twod",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 51usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("op_equals_spatially (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["op_equals_spatially".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "op_equals_spatially",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 52usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("op_knn_distance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["op_knn_distance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "op_knn_distance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 53usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("polygonize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["polygonize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "polygonize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 54usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_addbbox (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_addbbox".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_addbbox",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 55usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_dropbbox (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_dropbbox".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_dropbbox",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 56usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_full_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_full_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_full_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 57usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_geos_compiled_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_geos_compiled_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_geos_compiled_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 58usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_geos_noop (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_geos_noop".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_geos_noop",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 59usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_geos_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_geos_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_geos_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 60usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_getbbox (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_getbbox".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_getbbox",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 61usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_hasbbox (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_hasbbox".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_hasbbox",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 62usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_lib_build_date (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_lib_build_date".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_lib_build_date",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 63usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_lib_revision (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_lib_revision".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_lib_revision",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 64usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_lib_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_lib_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_lib_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 65usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_libjson_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_libjson_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_libjson_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 66usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_liblwgeom_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_liblwgeom_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_liblwgeom_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 67usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_libprotobuf_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_libprotobuf_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_libprotobuf_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 68usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_libxml_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_libxml_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_libxml_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 69usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_noop (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_noop".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_noop",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 70usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_proj_compiled_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_proj_compiled_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_proj_compiled_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 71usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_proj_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_proj_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_proj_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 72usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_scripts_build_date (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_scripts_build_date".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_scripts_build_date",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 73usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_scripts_installed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_scripts_installed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_scripts_installed",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 74usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_scripts_released (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_scripts_released".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_scripts_released",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 75usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_svn_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_svn_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_svn_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 76usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_topology_scripts_installed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_topology_scripts_installed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_topology_scripts_installed",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 77usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_type_name (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_type_name".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_type_name",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 78usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 79usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_wagyu_version (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_wagyu_version".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_wagyu_version",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 80usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("query_envelope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["query_envelope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "query_envelope",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 81usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("query_knn (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["query_knn".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "query_knn",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 82usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("query_within_distance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["query_within_distance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "query_within_distance",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 83usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("rem_edge_mod_face (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["rem_edge_mod_face".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "rem_edge_mod_face",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 84usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("rem_edge_new_face (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["rem_edge_new_face".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "rem_edge_new_face",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 85usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("remove_face (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["remove_face".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "remove_face",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 86usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("remove_iso_edge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["remove_iso_edge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "remove_iso_edge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 87usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("remove_iso_node (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["remove_iso_node".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "remove_iso_node",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 88usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("remove_unused_primitives (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["remove_unused_primitives".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "remove_unused_primitives",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 89usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("rename_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["rename_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "rename_topology",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 90usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_closest_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3d_closest_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_closest_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 91usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3d_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 92usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_d_within (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3d_d_within".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_d_within",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 93usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_dwithin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3d_dwithin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_dwithin",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 94usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_intersects (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3d_intersects".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_intersects",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 95usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_line_interpolate_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3d_line_interpolate_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_line_interpolate_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 96usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_longest_line (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3d_longest_line".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_longest_line",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 97usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_make_box (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3d_make_box".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_make_box",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 98usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_max_distance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3d_max_distance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_max_distance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 99usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_shortest_line (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3d_shortest_line".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_shortest_line",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 100usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dboundary (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dboundary".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dboundary",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 101usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dcentroid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dcentroid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dcentroid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 102usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dclosestpoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dclosestpoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dclosestpoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 103usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dcontains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dcontains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dcontains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 104usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dconvexhull (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dconvexhull".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dconvexhull",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 105usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3ddifference (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3ddifference".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3ddifference",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 106usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3ddistance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3ddistance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3ddistance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 107usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3ddwithin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3ddwithin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3ddwithin",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 108usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3denvelope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3denvelope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3denvelope",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 109usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dintersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dintersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dintersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 110usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dintersects (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dintersects".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dintersects",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 111usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dlength (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dlength".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dlength",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 112usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dlineinterpolatepoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dlineinterpolatepoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dlineinterpolatepoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 113usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dlongestline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dlongestline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dlongestline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 114usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dmakebox (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dmakebox".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dmakebox",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 115usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dmaxdistance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dmaxdistance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dmaxdistance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 116usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dperimeter (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dperimeter".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dperimeter",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 117usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dreverse (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dreverse".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dreverse",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 118usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3drotate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3drotate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3drotate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 119usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dscale (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dscale".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dscale",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 120usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dshortestline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dshortestline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dshortestline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 121usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dtranslate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dtranslate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dtranslate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 122usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dunion (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dunion".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dunion",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 123usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_add_band (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_add_band".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_add_band",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 124usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_add_face (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_add_face".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_add_face",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 125usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_add_iso_edge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_add_iso_edge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_add_iso_edge",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 126usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_add_iso_node (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_add_iso_node".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_add_iso_node",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 127usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_add_measure (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_add_measure".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_add_measure",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 128usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_add_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_add_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_add_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 129usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_addband (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_addband".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_addband",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 130usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_addface (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_addface".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_addface",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 131usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_addisoedge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_addisoedge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_addisoedge",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 132usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_addisonode (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_addisonode".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_addisonode",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 133usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_addmeasure (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_addmeasure".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_addmeasure",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 134usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_addpoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_addpoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_addpoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 135usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg6".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_affine (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_affine".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_affine",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 136usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_angle (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_angle".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_angle",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 137usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_histogram (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_approx_histogram".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_histogram",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 138usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_quantile (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_approx_quantile".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_quantile",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 139usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_quantiles (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_approx_quantiles".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_quantiles",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 140usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_summary_stats (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_approx_summary_stats".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_summary_stats",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 141usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_value_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_approx_value_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_value_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 142usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_value_counts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_approx_value_counts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_value_counts",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 143usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_value_percent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_approx_value_percent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_value_percent",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 144usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_value_percents (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_approx_value_percents".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_value_percents",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 145usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_approximate_medial_axis (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_approximate_medial_axis".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_approximate_medial_axis",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 146usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_approximatemedialaxis (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_approximatemedialaxis".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_approximatemedialaxis",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 147usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_area (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_area".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_area",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 148usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_area_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_area_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_area_threed",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 149usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_area_twod (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_area_twod".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_area_twod",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 150usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_areathreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_areathreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_areathreed",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 151usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_binary (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_binary".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_binary",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 152usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_encoded_polyline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_encoded_polyline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_encoded_polyline",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 153usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_ewkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_ewkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_ewkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 154usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_ewkt",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 155usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_ewkt_precision (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_ewkt_precision".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_ewkt_precision",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 156usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_flatgeobuf (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_flatgeobuf".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_flatgeobuf",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 157usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_gdal_raster (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_gdal_raster".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_gdal_raster",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 158usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_geobuf (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_geobuf".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_geobuf",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 159usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_geojson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_geojson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_geojson",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 160usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_geojson_full (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_geojson_full".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_geojson_full",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 161usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_gml (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_gml".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_gml",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 162usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_gml_full (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_gml_full".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_gml_full",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 163usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_hexewkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_hexewkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_hexewkb",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 164usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_jpeg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_jpeg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_jpeg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 165usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_kml (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_kml".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_kml",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 166usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_kml_full (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_kml_full".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_kml_full",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 167usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_lat_lon_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_lat_lon_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_lat_lon_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 168usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_marc21 (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_marc21".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_marc21",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 169usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_marc21_fmt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_marc21_fmt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_marc21_fmt",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 170usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_mvt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_mvt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_mvt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 171usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg6".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg7".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_mvt_geom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_mvt_geom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_mvt_geom",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 172usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_png (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_png".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_png",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 173usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_svg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_svg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_svg",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 174usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_svg_full (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_svg_full".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_svg_full",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 175usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_text",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 176usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_text_precision (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_text_precision".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_text_precision",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 177usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_tiff (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_tiff".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_tiff",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 178usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_topo_json (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_topo_json".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_topo_json",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 179usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_twkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_twkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_twkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 180usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_x3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_x3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_x3d",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 181usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_as_x3d_full (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_as_x3d_full".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_as_x3d_full",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 182usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_asbinary (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_asbinary".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_asbinary",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 183usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_asencodedpolyline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_asencodedpolyline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_asencodedpolyline",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 184usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_asewkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_asewkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_asewkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 185usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_asewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_asewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_asewkt",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 186usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_asgeojson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_asgeojson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_asgeojson",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 187usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_asgml (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_asgml".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_asgml",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 188usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_ashexewkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_ashexewkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_ashexewkb",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 189usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_askml (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_askml".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_askml",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 190usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_aslatlontext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_aslatlontext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_aslatlontext",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 191usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_asmarc21 (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_asmarc21".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_asmarc21",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 192usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_asmvt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_asmvt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_asmvt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 193usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg6".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg7".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_asmvtgeom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_asmvtgeom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_asmvtgeom",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 194usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_aspect (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_aspect".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_aspect",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 195usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_aspng (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_aspng".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_aspng",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 196usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_assvg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_assvg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_assvg",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 197usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_astext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_astext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_astext",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 198usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_astiff (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_astiff".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_astiff",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 199usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_astopojson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_astopojson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_astopojson",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 200usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_astwkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_astwkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_astwkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 201usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_asx3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_asx3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_asx3d",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 202usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_azimuth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_azimuth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_azimuth",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 203usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_band_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_band_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_band_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 204usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_band_max (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_band_max".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_band_max",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 205usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_band_mean (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_band_mean".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_band_mean",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 206usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_band_metadata (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_band_metadata".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_band_metadata",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 207usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_band_min (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_band_min".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_band_min",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 208usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_band_nodata_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_band_nodata_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_band_nodata_value",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 209usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_band_pixel_type (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_band_pixel_type".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_band_pixel_type",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 210usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_band_stddev (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_band_stddev".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_band_stddev",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 211usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_band_sum (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_band_sum".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_band_sum",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 212usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_bandnodatavalue (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_bandnodatavalue".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_bandnodatavalue",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 213usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_bandpixeltype (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_bandpixeltype".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_bandpixeltype",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 214usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_bands_metadata (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_bands_metadata".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_bands_metadata",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 215usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_bboxdistance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_bboxdistance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_bboxdistance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 216usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_bboxintersects (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_bboxintersects".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_bboxintersects",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 217usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_bboxintersectsnd (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_bboxintersectsnd".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_bboxintersectsnd",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 218usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_bd_mpoly_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_bd_mpoly_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_bd_mpoly_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 219usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_bd_poly_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_bd_poly_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_bd_poly_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 220usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_bdmpolyfromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_bdmpolyfromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_bdmpolyfromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 221usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_bdpolyfromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_bdpolyfromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_bdpolyfromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 222usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_boundary (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_boundary".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_boundary",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 223usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_boundary_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_boundary_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_boundary_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 224usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_boundarythreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_boundarythreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_boundarythreed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 225usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_bounding_diagonal (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_bounding_diagonal".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_bounding_diagonal",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 226usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_boundingdiagonal (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_boundingdiagonal".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_boundingdiagonal",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 227usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_box2d_from_geohash (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_box2d_from_geohash".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_box2d_from_geohash",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 228usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_box2dfromgeohash (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_box2dfromgeohash".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_box2dfromgeohash",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 229usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_box_from_geohash (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_box_from_geohash".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_box_from_geohash",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 230usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_boxfromgeohash (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_boxfromgeohash".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_boxfromgeohash",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 231usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_buffer (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_buffer".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_buffer",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 232usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_buffer_quad_segs (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_buffer_quad_segs".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_buffer_quad_segs",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 233usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_buffer_with_style (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_buffer_with_style".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_buffer_with_style",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 234usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_build_area (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_build_area".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_build_area",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 235usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_buildarea (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_buildarea".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_buildarea",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 236usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_centroid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_centroid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_centroid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 237usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_centroid_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_centroid_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_centroid_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 238usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_centroidthreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_centroidthreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_centroidthreed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 239usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_chaikin_smoothing (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_chaikin_smoothing".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_chaikin_smoothing",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 240usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_chaikinsmoothing (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_chaikinsmoothing".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_chaikinsmoothing",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 241usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_clip_by_box2d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_clip_by_box2d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_clip_by_box2d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 242usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_clipbybox2d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_clipbybox2d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_clipbybox2d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 243usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_closest_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_closest_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_closest_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 244usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_closest_point_3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_closest_point_3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_closest_point_3d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 245usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_closest_point_of_approach (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_closest_point_of_approach".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_closest_point_of_approach",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 246usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_closest_point_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_closest_point_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_closest_point_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 247usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_closestpoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_closestpoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_closestpoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 248usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_collect (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_collect".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_collect",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 249usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_collection_extract (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_collection_extract".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_collection_extract",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 250usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_collection_homogenize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_collection_homogenize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_collection_homogenize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 251usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_collectionextract (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_collectionextract".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_collectionextract",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 252usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_collectionhomogenize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_collectionhomogenize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_collectionhomogenize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 253usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_concave_hull (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_concave_hull".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_concave_hull",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 254usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_concave_hull_holes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_concave_hull_holes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_concave_hull_holes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 255usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_concavehull (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_concavehull".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_concavehull",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 256usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_constrained_delaunay_triangles (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_constrained_delaunay_triangles".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_constrained_delaunay_triangles",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 257usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_constraineddelaunaytriangles (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_constraineddelaunaytriangles".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_constraineddelaunaytriangles",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 258usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_constrainedelaunaytriangles (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_constrainedelaunaytriangles".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_constrainedelaunaytriangles",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 259usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 260usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_contains_3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_contains_3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_contains_3d",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 261usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_contains_properly (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_contains_properly".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_contains_properly",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 262usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_contains_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_contains_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_contains_threed",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 263usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_containsproperly (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_containsproperly".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_containsproperly",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 264usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_convex_hull (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_convex_hull".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_convex_hull",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 265usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_convexhull (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_convexhull".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_convexhull",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 266usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_coord_dim (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_coord_dim".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_coord_dim",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 267usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_coorddim (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_coorddim".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_coorddim",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 268usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 269usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_coverage_clean (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_coverage_clean".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_coverage_clean",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 270usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_coverage_invalid_edges (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_coverage_invalid_edges".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_coverage_invalid_edges",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 271usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_coverage_simplify (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_coverage_simplify".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_coverage_simplify",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 272usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_coverage_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_coverage_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_coverage_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 273usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_coverageclean (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_coverageclean".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_coverageclean",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 274usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_coverageinvalidedges (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_coverageinvalidedges".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_coverageinvalidedges",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 275usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_coveragesimplify (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_coveragesimplify".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_coveragesimplify",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 276usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_covered_by (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_covered_by".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_covered_by",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 277usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_coveredby (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_coveredby".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_coveredby",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 278usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_covers (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_covers".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_covers",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 279usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_covers_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_covers_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_covers_threed",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 280usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_cpa_within (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_cpa_within".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_cpa_within",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 281usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_cpawithin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_cpawithin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_cpawithin",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 282usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_create_topo_geo (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_create_topo_geo".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_create_topo_geo",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 283usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_create_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_create_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_create_topology",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 284usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_createtopology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_createtopology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_createtopology",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 285usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_crosses (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_crosses".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_crosses",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 286usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_curve_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_curve_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_curve_n",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 287usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_curve_to_line (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_curve_to_line".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_curve_to_line",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 288usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_curven (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_curven".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_curven",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 289usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_curvetoline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_curvetoline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_curvetoline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 290usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_d_within (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_d_within".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_d_within",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 291usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_delaunay_triangles (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_delaunay_triangles".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_delaunay_triangles",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 292usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_delaunaytriangles (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_delaunaytriangles".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_delaunaytriangles",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 293usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dfully_within (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dfully_within".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dfully_within",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 294usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dfully_within_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dfully_within_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dfully_within_threed",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 295usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dfullywithin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dfullywithin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dfullywithin",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 296usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_difference (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_difference".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_difference",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 297usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_difference_gridsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_difference_gridsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_difference_gridsize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 298usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dimension (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dimension".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dimension",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 299usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_disjoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_disjoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_disjoint",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 300usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_distance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_distance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_distance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 301usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_cpa (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_distance_cpa".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_cpa",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 302usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_sphere (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_distance_sphere".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_sphere",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 303usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_sphere_radius (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_distance_sphere_radius".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_sphere_radius",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 304usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_spheroid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_distance_spheroid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_spheroid",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 305usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_distance_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_threed",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 306usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_distancecpa (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_distancecpa".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_distancecpa",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 307usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_distancesphere (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_distancesphere".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_distancesphere",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 308usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_distancespheroid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_distancespheroid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_distancespheroid",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 309usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_distancethreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_distancethreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_distancethreed",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 310usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dump_as_polygons (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dump_as_polygons".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dump_as_polygons",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 311usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dump_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dump_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dump_values",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 312usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dump_values_multi (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dump_values_multi".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dump_values_multi",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 313usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dumpaspolygons (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dumpaspolygons".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dumpaspolygons",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 314usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dumpvalues (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dumpvalues".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dumpvalues",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 315usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dwithin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dwithin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dwithin",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 316usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dwithin_3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dwithin_3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dwithin_3d",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 317usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_dwithin_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_dwithin_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_dwithin_threed",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 318usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_end_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_end_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_end_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 319usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_endpoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_endpoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_endpoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 320usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_envelope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_envelope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_envelope",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 321usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_envelope_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_envelope_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_envelope_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 322usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_envelopethreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_envelopethreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_envelopethreed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 323usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_equals (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_equals".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_equals",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 324usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_equals_exact (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_equals_exact".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_equals_exact",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 325usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_equalsexact (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_equalsexact".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_equalsexact",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 326usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_expand (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_expand".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_expand",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 327usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_expand_xy (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_expand_xy".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_expand_xy",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 328usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_expand_xyz (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_expand_xyz".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_expand_xyz",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 329usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_expand_xyzm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_expand_xyzm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_expand_xyzm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 330usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_exterior_ring (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_exterior_ring".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_exterior_ring",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 331usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_exteriorring (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_exteriorring".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_exteriorring",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 332usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_filter_by_m (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_filter_by_m".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_filter_by_m",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 333usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_filterbym (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_filterbym".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_filterbym",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 334usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_flip_coordinates (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_flip_coordinates".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_flip_coordinates",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 335usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_flipcoordinates (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_flipcoordinates".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_flipcoordinates",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 336usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force2d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force2d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force2d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 337usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force3d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 338usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force3dm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force3dm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force3dm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 339usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force3dz (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force3dz".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force3dz",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 340usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force4d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force4d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force4d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 341usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_2d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_2d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_2d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 342usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_3d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 343usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_3dm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_3dm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_3dm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 344usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_4d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_4d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_4d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 345usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_collection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_collection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_collection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 346usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_curve (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_curve".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_curve",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 347usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_fourd (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_fourd".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_fourd",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 348usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_polygon_ccw (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_polygon_ccw".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_polygon_ccw",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 349usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_polygon_cw (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_polygon_cw".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_polygon_cw",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 350usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_rhr (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_rhr".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_rhr",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 351usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_sfs (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_sfs".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_sfs",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 352usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 353usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_threedm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_threedm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_threedm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 354usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_threedz (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_threedz".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_threedz",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 355usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_force_twod (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_force_twod".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_force_twod",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 356usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_forcecollection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_forcecollection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_forcecollection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 357usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_forcecurve (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_forcecurve".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_forcecurve",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 358usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_forcefourd (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_forcefourd".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_forcefourd",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 359usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_forcepolygonccw (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_forcepolygonccw".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_forcepolygonccw",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 360usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_forcepolygoncw (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_forcepolygoncw".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_forcepolygoncw",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 361usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_forcerhr (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_forcerhr".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_forcerhr",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 362usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_forcesfs (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_forcesfs".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_forcesfs",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 363usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_forcethreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_forcethreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_forcethreed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 364usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_forcethreedm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_forcethreedm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_forcethreedm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 365usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_forcethreedz (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_forcethreedz".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_forcethreedz",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 366usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_forcetwod (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_forcetwod".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_forcetwod",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 367usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_frechet_distance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_frechet_distance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_frechet_distance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 368usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_frechetdistance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_frechetdistance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_frechetdistance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 369usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_from_flatgeobuf (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_from_flatgeobuf".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_from_flatgeobuf",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 370usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_fromflatgeobuf (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_fromflatgeobuf".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_fromflatgeobuf",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 371usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_generate_points (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_generate_points".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_generate_points",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 372usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_generatepoints (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_generatepoints".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_generatepoints",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 373usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geo_reference (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geo_reference".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geo_reference",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 374usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_area (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_area".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_area",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 375usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_azimuth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_azimuth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_azimuth",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 376usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_buffer (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_buffer".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_buffer",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 377usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_buffer_with_segs (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_buffer_with_segs".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_buffer_with_segs",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 378usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_centroid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_centroid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_centroid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 379usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_closest_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_closest_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_closest_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 380usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_convex_hull (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_convex_hull".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_convex_hull",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 381usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_covered_by (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_covered_by".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_covered_by",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 382usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_covers (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_covers".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_covers",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 383usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_difference (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_difference".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_difference",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 384usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_distance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_distance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_distance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 385usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_dwithin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_dwithin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_dwithin",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 386usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_expand (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_expand".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_expand",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 387usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 388usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 389usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 390usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_geometry_type (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_geometry_type".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_geometry_type",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 391usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 392usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_intersects (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_intersects".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_intersects",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 393usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_is_closed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_is_closed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_is_closed",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 394usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_is_empty (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_is_empty".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_is_empty",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 395usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_is_simple (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_is_simple".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_is_simple",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 396usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_length (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_length".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_length",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 397usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_line_substring (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_line_substring".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_line_substring",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 398usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_npoints (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_npoints".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_npoints",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 399usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_perimeter (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_perimeter".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_perimeter",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 400usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 401usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_project (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_project".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_project",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 402usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_segmentize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_segmentize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_segmentize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 403usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_summary (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_summary".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_summary",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 404usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_sym_difference (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_sym_difference".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_sym_difference",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 405usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_to_geom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_to_geom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_to_geom",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 406usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geog_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 407usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogarea (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogarea".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogarea",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 408usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogazimuth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogazimuth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogazimuth",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 409usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogbuffer (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogbuffer".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogbuffer",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 410usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogbufferwithsegs (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogbufferwithsegs".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogbufferwithsegs",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 411usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogcentroid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogcentroid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogcentroid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 412usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogcoveredby (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogcoveredby".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogcoveredby",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 413usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogcovers (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogcovers".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogcovers",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 414usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogdistance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogdistance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogdistance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 415usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogdwithin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogdwithin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogdwithin",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 416usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogexpand (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogexpand".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogexpand",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 417usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogfromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogfromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogfromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 418usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogfromwkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogfromwkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogfromwkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 419usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geoggeometrytype (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geoggeometrytype".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geoggeometrytype",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 420usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogintersects (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogintersects".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogintersects",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 421usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogisclosed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogisclosed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogisclosed",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 422usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogisempty (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogisempty".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogisempty",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 423usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogissimple (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogissimple".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogissimple",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 424usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geoglength (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geoglength".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geoglength",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 425usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geognpoints (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geognpoints".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geognpoints",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 426usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogperimeter (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogperimeter".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogperimeter",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 427usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogpoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogpoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogpoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 428usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geography_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geography_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geography_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 429usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geographyfromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geographyfromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geographyfromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 430usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geogsummary (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geogsummary".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geogsummary",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 431usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geohash (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geohash".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geohash",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 432usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_ewkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_ewkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_ewkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 433usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_ewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_ewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_ewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 434usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_geobuf (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_geobuf".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_geobuf",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 435usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_geohash (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_geohash".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_geohash",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 436usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_geojson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_geojson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_geojson",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 437usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_gml (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_gml".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_gml",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 438usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_gml_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_gml_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_gml_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 439usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_hexewkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_hexewkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_hexewkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 440usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_kml (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_kml".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_kml",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 441usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_marc21 (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_marc21".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_marc21",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 442usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 443usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_text_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_text_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_text_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 444usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_twkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_twkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_twkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 445usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geom_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 446usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomcoll_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomcoll_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcoll_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 447usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomcoll_from_text_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomcoll_from_text_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcoll_from_text_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 448usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomcoll_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomcoll_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcoll_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 449usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomcoll_from_wkb_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomcoll_from_wkb_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcoll_from_wkb_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 450usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomcollfromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomcollfromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcollfromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 451usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomcollfromwkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomcollfromwkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcollfromwkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 452usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomequal (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomequal".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomequal",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 453usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geometric_median (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geometric_median".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geometric_median",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 454usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geometricmedian (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geometricmedian".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geometricmedian",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 455usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geometry_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geometry_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geometry_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 456usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geometry_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geometry_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geometry_n",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 457usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geometry_type (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geometry_type".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geometry_type",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 458usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geometryfromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geometryfromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geometryfromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 459usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geometryn (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geometryn".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geometryn",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 460usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geometrytype (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geometrytype".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geometrytype",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 461usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromewkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromewkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromewkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 462usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromewkt (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromewkt".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromewkt",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 463usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromgeobuf (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromgeobuf".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromgeobuf",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 464usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromgeohash (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromgeohash".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromgeohash",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 465usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromgeojson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromgeojson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromgeojson",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 466usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromgml (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromgml".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromgml",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 467usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromgmlsrid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromgmlsrid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromgmlsrid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 468usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromhexewkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromhexewkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromhexewkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 469usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromkml (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromkml".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromkml",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 470usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfrommarc21 (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfrommarc21".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfrommarc21",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 471usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 472usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromtextsrid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromtextsrid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromtextsrid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 473usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromtwkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromtwkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromtwkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 474usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromwkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_geomfromwkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromwkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 475usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_get_edge_by_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_get_edge_by_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_get_edge_by_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 476usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_get_edge_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_get_edge_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_get_edge_geometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 477usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_get_face_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_get_face_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_get_face_geometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 478usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_get_node_by_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_get_node_by_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_get_node_by_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 479usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_get_node_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_get_node_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_get_node_geometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 480usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_getedgebypoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_getedgebypoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_getedgebypoint",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 481usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_getedgegeometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_getedgegeometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_getedgegeometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 482usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_getfacegeometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_getfacegeometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_getfacegeometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 483usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_getnodebypoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_getnodebypoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_getnodebypoint",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 484usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_getnodegeometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_getnodegeometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_getnodegeometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 485usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_gml_to_sql (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_gml_to_sql".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_gml_to_sql",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 486usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_gmltosql (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_gmltosql".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_gmltosql",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 487usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_has_arc (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_has_arc".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_has_arc",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 488usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_has_m (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_has_m".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_has_m",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 489usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_has_no_band (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_has_no_band".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_has_no_band",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 490usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_has_z (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_has_z".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_has_z",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 491usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_hasarc (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_hasarc".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_hasarc",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 492usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_hasm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_hasm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_hasm",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 493usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_hasnoband (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_hasnoband".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_hasnoband",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 494usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_hasz (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_hasz".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_hasz",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 495usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_hausdorff_distance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_hausdorff_distance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_hausdorff_distance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 496usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_hausdorffdistance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_hausdorffdistance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_hausdorffdistance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 497usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_height (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_height".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_height",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 498usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_hexagon (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_hexagon".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_hexagon",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 499usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_hill_shade (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_hill_shade".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_hill_shade",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 500usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_hillshade (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_hillshade".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_hillshade",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 501usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_histogram (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_histogram".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_histogram",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 502usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_init_topo_geo (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_init_topo_geo".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_init_topo_geo",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 503usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_interior_ring_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_interior_ring_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_interior_ring_n",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 504usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_interiorringn (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_interiorringn".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_interiorringn",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 505usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_interpolate_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_interpolate_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_interpolate_point",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 506usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_intersection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_intersection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_intersection",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 507usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_intersection_gridsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_intersection_gridsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_intersection_gridsize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 508usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_intersects (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_intersects".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_intersects",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 509usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_intersects_3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_intersects_3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_intersects_3d",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 510usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_intersects_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_intersects_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_intersects_threed",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 511usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_inverse_transform_pipeline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_inverse_transform_pipeline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_inverse_transform_pipeline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 512usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_inversetransformpipeline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_inversetransformpipeline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_inversetransformpipeline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 513usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_closed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_closed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_closed",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 514usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_collection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_collection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_collection",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 515usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_empty (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_empty".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_empty",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 516usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_measured (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_measured".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_measured",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 517usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_polygon_ccw (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_polygon_ccw".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_polygon_ccw",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 518usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_polygon_cw (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_polygon_cw".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_polygon_cw",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 519usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_ring (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_ring".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_ring",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 520usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_simple (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_simple".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_simple",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 521usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_valid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 522usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_detail (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_valid_detail".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_detail",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 523usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_detail_flags (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_valid_detail_flags".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_detail_flags",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 524usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_flags (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_valid_flags".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_flags",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 525usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_reason (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_valid_reason".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_reason",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 526usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_reason_flags (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_valid_reason_flags".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_reason_flags",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 527usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_trajectory (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_is_valid_trajectory".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_trajectory",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 528usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_isclosed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_isclosed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_isclosed",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 529usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_iscollection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_iscollection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_iscollection",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 530usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_isempty (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_isempty".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_isempty",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 531usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_ispolygonccw (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_ispolygonccw".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_ispolygonccw",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 532usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_ispolygoncw (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_ispolygoncw".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_ispolygoncw",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 533usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_isring (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_isring".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_isring",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 534usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_issimple (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_issimple".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_issimple",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 535usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_isvalid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_isvalid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_isvalid",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 536usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_isvaliddetail (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_isvaliddetail".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_isvaliddetail",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 537usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_isvalidreason (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_isvalidreason".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_isvalidreason",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 538usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_isvalidtrajectory (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_isvalidtrajectory".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_isvalidtrajectory",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 539usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_knndistance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_knndistance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_knndistance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 540usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_largest_empty_circle (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_largest_empty_circle".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_largest_empty_circle",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 541usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_largestemptycircle (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_largestemptycircle".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_largestemptycircle",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 542usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_length (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_length".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_length",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 543usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_length2d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_length2d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_length2d",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 544usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_length_2d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_length_2d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_length_2d",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 545usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_length_spheroid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_length_spheroid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_length_spheroid",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 546usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_length_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_length_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_length_threed",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 547usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_length_twod (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_length_twod".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_length_twod",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 548usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_length_twod_spheroid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_length_twod_spheroid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_length_twod_spheroid",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 549usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_lengthspheroid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_lengthspheroid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_lengthspheroid",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 550usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_lengththreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_lengththreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_lengththreed",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 551usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_letters (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_letters".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_letters",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 552usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_crossing_direction (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_crossing_direction".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_crossing_direction",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 553usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_extend (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_extend".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_extend",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 554usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_encoded_polyline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_from_encoded_polyline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_encoded_polyline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 555usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_multi_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_from_multi_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_multi_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 556usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 557usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_text_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_from_text_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_text_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 558usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 559usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_wkb_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_from_wkb_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_wkb_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 560usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_interpolate_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_interpolate_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_interpolate_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 561usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_interpolate_point_3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_interpolate_point_3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_interpolate_point_3d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 562usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_interpolate_point_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_interpolate_point_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_interpolate_point_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 563usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_interpolate_points (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_interpolate_points".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_interpolate_points",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 564usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_locate_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_locate_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_locate_point",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 565usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_merge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_merge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_merge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 566usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_merge_directed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_merge_directed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_merge_directed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 567usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_line_to_curve (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_line_to_curve".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_line_to_curve",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 568usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_linecrossingdirection (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_linecrossingdirection".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_linecrossingdirection",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 569usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_lineextend (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_lineextend".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_lineextend",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 570usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_linefromencodedpolyline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_linefromencodedpolyline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_linefromencodedpolyline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 571usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_linefrommultipoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_linefrommultipoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_linefrommultipoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 572usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_linefromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_linefromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_linefromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 573usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_linefromwkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_linefromwkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_linefromwkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 574usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_lineinterpolatepoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_lineinterpolatepoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_lineinterpolatepoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 575usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_lineinterpolatepoints (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_lineinterpolatepoints".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_lineinterpolatepoints",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 576usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_linelocatepoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_linelocatepoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_linelocatepoint",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 577usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_linemerge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_linemerge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_linemerge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 578usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_linetocurve (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_linetocurve".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_linetocurve",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 579usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_locate_along (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_locate_along".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_locate_along",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 580usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_locate_between (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_locate_between".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_locate_between",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 581usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_locate_between_elevations (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_locate_between_elevations".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_locate_between_elevations",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 582usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_locatealong (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_locatealong".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_locatealong",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 583usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_locatebetween (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_locatebetween".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_locatebetween",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 584usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_locatebetweenelevations (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_locatebetweenelevations".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_locatebetweenelevations",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 585usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_longest_line (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_longest_line".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_longest_line",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 586usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_longest_line_3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_longest_line_3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_longest_line_3d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 587usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_longest_line_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_longest_line_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_longest_line_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 588usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_longestline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_longestline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_longestline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 589usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_m (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_m".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_m",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 590usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_box2d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_box2d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_box2d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 591usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_box3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_box3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_box3d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 592usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_box_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_box_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_box_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 593usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg6".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg7".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg8".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg9".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg10".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_empty_coverage (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_empty_coverage".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_empty_coverage",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 594usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg6".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg7".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg8".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_empty_raster (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_empty_raster".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_empty_raster",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 595usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_envelope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_envelope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_envelope",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 596usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_envelope_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_envelope_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_envelope_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 597usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_line (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_line".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_line",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 598usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_line_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_line_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_line_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 599usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_line_two (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_line_two".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_line_two",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 600usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 601usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_point_m (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_point_m".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_point_m",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 602usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_point_z (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_point_z".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_point_z",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 603usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_point_zm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_point_zm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_point_zm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 604usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_polygon (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_polygon".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_polygon",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 605usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_valid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_valid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_valid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 606usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makebox (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makebox".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makebox",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 607usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makebox2d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makebox2d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makebox2d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 608usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makebox3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makebox3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makebox3d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 609usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg6".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg7".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg8".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makeemptyraster (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makeemptyraster".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makeemptyraster",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 610usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makeenvelope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makeenvelope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makeenvelope",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 611usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makeenvelopesrid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makeenvelopesrid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makeenvelopesrid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 612usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makeline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makeline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makeline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 613usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makepoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makepoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makepoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 614usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makepointm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makepointm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makepointm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 615usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makepointz (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makepointz".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makepointz",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 616usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makepointzm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makepointzm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makepointzm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 617usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makepolygon (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makepolygon".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makepolygon",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 618usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makevalid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makevalid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makevalid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 619usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_map_algebra (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_map_algebra".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_map_algebra",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 620usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_map_algebra2 (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_map_algebra2".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_map_algebra2",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 621usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mapalgebra (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mapalgebra".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mapalgebra",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 622usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mapalgebra2 (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mapalgebra2".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mapalgebra2",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 623usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_max_distance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_max_distance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_max_distance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 624usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_max_distance_3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_max_distance_3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_max_distance_3d",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 625usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_max_distance_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_max_distance_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_max_distance_threed",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 626usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_max_x (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_max_x".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_max_x",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 627usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_max_y (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_max_y".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_max_y",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 628usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_maxdistance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_maxdistance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_maxdistance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 629usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_maximum_inscribed_circle (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_maximum_inscribed_circle".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_maximum_inscribed_circle",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 630usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_maximuminscribedcircle (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_maximuminscribedcircle".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_maximuminscribedcircle",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 631usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mem_size (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mem_size".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mem_size",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 632usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mem_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mem_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mem_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 633usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_memsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_memsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_memsize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 634usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_metadata (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_metadata".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_metadata",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 635usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_min_convex_hull (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_min_convex_hull".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_min_convex_hull",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 636usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_min_x (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_min_x".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_min_x",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 637usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_min_y (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_min_y".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_min_y",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 638usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_bounding_circle (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_minimum_bounding_circle".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_bounding_circle",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 639usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_bounding_circle_segs (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_minimum_bounding_circle_segs".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_bounding_circle_segs",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 640usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_bounding_radius (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_minimum_bounding_radius".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_bounding_radius",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 641usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_clearance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_minimum_clearance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_clearance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 642usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_clearance_line (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_minimum_clearance_line".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_clearance_line",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 643usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_minimumboundingcircle (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_minimumboundingcircle".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_minimumboundingcircle",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 644usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_minimumboundingradius (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_minimumboundingradius".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_minimumboundingradius",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 645usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_minimumclearance (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_minimumclearance".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_minimumclearance",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 646usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_minimumclearanceline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_minimumclearanceline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_minimumclearanceline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 647usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mline_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mline_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mline_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 648usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mline_from_text_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mline_from_text_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mline_from_text_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 649usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mline_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mline_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mline_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 650usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mline_from_wkb_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mline_from_wkb_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mline_from_wkb_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 651usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mlinefromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mlinefromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mlinefromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 652usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mlinefromwkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mlinefromwkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mlinefromwkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 653usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mmax (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mmax".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mmax",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 654usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mmin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mmin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mmin",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 655usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mod_edge_heal (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mod_edge_heal".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mod_edge_heal",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 656usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mod_edge_split (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mod_edge_split".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mod_edge_split",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 657usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_modedgeheal (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_modedgeheal".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_modedgeheal",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 658usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_modedgesplit (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_modedgesplit".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_modedgesplit",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 659usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoint_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpoint_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoint_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 660usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoint_from_text_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpoint_from_text_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoint_from_text_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 661usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoint_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpoint_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoint_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 662usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoint_from_wkb_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpoint_from_wkb_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoint_from_wkb_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 663usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpointfromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpointfromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpointfromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 664usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpointfromwkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpointfromwkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpointfromwkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 665usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoly_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpoly_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoly_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 666usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoly_from_text_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpoly_from_text_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoly_from_text_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 667usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoly_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpoly_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoly_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 668usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoly_from_wkb_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpoly_from_wkb_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoly_from_wkb_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 669usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpolyfromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpolyfromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpolyfromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 670usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mpolyfromwkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mpolyfromwkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mpolyfromwkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 671usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 672usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_line_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_line_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_line_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 673usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_line_from_wkb_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_line_from_wkb_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_line_from_wkb_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 674usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_line_string_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_line_string_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_line_string_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 675usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_line_string_from_text_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_line_string_from_text_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_line_string_from_text_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 676usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_point_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_point_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_point_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 677usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_point_from_text_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_point_from_text_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_point_from_text_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 678usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_point_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_point_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_point_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 679usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_point_from_wkb_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_point_from_wkb_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_point_from_wkb_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 680usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_poly_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_poly_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_poly_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 681usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_poly_from_wkb_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_poly_from_wkb_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_poly_from_wkb_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 682usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_polygon_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_polygon_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_polygon_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 683usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_polygon_from_text_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_multi_polygon_from_text_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_polygon_from_text_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 684usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_n_dims (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_n_dims".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_n_dims",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 685usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_ndims (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_ndims".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_ndims",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 686usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_nearest_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_nearest_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_nearest_value",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 687usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_nearestvalue (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_nearestvalue".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_nearestvalue",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 688usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_new_edges_split (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_new_edges_split".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_new_edges_split",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 689usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_newedgessplit (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_newedgessplit".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_newedgessplit",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 690usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_node (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_node".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_node",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 691usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_normalize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_normalize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_normalize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 692usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_npoints (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_npoints".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_npoints",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 693usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_nrings (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_nrings".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_nrings",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 694usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_num_bands (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_num_bands".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_num_bands",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 695usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_num_curves (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_num_curves".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_num_curves",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 696usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_num_geometries (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_num_geometries".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_num_geometries",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 697usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_num_interior_ring (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_num_interior_ring".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_num_interior_ring",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 698usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_num_interior_rings (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_num_interior_rings".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_num_interior_rings",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 699usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_num_points (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_num_points".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_num_points",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 700usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_numbands (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_numbands".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_numbands",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 701usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_numcurves (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_numcurves".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_numcurves",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 702usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_numgeometries (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_numgeometries".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_numgeometries",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 703usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_numinteriorring (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_numinteriorring".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_numinteriorring",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 704usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_numinteriorrings (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_numinteriorrings".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_numinteriorrings",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 705usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_numpoints (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_numpoints".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_numpoints",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 706usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_offset_curve (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_offset_curve".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_offset_curve",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 707usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_offsetcurve (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_offsetcurve".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_offsetcurve",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 708usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_ordering_equals (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_ordering_equals".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_ordering_equals",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 709usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_orderingequals (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_orderingequals".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_orderingequals",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 710usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_oriented_envelope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_oriented_envelope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_oriented_envelope",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 711usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_orientedenvelope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_orientedenvelope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_orientedenvelope",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 712usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 713usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_perimeter (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_perimeter".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeter",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 714usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_perimeter2d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_perimeter2d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeter2d",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 715usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_perimeter_2d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_perimeter_2d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeter_2d",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 716usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_perimeter_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_perimeter_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeter_threed",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 717usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_perimeter_twod (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_perimeter_twod".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeter_twod",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 718usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_perimeterthreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_perimeterthreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeterthreed",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 719usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_as_centroid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixel_as_centroid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_as_centroid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 720usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_as_centroids (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixel_as_centroids".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_as_centroids",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 721usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_as_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixel_as_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_as_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 722usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_as_points (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixel_as_points".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_as_points",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 723usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_as_polygon (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixel_as_polygon".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_as_polygon",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 724usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_as_polygons (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixel_as_polygons".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_as_polygons",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 725usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_height (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixel_height".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_height",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 726usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_of_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixel_of_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_of_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 727usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixel_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_width",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 728usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixelascentroid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixelascentroid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixelascentroid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 729usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixelaspoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixelaspoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixelaspoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 730usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixelaspolygon (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixelaspolygon".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixelaspolygon",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 731usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixelaspolygons (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixelaspolygons".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixelaspolygons",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 732usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixelofvalue (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixelofvalue".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixelofvalue",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 733usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pixels_of_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pixels_of_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pixels_of_values",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 734usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 735usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_from_geohash (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_from_geohash".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_from_geohash",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 736usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 737usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_from_text_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_from_text_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_from_text_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 738usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 739usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_from_wkb_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_from_wkb_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_from_wkb_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 740usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_inside_circle (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_inside_circle".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_inside_circle",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 741usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_m (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_m".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_m",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 742usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_m_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_m_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_m_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 743usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_n (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_n".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_n",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 744usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_on_surface (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_on_surface".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_on_surface",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 745usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 746usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_z (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_z".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_z",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 747usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_z_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_z_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_z_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 748usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_zm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_zm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_zm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 749usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_point_zm_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_point_zm_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_point_zm_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 750usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pointfromgeohash (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pointfromgeohash".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pointfromgeohash",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 751usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pointfromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pointfromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pointfromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 752usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pointfromwkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pointfromwkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pointfromwkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 753usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pointinsidecircle (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pointinsidecircle".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pointinsidecircle",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 754usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pointm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pointm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pointm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 755usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pointn (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pointn".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pointn",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 756usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pointonsurface (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pointonsurface".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pointonsurface",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 757usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_points (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_points".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_points",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 758usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pointz (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pointz".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pointz",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 759usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_pointzm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_pointzm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_pointzm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 760usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygon (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygon".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygon",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 761usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygon_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygon_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygon_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 762usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygon_from_text_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygon_from_text_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygon_from_text_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 763usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygon_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygon_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygon_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 764usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygon_from_wkb_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygon_from_wkb_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygon_from_wkb_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 765usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygonfromtext (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygonfromtext".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonfromtext",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 766usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygonfromwkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygonfromwkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonfromwkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 767usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygonize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygonize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 768usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygonize_as_raster (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygonize_as_raster".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonize_as_raster",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 769usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygonize_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygonize_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonize_topology",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 770usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polyhedralsurface_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polyhedralsurface_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polyhedralsurface_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 771usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polyhedralsurface_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polyhedralsurface_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polyhedralsurface_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 772usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_project (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_project".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_project",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 773usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_quantile (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_quantile".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_quantile",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 774usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_quantiles (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_quantiles".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_quantiles",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 775usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_quantize_coordinates (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_quantize_coordinates".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_quantize_coordinates",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 776usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_quantizecoordinates (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_quantizecoordinates".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_quantizecoordinates",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 777usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rastenvelope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rastenvelope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rastenvelope",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 778usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_contains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_contains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_contains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 779usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_contains_geom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_contains_geom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_contains_geom",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 780usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_convex_hull (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_convex_hull".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_convex_hull",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 781usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_covers (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_covers".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_covers",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 782usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_intersection_geom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_intersection_geom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_intersection_geom",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 783usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_intersects (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_intersects".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_intersects",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 784usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_intersects_geom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_intersects_geom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_intersects_geom",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 785usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_overlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_overlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_overlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 786usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_to_world_coord_x (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_to_world_coord_x".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_to_world_coord_x",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 787usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_to_world_coord_y (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_to_world_coord_y".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_to_world_coord_y",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 788usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_within (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_within".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_within",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 789usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rastercontains (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rastercontains".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rastercontains",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 790usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rastercontainsgeom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rastercontainsgeom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rastercontainsgeom",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 791usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rasterconvexhull (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rasterconvexhull".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rasterconvexhull",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 792usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rastercovers (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rastercovers".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rastercovers",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 793usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rasterintersects (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rasterintersects".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rasterintersects",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 794usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rasterintersectsgeom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rasterintersectsgeom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rasterintersectsgeom",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 795usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rasteroverlaps (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rasteroverlaps".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rasteroverlaps",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 796usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rastertoworldcoordx (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rastertoworldcoordx".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rastertoworldcoordx",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 797usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rastertoworldcoordy (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rastertoworldcoordy".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rastertoworldcoordy",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 798usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rasterwithin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rasterwithin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rasterwithin",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 799usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rastheight (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rastheight".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rastheight",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 800usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rastnumbands (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rastnumbands".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rastnumbands",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 801usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rastwidth (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rastwidth".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rastwidth",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 802usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_reclass (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_reclass".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_reclass",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 803usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_reclass_exact (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_reclass_exact".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_reclass_exact",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 804usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_reduce_precision (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_reduce_precision".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_reduce_precision",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 805usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_reduce_precision_flags (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_reduce_precision_flags".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_reduce_precision_flags",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 806usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_reduceprecision (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_reduceprecision".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_reduceprecision",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 807usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_relate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_relate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_relate",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 808usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_relate_bnr (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_relate_bnr".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_relate_bnr",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 809usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_relate_match (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_relate_match".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_relate_match",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 810usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_relate_with_pattern (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_relate_with_pattern".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_relate_with_pattern",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 811usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_relatematch (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_relatematch".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_relatematch",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 812usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_remove_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_remove_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_remove_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 813usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_remove_repeated_points (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_remove_repeated_points".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_remove_repeated_points",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 814usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_remove_small_parts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_remove_small_parts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_remove_small_parts",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 815usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_removepoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_removepoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_removepoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 816usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_removerepeatedpoints (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_removerepeatedpoints".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_removerepeatedpoints",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 817usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_removesmallparts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_removesmallparts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_removesmallparts",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 818usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rescale (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rescale".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rescale",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 819usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_resize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_resize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_resize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 820usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_retile (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_retile".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_retile",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 821usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_reverse (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_reverse".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_reverse",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 822usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_reverse_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_reverse_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_reverse_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 823usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_reversethreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_reversethreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_reversethreed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 824usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rotate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 825usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_around_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rotate_around_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_around_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 826usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_around_xy (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rotate_around_xy".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_around_xy",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 827usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rotate_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 828usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg6".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg7".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_threed_around_center (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rotate_threed_around_center".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_threed_around_center",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 829usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_twod_around_center (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rotate_twod_around_center".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_twod_around_center",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 830usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rotatethreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rotatethreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rotatethreed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 831usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rotation (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rotation".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rotation",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 832usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_roughness (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_roughness".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_roughness",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 833usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_scale (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_scale".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_scale",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 834usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_scale_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_scale_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_scale_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 835usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_scale_with_origin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_scale_with_origin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_scale_with_origin",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 836usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_scale_with_vector (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_scale_with_vector".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_scale_with_vector",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 837usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_scale_x (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_scale_x".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_scale_x",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 838usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_scale_y (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_scale_y".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_scale_y",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 839usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_scalethreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_scalethreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_scalethreed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 840usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_scalex (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_scalex".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_scalex",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 841usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_scaley (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_scaley".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_scaley",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 842usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_scroll (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_scroll".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_scroll",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 843usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_segmentize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_segmentize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_segmentize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 844usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_band_is_nodata (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_band_is_nodata".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_band_is_nodata",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 845usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_band_nodata_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_band_nodata_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_band_nodata_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 846usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_effective_area (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_effective_area".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_effective_area",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 847usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg6".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_geo_reference (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_geo_reference".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_geo_reference",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 848usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_geo_reference_string (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_geo_reference_string".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_geo_reference_string",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 849usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 850usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_rotation (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_rotation".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_rotation",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 851usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_scale (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_scale".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_scale",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 852usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_scale_xy (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_scale_xy".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_scale_xy",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 853usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_skew (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_skew".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_skew",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 854usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_skew_xy (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_skew_xy".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_skew_xy",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 855usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 856usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_upper_left (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_upper_left".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_upper_left",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 857usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_value",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 858usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_value_at_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_value_at_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_value_at_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 859usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_values (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_values".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_values",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 860usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg6".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_set_values_rect (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_set_values_rect".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_set_values_rect",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 861usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_seteffectivearea (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_seteffectivearea".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_seteffectivearea",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 862usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_setendm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_setendm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_setendm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 863usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_setendz (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_setendz".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_setendz",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 864usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_setm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_setm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_setm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 865usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_setpoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_setpoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_setpoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 866usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_setsrid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_setsrid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_setsrid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 867usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_setstartm (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_setstartm".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_setstartm",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 868usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_setstartz (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_setstartz".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_setstartz",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 869usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_setvalue (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_setvalue".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_setvalue",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 870usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_setx (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_setx".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_setx",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 871usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_sety (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_sety".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_sety",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 872usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_setz (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_setz".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_setz",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 873usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_shared_paths (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_shared_paths".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_shared_paths",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 874usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_sharedpaths (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_sharedpaths".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_sharedpaths",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 875usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_shift_longitude (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_shift_longitude".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_shift_longitude",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 876usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_shiftlongitude (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_shiftlongitude".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_shiftlongitude",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 877usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_shortest_line (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_shortest_line".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_shortest_line",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 878usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_shortest_line_3d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_shortest_line_3d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_shortest_line_3d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 879usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_shortest_line_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_shortest_line_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_shortest_line_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 880usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_shortestline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_shortestline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_shortestline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 881usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_simplify".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 882usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_polygon_hull (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_simplify_polygon_hull".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_polygon_hull",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 883usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_preserve_collapse (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_simplify_preserve_collapse".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_preserve_collapse",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 884usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_preserve_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_simplify_preserve_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_preserve_topology",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 885usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_vw (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_simplify_vw".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_vw",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 886usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_vw_preserve_collapse (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_simplify_vw_preserve_collapse".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_vw_preserve_collapse",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 887usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_simplifypolygonhull (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_simplifypolygonhull".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_simplifypolygonhull",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 888usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_simplifypreservetopology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_simplifypreservetopology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_simplifypreservetopology",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 889usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_simplifyvw (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_simplifyvw".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_simplifyvw",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 890usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_skew_x (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_skew_x".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_skew_x",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 891usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_skew_y (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_skew_y".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_skew_y",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 892usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_skewx (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_skewx".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_skewx",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 893usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_skewy (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_skewy".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_skewy",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 894usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_slope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_slope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_slope",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 895usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_snap (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_snap".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_snap",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 896usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_snap_to_grid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_snap_to_grid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_snap_to_grid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 897usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg5".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg6".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_snap_to_grid_4d (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_snap_to_grid_4d".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_snap_to_grid_4d",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 898usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_snap_to_grid_origin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_snap_to_grid_origin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_snap_to_grid_origin",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 899usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_snap_to_grid_xy (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_snap_to_grid_xy".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_snap_to_grid_xy",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 900usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_snaptogrid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_snaptogrid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_snaptogrid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 901usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_split (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_split".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_split",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 902usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_square (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_square".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_square",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 903usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_srid",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 904usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_start_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_start_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_start_point",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 905usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_startpoint (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_startpoint".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_startpoint",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 906usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_subdivide_gridsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_subdivide_gridsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_subdivide_gridsize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 907usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_summary (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_summary".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_summary",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 908usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_summary_stats (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_summary_stats".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_summary_stats",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 909usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_summarystats (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_summarystats".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_summarystats",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 910usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_swap_ordinates (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_swap_ordinates".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_swap_ordinates",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 911usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_swapordinates (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_swapordinates".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_swapordinates",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 912usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_sym_difference (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_sym_difference".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_sym_difference",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 913usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_sym_difference_gridsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_sym_difference_gridsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_sym_difference_gridsize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 914usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_sym_difference_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_sym_difference_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_sym_difference_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 915usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_symdifference (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_symdifference".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_symdifference",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 916usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_tile (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_tile".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_tile",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 917usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_tile_envelope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_tile_envelope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_tile_envelope",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 918usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_tileenvelope (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_tileenvelope".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_tileenvelope",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 919usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_tin_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_tin_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_tin_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 920usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_tin_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_tin_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_tin_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 921usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topology_edge_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topology_edge_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_edge_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 922usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topology_face_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topology_face_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_face_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 923usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topology_from_bytes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topology_from_bytes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_from_bytes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 924usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topology_name (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topology_name".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_name",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 925usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topology_node_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topology_node_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_node_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 926usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topology_precision (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topology_precision".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_precision",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 927usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topology_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topology_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_srid",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 928usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topology_to_bytes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topology_to_bytes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_to_bytes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 929usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topologyedgecount (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topologyedgecount".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topologyedgecount",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 930usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topologyfacecount (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topologyfacecount".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topologyfacecount",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 931usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topologyfrombytes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topologyfrombytes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topologyfrombytes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 932usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topologyname (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topologyname".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topologyname",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 933usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topologynodecount (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topologynodecount".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topologynodecount",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 934usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topologyprecision (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topologyprecision".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topologyprecision",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 935usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topologysrid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topologysrid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topologysrid",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 936usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_topologytobytes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_topologytobytes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_topologytobytes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 937usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_touches (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_touches".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_touches",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 938usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_tpi (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_tpi".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_tpi",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 939usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_transform (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_transform".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_transform",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 940usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_transform_pipeline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_transform_pipeline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_transform_pipeline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 941usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_transform_pipeline_to_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_transform_pipeline_to_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_transform_pipeline_to_srid",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 942usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_transformpipeline (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_transformpipeline".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_transformpipeline",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 943usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_translate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_translate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_translate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 944usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_translate_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_translate_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_translate_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 945usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_translatethreed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_translatethreed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_translatethreed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 946usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_transscale (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_transscale".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_transscale",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 947usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_tri (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_tri".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_tri",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 948usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_triangle_from_text (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_triangle_from_text".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_triangle_from_text",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 949usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_triangle_from_wkb (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_triangle_from_wkb".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_triangle_from_wkb",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 950usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_triangulate_polygon (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_triangulate_polygon".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_triangulate_polygon",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 951usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_triangulate_twod (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_triangulate_twod".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_triangulate_twod",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 952usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_triangulatepolygon (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_triangulatepolygon".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_triangulatepolygon",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 953usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_unary_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_unary_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_unary_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 954usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_unaryunion (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_unaryunion".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_unaryunion",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 955usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 956usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_union_gridsize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_union_gridsize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_union_gridsize",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 957usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_upper_left_x (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_upper_left_x".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_upper_left_x",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 958usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_upper_left_y (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_upper_left_y".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_upper_left_y",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 959usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_upperleftx (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_upperleftx".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_upperleftx",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 960usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_upperlefty (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_upperlefty".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_upperlefty",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 961usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_validate_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_validate_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_validate_topology",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 962usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_validatetopology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_validatetopology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_validatetopology",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 963usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_value",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 964usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_value_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_value_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_value_count",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 965usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_value_count_of_value (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_value_count_of_value".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_value_count_of_value",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 966usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_value_counts (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_value_counts".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_value_counts",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 967usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_value_percent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_value_percent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_value_percent",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 968usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_value_percents (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_value_percents".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_value_percents",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 969usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_valuecount (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_valuecount".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_valuecount",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 970usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_voronoi_lines (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_voronoi_lines".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_voronoi_lines",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 971usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_voronoi_polygons (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_voronoi_polygons".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_voronoi_polygons",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 972usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_voronoilines (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_voronoilines".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_voronoilines",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 973usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_voronoipolygons (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_voronoipolygons".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_voronoipolygons",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 974usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_width (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_width".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_width",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 975usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_within (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_within".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_within",
            &args,
            &Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 976usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_wkb_to_sql (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_wkb_to_sql".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_wkb_to_sql",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 977usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_wkbtosql (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_wkbtosql".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_wkbtosql",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 978usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_wkt_to_sql (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_wkt_to_sql".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_wkt_to_sql",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 979usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_wkttosql (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_wkttosql".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_wkttosql",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 980usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_world_to_raster_coord (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_world_to_raster_coord".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_world_to_raster_coord",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 981usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_world_to_raster_coord_col (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_world_to_raster_coord_col".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_world_to_raster_coord_col",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 982usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_world_to_raster_coord_row (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_world_to_raster_coord_row".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_world_to_raster_coord_row",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 983usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_worldtorastercoord (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_worldtorastercoord".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_worldtorastercoord",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 984usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_worldtorastercoordcol (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_worldtorastercoordcol".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_worldtorastercoordcol",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 985usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_worldtorastercoordrow (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_worldtorastercoordrow".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_worldtorastercoordrow",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 986usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_wrap_x (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_wrap_x".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_wrap_x",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 987usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_wrapx (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_wrapx".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_wrapx",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 988usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_x (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_x".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_x",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 989usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_xmax (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_xmax".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_xmax",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 990usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_xmin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_xmin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_xmin",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 991usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_y (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_y".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_y",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 992usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_ymax (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_ymax".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_ymax",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 993usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_ymin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_ymin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_ymin",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 994usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_z (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_z".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_z",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 995usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_zmax (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_zmax".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_zmax",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 996usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_zmflag (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_zmflag".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_zmflag",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 997usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_zmin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_zmin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_zmin",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 998usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("to_topogeom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["to_topogeom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "to_topogeom",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 999usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topo_element_array_append (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topo_element_array_append".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topo_element_array_append",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1000usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topogeo_add_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topogeo_add_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topogeo_add_geometry",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1001usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topogeo_add_linestring (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topogeo_add_linestring".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topogeo_add_linestring",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1002usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topogeo_add_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topogeo_add_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topogeo_add_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1003usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topogeo_add_polygon (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topogeo_add_polygon".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topogeo_add_polygon",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1004usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topogeo_load_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topogeo_load_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topogeo_load_geometry",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1005usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_edge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_add_edge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_edge",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1006usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_edge_mod_face (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_add_edge_mod_face".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_edge_mod_face",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1007usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_edge_new_faces (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_add_edge_new_faces".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_edge_new_faces",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1008usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_face (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_add_face".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_face",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1009usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_iso_edge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_add_iso_edge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_iso_edge",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1010usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_iso_node (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_add_iso_node".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_iso_node",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1011usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_node (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_add_node".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_node",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1012usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_topogeom_column (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_add_topogeom_column".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_topogeom_column",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1013usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_as_gml (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_as_gml".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_as_gml",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1014usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_as_topojson (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_as_topojson".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_as_topojson",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1015usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Boolean },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_as_topojson_with_edge_map (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_as_topojson_with_edge_map".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_as_topojson_with_edge_map",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1016usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_change_edge_geom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_change_edge_geom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_change_edge_geom",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1017usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_copy_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_copy_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_copy_topology",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1018usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_create (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_create".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_create",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1019usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_create_topo_geom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_create_topo_geom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_create_topo_geom",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1020usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_drop_topogeom_column (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_drop_topogeom_column".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_drop_topogeom_column",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1021usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_drop_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_drop_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_drop_topology",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1022usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_edge_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_edge_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_edge_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1023usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_face_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_face_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_face_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1024usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_from_bytes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_from_bytes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_from_bytes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1025usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_edge_by_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_edge_by_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_edge_by_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1026usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_edge_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_edge_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_edge_geometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1027usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_face_by_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_face_by_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_face_by_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1028usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_face_containing_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_face_containing_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_face_containing_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1029usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_face_edges (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_face_edges".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_face_edges",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1030usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_face_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_face_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_face_geometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1031usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_node_by_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_node_by_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_node_by_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1032usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_node_edges (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_node_edges".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_node_edges",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1033usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_node_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_node_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_node_geometry",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1034usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_ring_edges (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_ring_edges".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_ring_edges",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1035usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_topology_id (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_topology_id".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_topology_id",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1036usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_topology_name (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_topology_name".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_topology_name",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1037usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_topology_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_get_topology_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_topology_srid",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1038usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_make_topology_precise (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_make_topology_precise".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_make_topology_precise",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1039usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_mod_edge_heal (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_mod_edge_heal".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_mod_edge_heal",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1040usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_mod_edge_split (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_mod_edge_split".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_mod_edge_split",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1041usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_move_iso_node (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_move_iso_node".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_move_iso_node",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1042usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_name (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_name".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_name",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1043usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_new_edge_heal (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_new_edge_heal".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_new_edge_heal",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1044usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_new_edges_split (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_new_edges_split".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_new_edges_split",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1045usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_node_count (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_node_count".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_node_count",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1046usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_polygonize (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_polygonize".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_polygonize",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1047usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_precision (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_precision".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_precision",
            &args,
            &Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1048usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_rem_edge_mod_face (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_rem_edge_mod_face".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_rem_edge_mod_face",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1049usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_rem_edge_new_face (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_rem_edge_new_face".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_rem_edge_new_face",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1050usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_remove_face (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_remove_face".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_remove_face",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1051usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_remove_iso_edge (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_remove_iso_edge".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_remove_iso_edge",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1052usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_remove_iso_node (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_remove_iso_node".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_remove_iso_node",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1053usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_remove_unused_primitives (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_remove_unused_primitives".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_remove_unused_primitives",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1054usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Text },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_rename_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_rename_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_rename_topology",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1055usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_srid (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_srid".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_srid",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1056usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_summary (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_summary".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_summary",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1057usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_to_bytes (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_to_bytes".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_to_bytes",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1058usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_to_topogeom (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_to_topogeom".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_to_topogeom",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1059usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_topogeo_add_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_topogeo_add_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_topogeo_add_geometry",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1060usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_topogeo_add_linestring (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_topogeo_add_linestring".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_topogeo_add_linestring",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1061usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_topogeo_add_point (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_topogeo_add_point".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_topogeo_add_point",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1062usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_topogeo_add_polygon (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_topogeo_add_polygon".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_topogeo_add_polygon",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1063usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_topogeo_load_geometry (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_topogeo_load_geometry".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_topogeo_load_geometry",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1064usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_total_topology_size (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_total_topology_size".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_total_topology_size",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1065usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_upgrade_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_upgrade_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_upgrade_topology",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1066usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_validate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_validate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_validate",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1067usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_validate_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_validate_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_validate_topology",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1068usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("topology_validate_topology_precision (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["topology_validate_topology_precision".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "topology_validate_topology_precision",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1069usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("total_topology_size (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["total_topology_size".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "total_topology_size",
            &args,
            &Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1070usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("upgrade_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["upgrade_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "upgrade_topology",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1071usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("validate_topology (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["validate_topology".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "validate_topology",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1072usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("validate_topology_precision (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["validate_topology_precision".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "validate_topology_precision",
            &args,
            &Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    Ok(())
}

fn register_aggregates() -> Result<(), Duckerror> {
    // Best-effort: hosts that don't expose the aggregate capability
    // still let scalars register (the scalar-only bridges shipped in
    // Phase A). Log-and-continue so the emit stays useful in both
    // shapes.
    let Some(capability) = runtime::get_capability(Capabilitykind::Aggregate) else {
        if true {
            eprintln!("[dynlink-emit] host did not expose aggregate capability; skipping 30 aggregate(s)");
        }
        return Ok(());
    };
    let registry = match capability {
        runtime::Capability::Aggregate(r) => r,
        _ => {
            return Err(Duckerror::Internal(
                "aggregate capability returned unexpected variant".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("postgis_spatial_index_build (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["postgis_spatial_index_build".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "postgis_spatial_index_build",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3d_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_3dextent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_3dextent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_3dextent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_count_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_approx_count_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_count_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 4usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_clusterintersecting (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_clusterintersecting".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterintersecting",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 5usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_clusterintersectingaggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_clusterintersectingaggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterintersectingaggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 6usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_clusterwithin (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_clusterwithin".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterwithin",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 7usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_clusterwithinaggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_clusterwithinaggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterwithinaggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_collect_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_collect_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_collect_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 9usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_count_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_count_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_count_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 10usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_coverage_union_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_coverage_union_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_coverage_union_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 11usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_envelope_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_envelope_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_envelope_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 12usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_extent (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_extent".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_extent",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 13usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_extent_threed (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_extent_threed".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_extent_threed",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 14usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_make_line_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_make_line_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_make_line_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 16usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makelineagg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makelineagg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makelineagg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 17usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_makelineaggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_makelineaggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_makelineaggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 18usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_mem_union_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_mem_union_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_mem_union_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 19usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_memunion (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_memunion".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_memunion",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 20usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygonize_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygonize_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonize_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 21usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygonizeagg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygonizeagg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonizeagg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 22usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_polygonizeaggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_polygonizeaggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonizeaggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 23usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rast_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rast_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 24usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_rast_union_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_rast_union_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_union_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 25usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_union (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_raster_union".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_union",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 26usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_summary_stats_agg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_summary_stats_agg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_summary_stats_agg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 27usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_union_aggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_union_aggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_union_aggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 28usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_unionagg (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_unionagg".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_unionagg",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 29usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let opts = runtime::Funcopts {
            description: Some("st_unionaggregate (sqlink-shim-codegen --dynlink)".into()),
            tags: vec!["st_unionaggregate".into()],
            attributes: Funcflags::DETERMINISTIC | Funcflags::STATELESS,
        };
        registry.register(
            "st_unionaggregate",
            &args,
            &Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    Ok(())
}

fn register_tables() -> Result<(), Duckerror> {
    let Some(capability) = runtime::get_capability(Capabilitykind::Table) else {
        if true {
            eprintln!("[dynlink-emit] host did not expose table capability; skipping 13 table-function(s)");
        }
        return Ok(());
    };
    let registry = match capability {
        runtime::Capability::Table(r) => r,
        _ => {
            return Err(Duckerror::Internal(
                "table capability returned unexpected variant".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg2".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg3".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg4".into()), logical: Logicaltype::Float64 },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "item_id".into(), logical: Logicaltype::Int64 },
            ];
        registry.register(
            "postgis_spatial_index_query_envelope",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "geom".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_dump",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "geom".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_dump_points",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "geom".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_dump_rings",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 4usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "geom".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_dump_segments",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 5usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "point".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_dumppoints",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 6usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "geom".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_dumprings",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 7usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "geom".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_dumpsegments",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "geom".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_hexagon_grid",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 9usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "geom".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_hexagongrid",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 10usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "geom".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_square_grid",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 11usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Float64 },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Blob },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "geom".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_squaregrid",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, core::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 12usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
                runtime::Funcarg { name: Some("arg0".into()), logical: Logicaltype::Blob },
                runtime::Funcarg { name: Some("arg1".into()), logical: Logicaltype::Int64 },
            ];
        let columns: Vec<runtime::Columndef> = vec![
                runtime::Columndef { name: "geom".into(), logical: Logicaltype::Blob },
            ];
        registry.register(
            "st_subdivide",
            &args,
            &columns,
            callback,
            None,
        )?;
    }
    Ok(())
}

// -----------------------------------------------------------
// Guest impls.
// -----------------------------------------------------------

struct Component;

impl GuestGuest for Component {
    fn load() -> Result<Loadresult, Duckerror> {
        // Best-effort: a cast-registration failure is
        // non-fatal (typically means the target alias
        // is unknown to DuckDB stock, e.g. RASTER).
        let _ = register_logical_types();
        register_scalars()?;
        register_aggregates()?;
        register_tables()?;
        Ok(Loadresult {
            name: EXTENSION_ROOT.to_string(),
            version: Some(CATALOG_VERSION.to_string()),
            requires: vec![Capabilitykind::Scalar],
        })
    }
    fn reconfigure(_keys: Vec<String>) -> Result<bool, Duckerror> { Ok(false) }
    fn shutdown() -> Result<bool, Duckerror> { Ok(false) }
}

fn dispatch_call_scalar(
    handle: u32,
    args: Vec<Duckvalue>,
) -> Result<Duckvalue, Duckerror> {
    let arm_idx = handle_table()
        .lock()
        .expect("scalar handle mutex poisoned")
        .get(&handle)
        .copied()
        .ok_or_else(|| Duckerror::Internal(format!("unknown scalar handle {}", handle)))?;
    let name = scalar_name_by_arm_idx(arm_idx)
        .ok_or_else(|| Duckerror::Internal(format!("unknown scalar arm {}", arm_idx)))?;
    // Null-propagation: any NULL argument short-circuits to NULL.
    if args.iter().any(|v| matches!(v, Duckvalue::Null)) {
        return Ok(Duckvalue::Null);
    }
    // Alias translation: SQL name → WIT-canonical → provider method
    // (kebab-case). Compact-form SQL names (e.g. `st_geomfromtext`)
    // route through `canonical_for` to their long form
    // (`st_geom_from_text`) before kebab-casing; non-alias names
    // pass through unchanged. See `canonical_for` above.
    let canonical = canonical_for(name);
    let method = canonical.replace('_', "-");
    let cbor_args: Vec<CborValue> = args.iter().map(duckv_to_cbor).collect();
    let resp = call(&method, cbor_args)?;
    Ok(response_to_duckv(resp))
}

fn aggregate_name_by_arm_idx(arm: usize) -> Option<&'static str> {
    match arm as u32 {
        0 => Some("postgis_spatial_index_build"),
        1 => Some("st_3d_extent"),
        2 => Some("st_3dextent"),
        3 => Some("st_approx_count_agg"),
        4 => Some("st_clusterintersecting"),
        5 => Some("st_clusterintersectingaggregate"),
        6 => Some("st_clusterwithin"),
        7 => Some("st_clusterwithinaggregate"),
        8 => Some("st_collect_aggregate"),
        9 => Some("st_count_agg"),
        10 => Some("st_coverage_union_aggregate"),
        11 => Some("st_envelope_aggregate"),
        12 => Some("st_extent"),
        13 => Some("st_extent_threed"),
        14 => Some("st_make_line_aggregate"),
        15 => Some("st_makeline"),
        16 => Some("st_makelineagg"),
        17 => Some("st_makelineaggregate"),
        18 => Some("st_mem_union_aggregate"),
        19 => Some("st_memunion"),
        20 => Some("st_polygonize_aggregate"),
        21 => Some("st_polygonizeagg"),
        22 => Some("st_polygonizeaggregate"),
        23 => Some("st_rast_union"),
        24 => Some("st_rast_union_aggregate"),
        25 => Some("st_raster_union"),
        26 => Some("st_summary_stats_agg"),
        27 => Some("st_union_aggregate"),
        28 => Some("st_unionagg"),
        29 => Some("st_unionaggregate"),
        _ => None,
    }
}

fn table_name_by_arm_idx(arm: usize) -> Option<&'static str> {
    match arm as u32 {
        0 => Some("postgis_spatial_index_query_envelope"),
        1 => Some("st_dump"),
        2 => Some("st_dump_points"),
        3 => Some("st_dump_rings"),
        4 => Some("st_dump_segments"),
        5 => Some("st_dumppoints"),
        6 => Some("st_dumprings"),
        7 => Some("st_dumpsegments"),
        8 => Some("st_hexagon_grid"),
        9 => Some("st_hexagongrid"),
        10 => Some("st_square_grid"),
        11 => Some("st_squaregrid"),
        12 => Some("st_subdivide"),
        _ => None,
    }
}

/// Aggregate dispatch (Phase 9.3.next). Whole-group call: the host
/// hands us every row's arg columns; we lift each row to a CBOR list
/// and ship the accumulated list-of-lists to the provider under the
/// aggregate's canonical name. The provider owns the semantic
/// fold (average, extent, cluster, …); the bridge is a CBOR tunnel.
fn dispatch_call_aggregate(
    handle: u32,
    args: Vec<bindings::duckdb::extension::column_types::Colvec>,
) -> Result<Duckvalue, Duckerror> {
    let arm_idx = aggregate_handle_table()
        .lock()
        .expect("aggregate handle mutex poisoned")
        .get(&handle)
        .copied()
        .ok_or_else(|| Duckerror::Internal(format!("unknown aggregate handle {}", handle)))?;
    let name = aggregate_name_by_arm_idx(arm_idx)
        .ok_or_else(|| Duckerror::Internal(format!("unknown aggregate arm {}", arm_idx)))?;
    let n_rows = validate_colvec_rows(&args)?;
    let n_args = args.len();
    // Columnar wire shape for aggregate dispatch: each column becomes a
    // separate positional arg to the provider, containing a FLAT list
    // of that column's per-row CBOR values. For a single-arg aggregate
    // like `st_extent(geom)`, the provider sees `args=[List<Bytes>]` —
    // arg[0] is the flat geometry list, arg[0][0] is a Bytes geometry
    // (matches the shim's expected shape per the arg-shape checker's
    // error message on prior versions of this dispatch).
    //
    // Previous wire (broken) was `args=[List<List<CborValue>>]` — one
    // arg containing a list of per-row lists. The shim's arg-type
    // checker on `st-extent` etc. read `arg[0][0]` as List (a row) and
    // failed shape mismatch (expected Bytes for the geometry).
    //
    // SQL-aggregate NULL-row semantics: skip rows where ANY arg is
    // NULL (matches sum() / avg() defaults) — do the skip BEFORE
    // transposing to columns so all columns see the same row subset.
    let mut cols: Vec<Vec<CborValue>> = (0..n_args)
        .map(|_| Vec::with_capacity(n_rows))
        .collect();
    let mut row_buf: Vec<Duckvalue> = Vec::with_capacity(n_args);
    for i in 0..n_rows {
        materialize_row(&args, i, &mut row_buf);
        if row_buf.iter().any(|v| matches!(v, Duckvalue::Null)) {
            continue;
        }
        for (col_idx, v) in row_buf.iter().enumerate() {
            cols[col_idx].push(duckv_to_cbor(v));
        }
    }
    // Empty non-null row set → aggregate is NULL (matches SQL
    // `SELECT SUM(x) FROM t WHERE 1=0` = NULL). Skip the provider
    // call so the shim doesn't need to special-case empty inputs.
    if cols.first().map_or(true, |c| c.is_empty()) {
        return Ok(Duckvalue::Null);
    }
    let canonical = canonical_for(name);
    let method = canonical.replace('_', "-");
    let cbor_args: Vec<CborValue> = cols.into_iter().map(CborValue::List).collect();
    let resp = call(&method, cbor_args)?;
    Ok(response_to_duckv(resp))
}

/// Table-function dispatch (Phase 9.3.next). Single-shot call: the
/// host hands us the argv values; we ship them through the CBOR
/// envelope and expect a List-of-Bytes (one BLOB per row) response.
/// Any other shape is best-effort wrapped as a single Blob row so
/// the provider stays free to evolve its wire without breaking the
/// bridge.
fn dispatch_call_table(
    handle: u32,
    args: Vec<Duckvalue>,
) -> Result<Resultset, Duckerror> {
    let arm_idx = table_handle_table()
        .lock()
        .expect("table handle mutex poisoned")
        .get(&handle)
        .copied()
        .ok_or_else(|| Duckerror::Internal(format!("unknown table handle {}", handle)))?;
    let name = table_name_by_arm_idx(arm_idx)
        .ok_or_else(|| Duckerror::Internal(format!("unknown table arm {}", arm_idx)))?;
    let canonical = canonical_for(name);
    let method = canonical.replace('_', "-");
    let cbor_args: Vec<CborValue> = args.iter().map(duckv_to_cbor).collect();
    let resp = call(&method, cbor_args)?;
    let rows: Vec<Vec<Duckvalue>> = match resp {
        ResponseValue::List(items) => items
            .into_iter()
            .map(|item| match item {
                ResponseValue::Bytes(b) => vec![Duckvalue::Blob(b)],
                ResponseValue::List(cols) => cols.into_iter().map(response_to_duckv).collect(),
                other => vec![response_to_duckv(other)],
            })
            .collect(),
        ResponseValue::Null => Vec::new(),
        other => vec![vec![response_to_duckv(other)]],
    };
    Ok(rows)
}

impl CallbackGuest for Component {
    fn call_scalar(
        handle: u32,
        args: Vec<Duckvalue>,
        _ctx: Invokeinfo,
    ) -> Result<Duckvalue, Duckerror> {
        dispatch_call_scalar(handle, args)
    }

    fn call_scalar_batch_col(
        handle: u32,
        args: Vec<bindings::duckdb::extension::column_types::Colvec>,
        ctx: Invokeinfo,
    ) -> Result<bindings::duckdb::extension::column_types::Colvec, Duckerror> {
        // Columnar HOT path: convert per-row, delegate to the cold
        // row-major dispatch, then rebuild a colvec. Mirrors the
        // postgis-ducklink-bridge (~/git/postgis-ducklink-bridge/
        // src/lib.rs:2231-2261) reference discipline.
        let n_rows = validate_colvec_rows(&args)?;
        let n_args = args.len();
        let base = ctx.rowindex.unwrap_or(0);
        let mut out: Vec<Duckvalue> = Vec::with_capacity(n_rows);
        let mut row_buf: Vec<Duckvalue> = Vec::with_capacity(n_args);
        for i in 0..n_rows {
            materialize_row(&args, i, &mut row_buf);
            let _row_ctx = Invokeinfo {
                rowindex: Some(base + i as u64),
                iswindow: ctx.iswindow,
            };
            let row_args = core::mem::take(&mut row_buf);
            out.push(dispatch_call_scalar(handle, row_args)?);
        }
        values_to_colvec(out)
    }

    fn call_aggregate_col(
        handle: u32,
        args: Vec<bindings::duckdb::extension::column_types::Colvec>,
    ) -> Result<Duckvalue, Duckerror> {
        dispatch_call_aggregate(handle, args)
    }

    fn call_cast_col(
        handle: u32,
        arg: bindings::duckdb::extension::column_types::Colvec,
    ) -> Result<bindings::duckdb::extension::column_types::Colvec, Duckerror> {
        // Identity casts (GEOMETRY↔BLOB / BOX2D↔BLOB / ...) are
        // physical-representation-preserving: the source and target
        // are both physically BLOB, only the logical tag differs.
        // Return the input column unchanged.
        if identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .contains(&handle)
        {
            return Ok(arg);
        }
        Err(Duckerror::Internal("call_cast_col: handle not identity (Phase A)".to_string()))
    }

    fn call_table(handle: u32, args: Vec<Duckvalue>) -> Result<Resultset, Duckerror> {
        dispatch_call_table(handle, args)
    }

    fn call_pragma(_handle: u32, _args: Vec<Duckvalue>) -> Result<Option<Duckvalue>, Duckerror> {
        Err(Duckerror::Internal("call_pragma: unsupported (Phase A)".to_string()))
    }

    fn call_cast(handle: u32, value: Duckvalue) -> Result<Duckvalue, Duckerror> {
        if identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .contains(&handle)
        {
            return Ok(value);
        }
        Err(Duckerror::Internal("call_cast: handle not identity (Phase A)".to_string()))
    }
}

bindings::export!(Component with_types_in bindings);
