// === GENERATED by sqlink-shim-codegen (target=duckdb)  do not edit by hand ===

//! Generated by sqlink-shim-codegen (--target duckdb).
//!
//! Scalars / aggregates / casts / table-functions wire against
//! the `duckdb:extension@4.0.0` callback-dispatch surface. The
//! hot-path columnar methods (call-scalar-batch-col /
//! call-aggregate-col / call-cast-col, #653) lift their colvec
//! args to row-major up-front and route through the cold-path
//! row-major bodies. The call-pragma arm (#617 stub, refactored
//! in #625 into `build_pragma_dispatch_impl`) returns
//! `Duckerror::Unsupported` until a real pragma surface lands.
//! Window functions (#626) are classified by
//! `build_window_registry` so the maintainer sees coverage at
//! codegen time, but the bridge world does not yet export the
//! optional `aggregate-incr-dispatch` interface (which carries
//! `call-aggregate-window`) is wired via #661 -- the bridge world
//! exports `aggregate-incr-dispatch` so wit-bindgen generates the
//! 5-method trait; `build_window_dispatch_impl` emits the impl,
//! with `call_aggregate_window` routing per-handle to the
//! classified `WindowEntry` arm body.

#![allow(unused_imports, dead_code)]

extern crate alloc;

use alloc::format;
use alloc::string::{String, ToString};
use alloc::vec::Vec;

mod bindings {
    wit_bindgen::generate!({
        path: "wit",
        world: "bridge",
        generate_all,
        // Derive serde::Serialize + serde::Deserialize on
        // generated record types so the JSON-direct decode path
        // in the per-record helpers below resolves against the
        // upstream type (`serde_json::from_str::<UPSTREAM>(...)`).
        additional_derives: [serde::Serialize, serde::Deserialize],
        // Contract types + helper-component types + primary-shim
        // variants/flags can't derive serde out-of-the-box.
        additional_derives_ignore: [
            "access-mode",
            "area",
            "axis-mapping-strategy",
            "axis-orientation",
            "bbox",
            "bool-result",
            "buffer-spec",
            "cap-style",
            "capability",
            "capabilitykind",
            "cast-kind",
            "cast-spec",
            "cluster-result",
            "collation-spec",
            "color",
            "color-entry",
            "color-interp",
            "column",
            "column-type",
            "columndef",
            "colvec",
            "compare-op",
            "complexvalue",
            "configerror",
            "conn-events",
            "contour-options",
            "control-field",
            "coord",
            "coord-seq-result",
            "coordinate",
            "coordinate-dimension",
            "coordinate2d",
            "coordinate3d",
            "coordinate4d",
            "copy-from-bind-result",
            "copy-handler",
            "coverage-validation-result",
            "crs",
            "crs-def",
            "data-field",
            "data-type",
            "decimalvalue",
            "decode-error",
            "dem-mode",
            "dem-options",
            "detection-mode",
            "dimensions",
            "direct-result",
            "direction",
            "document",
            "driver-info",
            "duckerror",
            "duckvalue",
            "ellipsoid",
            "encode-error",
            "encode-options",
            "envelope",
            "error-code",
            "extopts",
            "extremes",
            "f64-result",
            "factors",
            "feature",
            "feature-info",
            "fgb-error",
            "field-type",
            "file-info",
            "file-open-result",
            "file-stat",
            "filter-op",
            "filterable-table",
            "folder",
            "font-error",
            "font-info",
            "funcarg",
            "funcflags",
            "funcopts",
            "gcp",
            "geobuf-metadata",
            "geodesic",
            "geojson-options",
            "geom-type",
            "geometry",
            "geometry-list-result",
            "geometry-parse-result",
            "geometry-result",
            "geometry-type",
            "geozero-error",
            "glyph-metrics",
            "glyph-outline",
            "glyph-position",
            "gml-version",
            "grid-algorithm",
            "grid-options",
            "header",
            "histogram",
            "icon-style",
            "index-hit",
            "inscribed-circle",
            "intervalvalue",
            "inverse-result",
            "invokeinfo",
            "join-style",
            "kml-error",
            "label-style",
            "layer",
            "library-info",
            "line-point",
            "line-style",
            "loadresult",
            "logfield",
            "logical-type",
            "logicaltype",
            "loglevel",
            "macro-def",
            "marc-error",
            "marc-record",
            "mercator-pos",
            "multi-geometry",
            "mvt-error",
            "null-handling",
            "number-result",
            "open-flag",
            "optimizer-spec",
            "palette-interp",
            "parse-error",
            "parse-outcome",
            "parser-spec",
            "path-command",
            "pixel-vec-entry",
            "placemark",
            "plan-node",
            "plan-shape",
            "poly-style",
            "polygon-result",
            "polygonize-options",
            "postgis-error",
            "pragma-spec",
            "precision-type",
            "prepared-result",
            "proj-error",
            "proj-info",
            "property",
            "property-value",
            "proximity-options",
            "raster-error",
            "raster-window",
            "rasterio-resample",
            "rat-field-type",
            "rat-field-usage",
            "rat-table-type",
            "replacement-scan",
            "resample-method",
            "rewrite-directive",
            "s32-result",
            "scan-filter",
            "scan-request",
            "secret-kv",
            "secret-param",
            "setting-scope",
            "setting-type",
            "sfcgal-error",
            "shaped-text",
            "shaping-error",
            "shaping-options",
            "sql-ext-error",
            "string-result",
            "structured-rewrite",
            "style",
            "subfield",
            "svg-options",
            "table-filter",
            "table-open-result",
            "tile",
            "tile-geometry",
            "tile-id",
            "tile-point",
            "tile-stats",
            "topology-error",
            "transform-options",
            "translate-options",
            "ts-filter",
            "ts-filter-op",
            "ts-open-result",
            "uuidvalue",
            "valid-detail",
            "value-type",
            "viewshed-mode",
            "viewshed-options",
            "void-result",
            "vrt-options",
            "vrt-source",
            "warp-options",
            "warp-options-ex",
            "wgs84-pos",
            "winding",
            "window-frame",
            "wkb-byte-order",
            "wkb-result",
            "wkt-result",
        ],
    });
}

use bindings::duckdb::extension::catalog;
use bindings::duckdb::extension::column_types;
use bindings::duckdb::extension::runtime;
use bindings::duckdb::extension::types;
use bindings::exports::duckdb::extension::callback_dispatch;
use bindings::exports::duckdb::extension::guest;

use bindings::postgis::wasm::postgis_accessors as pg_acc;
use bindings::postgis::wasm::postgis_aggregates as pg_agg;
use bindings::postgis::wasm::postgis_batch;
use bindings::postgis::wasm::postgis_clustering as pg_cluster;
use bindings::postgis::wasm::postgis_constructors as pg_ctor;
use bindings::postgis::wasm::postgis_geocoder as pg_geo;
use bindings::postgis::wasm::postgis_geodetic as pg_geog;
use bindings::postgis::wasm::postgis_linear_ref as pg_lin;
use bindings::postgis::wasm::postgis_measurements as pg_meas;
use bindings::postgis::wasm::postgis_metadata;
use bindings::postgis::wasm::postgis_operators as pg_op;
use bindings::postgis::wasm::postgis_output as pg_out;
use bindings::postgis::wasm::postgis_predicates as pg_pred;
use bindings::postgis::wasm::postgis_processing as pg_proc;
use bindings::postgis::wasm::postgis_raster_accessors as pg_rast_acc;
use bindings::postgis::wasm::postgis_raster_aggregates as pg_rast_agg;
use bindings::postgis::wasm::postgis_raster_constructors as pg_rast_ctor;
use bindings::postgis::wasm::postgis_raster_mapalgebra as pg_rast_ma;
use bindings::postgis::wasm::postgis_raster_output as pg_rast_out;
use bindings::postgis::wasm::postgis_raster_pixels as pg_rast_px;
use bindings::postgis::wasm::postgis_raster_predicates as pg_rast_pred;
use bindings::postgis::wasm::postgis_raster_processing as pg_rast_proc;
use bindings::postgis::wasm::postgis_raster_stats as pg_rast_stats;
use bindings::postgis::wasm::postgis_raster_tile;
use bindings::postgis::wasm::postgis_raster_types as pg_rast_types;
use bindings::postgis::wasm::postgis_raster_types::{Raster, RasterError};
use bindings::postgis::wasm::postgis_raster_vector as pg_rast_vec;
use bindings::postgis::wasm::postgis_sfcgal as pg_sfcgal;
use bindings::postgis::wasm::postgis_spatial_index as pg_strtree;
use bindings::postgis::wasm::postgis_three_d as pg_threed;
use bindings::postgis::wasm::postgis_topology_edit as pg_topo_edit;
use bindings::postgis::wasm::postgis_topology_output as pg_topo_out;
use bindings::postgis::wasm::postgis_topology_query as pg_topo_query;
use bindings::postgis::wasm::postgis_topology_topogeom as pg_topogeom;
use bindings::postgis::wasm::postgis_topology_types as pg_topo_types;
use bindings::postgis::wasm::postgis_topology_types::{Topology, TopologyError};
use bindings::postgis::wasm::postgis_transformations as pg_xform;
use bindings::postgis::wasm::postgis_types::{Geography, Geometry, PostgisError};
use bindings::postgis::wasm::postgis_version;

struct PostgisBridge;

// ─── Duckvalue arg helpers ───
//
// Mirror of sqlite-emit's `arg_text`/`arg_blob`/`arg_i64` set
// but unpacking from `types::Duckvalue` arms. The DuckDB FROZEN
// set is wider than SQLite's: integer / unsigned / date / etc.
// arms all coerce to a common Rust primitive via the four
// helpers below.

fn dv_text<'a>(
    args: &'a [types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<&'a str, types::Duckerror> {
    match args.get(idx) {
        Some(types::Duckvalue::Text(s)) => Ok(s.as_str()),
        _ => Err(types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be VARCHAR"
        ))),
    }
}

fn dv_blob<'a>(
    args: &'a [types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<&'a [u8], types::Duckerror> {
    match args.get(idx) {
        Some(types::Duckvalue::Blob(b)) => Ok(b.as_slice()),
        Some(types::Duckvalue::Text(s)) => Ok(s.as_bytes()),
        _ => Err(types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be BLOB"
        ))),
    }
}

fn dv_f64(args: &[types::Duckvalue], idx: usize, name: &str) -> Result<f64, types::Duckerror> {
    match args.get(idx) {
        Some(types::Duckvalue::Float64(v)) => Ok(*v),
        Some(types::Duckvalue::Float32(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Int64(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Int32(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Uint64(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Uint32(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Int16(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Int8(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Uint16(v)) => Ok(*v as f64),
        Some(types::Duckvalue::Uint8(v)) => Ok(*v as f64),
        _ => Err(types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be DOUBLE"
        ))),
    }
}

fn dv_i64(args: &[types::Duckvalue], idx: usize, name: &str) -> Result<i64, types::Duckerror> {
    match args.get(idx) {
        Some(types::Duckvalue::Int64(v)) => Ok(*v),
        Some(types::Duckvalue::Int32(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Uint64(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Uint32(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Int16(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Int8(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Uint16(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Uint8(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Boolean(b)) => Ok(if *b { 1 } else { 0 }),
        Some(types::Duckvalue::Float64(v)) => Ok(*v as i64),
        Some(types::Duckvalue::Float32(v)) => Ok(*v as i64),
        _ => Err(types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be INTEGER"
        ))),
    }
}

fn dv_bool(args: &[types::Duckvalue], idx: usize, name: &str) -> Result<bool, types::Duckerror> {
    match args.get(idx) {
        Some(types::Duckvalue::Boolean(b)) => Ok(*b),
        Some(types::Duckvalue::Int64(v)) => Ok(*v != 0),
        Some(types::Duckvalue::Int32(v)) => Ok(*v != 0),
        _ => Err(types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be BOOLEAN"
        ))),
    }
}

/// Generic error formatter for fallible upstream WIT calls. The
/// dispatch arm wraps the upstream error via this fn into a
/// String the Duckerror::Invalidargument arm carries.
fn shim_err_string<E: core::fmt::Debug>(e: E) -> String {
    format!("{:?}", e)
}

// ── JSON-as-TEXT primitive `list<X>` param helpers ──
//
// Mirror of sqlite-emit's `parse_json_list_<suffix>` set but
// error-wrapped into `types::Duckerror::Invalidargument`. The SQL
// caller passes a JSON-array literal in the TEXT/VARCHAR arg
// (e.g. `'[1.0, 2.0, 3.0]'`); the helper decodes via serde_json
// into a `Vec<T>` which the dispatch arm passes to the WIT
// function as `&[T]`.

fn parse_json_list_f64(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<f64>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<f64>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of f64 ({e})"
        ))
    })
}

fn parse_json_list_i32(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<i32>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<i32>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of s32 ({e})"
        ))
    })
}

fn parse_json_list_i64(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<i64>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<i64>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of s64 ({e})"
        ))
    })
}

fn parse_json_list_u32(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<u32>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<u32>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of u32 ({e})"
        ))
    })
}

fn parse_json_list_u64(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<u64>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<u64>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of u64 ({e})"
        ))
    })
}

fn parse_json_list_u8(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<u8>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<u8>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of u8 ({e})"
        ))
    })
}

fn parse_json_list_bool(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bool>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bool>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of bool ({e})"
        ))
    })
}

fn parse_json_list_string(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<String>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<String>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of string ({e})"
        ))
    })
}

// #674: `list<list<u8>>` param helper — batched WKB blobs for the
// postgis `st_*_batch` family. SQL passes JSON text matching
// `Vec<Vec<u8>>` (nested arrays of byte integers); symmetric with
// the `list<list<X>>` return convention.
fn parse_json_list_list_u8(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<u8>>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<u8>>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of byte arrays ({e})"
        ))
    })
}

// #695: `list<list<X>>` param helpers for primitive non-u8
// elements. Today's surface: postgis raster `st-set-values`
// (`list<list<f64>>`) + flatgeobuf coord-list constructors.
// Symmetric with the `RetShape::JsonText { ListListPrim }` return.
fn parse_json_list_list_f64(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<f64>>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<f64>>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of f64 arrays ({e})"
        ))
    })
}

fn parse_json_list_list_i32(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<i32>>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<i32>>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of s32 arrays ({e})"
        ))
    })
}

fn parse_json_list_list_i64(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<i64>>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<i64>>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of s64 arrays ({e})"
        ))
    })
}

fn parse_json_list_list_u32(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<u32>>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<u32>>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of u32 arrays ({e})"
        ))
    })
}

fn parse_json_list_list_u64(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<u64>>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<u64>>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of u64 arrays ({e})"
        ))
    })
}

fn parse_json_list_list_bool(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<bool>>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bool>>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of bool arrays ({e})"
        ))
    })
}

fn parse_json_list_list_string(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<String>>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<String>>>(text).map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of string arrays ({e})"
        ))
    })
}

// --- Colvec <-> row-major helpers (#653 columnar callback-dispatch,
//     #659 micro-opts on the lift/lower hot path) ---

/// Validity-bitmap probe. An empty bitmap means "all rows valid"
/// (matching DuckDB's null-validity pointer convention); otherwise
/// bit i of the little-endian byte stream is set iff row i is non-NULL.
fn cv_is_valid(validity: &[u8], i: usize) -> bool {
    if validity.is_empty() {
        return true;
    }
    match validity.get(i / 8) {
        Some(byte) => (byte & (1u8 << (i % 8))) != 0,
        None => true,
    }
}

/// (#659 Opt 1) Lift a single cell from a colvec into a Duckvalue
/// without materializing the whole column. Used by the per-row
/// dispatch loops in `call_scalar_batch_col` / `call_cast_col` to
/// avoid the `Vec<Vec<Duckvalue>>` intermediate. Variable-width
/// arms (Text / Blob / Complex) clone; fixed-width arms copy.
fn colvec_get(cv: &column_types::Colvec, i: usize) -> types::Duckvalue {
    if !cv_is_valid(cv.validity.as_slice(), i) {
        return types::Duckvalue::Null;
    }
    let v = match &cv.data {
        column_types::Column::Boolean(xs) => xs.get(i).copied().map(types::Duckvalue::Boolean),
        column_types::Column::Int64(xs) => xs.get(i).copied().map(types::Duckvalue::Int64),
        column_types::Column::Uint64(xs) => xs.get(i).copied().map(types::Duckvalue::Uint64),
        column_types::Column::Float64(xs) => xs.get(i).copied().map(types::Duckvalue::Float64),
        column_types::Column::Int32(xs) => xs.get(i).copied().map(types::Duckvalue::Int32),
        column_types::Column::Timestamp(xs) => xs.get(i).copied().map(types::Duckvalue::Timestamp),
        column_types::Column::Int8(xs) => xs.get(i).copied().map(types::Duckvalue::Int8),
        column_types::Column::Int16(xs) => xs.get(i).copied().map(types::Duckvalue::Int16),
        column_types::Column::Uint8(xs) => xs.get(i).copied().map(types::Duckvalue::Uint8),
        column_types::Column::Uint16(xs) => xs.get(i).copied().map(types::Duckvalue::Uint16),
        column_types::Column::Uint32(xs) => xs.get(i).copied().map(types::Duckvalue::Uint32),
        column_types::Column::Float32(xs) => xs.get(i).copied().map(types::Duckvalue::Float32),
        column_types::Column::Date(xs) => xs.get(i).copied().map(types::Duckvalue::Date),
        column_types::Column::Time(xs) => xs.get(i).copied().map(types::Duckvalue::Time),
        column_types::Column::Timestamptz(xs) => {
            xs.get(i).copied().map(types::Duckvalue::Timestamptz)
        }
        column_types::Column::Decimal(xs) => xs.get(i).cloned().map(|d| {
            types::Duckvalue::Decimal(types::Decimalvalue {
                lower: d.lower,
                upper: d.upper,
                width: d.width,
                scale: d.scale,
            })
        }),
        column_types::Column::Interval(xs) => xs.get(i).cloned().map(|d| {
            types::Duckvalue::Interval(types::Intervalvalue {
                months: d.months,
                days: d.days,
                micros: d.micros,
            })
        }),
        column_types::Column::Uuid(xs) => xs
            .get(i)
            .cloned()
            .map(|d| types::Duckvalue::Uuid(types::Uuidvalue { hi: d.hi, lo: d.lo })),
        column_types::Column::Text(xs) => xs.get(i).cloned().map(types::Duckvalue::Text),
        column_types::Column::Blob(xs) => xs.get(i).cloned().map(types::Duckvalue::Blob),
        column_types::Column::Complex(xs) => xs.get(i).cloned().map(|c| {
            types::Duckvalue::Complex(types::Complexvalue {
                type_expr: c.type_expr,
                json: c.json,
            })
        }),
    };
    v.unwrap_or(types::Duckvalue::Null)
}

/// (#659 Opt 1) Validate that every input colvec shares the same
/// row count and return it. Mismatched lengths return an Internal
/// error -- the host-side dispatcher should never deliver ragged
/// column slices.
fn validate_colvec_rows(args: &[column_types::Colvec]) -> Result<usize, types::Duckerror> {
    let n_rows = if args.is_empty() {
        0
    } else {
        args[0].rows as usize
    };
    for (j, cv) in args.iter().enumerate() {
        if cv.rows as usize != n_rows {
            return Err(types::Duckerror::Internal(format!(
                "columnar dispatch: arg-column {j} has rows={} but expected {n_rows}",
                cv.rows,
            )));
        }
    }
    Ok(n_rows)
}

/// (#659 Opt 1 + Opt 3) Materialize row `i` from a slice of colvecs
/// into the provided buffer. The buffer is cleared first; capacity
/// is preserved across calls, so the dispatch loop can reuse a
/// single allocation across rows. This avoids the
/// `Vec<Vec<Duckvalue>>` intermediate (one column-Vec per arg plus
/// one row-Vec per row) that the original `colvecs_to_rows`
/// allocated.
fn materialize_row(args: &[column_types::Colvec], i: usize, out: &mut Vec<types::Duckvalue>) {
    out.clear();
    if out.capacity() < args.len() {
        out.reserve(args.len() - out.capacity());
    }
    for cv in args {
        out.push(colvec_get(cv, i));
    }
}

/// Lift a single `colvec` to a row-major `Vec<Duckvalue>`.
/// (Retained for the aggregate dispatch path; the scalar-batch and
/// cast paths now use `colvec_get` directly via `materialize_row`.)
fn colvec_to_values(cv: &column_types::Colvec) -> Vec<types::Duckvalue> {
    let n = cv.rows as usize;
    let mut out = Vec::with_capacity(n);
    for i in 0..n {
        out.push(colvec_get(cv, i));
    }
    out
}

/// Lift `args: Vec<Colvec>` to `Vec<Vec<Duckvalue>>`. (Retained for
/// the aggregate dispatch path -- aggregate arms iterate `rows`
/// directly. The scalar-batch and cast paths use `validate_colvec_rows`
/// + `materialize_row` to skip the intermediate `Vec<Vec<_>>`.)
fn colvecs_to_rows(
    args: &[column_types::Colvec],
) -> Result<Vec<Vec<types::Duckvalue>>, types::Duckerror> {
    let n_rows = validate_colvec_rows(args)?;
    let n_args = args.len();
    let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(n_rows);
    for i in 0..n_rows {
        let mut row: Vec<types::Duckvalue> = Vec::with_capacity(n_args);
        for cv in args {
            row.push(colvec_get(cv, i));
        }
        rows.push(row);
    }
    Ok(rows)
}

/// (#659 Opt 2) Lower a row-major `Vec<Duckvalue>` back into a
/// `colvec`. Fuses the original two passes (`build_validity_bitmap`
/// then `iter().find()` for the type sniff) into a single sweep,
/// then dispatches once on the captured arm-kind to pre-allocate
/// the typed `Vec<T>`. An all-NULL / empty input falls back to a
/// boolean column (the validity bitmap carries the NULLs, so the
/// data arm is just a placeholder of the right length).
///
/// Note: the underlying lift-arm signature (handle -> Duckvalue)
/// forces a per-cell tag dispatch in the build loops. Threading
/// the arm's `RetShape` (known at codegen time) through a per-handle
/// shape registry would let us skip the tag check entirely, but
/// that touches register.rs + the dispatch surface and is out of
/// scope for #659. The sniff path here keeps the change local to
/// emit_lib.rs while still removing one full pass over `values`.
fn values_to_colvec(
    values: Vec<types::Duckvalue>,
) -> Result<column_types::Colvec, types::Duckerror> {
    // Local arm-kind tag captured during the validity pass. Cheaper
    // than holding a `&Duckvalue` borrow into `values` once we want
    // to consume `values` for the variable-width arms.
    #[derive(Clone, Copy)]
    enum K {
        Bool,
        I64,
        U64,
        F64,
        I32,
        Ts,
        I8,
        I16,
        U8,
        U16,
        U32,
        F32,
        Date,
        Time,
        Tstz,
        Decimal,
        Interval,
        Uuid,
        Text,
        Blob,
        Complex,
    }
    fn kind_of(v: &types::Duckvalue) -> Option<K> {
        match v {
            types::Duckvalue::Null => None,
            types::Duckvalue::Boolean(_) => Some(K::Bool),
            types::Duckvalue::Int64(_) => Some(K::I64),
            types::Duckvalue::Uint64(_) => Some(K::U64),
            types::Duckvalue::Float64(_) => Some(K::F64),
            types::Duckvalue::Int32(_) => Some(K::I32),
            types::Duckvalue::Timestamp(_) => Some(K::Ts),
            types::Duckvalue::Int8(_) => Some(K::I8),
            types::Duckvalue::Int16(_) => Some(K::I16),
            types::Duckvalue::Uint8(_) => Some(K::U8),
            types::Duckvalue::Uint16(_) => Some(K::U16),
            types::Duckvalue::Uint32(_) => Some(K::U32),
            types::Duckvalue::Float32(_) => Some(K::F32),
            types::Duckvalue::Date(_) => Some(K::Date),
            types::Duckvalue::Time(_) => Some(K::Time),
            types::Duckvalue::Timestamptz(_) => Some(K::Tstz),
            types::Duckvalue::Decimal(_) => Some(K::Decimal),
            types::Duckvalue::Interval(_) => Some(K::Interval),
            types::Duckvalue::Uuid(_) => Some(K::Uuid),
            types::Duckvalue::Text(_) => Some(K::Text),
            types::Duckvalue::Blob(_) => Some(K::Blob),
            types::Duckvalue::Complex(_) => Some(K::Complex),
        }
    }

    let n = values.len();
    let rows = n as u32;

    // Single-pass validity build + first-non-null sniff.
    let mut bits: Vec<u8> = alloc::vec![0u8; (n + 7) / 8];
    let mut any_null = false;
    let mut kind: Option<K> = None;
    for (i, v) in values.iter().enumerate() {
        if matches!(v, types::Duckvalue::Null) {
            any_null = true;
        } else {
            bits[i / 8] |= 1u8 << (i % 8);
            if kind.is_none() {
                kind = kind_of(v);
            }
        }
    }
    let validity = if any_null { bits } else { Vec::new() };

    // Typed pre-allocation per arm. `Vec::with_capacity(n)` +
    // pushes is semantically identical to `iter().map().collect()`
    // for a slice iterator (which already calls `size_hint`), but
    // hoisting the allocation up to the arm head makes the pattern
    // obvious for future per-shape specialization.
    let data = match kind {
        None => column_types::Column::Boolean(alloc::vec![false; n]),
        Some(K::Bool) => {
            let mut xs: Vec<bool> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Boolean(b) = v {
                    *b
                } else {
                    false
                });
            }
            column_types::Column::Boolean(xs)
        }
        Some(K::I64) => {
            let mut xs: Vec<i64> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Int64(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Int64(xs)
        }
        Some(K::U64) => {
            let mut xs: Vec<u64> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Uint64(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Uint64(xs)
        }
        Some(K::F64) => {
            let mut xs: Vec<f64> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Float64(x) = v {
                    *x
                } else {
                    0.0
                });
            }
            column_types::Column::Float64(xs)
        }
        Some(K::I32) => {
            let mut xs: Vec<i32> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Int32(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Int32(xs)
        }
        Some(K::Ts) => {
            let mut xs: Vec<i64> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Timestamp(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Timestamp(xs)
        }
        Some(K::I8) => {
            let mut xs: Vec<i8> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Int8(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Int8(xs)
        }
        Some(K::I16) => {
            let mut xs: Vec<i16> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Int16(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Int16(xs)
        }
        Some(K::U8) => {
            let mut xs: Vec<u8> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Uint8(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Uint8(xs)
        }
        Some(K::U16) => {
            let mut xs: Vec<u16> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Uint16(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Uint16(xs)
        }
        Some(K::U32) => {
            let mut xs: Vec<u32> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Uint32(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Uint32(xs)
        }
        Some(K::F32) => {
            let mut xs: Vec<f32> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Float32(x) = v {
                    *x
                } else {
                    0.0
                });
            }
            column_types::Column::Float32(xs)
        }
        Some(K::Date) => {
            let mut xs: Vec<i32> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Date(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Date(xs)
        }
        Some(K::Time) => {
            let mut xs: Vec<i64> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Time(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Time(xs)
        }
        Some(K::Tstz) => {
            let mut xs: Vec<i64> = Vec::with_capacity(n);
            for v in &values {
                xs.push(if let types::Duckvalue::Timestamptz(x) = v {
                    *x
                } else {
                    0
                });
            }
            column_types::Column::Timestamptz(xs)
        }
        Some(K::Decimal) => {
            let mut xs: Vec<column_types::Decimalvalue> = Vec::with_capacity(n);
            for v in &values {
                xs.push(match v {
                    types::Duckvalue::Decimal(d) => column_types::Decimalvalue {
                        lower: d.lower,
                        upper: d.upper,
                        width: d.width,
                        scale: d.scale,
                    },
                    _ => column_types::Decimalvalue {
                        lower: 0,
                        upper: 0,
                        width: 0,
                        scale: 0,
                    },
                });
            }
            column_types::Column::Decimal(xs)
        }
        Some(K::Interval) => {
            let mut xs: Vec<column_types::Intervalvalue> = Vec::with_capacity(n);
            for v in &values {
                xs.push(match v {
                    types::Duckvalue::Interval(d) => column_types::Intervalvalue {
                        months: d.months,
                        days: d.days,
                        micros: d.micros,
                    },
                    _ => column_types::Intervalvalue {
                        months: 0,
                        days: 0,
                        micros: 0,
                    },
                });
            }
            column_types::Column::Interval(xs)
        }
        Some(K::Uuid) => {
            let mut xs: Vec<column_types::Uuidvalue> = Vec::with_capacity(n);
            for v in &values {
                xs.push(match v {
                    types::Duckvalue::Uuid(d) => column_types::Uuidvalue { hi: d.hi, lo: d.lo },
                    _ => column_types::Uuidvalue { hi: 0, lo: 0 },
                });
            }
            column_types::Column::Uuid(xs)
        }
        Some(K::Text) => {
            let mut xs: Vec<String> = Vec::with_capacity(n);
            for v in values.into_iter() {
                xs.push(match v {
                    types::Duckvalue::Text(s) => s,
                    _ => String::new(),
                });
            }
            column_types::Column::Text(xs)
        }
        Some(K::Blob) => {
            let mut xs: Vec<Vec<u8>> = Vec::with_capacity(n);
            for v in values.into_iter() {
                xs.push(match v {
                    types::Duckvalue::Blob(b) => b,
                    _ => Vec::new(),
                });
            }
            column_types::Column::Blob(xs)
        }
        Some(K::Complex) => {
            let mut xs: Vec<column_types::Complexvalue> = Vec::with_capacity(n);
            for v in values.into_iter() {
                xs.push(match v {
                    types::Duckvalue::Complex(c) => column_types::Complexvalue {
                        type_expr: c.type_expr,
                        json: c.json,
                    },
                    _ => column_types::Complexvalue {
                        type_expr: String::new(),
                        json: String::new(),
                    },
                });
            }
            column_types::Column::Complex(xs)
        }
    };

    Ok(column_types::Colvec {
        data,
        validity,
        rows,
    })
}

// ─── PostGIS WKB decoders ───

fn from_wkb(bytes: &[u8], name: &str) -> Result<Geometry, types::Duckerror> {
    Geometry::from_wkb(bytes).map_err(|e| {
        types::Duckerror::Invalidargument(format!("{name}: {}", postgis_err_string(e)))
    })
}

fn geog_from_wkb(bytes: &[u8], name: &str) -> Result<Geography, types::Duckerror> {
    Geography::from_wkb(bytes).map_err(|e| {
        types::Duckerror::Invalidargument(format!("{name}: {}", postgis_err_string(e)))
    })
}

fn postgis_err_string(e: PostgisError) -> String {
    match e {
        PostgisError::InvalidGeometry(s)
        | PostgisError::ParseError(s)
        | PostgisError::UnsupportedOperation(s)
        | PostgisError::NumericError(s)
        | PostgisError::SridMismatch(s)
        | PostgisError::General(s) => s,
    }
}

// ─── PostGIS raster decoders ───

fn from_raster_binary(bytes: &[u8], name: &str) -> Result<Raster, types::Duckerror> {
    bindings::postgis::wasm::postgis_raster_types::from_binary(bytes).map_err(|e| {
        types::Duckerror::Invalidargument(format!("{}: {}", name, raster_err_string(e)))
    })
}

fn raster_err_string(e: RasterError) -> String {
    match e {
        RasterError::ParseError(s)
        | RasterError::OutOfBounds(s)
        | RasterError::TypeMismatch(s)
        | RasterError::General(s) => s,
    }
}

// ─── PostGIS topology decoders ───

fn from_topology_bytes(bytes: &[u8], name: &str) -> Result<Topology, types::Duckerror> {
    bindings::postgis::wasm::postgis_topology_types::from_bytes(bytes).map_err(|e| {
        types::Duckerror::Invalidargument(format!("{}: {}", name, topology_err_string(e)))
    })
}

fn topology_err_string(e: TopologyError) -> String {
    match e {
        TopologyError::InvalidTopology(s) | TopologyError::General(s) => s,
        TopologyError::NodeNotFound(id) => format!("node not found: {}", id),
        TopologyError::EdgeNotFound(id) => format!("edge not found: {}", id),
        TopologyError::FaceNotFound(id) => format!("face not found: {}", id),
    }
}

// ─── Per-record wit-value marshaling helpers ───
// Records ride on `Duckvalue::Complex { type_expr, json }`.
// Decode parses `json` straight into UPSTREAM via
// serde_json::from_str (UPSTREAM derives serde derives via
// wit-bindgen's `additional_derives` arg above). Encode
// round-trips UPSTREAM → JSON → Complex carrier.
fn arg_witvalue_address_component(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_geocoder::AddressComponent, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_geocoder::AddressComponent>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_address_component(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_geocoder::AddressComponent>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_geocoder::AddressComponent>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of address-component ({e})"
            ))
        })
}

fn parse_json_list_list_record_address_component(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<bindings::postgis::wasm::postgis_geocoder::AddressComponent>>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_geocoder::AddressComponent>>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of arrays of address-component ({e})"
        ))
    })
}

fn ret_to_witvalue_address_component(
    upstream: bindings::postgis::wasm::postgis_geocoder::AddressComponent,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode address_component wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-geocoder/address-component".into(),
        json,
    }))
}

fn arg_witvalue_parsed_address(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_geocoder::ParsedAddress, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_geocoder::ParsedAddress>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_parsed_address(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_geocoder::ParsedAddress>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_geocoder::ParsedAddress>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of parsed-address ({e})"
            ))
        })
}

fn parse_json_list_list_record_parsed_address(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<bindings::postgis::wasm::postgis_geocoder::ParsedAddress>>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_geocoder::ParsedAddress>>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of arrays of parsed-address ({e})"
            ))
        })
}

fn ret_to_witvalue_parsed_address(
    upstream: bindings::postgis::wasm::postgis_geocoder::ParsedAddress,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode parsed_address wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-geocoder/parsed-address".into(),
        json,
    }))
}

fn arg_witvalue_cast_rewrite(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_metadata::CastRewrite, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_metadata::CastRewrite>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_cast_rewrite(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_metadata::CastRewrite>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_metadata::CastRewrite>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of cast-rewrite ({e})"
            ))
        })
}

fn parse_json_list_list_record_cast_rewrite(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<bindings::postgis::wasm::postgis_metadata::CastRewrite>>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_metadata::CastRewrite>>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of arrays of cast-rewrite ({e})"
            ))
        })
}

fn ret_to_witvalue_cast_rewrite(
    upstream: bindings::postgis::wasm::postgis_metadata::CastRewrite,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode cast_rewrite wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-metadata/cast-rewrite".into(),
        json,
    }))
}

fn arg_witvalue_operator_rewrite(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_metadata::OperatorRewrite, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_metadata::OperatorRewrite>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_operator_rewrite(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_metadata::OperatorRewrite>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_metadata::OperatorRewrite>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of operator-rewrite ({e})"
            ))
        })
}

fn parse_json_list_list_record_operator_rewrite(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<bindings::postgis::wasm::postgis_metadata::OperatorRewrite>>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_metadata::OperatorRewrite>>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of arrays of operator-rewrite ({e})"
        ))
    })
}

fn ret_to_witvalue_operator_rewrite(
    upstream: bindings::postgis::wasm::postgis_metadata::OperatorRewrite,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode operator_rewrite wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-metadata/operator-rewrite".into(),
        json,
    }))
}

fn arg_witvalue_preprocessor_pattern(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_metadata::PreprocessorPattern, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_metadata::PreprocessorPattern>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_preprocessor_pattern(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_metadata::PreprocessorPattern>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_metadata::PreprocessorPattern>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of preprocessor-pattern ({e})"
        ))
    })
}

fn parse_json_list_list_record_preprocessor_pattern(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    Vec<Vec<bindings::postgis::wasm::postgis_metadata::PreprocessorPattern>>,
    types::Duckerror,
> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_metadata::PreprocessorPattern>>>(text)
        .map_err(|e| types::Duckerror::Invalidargument(
            format!("{name}: arg {idx} must be JSON array of arrays of preprocessor-pattern ({e})")))
}

fn ret_to_witvalue_preprocessor_pattern(
    upstream: bindings::postgis::wasm::postgis_metadata::PreprocessorPattern,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode preprocessor_pattern wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-metadata/preprocessor-pattern".into(),
        json,
    }))
}

fn arg_witvalue_raster_metadata(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_raster_types::RasterMetadata, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_raster_types::RasterMetadata>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_raster_metadata(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_raster_types::RasterMetadata>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_raster_types::RasterMetadata>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of raster-metadata ({e})"
            ))
        })
}

fn parse_json_list_list_record_raster_metadata(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<bindings::postgis::wasm::postgis_raster_types::RasterMetadata>>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_raster_types::RasterMetadata>>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of arrays of raster-metadata ({e})"
        ))
    })
}

fn ret_to_witvalue_raster_metadata(
    upstream: bindings::postgis::wasm::postgis_raster_types::RasterMetadata,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode raster_metadata wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-raster-types/raster-metadata".into(),
        json,
    }))
}

fn arg_witvalue_band_metadata(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_raster_accessors::BandMetadata, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_raster_accessors::BandMetadata>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_band_metadata(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_raster_accessors::BandMetadata>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_raster_accessors::BandMetadata>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of band-metadata ({e})"
        ))
    })
}

fn parse_json_list_list_record_band_metadata(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    Vec<Vec<bindings::postgis::wasm::postgis_raster_accessors::BandMetadata>>,
    types::Duckerror,
> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_raster_accessors::BandMetadata>>>(text)
        .map_err(|e| types::Duckerror::Invalidargument(
            format!("{name}: arg {idx} must be JSON array of arrays of band-metadata ({e})")))
}

fn ret_to_witvalue_band_metadata(
    upstream: bindings::postgis::wasm::postgis_raster_accessors::BandMetadata,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode band_metadata wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-raster-accessors/band-metadata".into(),
        json,
    }))
}

fn arg_witvalue_pixel_coord(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_raster_pixels::PixelCoord, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_raster_pixels::PixelCoord>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_pixel_coord(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_raster_pixels::PixelCoord>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_raster_pixels::PixelCoord>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of pixel-coord ({e})"
            ))
        })
}

fn parse_json_list_list_record_pixel_coord(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<bindings::postgis::wasm::postgis_raster_pixels::PixelCoord>>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_raster_pixels::PixelCoord>>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of arrays of pixel-coord ({e})"
        ))
    })
}

fn ret_to_witvalue_pixel_coord(
    upstream: bindings::postgis::wasm::postgis_raster_pixels::PixelCoord,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode pixel_coord wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-raster-pixels/pixel-coord".into(),
        json,
    }))
}

fn arg_witvalue_pixel_value_coord(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_raster_pixels::PixelValueCoord, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_raster_pixels::PixelValueCoord>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_pixel_value_coord(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_raster_pixels::PixelValueCoord>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_raster_pixels::PixelValueCoord>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of pixel-value-coord ({e})"
        ))
    })
}

fn parse_json_list_list_record_pixel_value_coord(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    Vec<Vec<bindings::postgis::wasm::postgis_raster_pixels::PixelValueCoord>>,
    types::Duckerror,
> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_raster_pixels::PixelValueCoord>>>(text)
        .map_err(|e| types::Duckerror::Invalidargument(
            format!("{name}: arg {idx} must be JSON array of arrays of pixel-value-coord ({e})")))
}

fn ret_to_witvalue_pixel_value_coord(
    upstream: bindings::postgis::wasm::postgis_raster_pixels::PixelValueCoord,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode pixel_value_coord wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-raster-pixels/pixel-value-coord".into(),
        json,
    }))
}

fn arg_witvalue_band_values_entry(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_raster_pixels::BandValuesEntry, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_raster_pixels::BandValuesEntry>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_band_values_entry(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_raster_pixels::BandValuesEntry>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_raster_pixels::BandValuesEntry>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of band-values-entry ({e})"
        ))
    })
}

fn parse_json_list_list_record_band_values_entry(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    Vec<Vec<bindings::postgis::wasm::postgis_raster_pixels::BandValuesEntry>>,
    types::Duckerror,
> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_raster_pixels::BandValuesEntry>>>(text)
        .map_err(|e| types::Duckerror::Invalidargument(
            format!("{name}: arg {idx} must be JSON array of arrays of band-values-entry ({e})")))
}

fn ret_to_witvalue_band_values_entry(
    upstream: bindings::postgis::wasm::postgis_raster_pixels::BandValuesEntry,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode band_values_entry wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-raster-pixels/band-values-entry".into(),
        json,
    }))
}

fn arg_witvalue_summary_stats(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_raster_stats::SummaryStats, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_raster_stats::SummaryStats>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_summary_stats(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_raster_stats::SummaryStats>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_raster_stats::SummaryStats>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of summary-stats ({e})"
            ))
        })
}

fn parse_json_list_list_record_summary_stats(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<bindings::postgis::wasm::postgis_raster_stats::SummaryStats>>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_raster_stats::SummaryStats>>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of arrays of summary-stats ({e})"
        ))
    })
}

fn ret_to_witvalue_summary_stats(
    upstream: bindings::postgis::wasm::postgis_raster_stats::SummaryStats,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode summary_stats wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-raster-stats/summary-stats".into(),
        json,
    }))
}

fn arg_witvalue_histogram_bin(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_raster_stats::HistogramBin, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_raster_stats::HistogramBin>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_histogram_bin(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_raster_stats::HistogramBin>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_raster_stats::HistogramBin>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of histogram-bin ({e})"
            ))
        })
}

fn parse_json_list_list_record_histogram_bin(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<bindings::postgis::wasm::postgis_raster_stats::HistogramBin>>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_raster_stats::HistogramBin>>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of arrays of histogram-bin ({e})"
        ))
    })
}

fn ret_to_witvalue_histogram_bin(
    upstream: bindings::postgis::wasm::postgis_raster_stats::HistogramBin,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode histogram_bin wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-raster-stats/histogram-bin".into(),
        json,
    }))
}

fn arg_witvalue_value_count(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_raster_stats::ValueCount, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_raster_stats::ValueCount>(&cmplx.json)
        .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_value_count(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_raster_stats::ValueCount>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_raster_stats::ValueCount>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of value-count ({e})"
            ))
        })
}

fn parse_json_list_list_record_value_count(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<bindings::postgis::wasm::postgis_raster_stats::ValueCount>>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_raster_stats::ValueCount>>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of arrays of value-count ({e})"
        ))
    })
}

fn ret_to_witvalue_value_count(
    upstream: bindings::postgis::wasm::postgis_raster_stats::ValueCount,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode value_count wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-raster-stats/value-count".into(),
        json,
    }))
}

fn arg_witvalue_quantile_entry(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_raster_stats::QuantileEntry, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_raster_stats::QuantileEntry>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_quantile_entry(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_raster_stats::QuantileEntry>, types::Duckerror> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_raster_stats::QuantileEntry>>(text)
        .map_err(|e| {
            types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be JSON array of quantile-entry ({e})"
            ))
        })
}

fn parse_json_list_list_record_quantile_entry(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<Vec<bindings::postgis::wasm::postgis_raster_stats::QuantileEntry>>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_raster_stats::QuantileEntry>>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of arrays of quantile-entry ({e})"
        ))
    })
}

fn ret_to_witvalue_quantile_entry(
    upstream: bindings::postgis::wasm::postgis_raster_stats::QuantileEntry,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode quantile_entry wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-raster-stats/quantile-entry".into(),
        json,
    }))
}

fn arg_witvalue_value_percent_entry(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_raster_stats::ValuePercentEntry, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_raster_stats::ValuePercentEntry>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_value_percent_entry(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_raster_stats::ValuePercentEntry>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_raster_stats::ValuePercentEntry>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of value-percent-entry ({e})"
        ))
    })
}

fn parse_json_list_list_record_value_percent_entry(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    Vec<Vec<bindings::postgis::wasm::postgis_raster_stats::ValuePercentEntry>>,
    types::Duckerror,
> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_raster_stats::ValuePercentEntry>>>(text)
        .map_err(|e| types::Duckerror::Invalidargument(
            format!("{name}: arg {idx} must be JSON array of arrays of value-percent-entry ({e})")))
}

fn ret_to_witvalue_value_percent_entry(
    upstream: bindings::postgis::wasm::postgis_raster_stats::ValuePercentEntry,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream).map_err(|e| {
        types::Duckerror::Internal(format!("encode value_percent_entry wit-value: {}", e))
    })?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-raster-stats/value-percent-entry".into(),
        json,
    }))
}

fn arg_witvalue_topo_element(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<bindings::postgis::wasm::postgis_topology_topogeom::TopoElement, types::Duckerror> {
    let cmplx = match args.get(idx) {
        Some(types::Duckvalue::Complex(c)) => c,
        _ => {
            return Err(types::Duckerror::Invalidargument(format!(
                "{name}: arg {idx} must be COMPLEX (wit-value record)"
            )))
        }
    };
    serde_json::from_str::<bindings::postgis::wasm::postgis_topology_topogeom::TopoElement>(
        &cmplx.json,
    )
    .map_err(|e| types::Duckerror::Invalidargument(format!("{name}: decode arg {idx}: {}", e)))
}

fn parse_json_list_record_topo_element(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<Vec<bindings::postgis::wasm::postgis_topology_topogeom::TopoElement>, types::Duckerror>
{
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<bindings::postgis::wasm::postgis_topology_topogeom::TopoElement>>(
        text,
    )
    .map_err(|e| {
        types::Duckerror::Invalidargument(format!(
            "{name}: arg {idx} must be JSON array of topo-element ({e})"
        ))
    })
}

fn parse_json_list_list_record_topo_element(
    args: &[types::Duckvalue],
    idx: usize,
    name: &str,
) -> Result<
    Vec<Vec<bindings::postgis::wasm::postgis_topology_topogeom::TopoElement>>,
    types::Duckerror,
> {
    let text = dv_text(args, idx, name)?;
    serde_json::from_str::<Vec<Vec<bindings::postgis::wasm::postgis_topology_topogeom::TopoElement>>>(text)
        .map_err(|e| types::Duckerror::Invalidargument(
            format!("{name}: arg {idx} must be JSON array of arrays of topo-element ({e})")))
}

fn ret_to_witvalue_topo_element(
    upstream: bindings::postgis::wasm::postgis_topology_topogeom::TopoElement,
) -> Result<types::Duckvalue, types::Duckerror> {
    let json = serde_json::to_string(&upstream)
        .map_err(|e| types::Duckerror::Internal(format!("encode topo_element wit-value: {}", e)))?;
    Ok(types::Duckvalue::Complex(types::Complexvalue {
        type_expr: "postgis:wasm@0.1.0/postgis-topology-topogeom/topo-element".into(),
        json,
    }))
}

// ─── Handle tables (u32 -> arm-index, one per dispatch family) ───
//
// `register_scalars()` / `register_aggregates()` / `register_tables()`
// allocate one handle per registration and store its arm index in the
// family-specific table. The corresponding `call_*` dispatch arm
// looks up the index and routes to the per-arm match.

fn handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

fn aggregate_handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

fn table_handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

// #661: window-function dispatch handle table. `register_windows()`
// allocates one handle per classified WindowEntry (via the existing
// aggregate-registry surface -- the @4.0.0 contract has no separate
// window-registry; the engine treats window=aggregate+frame). The
// `call_aggregate_window` arm of `aggregate_incr_dispatch::Guest`
// reads this table to route the partition+frame to the matching
// per-arm dispatch body.
fn window_handle_table() -> &'static std::sync::Mutex<std::collections::HashMap<u32, usize>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashMap<u32, usize>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashMap::new()))
}

// Identity cast handles (#64/#67): a set of `u32` handles registered by
// `register_logical_types()` for the physical-BLOB-backed logical types
// (GEOMETRY, GEOGRAPHY, RASTER, TOPOLOGY) whose `register-cast`
// callbacks are identity operations. When `call_cast` receives one of
// these handles it returns the Duckvalue unchanged instead of routing
// into the scalar dispatch. Keeps the identity-cast surface separate
// from the wired scalar `handle_table` so the (handle -> arm_index)
// map isn't polluted with sentinel slots.
fn identity_cast_handles() -> &'static std::sync::Mutex<std::collections::HashSet<u32>> {
    static T: std::sync::OnceLock<std::sync::Mutex<std::collections::HashSet<u32>>> =
        std::sync::OnceLock::new();
    T.get_or_init(|| std::sync::Mutex::new(std::collections::HashSet::new()))
}

static NEXT_HANDLE: std::sync::atomic::AtomicU32 = std::sync::atomic::AtomicU32::new(1);

impl guest::Guest for PostgisBridge {
    fn load() -> Result<types::Loadresult, types::Duckerror> {
        register_logical_types()?;
        register_scalars()?;
        register_aggregates()?;
        register_tables()?;
        register_casts()?;
        register_windows()?;
        Ok(types::Loadresult {
            name: "postgis".into(),
            version: Some("0.1.0".into()),
            requires: Vec::new().into(),
        })
    }
    fn reconfigure(_keys: Vec<String>) -> Result<bool, types::Duckerror> {
        Ok(false)
    }
    fn shutdown() -> Result<bool, types::Duckerror> {
        Ok(false)
    }
}

impl callback_dispatch::Guest for PostgisBridge {
    // === HOT PATH (duckdb:extension@4.0.0 columnar dispatch) ===
    //
    // The major-4 contract replaced the row-major call_scalar_batch /
    // call_aggregate / (and added call_cast_col) with columnar
    // variants that cross the ABI as `list<colvec>`. Correctness-first
    // migration: convert columns -> rows on demand, route through the
    // row-major cold paths, then rebuild a colvec on the way out.
    //
    // #659 micro-opts on the lift/lower paths:
    //   * Opt 1: scalar-batch / cast paths skip the
    //     `Vec<Vec<Duckvalue>>` intermediate -- they materialize one
    //     row at a time via `materialize_row` / `colvec_get` (the
    //     aggregate path still uses `colvecs_to_rows` because its
    //     arm bodies iterate `rows` directly).
    //   * Opt 2: `values_to_colvec` fuses validity + type-sniff
    //     into a single pass and pre-allocates the typed `Vec<T>`
    //     from the captured arm-kind.
    //   * Opt 3: scalar-batch reuses a single per-row buffer across
    //     iterations -- the buffer's capacity is preserved by
    //     `materialize_row` (`std::mem::take` then re-grow to
    //     `args.len()`).
    //
    // The contract envisions bulk memcpy on fixed-width columns;
    // that is a future optimisation -- mobilitydb-style workloads
    // are dominated by per-row binary/text decode in the arm
    // bodies, so the savings here are on the lift/lower prologue
    // rather than the arm dispatch itself.

    fn call_scalar_batch_col(
        handle: u32,
        args: Vec<column_types::Colvec>,
        ctx: types::Invokeinfo,
    ) -> Result<column_types::Colvec, types::Duckerror> {
        let n_rows = validate_colvec_rows(&args)?;
        let n_args = args.len();
        let base = ctx.rowindex.unwrap_or(0);
        let mut out: Vec<types::Duckvalue> = Vec::with_capacity(n_rows);
        // Pooled per-row buffer (#659 Opt 3). `materialize_row`
        // clears + re-grows on each iteration, preserving capacity
        // across rows; we mem::take it into the Guest call (which
        // consumes the Vec by value), then re-grow on the next
        // iteration. The per-row alloc is unavoidable as long as
        // call_scalar's WIT-derived signature takes
        // `args: Vec<Duckvalue>` by value -- the capacity hint at
        // least keeps the alloc right-sized.
        let mut row_buf: Vec<types::Duckvalue> = Vec::with_capacity(n_args);
        for i in 0..n_rows {
            materialize_row(&args, i, &mut row_buf);
            let row_ctx = types::Invokeinfo {
                rowindex: Some(base + i as u64),
                iswindow: ctx.iswindow,
            };
            let row_args = core::mem::take(&mut row_buf);
            out.push(<Self as callback_dispatch::Guest>::call_scalar(
                handle, row_args, row_ctx,
            )?);
        }
        values_to_colvec(out)
    }

    fn call_aggregate_col(
        handle: u32,
        args: Vec<column_types::Colvec>,
    ) -> Result<types::Duckvalue, types::Duckerror> {
        let rows = colvecs_to_rows(&args)?;
        let arm_idx = aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .get(&handle)
            .copied()
            .ok_or_else(|| types::Duckerror::Internal("unknown aggregate handle".into()))?;
        match arm_idx {
            0usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_3d_extent")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3d_extent: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let __bb = pg_agg::st_extent_threed(&refs);
                let mut __wkb: Vec<u8> = Vec::with_capacity(57);
                // ISO-WKB header: little-endian, type 1002 (LINESTRING Z), 2 points.
                __wkb.push(0x01u8);
                __wkb.extend_from_slice(&1002u32.to_le_bytes());
                __wkb.extend_from_slice(&2u32.to_le_bytes());
                __wkb.extend_from_slice(&__bb.min_x.to_le_bytes());
                __wkb.extend_from_slice(&__bb.min_y.to_le_bytes());
                __wkb.extend_from_slice(&__bb.min_z.to_le_bytes());
                __wkb.extend_from_slice(&__bb.max_x.to_le_bytes());
                __wkb.extend_from_slice(&__bb.max_y.to_le_bytes());
                __wkb.extend_from_slice(&__bb.max_z.to_le_bytes());
                Ok(types::Duckvalue::Blob(__wkb))
            }
            1usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_3dextent")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dextent: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let __bb = pg_agg::st_extent_threed(&refs);
                let mut __wkb: Vec<u8> = Vec::with_capacity(57);
                // ISO-WKB header: little-endian, type 1002 (LINESTRING Z), 2 points.
                __wkb.push(0x01u8);
                __wkb.extend_from_slice(&1002u32.to_le_bytes());
                __wkb.extend_from_slice(&2u32.to_le_bytes());
                __wkb.extend_from_slice(&__bb.min_x.to_le_bytes());
                __wkb.extend_from_slice(&__bb.min_y.to_le_bytes());
                __wkb.extend_from_slice(&__bb.min_z.to_le_bytes());
                __wkb.extend_from_slice(&__bb.max_x.to_le_bytes());
                __wkb.extend_from_slice(&__bb.max_y.to_le_bytes());
                __wkb.extend_from_slice(&__bb.max_z.to_le_bytes());
                Ok(types::Duckvalue::Blob(__wkb))
            }
            2usize => {
                let mut extras: Option<Vec<types::Duckvalue>> = None;
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_approx_count_agg")?;
                    blobs.push(bytes.to_vec());
                    if extras.is_none() {
                        extras = Some(row[1..].to_vec());
                    }
                }
                let resources: Vec<Raster> = blobs
                    .iter()
                    .map(|b| from_raster_binary(b.as_slice(), "st_approx_count_agg"))
                    .collect::<Result<Vec<_>, _>>()?;
                let refs: Vec<&Raster> = resources.iter().collect();
                let extras = extras.unwrap_or_default();
                let extra0 = dv_i64(&extras, 0, "st_approx_count_agg")? as u32;
                let extra1 = dv_f64(&extras, 1, "st_approx_count_agg")?;
                Err(types::Duckerror::Unsupported(format!(
                    "st_approx_count_agg: aggregate return shape not wired"
                )))
            }
            3usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_clusterintersecting")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterintersecting: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r: Vec<Geometry> =
                    pg_agg::st_cluster_intersecting_aggregate(&refs).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterintersecting: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                match r.first() {
                    Some(g) => Ok(types::Duckvalue::Blob(g.as_wkb())),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            4usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_clusterintersectingaggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterintersectingaggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r: Vec<Geometry> =
                    pg_agg::st_cluster_intersecting_aggregate(&refs).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterintersectingaggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                match r.first() {
                    Some(g) => Ok(types::Duckvalue::Blob(g.as_wkb())),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            5usize => {
                let mut extras: Option<Vec<types::Duckvalue>> = None;
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_clusterwithin")?;
                    blobs.push(bytes.to_vec());
                    if extras.is_none() {
                        extras = Some(row[1..].to_vec());
                    }
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterwithin: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let extras = extras.unwrap_or_default();
                let extra0 = dv_f64(&extras, 0, "st_clusterwithin")?;
                let r: Vec<Geometry> =
                    pg_agg::st_cluster_within_aggregate(&refs, extra0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterwithin: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                match r.first() {
                    Some(g) => Ok(types::Duckvalue::Blob(g.as_wkb())),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            6usize => {
                let mut extras: Option<Vec<types::Duckvalue>> = None;
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_clusterwithinaggregate")?;
                    blobs.push(bytes.to_vec());
                    if extras.is_none() {
                        extras = Some(row[1..].to_vec());
                    }
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterwithinaggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let extras = extras.unwrap_or_default();
                let extra0 = dv_f64(&extras, 0, "st_clusterwithinaggregate")?;
                let r: Vec<Geometry> =
                    pg_agg::st_cluster_within_aggregate(&refs, extra0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterwithinaggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                match r.first() {
                    Some(g) => Ok(types::Duckvalue::Blob(g.as_wkb())),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            7usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_collect_aggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_collect_aggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_collect_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_collect_aggregate: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            8usize => {
                let mut extras: Option<Vec<types::Duckvalue>> = None;
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_count_agg")?;
                    blobs.push(bytes.to_vec());
                    if extras.is_none() {
                        extras = Some(row[1..].to_vec());
                    }
                }
                let resources: Vec<Raster> = blobs
                    .iter()
                    .map(|b| from_raster_binary(b.as_slice(), "st_count_agg"))
                    .collect::<Result<Vec<_>, _>>()?;
                let refs: Vec<&Raster> = resources.iter().collect();
                let extras = extras.unwrap_or_default();
                let extra0 = dv_i64(&extras, 0, "st_count_agg")? as u32;
                Err(types::Duckerror::Unsupported(format!(
                    "st_count_agg: aggregate return shape not wired"
                )))
            }
            9usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_coverage_union_aggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_coverage_union_aggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_coverage_union_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_coverage_union_aggregate: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            10usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_envelope_aggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_envelope_aggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_envelope_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_envelope_aggregate: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            11usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_extent")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_extent: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                Err(types::Duckerror::Unsupported(format!(
                    "st_extent: aggregate return shape not wired"
                )))
            }
            12usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_extent_threed")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_extent_threed: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let __bb = pg_agg::st_extent_threed(&refs);
                let mut __wkb: Vec<u8> = Vec::with_capacity(57);
                // ISO-WKB header: little-endian, type 1002 (LINESTRING Z), 2 points.
                __wkb.push(0x01u8);
                __wkb.extend_from_slice(&1002u32.to_le_bytes());
                __wkb.extend_from_slice(&2u32.to_le_bytes());
                __wkb.extend_from_slice(&__bb.min_x.to_le_bytes());
                __wkb.extend_from_slice(&__bb.min_y.to_le_bytes());
                __wkb.extend_from_slice(&__bb.min_z.to_le_bytes());
                __wkb.extend_from_slice(&__bb.max_x.to_le_bytes());
                __wkb.extend_from_slice(&__bb.max_y.to_le_bytes());
                __wkb.extend_from_slice(&__bb.max_z.to_le_bytes());
                Ok(types::Duckvalue::Blob(__wkb))
            }
            13usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_make_line_aggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_line_aggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_make_line_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_make_line_aggregate: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            14usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_makeline")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_makeline: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_make_line_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_makeline: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            15usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_makelineagg")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_makelineagg: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_make_line_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_makelineagg: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            16usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_makelineaggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_makelineaggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_make_line_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_makelineaggregate: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            17usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_mem_union_aggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mem_union_aggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_mem_union_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_mem_union_aggregate: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            18usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_memunion")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_memunion: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_mem_union_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_memunion: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            19usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_polygonize_aggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygonize_aggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_polygonize_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_polygonize_aggregate: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            20usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_polygonizeagg")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygonizeagg: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_polygonize_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_polygonizeagg: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            21usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_polygonizeaggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygonizeaggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_polygonize_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_polygonizeaggregate: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            22usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_rast_union")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Raster> = blobs
                    .iter()
                    .map(|b| from_raster_binary(b.as_slice(), "st_rast_union"))
                    .collect::<Result<Vec<_>, _>>()?;
                let refs: Vec<&Raster> = resources.iter().collect();
                let r = pg_rast_agg::st_rast_union_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_rast_union: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_binary()))
            }
            23usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_rast_union_aggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Raster> = blobs
                    .iter()
                    .map(|b| from_raster_binary(b.as_slice(), "st_rast_union_aggregate"))
                    .collect::<Result<Vec<_>, _>>()?;
                let refs: Vec<&Raster> = resources.iter().collect();
                let r = pg_rast_agg::st_rast_union_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_rast_union_aggregate: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_binary()))
            }
            24usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_raster_union")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Raster> = blobs
                    .iter()
                    .map(|b| from_raster_binary(b.as_slice(), "st_raster_union"))
                    .collect::<Result<Vec<_>, _>>()?;
                let refs: Vec<&Raster> = resources.iter().collect();
                let r = pg_rast_agg::st_rast_union_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_raster_union: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_binary()))
            }
            25usize => {
                let mut extras: Option<Vec<types::Duckvalue>> = None;
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_summary_stats_agg")?;
                    blobs.push(bytes.to_vec());
                    if extras.is_none() {
                        extras = Some(row[1..].to_vec());
                    }
                }
                let resources: Vec<Raster> = blobs
                    .iter()
                    .map(|b| from_raster_binary(b.as_slice(), "st_summary_stats_agg"))
                    .collect::<Result<Vec<_>, _>>()?;
                let refs: Vec<&Raster> = resources.iter().collect();
                let extras = extras.unwrap_or_default();
                let extra0 = dv_i64(&extras, 0, "st_summary_stats_agg")? as u32;
                Err(types::Duckerror::Unsupported(format!(
                    "st_summary_stats_agg: aggregate return shape not wired"
                )))
            }
            26usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_union_aggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_union_aggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_union_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_union_aggregate: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            27usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_unionagg")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_unionagg: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_union_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_unionagg: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            28usize => {
                let mut blobs: Vec<Vec<u8>> = Vec::with_capacity(rows.len());
                for row in &rows {
                    if row.is_empty() {
                        continue;
                    }
                    if matches!(row[0], types::Duckvalue::Null) {
                        continue;
                    }
                    let bytes = dv_blob(row, 0, "st_unionaggregate")?;
                    blobs.push(bytes.to_vec());
                }
                let resources: Vec<Geometry> = blobs
                    .iter()
                    .map(|b| Geometry::from_wkb(b))
                    .collect::<Result<Vec<_>, _>>()
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_unionaggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                let refs: Vec<&Geometry> = resources.iter().collect();
                let r = pg_agg::st_union_aggregate(&refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_unionaggregate: {}",
                        postgis_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Blob(r.as_wkb()))
            }
            _ => Err(types::Duckerror::Internal(format!(
                "unknown aggregate arm index {}",
                arm_idx
            ))),
        }
    }

    fn call_cast_col(
        handle: u32,
        arg: column_types::Colvec,
    ) -> Result<column_types::Colvec, types::Duckerror> {
        // #659 Opt 1: lift one cell at a time via `colvec_get`
        // rather than building the full `Vec<Duckvalue>` upfront.
        let n_rows = arg.rows as usize;
        let mut out: Vec<types::Duckvalue> = Vec::with_capacity(n_rows);
        for i in 0..n_rows {
            let v = colvec_get(&arg, i);
            out.push(<Self as callback_dispatch::Guest>::call_cast(handle, v)?);
        }
        values_to_colvec(out)
    }

    // === COLD SINGLETON PATHS (row-major, duckdb:extension@4.0.0) ===

    fn call_scalar(
        handle: u32,
        args: Vec<types::Duckvalue>,
        _ctx: types::Invokeinfo,
    ) -> Result<types::Duckvalue, types::Duckerror> {
        let arm_idx = handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .get(&handle)
            .copied()
            .ok_or_else(|| types::Duckerror::Internal("unknown scalar handle".into()))?;
        // Default DuckDB null semantics: a NULL arg short-circuits
        // to NULL before the function runs. The base
        // `runtime.scalar-registry.register` API the codegen uses
        // here doesn't take a null-handling arm (that's
        // `runtime-ext.register-scalar-ex`), so DuckDB enforces
        // the propagate path engine-side. We defensively check
        // here too in case the host plumbs a NULL through.
        if args.iter().any(|v| matches!(v, types::Duckvalue::Null)) {
            return Ok(types::Duckvalue::Null);
        }
        match arm_idx {
            0usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "add_edge")?, "add_edge")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "add_edge")?, "add_edge")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_edge(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "add_edge: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "add_edge_mod_face")?,
                    "add_edge_mod_face",
                )?;
                let arg1 = dv_i64(&args, 1, "add_edge_mod_face")? as u32;
                let arg2 = dv_i64(&args, 2, "add_edge_mod_face")? as u32;
                let arg3 = from_wkb(dv_blob(&args, 3, "add_edge_mod_face")?, "add_edge_mod_face")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_edge_mod_face(&arg0, arg1, arg2, &arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "add_edge_mod_face: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            2usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "add_edge_new_faces")?,
                    "add_edge_new_faces",
                )?;
                let arg1 = dv_i64(&args, 1, "add_edge_new_faces")? as u32;
                let arg2 = dv_i64(&args, 2, "add_edge_new_faces")? as u32;
                let arg3 = from_wkb(
                    dv_blob(&args, 3, "add_edge_new_faces")?,
                    "add_edge_new_faces",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_edge_new_faces(&arg0, arg1, arg2, &arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "add_edge_new_faces: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            3usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "add_face")?, "add_face")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "add_face")?, "add_face")?;
                let arg2 = dv_bool(&args, 2, "add_face")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_face(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "add_face: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            4usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "add_iso_edge")?, "add_iso_edge")?;
                let arg1 = dv_i64(&args, 1, "add_iso_edge")? as u32;
                let arg2 = dv_i64(&args, 2, "add_iso_edge")? as u32;
                let arg3 = from_wkb(dv_blob(&args, 3, "add_iso_edge")?, "add_iso_edge")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_iso_edge(&arg0, arg1, arg2, &arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "add_iso_edge: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            5usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "add_iso_node")?, "add_iso_node")?;
                let arg2 = from_wkb(dv_blob(&args, 2, "add_iso_node")?, "add_iso_node")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_iso_node(&arg0, None, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "add_iso_node: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            6usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "add_node")?, "add_node")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "add_node")?, "add_node")?;
                let arg2 = dv_f64(&args, 2, "add_node")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_node(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "add_node: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            7usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "add_topogeom_column")?,
                    "add_topogeom_column",
                )?;
                let arg1 = dv_text(&args, 1, "add_topogeom_column")?;
                let arg2 = dv_text(&args, 2, "add_topogeom_column")?;
                let arg3 = dv_text(&args, 3, "add_topogeom_column")?;
                Ok(types::Duckvalue::Int64(pg_topo_edit::add_topogeom_column(
                    &arg0, arg1, arg2, arg3,
                ) as i64))
            }
            8usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "as_gml")?, "as_gml")?;
                let arg1 = dv_text(&args, 1, "as_gml")?;
                let arg2 = dv_i64(&args, 2, "as_gml")? as i32;
                Ok(types::Duckvalue::Text(
                    (pg_topo_out::as_gml(&arg0, arg1, arg2)).into(),
                ))
            }
            9usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "as_topojson")?, "as_topojson")?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_out::as_topojson(&arg0)).into(),
                ))
            }
            10usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "as_topojson_with_edge_map")?,
                    "as_topojson_with_edge_map",
                )?;
                let arg1 = dv_bool(&args, 1, "as_topojson_with_edge_map")?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_out::as_topojson_with_edge_map(&arg0, arg1)).into(),
                ))
            }
            11usize => {
                let arg0 = dv_i64(&args, 0, "build")? as u64;
                Ok(types::Duckvalue::Boolean(pg_strtree::build(arg0)))
            }
            12usize => {
                let arg0 = dv_blob(&args, 0, "cg_constrained_delaunay_triangles")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::cg_constrained_delaunay_triangles(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "cg_constrained_delaunay_triangles: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            13usize => {
                let arg0 = dv_blob(&args, 0, "cg_polygon_repair")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::cg_polygon_repair(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "cg_polygon_repair: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            14usize => {
                let arg0 = dv_blob(&args, 0, "cg_threed_alpha_wrapping")?;
                let arg1 = dv_f64(&args, 1, "cg_threed_alpha_wrapping")?;
                let arg2 = dv_f64(&args, 2, "cg_threed_alpha_wrapping")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::cg_threed_alpha_wrapping(arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "cg_threed_alpha_wrapping: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            15usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "change_edge_geom")?,
                    "change_edge_geom",
                )?;
                let arg1 = dv_i64(&args, 1, "change_edge_geom")? as u32;
                let arg2 = from_wkb(dv_blob(&args, 2, "change_edge_geom")?, "change_edge_geom")?;
                let _ = pg_topo_edit::change_edge_geom(&arg0, arg1, &arg2).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "change_edge_geom: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Null)
            }
            16usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "copy_topology")?, "copy_topology")?;
                let arg1 = dv_text(&args, 1, "copy_topology")?;
                Ok(types::Duckvalue::Blob(
                    pg_topo_types::copy_topology(&arg0, arg1).to_bytes().into(),
                ))
            }
            17usize => {
                let arg0 = dv_text(&args, 0, "count_words")?;
                Ok(types::Duckvalue::Int64(pg_geo::count_words(arg0) as i64))
            }
            18usize => {
                let arg0 = dv_i64(&args, 0, "create_index")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_strtree::create_index(arg0) as i64
                ))
            }
            19usize => Ok(types::Duckvalue::Blob(Raster::new().as_binary().into())),
            20usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "create_topo_geom")?,
                    "create_topo_geom",
                )?;
                let arg1 = dv_i64(&args, 1, "create_topo_geom")? as u32;
                let arg2 = parse_json_list_record_topo_element(&args, 2, "create_topo_geom")?;
                {
                    let __r = pg_topogeom::create_topo_geom(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "create_topo_geom: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.geometry().as_wkb().into()))
                }
            }
            21usize => {
                let arg0 = dv_text(&args, 0, "create_topology")?;
                let arg1 = dv_i64(&args, 1, "create_topology")? as i32;
                let arg2 = dv_f64(&args, 2, "create_topology")?;
                Ok(types::Duckvalue::Blob(
                    Topology::new(arg0, arg1, arg2).to_bytes().into(),
                ))
            }
            22usize => {
                let arg0 = dv_text(&args, 0, "cull_null")?;
                Ok(match pg_geo::cull_null(arg0) {
                    Some(v) => types::Duckvalue::Text(v.into()),
                    None => types::Duckvalue::Null,
                })
            }
            23usize => {
                let arg0 = dv_i64(&args, 0, "destroy_index")? as u64;
                let _ = pg_strtree::destroy_index(arg0);
                Ok(types::Duckvalue::Null)
            }
            24usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "drop_topogeom_column")?,
                    "drop_topogeom_column",
                )?;
                let arg1 = dv_text(&args, 1, "drop_topogeom_column")?;
                let arg2 = dv_text(&args, 2, "drop_topogeom_column")?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_edit::drop_topogeom_column(&arg0, arg1, arg2)).into(),
                ))
            }
            25usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "drop_topology")?, "drop_topology")?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_edit::drop_topology(&arg0)).into(),
                ))
            }
            26usize => {
                let arg0 = dv_blob(&args, 0, "from_binary")?;
                {
                    let __r = pg_rast_types::from_binary(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "from_binary: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            27usize => {
                let arg0 = dv_blob(&args, 0, "from_bytes")?;
                {
                    let __r = pg_topo_types::from_bytes(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "from_bytes: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.to_bytes().into()))
                }
            }
            28usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "get_edge_by_point")?,
                    "get_edge_by_point",
                )?;
                let arg1 = from_wkb(dv_blob(&args, 1, "get_edge_by_point")?, "get_edge_by_point")?;
                let arg2 = dv_f64(&args, 2, "get_edge_by_point")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_edge_by_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "get_edge_by_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            29usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "get_edge_geometry")?,
                    "get_edge_geometry",
                )?;
                let arg1 = dv_i64(&args, 1, "get_edge_geometry")? as u32;
                {
                    let __r = pg_topo_out::get_edge_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "get_edge_geometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            30usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "get_face_by_point")?,
                    "get_face_by_point",
                )?;
                let arg1 = from_wkb(dv_blob(&args, 1, "get_face_by_point")?, "get_face_by_point")?;
                let arg2 = dv_f64(&args, 2, "get_face_by_point")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_face_by_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "get_face_by_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            31usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "get_face_containing_point")?,
                    "get_face_containing_point",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "get_face_containing_point")?,
                    "get_face_containing_point",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_face_containing_point(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "get_face_containing_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            32usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "get_face_edges")?, "get_face_edges")?;
                let arg1 = dv_i64(&args, 1, "get_face_edges")? as u32;
                {
                    let __r = pg_topo_query::get_face_edges(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "get_face_edges: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            33usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "get_face_geometry")?,
                    "get_face_geometry",
                )?;
                let arg1 = dv_i64(&args, 1, "get_face_geometry")? as u32;
                {
                    let __r = pg_topo_out::get_face_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "get_face_geometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            34usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "get_node_by_point")?,
                    "get_node_by_point",
                )?;
                let arg1 = from_wkb(dv_blob(&args, 1, "get_node_by_point")?, "get_node_by_point")?;
                let arg2 = dv_f64(&args, 2, "get_node_by_point")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_node_by_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "get_node_by_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            35usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "get_node_edges")?, "get_node_edges")?;
                let arg1 = dv_i64(&args, 1, "get_node_edges")? as u32;
                {
                    let __r = pg_topo_query::get_node_edges(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "get_node_edges: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            36usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "get_node_geometry")?,
                    "get_node_geometry",
                )?;
                let arg1 = dv_i64(&args, 1, "get_node_geometry")? as u32;
                {
                    let __r = pg_topo_out::get_node_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "get_node_geometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            37usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "get_ring_edges")?, "get_ring_edges")?;
                let arg1 = dv_i64(&args, 1, "get_ring_edges")? as u32;
                {
                    let __r = pg_topo_query::get_ring_edges(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "get_ring_edges: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            38usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "get_topology_id")?, "get_topology_id")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_topology_id(&arg0) as i64,
                ))
            }
            39usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "get_topology_name")?,
                    "get_topology_name",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_query::get_topology_name(&arg0)).into(),
                ))
            }
            40usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "get_topology_srid")?,
                    "get_topology_srid",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_topology_srid(&arg0) as i64,
                ))
            }
            41usize => {
                let arg0 = dv_i64(&args, 0, "insert_wkb")? as u64;
                let arg1 = dv_blob(&args, 1, "insert_wkb")?;
                let arg2 = dv_i64(&args, 2, "insert_wkb")? as u64;
                Ok(types::Duckvalue::Boolean(pg_strtree::insert_wkb(
                    arg0, arg1, arg2,
                )))
            }
            42usize => {
                let __r = postgis_metadata::list_cast_rewrites().map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "list_cast_rewrites: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut __it = __r.into_iter();
                match __it.next() {
                    Some(__rec) => ret_to_witvalue_cast_rewrite(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            43usize => {
                let __r = postgis_metadata::list_operator_rewrites().map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "list_operator_rewrites: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut __it = __r.into_iter();
                match __it.next() {
                    Some(__rec) => ret_to_witvalue_operator_rewrite(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            44usize => {
                let __r = postgis_metadata::list_preprocessor_patterns().map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "list_preprocessor_patterns: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut __it = __r.into_iter();
                match __it.next() {
                    Some(__rec) => ret_to_witvalue_preprocessor_pattern(__rec),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            45usize => {
                let arg0 = dv_i64(&args, 0, "make_topo_element")? as u32;
                let arg1 = dv_i64(&args, 1, "make_topo_element")? as u32;
                ret_to_witvalue_topo_element(pg_topogeom::make_topo_element(arg0, arg1))
            }
            46usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "make_topology_precise")?,
                    "make_topology_precise",
                )?;
                let arg1 = dv_f64(&args, 1, "make_topology_precise")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::make_topology_precise(&arg0, arg1) as i64,
                ))
            }
            47usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "mod_edge_heal")?, "mod_edge_heal")?;
                let arg1 = dv_i64(&args, 1, "mod_edge_heal")? as u32;
                let arg2 = dv_i64(&args, 2, "mod_edge_heal")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::mod_edge_heal(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "mod_edge_heal: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            48usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "mod_edge_split")?, "mod_edge_split")?;
                let arg1 = dv_i64(&args, 1, "mod_edge_split")? as u32;
                let arg2 = from_wkb(dv_blob(&args, 2, "mod_edge_split")?, "mod_edge_split")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::mod_edge_split(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "mod_edge_split: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            49usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "move_iso_node")?, "move_iso_node")?;
                let arg1 = dv_i64(&args, 1, "move_iso_node")? as u32;
                let arg2 = from_wkb(dv_blob(&args, 2, "move_iso_node")?, "move_iso_node")?;
                let _ = pg_topo_edit::move_iso_node(&arg0, arg1, &arg2).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "move_iso_node: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Null)
            }
            50usize => {
                let arg0 = dv_i64(&args, 0, "nearest")? as u64;
                let arg1 = dv_blob(&args, 1, "nearest")?;
                Ok(types::Duckvalue::Int64(
                    pg_strtree::nearest(arg0, arg1) as i64
                ))
            }
            51usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "new_edge_heal")?, "new_edge_heal")?;
                let arg1 = dv_i64(&args, 1, "new_edge_heal")? as u32;
                let arg2 = dv_i64(&args, 2, "new_edge_heal")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::new_edge_heal(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "new_edge_heal: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            52usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "new_edges_split")?, "new_edges_split")?;
                let arg1 = dv_i64(&args, 1, "new_edges_split")? as u32;
                let arg2 = from_wkb(dv_blob(&args, 2, "new_edges_split")?, "new_edges_split")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::new_edges_split(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "new_edges_split: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            53usize => {
                let arg0 = dv_text(&args, 0, "normalize_address")?;
                {
                    let __r = pg_geo::normalize_address(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "normalize_address: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_address_component(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            54usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "op_bbox_distance")?, "op_bbox_distance")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "op_bbox_distance")?, "op_bbox_distance")?;
                Ok(types::Duckvalue::Float64(pg_op::op_bbox_distance(
                    &arg0, &arg1,
                )))
            }
            55usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "op_bbox_intersects_nd")?,
                    "op_bbox_intersects_nd",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "op_bbox_intersects_nd")?,
                    "op_bbox_intersects_nd",
                )?;
                Ok(types::Duckvalue::Boolean(pg_op::op_bbox_intersects_nd(
                    &arg0, &arg1,
                )))
            }
            56usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "op_bbox_intersects_twod")?,
                    "op_bbox_intersects_twod",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "op_bbox_intersects_twod")?,
                    "op_bbox_intersects_twod",
                )?;
                Ok(types::Duckvalue::Boolean(pg_op::op_bbox_intersects_twod(
                    &arg0, &arg1,
                )))
            }
            57usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "op_equals_spatially")?,
                    "op_equals_spatially",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "op_equals_spatially")?,
                    "op_equals_spatially",
                )?;
                Ok(types::Duckvalue::Boolean(pg_op::op_equals_spatially(
                    &arg0, &arg1,
                )))
            }
            58usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "op_knn_distance")?, "op_knn_distance")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "op_knn_distance")?, "op_knn_distance")?;
                Ok(types::Duckvalue::Float64(pg_op::op_knn_distance(
                    &arg0, &arg1,
                )))
            }
            59usize => {
                let arg0 = dv_text(&args, 0, "pagc_normalize_address")?;
                {
                    let __r = pg_geo::pagc_normalize_address(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "pagc_normalize_address: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_address_component(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            60usize => {
                let arg0 = dv_text(&args, 0, "parse_address")?;
                ret_to_witvalue_parsed_address(pg_geo::parse_address(arg0).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "parse_address: {}",
                        shim_err_string(e)
                    ))
                })?)
            }
            61usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "polygonize")?, "polygonize")?;
                {
                    let __r = pg_topo_out::polygonize(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "polygonize: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            62usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "postgis_addbbox")?, "postgis_addbbox")?;
                Ok(types::Duckvalue::Blob(
                    postgis_version::postgis_addbbox(&arg0).as_wkb().into(),
                ))
            }
            63usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "postgis_dropbbox")?, "postgis_dropbbox")?;
                Ok(types::Duckvalue::Blob(
                    postgis_version::postgis_dropbbox(&arg0).as_wkb().into(),
                ))
            }
            64usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_full_version()).into(),
            )),
            65usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_geos_compiled_version()).into(),
            )),
            66usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "postgis_geos_noop")?, "postgis_geos_noop")?;
                {
                    let __r = postgis_version::postgis_geos_noop(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "postgis_geos_noop: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            67usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_geos_version()).into(),
            )),
            68usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "postgis_getbbox")?, "postgis_getbbox")?;
                {
                    let __r = postgis_version::postgis_getbbox(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "postgis_getbbox: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            69usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "postgis_hasbbox")?, "postgis_hasbbox")?;
                Ok(types::Duckvalue::Boolean(postgis_version::postgis_hasbbox(
                    &arg0,
                )))
            }
            70usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_lib_build_date()).into(),
            )),
            71usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_lib_revision()).into(),
            )),
            72usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_lib_version()).into(),
            )),
            73usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_libjson_version()).into(),
            )),
            74usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_liblwgeom_version()).into(),
            )),
            75usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_libprotobuf_version()).into(),
            )),
            76usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_libxml_version()).into(),
            )),
            77usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "postgis_noop")?, "postgis_noop")?;
                Ok(types::Duckvalue::Blob(
                    postgis_version::postgis_noop(&arg0).as_wkb().into(),
                ))
            }
            78usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_proj_compiled_version()).into(),
            )),
            79usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_proj_version()).into(),
            )),
            80usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_scripts_build_date()).into(),
            )),
            81usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_scripts_installed()).into(),
            )),
            82usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_scripts_released()).into(),
            )),
            83usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_svn_version()).into(),
            )),
            84usize => Ok(types::Duckvalue::Text(
                (pg_topo_out::postgis_topology_scripts_installed()).into(),
            )),
            85usize => {
                let arg0 = dv_i64(&args, 0, "postgis_type_name")? as u32;
                let arg1 = dv_i64(&args, 1, "postgis_type_name")? as u32;
                let arg2 = dv_bool(&args, 2, "postgis_type_name")?;
                Ok(types::Duckvalue::Text(
                    (postgis_version::postgis_type_name(arg0, arg1, arg2)).into(),
                ))
            }
            86usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_version()).into(),
            )),
            87usize => Ok(types::Duckvalue::Text(
                (postgis_version::postgis_wagyu_version()).into(),
            )),
            88usize => {
                let arg0 = dv_i64(&args, 0, "query_envelope")? as u64;
                let arg1 = dv_f64(&args, 1, "query_envelope")?;
                let arg2 = dv_f64(&args, 2, "query_envelope")?;
                let arg3 = dv_f64(&args, 3, "query_envelope")?;
                let arg4 = dv_f64(&args, 4, "query_envelope")?;
                {
                    let __r = pg_strtree::query_envelope(arg0, arg1, arg2, arg3, arg4);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            89usize => {
                let arg0 = dv_i64(&args, 0, "query_knn")? as u64;
                let arg1 = dv_blob(&args, 1, "query_knn")?;
                let arg2 = dv_i64(&args, 2, "query_knn")? as u32;
                {
                    let __r = pg_strtree::query_knn(arg0, arg1, arg2);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            90usize => {
                let arg0 = dv_i64(&args, 0, "query_within_distance")? as u64;
                let arg1 = dv_blob(&args, 1, "query_within_distance")?;
                let arg2 = dv_f64(&args, 2, "query_within_distance")?;
                {
                    let __r = pg_strtree::query_within_distance(arg0, arg1, arg2);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            91usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "rast_dfer_all_batch")?;
                Ok(types::Duckvalue::Boolean(
                    postgis_batch::rast_dfer_all_batch(&arg0),
                ))
            }
            92usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "rast_metadata_all_batch")?;
                Ok(types::Duckvalue::Boolean(
                    postgis_batch::rast_metadata_all_batch(&arg0),
                ))
            }
            93usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "rem_edge_mod_face")?,
                    "rem_edge_mod_face",
                )?;
                let arg1 = dv_i64(&args, 1, "rem_edge_mod_face")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::rem_edge_mod_face(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "rem_edge_mod_face: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            94usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "rem_edge_new_face")?,
                    "rem_edge_new_face",
                )?;
                let arg1 = dv_i64(&args, 1, "rem_edge_new_face")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::rem_edge_new_face(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "rem_edge_new_face: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            95usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "remove_face")?, "remove_face")?;
                let arg1 = dv_i64(&args, 1, "remove_face")? as u32;
                let _ = pg_topo_edit::remove_face(&arg0, arg1).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "remove_face: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Null)
            }
            96usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "remove_iso_edge")?, "remove_iso_edge")?;
                let arg1 = dv_i64(&args, 1, "remove_iso_edge")? as u32;
                let _ = pg_topo_edit::remove_iso_edge(&arg0, arg1).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "remove_iso_edge: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Null)
            }
            97usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "remove_iso_node")?, "remove_iso_node")?;
                let arg1 = dv_i64(&args, 1, "remove_iso_node")? as u32;
                let _ = pg_topo_edit::remove_iso_node(&arg0, arg1).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "remove_iso_node: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Null)
            }
            98usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "remove_unused_primitives")?,
                    "remove_unused_primitives",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::remove_unused_primitives(&arg0) as i64,
                ))
            }
            99usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "rename_topology")?, "rename_topology")?;
                let arg1 = dv_text(&args, 1, "rename_topology")?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_edit::rename_topology(&arg0, arg1)).into(),
                ))
            }
            100usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_3d_closest_point")?,
                    "st_3d_closest_point",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_3d_closest_point")?,
                    "st_3d_closest_point",
                )?;
                {
                    let __r = pg_meas::st_closest_point_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3d_closest_point: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            101usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3d_contains")?, "st_3d_contains")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_3d_contains")?, "st_3d_contains")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_contains_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3d_contains: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            102usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3d_d_within")?, "st_3d_d_within")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_3d_d_within")?, "st_3d_d_within")?;
                let arg2 = dv_f64(&args, 2, "st_3d_d_within")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_dwithin_threed(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3d_d_within: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            103usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3d_dwithin")?, "st_3d_dwithin")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_3d_dwithin")?, "st_3d_dwithin")?;
                let arg2 = dv_f64(&args, 2, "st_3d_dwithin")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_dwithin_threed(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3d_dwithin: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            104usize => {
                let arg0 = dv_blob(&args, 0, "st_3d_intersects")?;
                let arg1 = dv_blob(&args, 1, "st_3d_intersects")?;
                Ok(types::Duckvalue::Boolean(pg_sfcgal::st_intersects_threed(
                    arg0, arg1,
                )))
            }
            105usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_3d_line_interpolate_point")?,
                    "st_3d_line_interpolate_point",
                )?;
                let arg1 = dv_f64(&args, 1, "st_3d_line_interpolate_point")?;
                {
                    let __r =
                        pg_lin::st_line_interpolate_point_threed(&arg0, arg1).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_3d_line_interpolate_point: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            106usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_3d_longest_line")?,
                    "st_3d_longest_line",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_3d_longest_line")?,
                    "st_3d_longest_line",
                )?;
                {
                    let __r = pg_meas::st_longest_line_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3d_longest_line: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            107usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3d_make_box")?, "st_3d_make_box")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_3d_make_box")?, "st_3d_make_box")?;
                {
                    let __r = pg_threed::st_make_box_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3d_make_box: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            108usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_3d_max_distance")?,
                    "st_3d_max_distance",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_3d_max_distance")?,
                    "st_3d_max_distance",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_max_distance_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3d_max_distance: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            109usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_3d_shortest_line")?,
                    "st_3d_shortest_line",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_3d_shortest_line")?,
                    "st_3d_shortest_line",
                )?;
                {
                    let __r = pg_meas::st_shortest_line_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3d_shortest_line: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            110usize => {
                let arg0 = dv_blob(&args, 0, "st_3darea")?;
                Ok(types::Duckvalue::Float64(pg_sfcgal::st_area_threed(arg0)))
            }
            111usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dboundary")?, "st_3dboundary")?;
                {
                    let __r = pg_threed::st_boundary_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dboundary: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            112usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dcentroid")?, "st_3dcentroid")?;
                {
                    let __r = pg_threed::st_centroid_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dcentroid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            113usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dclosestpoint")?, "st_3dclosestpoint")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_3dclosestpoint")?, "st_3dclosestpoint")?;
                {
                    let __r = pg_meas::st_closest_point_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dclosestpoint: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            114usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dcontains")?, "st_3dcontains")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_3dcontains")?, "st_3dcontains")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_contains_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dcontains: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            115usize => {
                let arg0 = dv_blob(&args, 0, "st_3dconvexhull")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_convex_hull_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dconvexhull: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            116usize => {
                let arg0 = dv_blob(&args, 0, "st_3ddifference")?;
                let arg1 = dv_blob(&args, 1, "st_3ddifference")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_difference_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3ddifference: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            117usize => {
                let arg0 = dv_blob(&args, 0, "st_3ddistance")?;
                let arg1 = dv_blob(&args, 1, "st_3ddistance")?;
                Ok(types::Duckvalue::Float64(pg_sfcgal::st_distance_threed(
                    arg0, arg1,
                )))
            }
            118usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3ddwithin")?, "st_3ddwithin")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_3ddwithin")?, "st_3ddwithin")?;
                let arg2 = dv_f64(&args, 2, "st_3ddwithin")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_dwithin_threed(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3ddwithin: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            119usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3denvelope")?, "st_3denvelope")?;
                {
                    let __r = pg_threed::st_envelope_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3denvelope: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            120usize => {
                let arg0 = dv_blob(&args, 0, "st_3dintersection")?;
                let arg1 = dv_blob(&args, 1, "st_3dintersection")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_intersection_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dintersection: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            121usize => {
                let arg0 = dv_blob(&args, 0, "st_3dintersects")?;
                let arg1 = dv_blob(&args, 1, "st_3dintersects")?;
                Ok(types::Duckvalue::Boolean(pg_sfcgal::st_intersects_threed(
                    arg0, arg1,
                )))
            }
            122usize => {
                let arg0 = dv_blob(&args, 0, "st_3dlength")?;
                Ok(types::Duckvalue::Float64(pg_sfcgal::st_length_threed(arg0)))
            }
            123usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_3dlineinterpolatepoint")?,
                    "st_3dlineinterpolatepoint",
                )?;
                let arg1 = dv_f64(&args, 1, "st_3dlineinterpolatepoint")?;
                {
                    let __r =
                        pg_lin::st_line_interpolate_point_threed(&arg0, arg1).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_3dlineinterpolatepoint: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            124usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dlongestline")?, "st_3dlongestline")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_3dlongestline")?, "st_3dlongestline")?;
                {
                    let __r = pg_meas::st_longest_line_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dlongestline: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            125usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dmakebox")?, "st_3dmakebox")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_3dmakebox")?, "st_3dmakebox")?;
                {
                    let __r = pg_threed::st_make_box_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dmakebox: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            126usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dmaxdistance")?, "st_3dmaxdistance")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_3dmaxdistance")?, "st_3dmaxdistance")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_max_distance_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dmaxdistance: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            127usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dperimeter")?, "st_3dperimeter")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_perimeter_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dperimeter: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            128usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dreverse")?, "st_3dreverse")?;
                {
                    let __r = pg_threed::st_reverse_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dreverse: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            129usize => {
                let arg0 = dv_blob(&args, 0, "st_3drotate")?;
                let arg1 = dv_f64(&args, 1, "st_3drotate")?;
                let arg2 = dv_f64(&args, 2, "st_3drotate")?;
                let arg3 = dv_f64(&args, 3, "st_3drotate")?;
                let arg4 = dv_f64(&args, 4, "st_3drotate")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_rotate_threed(arg0, arg1, arg2, arg3, arg4).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3drotate: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            130usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dscale")?, "st_3dscale")?;
                let arg1 = dv_f64(&args, 1, "st_3dscale")?;
                let arg2 = dv_f64(&args, 2, "st_3dscale")?;
                let arg3 = dv_f64(&args, 3, "st_3dscale")?;
                {
                    let __r = pg_xform::st_scale_threed(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dscale: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            131usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dshortestline")?, "st_3dshortestline")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_3dshortestline")?, "st_3dshortestline")?;
                {
                    let __r = pg_meas::st_shortest_line_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dshortestline: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            132usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_3dtranslate")?, "st_3dtranslate")?;
                let arg1 = dv_f64(&args, 1, "st_3dtranslate")?;
                let arg2 = dv_f64(&args, 2, "st_3dtranslate")?;
                let arg3 = dv_f64(&args, 3, "st_3dtranslate")?;
                {
                    let __r =
                        pg_xform::st_translate_threed(&arg0, arg1, arg2, arg3).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_3dtranslate: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            133usize => {
                let arg0 = dv_blob(&args, 0, "st_3dunion")?;
                let arg1 = dv_blob(&args, 1, "st_3dunion")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_union_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_3dunion: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            134usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_add_band")?, "st_add_band")?;
                let arg1 = match dv_i64(&args, 1, "st_add_band")? {
                    0 => pg_rast_types::PixelType::Bool1,
                    1 => pg_rast_types::PixelType::Uint8,
                    2 => pg_rast_types::PixelType::Int8,
                    3 => pg_rast_types::PixelType::Uint16,
                    4 => pg_rast_types::PixelType::Int16,
                    5 => pg_rast_types::PixelType::Uint32,
                    6 => pg_rast_types::PixelType::Int32,
                    7 => pg_rast_types::PixelType::Float32,
                    8 => pg_rast_types::PixelType::Float64,
                    other => {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_add_band: arg 1 (pixel-type) out of range: {} (valid 0..9)",
                            other,
                        )))
                    }
                };
                let arg2 = dv_f64(&args, 2, "st_add_band")?;
                {
                    let __r = pg_rast_ctor::st_add_band(&arg0, arg1, arg2, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_add_band: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            135usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "st_add_face")?, "st_add_face")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_add_face")?, "st_add_face")?;
                let arg2 = dv_bool(&args, 2, "st_add_face")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_face(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_add_face: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            136usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "st_add_iso_edge")?, "st_add_iso_edge")?;
                let arg1 = dv_i64(&args, 1, "st_add_iso_edge")? as u32;
                let arg2 = dv_i64(&args, 2, "st_add_iso_edge")? as u32;
                let arg3 = from_wkb(dv_blob(&args, 3, "st_add_iso_edge")?, "st_add_iso_edge")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_iso_edge(&arg0, arg1, arg2, &arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_add_iso_edge: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            137usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "st_add_iso_node")?, "st_add_iso_node")?;
                let arg2 = from_wkb(dv_blob(&args, 2, "st_add_iso_node")?, "st_add_iso_node")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_iso_node(&arg0, None, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_add_iso_node: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            138usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_add_measure")?, "st_add_measure")?;
                let arg1 = dv_f64(&args, 1, "st_add_measure")?;
                let arg2 = dv_f64(&args, 2, "st_add_measure")?;
                {
                    let __r = pg_lin::st_add_measure(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_add_measure: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            139usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_add_point")?, "st_add_point")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_add_point")?, "st_add_point")?;
                {
                    let __r = pg_lin::st_add_point(&arg0, &arg1, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_add_point: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            140usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_addband")?, "st_addband")?;
                let arg1 = match dv_i64(&args, 1, "st_addband")? {
                    0 => pg_rast_types::PixelType::Bool1,
                    1 => pg_rast_types::PixelType::Uint8,
                    2 => pg_rast_types::PixelType::Int8,
                    3 => pg_rast_types::PixelType::Uint16,
                    4 => pg_rast_types::PixelType::Int16,
                    5 => pg_rast_types::PixelType::Uint32,
                    6 => pg_rast_types::PixelType::Int32,
                    7 => pg_rast_types::PixelType::Float32,
                    8 => pg_rast_types::PixelType::Float64,
                    other => {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_addband: arg 1 (pixel-type) out of range: {} (valid 0..9)",
                            other,
                        )))
                    }
                };
                let arg2 = dv_f64(&args, 2, "st_addband")?;
                {
                    let __r = pg_rast_ctor::st_add_band(&arg0, arg1, arg2, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_addband: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            141usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "st_addface")?, "st_addface")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_addface")?, "st_addface")?;
                let arg2 = dv_bool(&args, 2, "st_addface")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_face(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_addface: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            142usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "st_addisoedge")?, "st_addisoedge")?;
                let arg1 = dv_i64(&args, 1, "st_addisoedge")? as u32;
                let arg2 = dv_i64(&args, 2, "st_addisoedge")? as u32;
                let arg3 = from_wkb(dv_blob(&args, 3, "st_addisoedge")?, "st_addisoedge")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_iso_edge(&arg0, arg1, arg2, &arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_addisoedge: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            143usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "st_addisonode")?, "st_addisonode")?;
                let arg2 = from_wkb(dv_blob(&args, 2, "st_addisonode")?, "st_addisonode")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_iso_node(&arg0, None, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_addisonode: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            144usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_addmeasure")?, "st_addmeasure")?;
                let arg1 = dv_f64(&args, 1, "st_addmeasure")?;
                let arg2 = dv_f64(&args, 2, "st_addmeasure")?;
                {
                    let __r = pg_lin::st_add_measure(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_addmeasure: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            145usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_addpoint")?, "st_addpoint")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_addpoint")?, "st_addpoint")?;
                {
                    let __r = pg_lin::st_add_point(&arg0, &arg1, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_addpoint: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            146usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_affine")?, "st_affine")?;
                let arg1 = dv_f64(&args, 1, "st_affine")?;
                let arg2 = dv_f64(&args, 2, "st_affine")?;
                let arg3 = dv_f64(&args, 3, "st_affine")?;
                let arg4 = dv_f64(&args, 4, "st_affine")?;
                let arg5 = dv_f64(&args, 5, "st_affine")?;
                let arg6 = dv_f64(&args, 6, "st_affine")?;
                {
                    let __r = pg_xform::st_affine(&arg0, arg1, arg2, arg3, arg4, arg5, arg6)
                        .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_affine: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            147usize => {
                let arg0 = dv_blob(&args, 0, "st_alpha_shape")?;
                let arg1 = dv_f64(&args, 1, "st_alpha_shape")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_alpha_shape(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_alpha_shape: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            148usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_angle")?, "st_angle")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_angle")?, "st_angle")?;
                let arg2 = from_wkb(dv_blob(&args, 2, "st_angle")?, "st_angle")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_angle(&arg0, &arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_angle: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            149usize => {
                let arg0 = dv_blob(&args, 0, "st_approx_convex_partition")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_approx_convex_partition(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_approx_convex_partition: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            150usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_approx_histogram")?,
                    "st_approx_histogram",
                )?;
                let arg1 = dv_i64(&args, 1, "st_approx_histogram")? as u32;
                let arg2 = dv_i64(&args, 2, "st_approx_histogram")? as u32;
                let arg3 = dv_f64(&args, 3, "st_approx_histogram")?;
                {
                    let __r = pg_rast_stats::st_approx_histogram(&arg0, arg1, arg2, arg3).map_err(
                        |e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_approx_histogram: {}",
                                shim_err_string(e)
                            ))
                        },
                    )?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_histogram_bin(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            151usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_approx_quantile")?,
                    "st_approx_quantile",
                )?;
                let arg1 = dv_i64(&args, 1, "st_approx_quantile")? as u32;
                let arg2 = dv_f64(&args, 2, "st_approx_quantile")?;
                let arg3 = dv_f64(&args, 3, "st_approx_quantile")?;
                Ok(types::Duckvalue::Float64(
                    pg_rast_stats::st_approx_quantile(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_approx_quantile: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            152usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_approx_quantiles")?,
                    "st_approx_quantiles",
                )?;
                let arg1 = dv_i64(&args, 1, "st_approx_quantiles")? as u32;
                let arg2 = dv_f64(&args, 2, "st_approx_quantiles")?;
                let arg3: Vec<f64> = parse_json_list_f64(&args, 3, "st_approx_quantiles")?;
                {
                    let __r = pg_rast_stats::st_approx_quantiles(&arg0, arg1, arg2, &arg3)
                        .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_approx_quantiles: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_quantile_entry(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            153usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_approx_summary_stats")?,
                    "st_approx_summary_stats",
                )?;
                let arg1 = dv_i64(&args, 1, "st_approx_summary_stats")? as u32;
                let arg2 = dv_f64(&args, 2, "st_approx_summary_stats")?;
                ret_to_witvalue_summary_stats(
                    pg_rast_stats::st_approx_summary_stats(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_approx_summary_stats: {}",
                            shim_err_string(e)
                        ))
                    })?,
                )
            }
            154usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_approx_value_count")?,
                    "st_approx_value_count",
                )?;
                let arg1 = dv_i64(&args, 1, "st_approx_value_count")? as u32;
                let arg2 = dv_f64(&args, 2, "st_approx_value_count")?;
                let arg3 = dv_f64(&args, 3, "st_approx_value_count")?;
                Ok(types::Duckvalue::Int64(
                    pg_rast_stats::st_approx_value_count(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_approx_value_count: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            155usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_approx_value_counts")?,
                    "st_approx_value_counts",
                )?;
                let arg1 = dv_i64(&args, 1, "st_approx_value_counts")? as u32;
                let arg2: Vec<f64> = parse_json_list_f64(&args, 2, "st_approx_value_counts")?;
                let arg3 = dv_f64(&args, 3, "st_approx_value_counts")?;
                let arg4 = dv_f64(&args, 4, "st_approx_value_counts")?;
                {
                    let __r = pg_rast_stats::st_approx_value_counts(&arg0, arg1, &arg2, arg3, arg4)
                        .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_approx_value_counts: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_value_count(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            156usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_approx_value_percent")?,
                    "st_approx_value_percent",
                )?;
                let arg1 = dv_i64(&args, 1, "st_approx_value_percent")? as u32;
                let arg2 = dv_f64(&args, 2, "st_approx_value_percent")?;
                let arg3 = dv_f64(&args, 3, "st_approx_value_percent")?;
                Ok(types::Duckvalue::Float64(
                    pg_rast_stats::st_approx_value_percent(&arg0, arg1, arg2, arg3).map_err(
                        |e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_approx_value_percent: {}",
                                shim_err_string(e)
                            ))
                        },
                    )?,
                ))
            }
            157usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_approx_value_percents")?,
                    "st_approx_value_percents",
                )?;
                let arg1 = dv_i64(&args, 1, "st_approx_value_percents")? as u32;
                let arg2: Vec<f64> = parse_json_list_f64(&args, 2, "st_approx_value_percents")?;
                let arg3 = dv_f64(&args, 3, "st_approx_value_percents")?;
                let arg4 = dv_f64(&args, 4, "st_approx_value_percents")?;
                {
                    let __r =
                        pg_rast_stats::st_approx_value_percents(&arg0, arg1, &arg2, arg3, arg4)
                            .map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_approx_value_percents: {}",
                                    shim_err_string(e)
                                ))
                            })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_value_percent_entry(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            158usize => {
                let arg0 = dv_blob(&args, 0, "st_approximate_medial_axis")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_approximate_medial_axis(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_approximate_medial_axis: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            159usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_approximatemedialaxis")?,
                    "st_approximatemedialaxis",
                )?;
                {
                    let __r = pg_proc::st_approximate_medial_axis(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_approximatemedialaxis: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            160usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_area")?, "st_area")?;
                Ok(types::Duckvalue::Float64(pg_meas::st_area(&arg0).map_err(
                    |e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_area: {}",
                            shim_err_string(e)
                        ))
                    },
                )?))
            }
            161usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_area_batch")?;
                {
                    let __r = postgis_batch::st_area_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            162usize => {
                let arg0 = dv_blob(&args, 0, "st_area_threed")?;
                Ok(types::Duckvalue::Float64(pg_sfcgal::st_area_threed(arg0)))
            }
            163usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_area_twod")?, "st_area_twod")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_area_twod(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_area_twod: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            164usize => {
                let arg0 = dv_blob(&args, 0, "st_areathreed")?;
                Ok(types::Duckvalue::Float64(pg_sfcgal::st_area_threed(arg0)))
            }
            165usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_binary")?, "st_as_binary")?;
                Ok(types::Duckvalue::Blob((pg_out::st_as_binary(&arg0)).into()))
            }
            166usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_as_encoded_polyline")?,
                    "st_as_encoded_polyline",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_encoded_polyline(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_encoded_polyline: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            167usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_ewkb")?, "st_as_ewkb")?;
                Ok(types::Duckvalue::Blob((pg_out::st_as_ewkb(&arg0)).into()))
            }
            168usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_ewkt")?, "st_as_ewkt")?;
                Ok(types::Duckvalue::Text((pg_out::st_as_ewkt(&arg0)).into()))
            }
            169usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_as_ewkt_precision")?,
                    "st_as_ewkt_precision",
                )?;
                let arg1 = dv_i64(&args, 1, "st_as_ewkt_precision")? as u32;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_ewkt_precision(&arg0, arg1)).into(),
                ))
            }
            170usize => {
                let arg0_one =
                    from_wkb(dv_blob(&args, 0, "st_as_flatgeobuf")?, "st_as_flatgeobuf")?;
                let arg0_owned: Vec<Geometry> = alloc::vec![arg0_one];
                let arg0: Vec<&Geometry> = arg0_owned.iter().collect();
                let arg1 = dv_text(&args, 1, "st_as_flatgeobuf")?;
                Ok(types::Duckvalue::Blob(
                    (pg_out::st_as_flatgeobuf(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_flatgeobuf: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            171usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_as_gdal_raster")?,
                    "st_as_gdal_raster",
                )?;
                let arg1 = dv_text(&args, 1, "st_as_gdal_raster")?;
                Ok(types::Duckvalue::Blob(
                    (pg_rast_out::st_as_gdal_raster(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_gdal_raster: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            172usize => {
                let arg0_one = from_wkb(dv_blob(&args, 0, "st_as_geobuf")?, "st_as_geobuf")?;
                let arg0_owned: Vec<Geometry> = alloc::vec![arg0_one];
                let arg0: Vec<&Geometry> = arg0_owned.iter().collect();
                Ok(types::Duckvalue::Blob(
                    (pg_out::st_as_geobuf(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_geobuf: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            173usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_geojson")?, "st_as_geojson")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_geojson(&arg0)).into(),
                ))
            }
            174usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_as_geojson_full")?,
                    "st_as_geojson_full",
                )?;
                let arg1 = dv_i64(&args, 1, "st_as_geojson_full")? as u32;
                let arg2 = dv_i64(&args, 2, "st_as_geojson_full")? as u32;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_geojson_full(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_geojson_full: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            175usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_gml")?, "st_as_gml")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_gml(&arg0, None, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_gml: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            176usize => {
                let arg0 = dv_i64(&args, 0, "st_as_gml_full")? as u32;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_as_gml_full")?, "st_as_gml_full")?;
                let arg2 = dv_i64(&args, 2, "st_as_gml_full")? as u32;
                let arg3 = dv_i64(&args, 3, "st_as_gml_full")? as u32;
                let arg4 = dv_text(&args, 4, "st_as_gml_full")?;
                let arg5 = dv_text(&args, 5, "st_as_gml_full")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_gml_full(arg0, &arg1, arg2, arg3, arg4, arg5).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_gml_full: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            177usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_hexewkb")?, "st_as_hexewkb")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_hexewkb(&arg0)).into(),
                ))
            }
            178usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_as_jpeg")?, "st_as_jpeg")?;
                let arg1 = dv_i64(&args, 1, "st_as_jpeg")? as u32;
                Ok(types::Duckvalue::Blob(
                    (pg_rast_out::st_as_jpeg(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_jpeg: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            179usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_kml")?, "st_as_kml")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_kml(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_kml: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            180usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_kml_full")?, "st_as_kml_full")?;
                let arg1 = dv_i64(&args, 1, "st_as_kml_full")? as u32;
                let arg2 = dv_text(&args, 2, "st_as_kml_full")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_kml_full(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_kml_full: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            181usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_as_lat_lon_text")?,
                    "st_as_lat_lon_text",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_lat_lon_text(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_lat_lon_text: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            182usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_marc21")?, "st_as_marc21")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_marc21(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_marc21: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            183usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_marc21_fmt")?, "st_as_marc21_fmt")?;
                let arg1 = dv_text(&args, 1, "st_as_marc21_fmt")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_marc21_fmt(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_marc21_fmt: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            184usize => {
                let arg0_one = from_wkb(dv_blob(&args, 0, "st_as_mvt")?, "st_as_mvt")?;
                let arg0_owned: Vec<Geometry> = alloc::vec![arg0_one];
                let arg0: Vec<&Geometry> = arg0_owned.iter().collect();
                let arg1 = dv_text(&args, 1, "st_as_mvt")?;
                Ok(types::Duckvalue::Blob(
                    (pg_out::st_as_mvt(&arg0, arg1, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_mvt: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            185usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_mvt_geom")?, "st_as_mvt_geom")?;
                let arg1 = dv_f64(&args, 1, "st_as_mvt_geom")?;
                let arg2 = dv_f64(&args, 2, "st_as_mvt_geom")?;
                let arg3 = dv_f64(&args, 3, "st_as_mvt_geom")?;
                let arg4 = dv_f64(&args, 4, "st_as_mvt_geom")?;
                {
                    let __r =
                        pg_out::st_as_mvt_geom(&arg0, arg1, arg2, arg3, arg4, None, None, None)
                            .map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_as_mvt_geom: {}",
                                    shim_err_string(e)
                                ))
                            })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            186usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_as_png")?, "st_as_png")?;
                let arg1 = dv_i64(&args, 1, "st_as_png")? as u32;
                Ok(types::Duckvalue::Blob(
                    (pg_rast_out::st_as_png(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_png: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            187usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_svg")?, "st_as_svg")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_svg(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_svg: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            188usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_svg_full")?, "st_as_svg_full")?;
                let arg1 = dv_i64(&args, 1, "st_as_svg_full")? as u32;
                let arg2 = dv_i64(&args, 2, "st_as_svg_full")? as u32;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_svg_full(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_svg_full: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            189usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_text")?, "st_as_text")?;
                Ok(types::Duckvalue::Text((pg_out::st_as_text(&arg0)).into()))
            }
            190usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_as_text_precision")?,
                    "st_as_text_precision",
                )?;
                let arg1 = dv_i64(&args, 1, "st_as_text_precision")? as u32;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_text_precision(&arg0, arg1)).into(),
                ))
            }
            191usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_as_tiff")?, "st_as_tiff")?;
                Ok(types::Duckvalue::Blob(
                    (pg_rast_out::st_as_tiff(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_tiff: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            192usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "st_as_topo_json")?, "st_as_topo_json")?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_out::as_topojson(&arg0)).into(),
                ))
            }
            193usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_twkb")?, "st_as_twkb")?;
                Ok(types::Duckvalue::Blob(
                    (pg_out::st_as_twkb(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_twkb: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            194usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_x3d")?, "st_as_x3d")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_x3d(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_x3d: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            195usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_as_x3d_full")?, "st_as_x3d_full")?;
                let arg1 = dv_i64(&args, 1, "st_as_x3d_full")? as u32;
                let arg2 = dv_i64(&args, 2, "st_as_x3d_full")? as u32;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_x3d_full(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_as_x3d_full: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            196usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_asbinary")?, "st_asbinary")?;
                Ok(types::Duckvalue::Blob((pg_out::st_as_binary(&arg0)).into()))
            }
            197usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_asencodedpolyline")?,
                    "st_asencodedpolyline",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_encoded_polyline(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_asencodedpolyline: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            198usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_asewkb")?, "st_asewkb")?;
                Ok(types::Duckvalue::Blob((pg_out::st_as_ewkb(&arg0)).into()))
            }
            199usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_asewkt")?, "st_asewkt")?;
                Ok(types::Duckvalue::Text((pg_out::st_as_ewkt(&arg0)).into()))
            }
            200usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_asgeojson")?, "st_asgeojson")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_geojson(&arg0)).into(),
                ))
            }
            201usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_asgml")?, "st_asgml")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_gml(&arg0, None, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_asgml: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            202usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_ashexewkb")?, "st_ashexewkb")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_hexewkb(&arg0)).into(),
                ))
            }
            203usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_askml")?, "st_askml")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_kml(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_askml: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            204usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_aslatlontext")?, "st_aslatlontext")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_lat_lon_text(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_aslatlontext: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            205usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_asmarc21")?, "st_asmarc21")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_marc21(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_asmarc21: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            206usize => {
                let arg0_one = from_wkb(dv_blob(&args, 0, "st_asmvt")?, "st_asmvt")?;
                let arg0_owned: Vec<Geometry> = alloc::vec![arg0_one];
                let arg0: Vec<&Geometry> = arg0_owned.iter().collect();
                let arg1 = dv_text(&args, 1, "st_asmvt")?;
                Ok(types::Duckvalue::Blob(
                    (pg_out::st_as_mvt(&arg0, arg1, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_asmvt: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            207usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_asmvtgeom")?, "st_asmvtgeom")?;
                let arg1 = dv_f64(&args, 1, "st_asmvtgeom")?;
                let arg2 = dv_f64(&args, 2, "st_asmvtgeom")?;
                let arg3 = dv_f64(&args, 3, "st_asmvtgeom")?;
                let arg4 = dv_f64(&args, 4, "st_asmvtgeom")?;
                {
                    let __r =
                        pg_out::st_as_mvt_geom(&arg0, arg1, arg2, arg3, arg4, None, None, None)
                            .map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_asmvtgeom: {}",
                                    shim_err_string(e)
                                ))
                            })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            208usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_aspect")?, "st_aspect")?;
                let arg1 = dv_i64(&args, 1, "st_aspect")? as u32;
                {
                    let __r = pg_rast_proc::st_aspect(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_aspect: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            209usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_aspng")?, "st_aspng")?;
                let arg1 = dv_i64(&args, 1, "st_aspng")? as u32;
                Ok(types::Duckvalue::Blob(
                    (pg_rast_out::st_as_png(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_aspng: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            210usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_assvg")?, "st_assvg")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_svg(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_assvg: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            211usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_astext")?, "st_astext")?;
                Ok(types::Duckvalue::Text((pg_out::st_as_text(&arg0)).into()))
            }
            212usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_astiff")?, "st_astiff")?;
                Ok(types::Duckvalue::Blob(
                    (pg_rast_out::st_as_tiff(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_astiff: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            213usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "st_astopojson")?, "st_astopojson")?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_out::as_topojson(&arg0)).into(),
                ))
            }
            214usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_astwkb")?, "st_astwkb")?;
                Ok(types::Duckvalue::Blob(
                    (pg_out::st_as_twkb(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_astwkb: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            215usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_asx3d")?, "st_asx3d")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_as_x3d(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_asx3d: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            216usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_azimuth")?, "st_azimuth")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_azimuth")?, "st_azimuth")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_azimuth(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_azimuth: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            217usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_band_count")?, "st_band_count")?;
                let arg1 = dv_i64(&args, 1, "st_band_count")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_rast_acc::st_band_count(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_band_count: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            218usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_band_max")?, "st_band_max")?;
                let arg1 = dv_i64(&args, 1, "st_band_max")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_acc::st_band_max(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_band_max: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            219usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_band_mean")?, "st_band_mean")?;
                let arg1 = dv_i64(&args, 1, "st_band_mean")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_acc::st_band_mean(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_band_mean: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            220usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_band_metadata")?, "st_band_metadata")?;
                let arg1 = dv_i64(&args, 1, "st_band_metadata")? as u32;
                ret_to_witvalue_band_metadata(pg_rast_acc::st_band_metadata(&arg0, arg1).map_err(
                    |e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_band_metadata: {}",
                            shim_err_string(e)
                        ))
                    },
                )?)
            }
            221usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_band_min")?, "st_band_min")?;
                let arg1 = dv_i64(&args, 1, "st_band_min")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_acc::st_band_min(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_band_min: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            222usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_band_nodata_value")?,
                    "st_band_nodata_value",
                )?;
                let arg1 = dv_i64(&args, 1, "st_band_nodata_value")? as u32;
                Ok(
                    match pg_rast_acc::st_band_nodata_value(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_band_nodata_value: {}",
                            shim_err_string(e)
                        ))
                    })? {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            223usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_band_pixel_type")?,
                    "st_band_pixel_type",
                )?;
                let arg1 = dv_i64(&args, 1, "st_band_pixel_type")? as u32;
                {
                    let __r = pg_rast_acc::st_band_pixel_type(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_band_pixel_type: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let __disc: i64 = match __r {
                        pg_rast_types::PixelType::Bool1 => 0,
                        pg_rast_types::PixelType::Uint8 => 1,
                        pg_rast_types::PixelType::Int8 => 2,
                        pg_rast_types::PixelType::Uint16 => 3,
                        pg_rast_types::PixelType::Int16 => 4,
                        pg_rast_types::PixelType::Uint32 => 5,
                        pg_rast_types::PixelType::Int32 => 6,
                        pg_rast_types::PixelType::Float32 => 7,
                        pg_rast_types::PixelType::Float64 => 8,
                    };
                    Ok(types::Duckvalue::Int64(__disc))
                }
            }
            224usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_band_stddev")?, "st_band_stddev")?;
                let arg1 = dv_i64(&args, 1, "st_band_stddev")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_acc::st_band_stddev(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_band_stddev: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            225usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_band_sum")?, "st_band_sum")?;
                let arg1 = dv_i64(&args, 1, "st_band_sum")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_acc::st_band_sum(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_band_sum: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            226usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_bandnodatavalue")?,
                    "st_bandnodatavalue",
                )?;
                let arg1 = dv_i64(&args, 1, "st_bandnodatavalue")? as u32;
                Ok(
                    match pg_rast_acc::st_band_nodata_value(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_bandnodatavalue: {}",
                            shim_err_string(e)
                        ))
                    })? {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            227usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_bandpixeltype")?, "st_bandpixeltype")?;
                let arg1 = dv_i64(&args, 1, "st_bandpixeltype")? as u32;
                {
                    let __r = pg_rast_acc::st_band_pixel_type(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_bandpixeltype: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let __disc: i64 = match __r {
                        pg_rast_types::PixelType::Bool1 => 0,
                        pg_rast_types::PixelType::Uint8 => 1,
                        pg_rast_types::PixelType::Int8 => 2,
                        pg_rast_types::PixelType::Uint16 => 3,
                        pg_rast_types::PixelType::Int16 => 4,
                        pg_rast_types::PixelType::Uint32 => 5,
                        pg_rast_types::PixelType::Int32 => 6,
                        pg_rast_types::PixelType::Float32 => 7,
                        pg_rast_types::PixelType::Float64 => 8,
                    };
                    Ok(types::Duckvalue::Int64(__disc))
                }
            }
            228usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_bands_metadata")?,
                    "st_bands_metadata",
                )?;
                let arg1: Vec<u32> = parse_json_list_u32(&args, 1, "st_bands_metadata")?;
                {
                    let __r = pg_rast_acc::st_bands_metadata(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_bands_metadata: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_band_metadata(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            229usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_bboxdistance")?, "st_bboxdistance")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_bboxdistance")?, "st_bboxdistance")?;
                Ok(types::Duckvalue::Float64(pg_op::op_bbox_distance(
                    &arg0, &arg1,
                )))
            }
            230usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_bboxintersects")?, "st_bboxintersects")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_bboxintersects")?, "st_bboxintersects")?;
                Ok(types::Duckvalue::Boolean(pg_op::op_bbox_intersects_twod(
                    &arg0, &arg1,
                )))
            }
            231usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_bboxintersectsnd")?,
                    "st_bboxintersectsnd",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_bboxintersectsnd")?,
                    "st_bboxintersectsnd",
                )?;
                Ok(types::Duckvalue::Boolean(pg_op::op_bbox_intersects_nd(
                    &arg0, &arg1,
                )))
            }
            232usize => {
                let arg0 = dv_text(&args, 0, "st_bd_mpoly_from_text")?;
                {
                    let __r = pg_ctor::st_bd_mpoly_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_bd_mpoly_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            233usize => {
                let arg0 = dv_text(&args, 0, "st_bd_poly_from_text")?;
                {
                    let __r = pg_ctor::st_bd_poly_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_bd_poly_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            234usize => {
                let arg0 = dv_text(&args, 0, "st_bdmpolyfromtext")?;
                {
                    let __r = pg_ctor::st_bd_mpoly_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_bdmpolyfromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            235usize => {
                let arg0 = dv_text(&args, 0, "st_bdpolyfromtext")?;
                {
                    let __r = pg_ctor::st_bd_poly_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_bdpolyfromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            236usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_boundary")?, "st_boundary")?;
                {
                    let __r = pg_proc::st_boundary(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_boundary: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            237usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_boundary_batch")?;
                {
                    let __r = postgis_batch::st_boundary_batch(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_boundary_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            238usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_boundary_threed")?,
                    "st_boundary_threed",
                )?;
                {
                    let __r = pg_threed::st_boundary_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_boundary_threed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            239usize => {
                let arg0 = dv_blob(&args, 0, "st_boundarythreed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_boundary_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_boundarythreed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            240usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_bounding_diagonal")?,
                    "st_bounding_diagonal",
                )?;
                {
                    let __r = pg_acc::st_bounding_diagonal(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_bounding_diagonal: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            241usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_boundingdiagonal")?,
                    "st_boundingdiagonal",
                )?;
                {
                    let __r = pg_acc::st_bounding_diagonal(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_boundingdiagonal: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            242usize => {
                let arg0 = dv_text(&args, 0, "st_box2d_from_geohash")?;
                {
                    let __bb = pg_ctor::st_box_from_geohash(arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_box2d_from_geohash: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let __env =
                        pg_ctor::st_make_envelope(__bb.min_x, __bb.min_y, __bb.max_x, __bb.max_y);
                    Ok(types::Duckvalue::Blob(__env.as_wkb().into()))
                }
            }
            243usize => {
                let arg0 = dv_text(&args, 0, "st_box2dfromgeohash")?;
                {
                    let __bb = pg_ctor::st_box_from_geohash(arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_box2dfromgeohash: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let __env =
                        pg_ctor::st_make_envelope(__bb.min_x, __bb.min_y, __bb.max_x, __bb.max_y);
                    Ok(types::Duckvalue::Blob(__env.as_wkb().into()))
                }
            }
            244usize => {
                let arg0 = dv_text(&args, 0, "st_box_from_geohash")?;
                {
                    let __bb = pg_ctor::st_box_from_geohash(arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_box_from_geohash: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let __env =
                        pg_ctor::st_make_envelope(__bb.min_x, __bb.min_y, __bb.max_x, __bb.max_y);
                    Ok(types::Duckvalue::Blob(__env.as_wkb().into()))
                }
            }
            245usize => {
                let arg0 = dv_text(&args, 0, "st_boxfromgeohash")?;
                {
                    let __bb = pg_ctor::st_box_from_geohash(arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_boxfromgeohash: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let __env =
                        pg_ctor::st_make_envelope(__bb.min_x, __bb.min_y, __bb.max_x, __bb.max_y);
                    Ok(types::Duckvalue::Blob(__env.as_wkb().into()))
                }
            }
            246usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_buffer")?, "st_buffer")?;
                let arg1 = dv_f64(&args, 1, "st_buffer")?;
                {
                    let __r = pg_proc::st_buffer(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_buffer: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            247usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_buffer_batch")?;
                let arg1: Vec<f64> = parse_json_list_f64(&args, 1, "st_buffer_batch")?;
                {
                    let __r = postgis_batch::st_buffer_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_buffer_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            248usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_buffer_quad_segs")?,
                    "st_buffer_quad_segs",
                )?;
                let arg1 = dv_f64(&args, 1, "st_buffer_quad_segs")?;
                let arg2 = dv_i64(&args, 2, "st_buffer_quad_segs")? as u32;
                {
                    let __r = pg_proc::st_buffer_quad_segs(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_buffer_quad_segs: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            249usize => {
                let arg0 = dv_blob(&args, 0, "st_buffer_threed")?;
                let arg1 = dv_f64(&args, 1, "st_buffer_threed")?;
                let arg2 = dv_i64(&args, 2, "st_buffer_threed")? as i32;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_buffer_threed(arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_buffer_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            250usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_buffer_with_style")?,
                    "st_buffer_with_style",
                )?;
                let arg1 = dv_f64(&args, 1, "st_buffer_with_style")?;
                let arg2 = dv_text(&args, 2, "st_buffer_with_style")?;
                {
                    let __r = pg_proc::st_buffer_with_style(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_buffer_with_style: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            251usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_build_area")?, "st_build_area")?;
                {
                    let __r = pg_proc::st_build_area(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_build_area: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            252usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_buildarea")?, "st_buildarea")?;
                {
                    let __r = pg_proc::st_build_area(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_buildarea: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            253usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_centroid")?, "st_centroid")?;
                {
                    let __r = pg_proc::st_centroid(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_centroid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            254usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_centroid_batch")?;
                {
                    let __r = postgis_batch::st_centroid_batch(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_centroid_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            255usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_centroid_threed")?,
                    "st_centroid_threed",
                )?;
                {
                    let __r = pg_threed::st_centroid_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_centroid_threed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            256usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_centroidthreed")?, "st_centroidthreed")?;
                {
                    let __r = pg_threed::st_centroid_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_centroidthreed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            257usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_chaikin_smoothing")?,
                    "st_chaikin_smoothing",
                )?;
                let arg1 = dv_i64(&args, 1, "st_chaikin_smoothing")? as u32;
                {
                    let __r = pg_proc::st_chaikin_smoothing(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_chaikin_smoothing: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            258usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_chaikinsmoothing")?,
                    "st_chaikinsmoothing",
                )?;
                let arg1 = dv_i64(&args, 1, "st_chaikinsmoothing")? as u32;
                {
                    let __r = pg_proc::st_chaikin_smoothing(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_chaikinsmoothing: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            259usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_clip_by_box2d")?, "st_clip_by_box2d")?;
                let arg1 = dv_f64(&args, 1, "st_clip_by_box2d")?;
                let arg2 = dv_f64(&args, 2, "st_clip_by_box2d")?;
                let arg3 = dv_f64(&args, 3, "st_clip_by_box2d")?;
                let arg4 = dv_f64(&args, 4, "st_clip_by_box2d")?;
                {
                    let __r =
                        pg_proc::st_clip_by_box2d(&arg0, arg1, arg2, arg3, arg4).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_clip_by_box2d: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            260usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_clipbybox2d")?, "st_clipbybox2d")?;
                let arg1 = dv_f64(&args, 1, "st_clipbybox2d")?;
                let arg2 = dv_f64(&args, 2, "st_clipbybox2d")?;
                let arg3 = dv_f64(&args, 3, "st_clipbybox2d")?;
                let arg4 = dv_f64(&args, 4, "st_clipbybox2d")?;
                {
                    let __r =
                        pg_proc::st_clip_by_box2d(&arg0, arg1, arg2, arg3, arg4).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_clipbybox2d: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            261usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_closest_point")?, "st_closest_point")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_closest_point")?, "st_closest_point")?;
                {
                    let __r = pg_meas::st_closest_point(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_closest_point: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            262usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_closest_point_3d")?,
                    "st_closest_point_3d",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_closest_point_3d")?,
                    "st_closest_point_3d",
                )?;
                {
                    let __r = pg_meas::st_closest_point_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_closest_point_3d: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            263usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_closest_point_of_approach")?,
                    "st_closest_point_of_approach",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_closest_point_of_approach")?,
                    "st_closest_point_of_approach",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_closest_point_of_approach(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_closest_point_of_approach: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            264usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_closest_point_threed")?,
                    "st_closest_point_threed",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_closest_point_threed")?,
                    "st_closest_point_threed",
                )?;
                {
                    let __r = pg_meas::st_closest_point_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_closest_point_threed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            265usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_closestpoint")?, "st_closestpoint")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_closestpoint")?, "st_closestpoint")?;
                {
                    let __r = pg_meas::st_closest_point(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_closestpoint: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            266usize => {
                let arg0_owned: Vec<Geometry> = args[0..]
                    .iter()
                    .enumerate()
                    .map(|(j, v)| match v {
                        types::Duckvalue::Blob(b) => {
                            Geometry::from_wkb(b.as_slice()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_collect: arg {}: {}",
                                    0 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        types::Duckvalue::Text(t) => {
                            Geometry::from_wkb(t.as_bytes()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_collect: arg {}: {}",
                                    0 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        _ => Err(types::Duckerror::Invalidargument(format!(
                            "st_collect: arg {} must be BLOB",
                            0 + j
                        ))),
                    })
                    .collect::<Result<Vec<_>, _>>()?;
                let arg0: Vec<&Geometry> = arg0_owned.iter().collect();
                {
                    let __r = pg_acc::st_collect(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_collect: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            267usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_collection_extract")?,
                    "st_collection_extract",
                )?;
                let arg1 = dv_i64(&args, 1, "st_collection_extract")? as u32;
                {
                    let __r = pg_acc::st_collection_extract(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_collection_extract: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            268usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_collection_homogenize")?,
                    "st_collection_homogenize",
                )?;
                {
                    let __r = pg_acc::st_collection_homogenize(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_collection_homogenize: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            269usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_collectionextract")?,
                    "st_collectionextract",
                )?;
                let arg1 = dv_i64(&args, 1, "st_collectionextract")? as u32;
                {
                    let __r = pg_acc::st_collection_extract(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_collectionextract: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            270usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_collectionhomogenize")?,
                    "st_collectionhomogenize",
                )?;
                {
                    let __r = pg_acc::st_collection_homogenize(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_collectionhomogenize: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            271usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_concave_hull")?, "st_concave_hull")?;
                let arg1 = dv_f64(&args, 1, "st_concave_hull")?;
                {
                    let __r = pg_proc::st_concave_hull(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_concave_hull: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            272usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_concave_hull_holes")?,
                    "st_concave_hull_holes",
                )?;
                let arg1 = dv_f64(&args, 1, "st_concave_hull_holes")?;
                let arg2 = dv_bool(&args, 2, "st_concave_hull_holes")?;
                {
                    let __r = pg_proc::st_concave_hull_holes(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_concave_hull_holes: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            273usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_concavehull")?, "st_concavehull")?;
                let arg1 = dv_f64(&args, 1, "st_concavehull")?;
                {
                    let __r = pg_proc::st_concave_hull(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_concavehull: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            274usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_constrained_delaunay_triangles")?,
                    "st_constrained_delaunay_triangles",
                )?;
                {
                    let __r = pg_proc::st_constrained_delaunay_triangles(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_constrained_delaunay_triangles: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            275usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_constraineddelaunaytriangles")?,
                    "st_constraineddelaunaytriangles",
                )?;
                {
                    let __r = pg_proc::st_constrained_delaunay_triangles(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_constraineddelaunaytriangles: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            276usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_constrainedelaunaytriangles")?,
                    "st_constrainedelaunaytriangles",
                )?;
                {
                    let __r = pg_proc::st_constrained_delaunay_triangles(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_constrainedelaunaytriangles: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            277usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_contains")?, "st_contains")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_contains")?, "st_contains")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_contains(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_contains: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            278usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_contains_3d")?, "st_contains_3d")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_contains_3d")?, "st_contains_3d")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_contains_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_contains_3d: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            279usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_contains_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_contains_batch")?;
                {
                    let __r = postgis_batch::st_contains_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_contains_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            280usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_contains_properly")?,
                    "st_contains_properly",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_contains_properly")?,
                    "st_contains_properly",
                )?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_contains_properly(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_contains_properly: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            281usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_contains_threed")?,
                    "st_contains_threed",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_contains_threed")?,
                    "st_contains_threed",
                )?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_contains_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_contains_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            282usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_containsproperly")?,
                    "st_containsproperly",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_containsproperly")?,
                    "st_containsproperly",
                )?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_contains_properly(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_containsproperly: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            283usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_convex_hull")?, "st_convex_hull")?;
                {
                    let __r = pg_proc::st_convex_hull(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_convex_hull: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            284usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_convex_hull_batch")?;
                {
                    let __r = postgis_batch::st_convex_hull_batch(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_convex_hull_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            285usize => {
                let arg0 = dv_blob(&args, 0, "st_convex_hull_threed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_convex_hull_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_convex_hull_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            286usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_convexhull")?, "st_convexhull")?;
                {
                    let __r = pg_proc::st_convex_hull(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_convexhull: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            287usize => {
                let arg0 = dv_blob(&args, 0, "st_convexhullthreed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_convex_hull_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_convexhullthreed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            288usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_coord_dim")?, "st_coord_dim")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_coord_dim(&arg0) as i64))
            }
            289usize => {
                let arg0 = dv_blob(&args, 0, "st_coord_dim_threed")?;
                Ok(types::Duckvalue::Int64(
                    pg_sfcgal::st_coord_dim_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_coord_dim_threed: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            290usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_coorddim")?, "st_coorddim")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_coord_dim(&arg0) as i64))
            }
            291usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_count")?, "st_count")?;
                let arg1 = dv_i64(&args, 1, "st_count")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_rast_stats::st_count(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_count: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            292usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_coverage_clean")?, "st_coverage_clean")?;
                let arg1 = dv_f64(&args, 1, "st_coverage_clean")?;
                let arg2 = dv_f64(&args, 2, "st_coverage_clean")?;
                {
                    let __r = pg_proc::st_coverage_clean(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_coverage_clean: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            293usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_coverage_invalid_edges")?,
                    "st_coverage_invalid_edges",
                )?;
                let arg1 = dv_f64(&args, 1, "st_coverage_invalid_edges")?;
                {
                    let __r = pg_proc::st_coverage_invalid_edges(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_coverage_invalid_edges: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            294usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_coverage_simplify")?,
                    "st_coverage_simplify",
                )?;
                let arg1 = dv_f64(&args, 1, "st_coverage_simplify")?;
                let arg2 = dv_bool(&args, 2, "st_coverage_simplify")?;
                {
                    let __r = pg_proc::st_coverage_simplify(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_coverage_simplify: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            295usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_coverage_union")?, "st_coverage_union")?;
                {
                    let __r = pg_proc::st_coverage_union(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_coverage_union: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            296usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_coverageclean")?, "st_coverageclean")?;
                let arg1 = dv_f64(&args, 1, "st_coverageclean")?;
                let arg2 = dv_f64(&args, 2, "st_coverageclean")?;
                {
                    let __r = pg_proc::st_coverage_clean(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_coverageclean: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            297usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_coverageinvalidedges")?,
                    "st_coverageinvalidedges",
                )?;
                let arg1 = dv_f64(&args, 1, "st_coverageinvalidedges")?;
                {
                    let __r = pg_proc::st_coverage_invalid_edges(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_coverageinvalidedges: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            298usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_coveragesimplify")?,
                    "st_coveragesimplify",
                )?;
                let arg1 = dv_f64(&args, 1, "st_coveragesimplify")?;
                let arg2 = dv_bool(&args, 2, "st_coveragesimplify")?;
                {
                    let __r = pg_proc::st_coverage_simplify(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_coveragesimplify: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            299usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_covered_by")?, "st_covered_by")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_covered_by")?, "st_covered_by")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_covered_by(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_covered_by: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            300usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_covered_by_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_covered_by_batch")?;
                {
                    let __r = postgis_batch::st_covered_by_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_covered_by_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            301usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_coveredby")?, "st_coveredby")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_coveredby")?, "st_coveredby")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_covered_by(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_coveredby: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            302usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_covers")?, "st_covers")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_covers")?, "st_covers")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_covers(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_covers: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            303usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_covers_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_covers_batch")?;
                {
                    let __r = postgis_batch::st_covers_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_covers_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            304usize => {
                let arg0 = dv_blob(&args, 0, "st_covers_threed")?;
                let arg1 = dv_blob(&args, 1, "st_covers_threed")?;
                Ok(types::Duckvalue::Boolean(pg_sfcgal::st_covers_threed(
                    arg0, arg1,
                )))
            }
            305usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_cpa_within")?, "st_cpa_within")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_cpa_within")?, "st_cpa_within")?;
                let arg2 = dv_f64(&args, 2, "st_cpa_within")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_cpa_within(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cpa_within: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            306usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_cpawithin")?, "st_cpawithin")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_cpawithin")?, "st_cpawithin")?;
                let arg2 = dv_f64(&args, 2, "st_cpawithin")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_cpa_within(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cpawithin: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            307usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_create_topo_geo")?,
                    "st_create_topo_geo",
                )?;
                let arg1_owned: Vec<Geometry> = args[1..]
                    .iter()
                    .enumerate()
                    .map(|(j, v)| match v {
                        types::Duckvalue::Blob(b) => {
                            Geometry::from_wkb(b.as_slice()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_create_topo_geo: arg {}: {}",
                                    1 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        types::Duckvalue::Text(t) => {
                            Geometry::from_wkb(t.as_bytes()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_create_topo_geo: arg {}: {}",
                                    1 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        _ => Err(types::Duckerror::Invalidargument(format!(
                            "st_create_topo_geo: arg {} must be BLOB",
                            1 + j
                        ))),
                    })
                    .collect::<Result<Vec<_>, _>>()?;
                let arg1: Vec<&Geometry> = arg1_owned.iter().collect();
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::st_create_topo_geo(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_create_topo_geo: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            308usize => {
                let arg0 = dv_text(&args, 0, "st_create_topology")?;
                let arg1 = dv_i64(&args, 1, "st_create_topology")? as i32;
                let arg2 = dv_f64(&args, 2, "st_create_topology")?;
                Ok(types::Duckvalue::Blob(
                    Topology::new(arg0, arg1, arg2).to_bytes().into(),
                ))
            }
            309usize => {
                let arg0 = dv_text(&args, 0, "st_createtopology")?;
                let arg1 = dv_i64(&args, 1, "st_createtopology")? as i32;
                let arg2 = dv_f64(&args, 2, "st_createtopology")?;
                Ok(types::Duckvalue::Blob(
                    Topology::new(arg0, arg1, arg2).to_bytes().into(),
                ))
            }
            310usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_crosses")?, "st_crosses")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_crosses")?, "st_crosses")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_crosses(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_crosses: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            311usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_crosses_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_crosses_batch")?;
                {
                    let __r = postgis_batch::st_crosses_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_crosses_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            312usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_curve_n")?, "st_curve_n")?;
                let arg1 = dv_i64(&args, 1, "st_curve_n")? as u32;
                {
                    let __r = pg_proc::st_curve_n(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_curve_n: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            313usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_curve_to_line")?, "st_curve_to_line")?;
                let arg1 = dv_f64(&args, 1, "st_curve_to_line")?;
                let arg2 = dv_i64(&args, 2, "st_curve_to_line")? as u32;
                let arg3 = dv_i64(&args, 3, "st_curve_to_line")? as u32;
                {
                    let __r = pg_proc::st_curve_to_line(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_curve_to_line: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            314usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_curven")?, "st_curven")?;
                let arg1 = dv_i64(&args, 1, "st_curven")? as u32;
                {
                    let __r = pg_proc::st_curve_n(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_curven: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            315usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_curvetoline")?, "st_curvetoline")?;
                let arg1 = dv_f64(&args, 1, "st_curvetoline")?;
                let arg2 = dv_i64(&args, 2, "st_curvetoline")? as u32;
                let arg3 = dv_i64(&args, 3, "st_curvetoline")? as u32;
                {
                    let __r = pg_proc::st_curve_to_line(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_curvetoline: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            316usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_d_within")?, "st_d_within")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_d_within")?, "st_d_within")?;
                let arg2 = dv_f64(&args, 2, "st_d_within")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_dwithin(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_d_within: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            317usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_delaunay_triangles")?,
                    "st_delaunay_triangles",
                )?;
                let arg1 = dv_f64(&args, 1, "st_delaunay_triangles")?;
                {
                    let __r = pg_proc::st_delaunay_triangles(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_delaunay_triangles: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            318usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_delaunaytriangles")?,
                    "st_delaunaytriangles",
                )?;
                let arg1 = dv_f64(&args, 1, "st_delaunaytriangles")?;
                {
                    let __r = pg_proc::st_delaunay_triangles(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_delaunaytriangles: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            319usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dfully_within")?, "st_dfully_within")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_dfully_within")?, "st_dfully_within")?;
                let arg2 = dv_f64(&args, 2, "st_dfully_within")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_dfully_within(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_dfully_within: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            320usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_dfully_within_threed")?,
                    "st_dfully_within_threed",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_dfully_within_threed")?,
                    "st_dfully_within_threed",
                )?;
                let arg2 = dv_f64(&args, 2, "st_dfully_within_threed")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_dfully_within_threed(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_dfully_within_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            321usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dfullywithin")?, "st_dfullywithin")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_dfullywithin")?, "st_dfullywithin")?;
                let arg2 = dv_f64(&args, 2, "st_dfullywithin")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_dfully_within(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_dfullywithin: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            322usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_difference")?, "st_difference")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_difference")?, "st_difference")?;
                {
                    let __r = pg_proc::st_difference(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_difference: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            323usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_difference_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_difference_batch")?;
                {
                    let __r = postgis_batch::st_difference_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_difference_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            324usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_difference_gridsize")?,
                    "st_difference_gridsize",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_difference_gridsize")?,
                    "st_difference_gridsize",
                )?;
                let arg2 = dv_f64(&args, 2, "st_difference_gridsize")?;
                {
                    let __r = pg_proc::st_difference_gridsize(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_difference_gridsize: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            325usize => {
                let arg0 = dv_blob(&args, 0, "st_difference_threed")?;
                let arg1 = dv_blob(&args, 1, "st_difference_threed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_difference_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_difference_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            326usize => {
                let arg0 = dv_blob(&args, 0, "st_differencethreed")?;
                let arg1 = dv_blob(&args, 1, "st_differencethreed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_difference_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_differencethreed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            327usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dimension")?, "st_dimension")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_dimension(&arg0) as i64))
            }
            328usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_disjoint")?, "st_disjoint")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_disjoint")?, "st_disjoint")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_disjoint(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_disjoint: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            329usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_disjoint_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_disjoint_batch")?;
                {
                    let __r = postgis_batch::st_disjoint_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_disjoint_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            330usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_distance")?, "st_distance")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_distance")?, "st_distance")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_distance(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_distance: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            331usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_distance_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_distance_batch")?;
                {
                    let __r = postgis_batch::st_distance_batch(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            332usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_distance_cpa")?, "st_distance_cpa")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_distance_cpa")?, "st_distance_cpa")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_distance_cpa(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_distance_cpa: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            333usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_distance_sphere")?,
                    "st_distance_sphere",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_distance_sphere")?,
                    "st_distance_sphere",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_geog::st_distance_sphere(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_distance_sphere: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            334usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_distance_sphere_batch")?;
                let arg1: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 1, "st_distance_sphere_batch")?;
                {
                    let __r = postgis_batch::st_distance_sphere_batch(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            335usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_distance_sphere_radius")?,
                    "st_distance_sphere_radius",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_distance_sphere_radius")?,
                    "st_distance_sphere_radius",
                )?;
                let arg2 = dv_f64(&args, 2, "st_distance_sphere_radius")?;
                Ok(types::Duckvalue::Float64(
                    pg_geog::st_distance_sphere_radius(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_distance_sphere_radius: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            336usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_distance_spheroid")?,
                    "st_distance_spheroid",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_distance_spheroid")?,
                    "st_distance_spheroid",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_distance_spheroid(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_distance_spheroid: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            337usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_distance_spheroid_batch")?;
                let arg1: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 1, "st_distance_spheroid_batch")?;
                {
                    let __r = postgis_batch::st_distance_spheroid_batch(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            338usize => {
                let arg0 = dv_blob(&args, 0, "st_distance_threed")?;
                let arg1 = dv_blob(&args, 1, "st_distance_threed")?;
                Ok(types::Duckvalue::Float64(pg_sfcgal::st_distance_threed(
                    arg0, arg1,
                )))
            }
            339usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_distancecpa")?, "st_distancecpa")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_distancecpa")?, "st_distancecpa")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_distance_cpa(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_distancecpa: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            340usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_distancesphere")?, "st_distancesphere")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_distancesphere")?, "st_distancesphere")?;
                Ok(types::Duckvalue::Float64(
                    pg_geog::st_distance_sphere(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_distancesphere: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            341usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_distancespheroid")?,
                    "st_distancespheroid",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_distancespheroid")?,
                    "st_distancespheroid",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_distance_spheroid(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_distancespheroid: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            342usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_distancethreed")?, "st_distancethreed")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_distancethreed")?, "st_distancethreed")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_distance_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_distancethreed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            343usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_dump_as_polygons")?,
                    "st_dump_as_polygons",
                )?;
                let arg1 = dv_i64(&args, 1, "st_dump_as_polygons")? as u32;
                {
                    let __r = pg_rast_vec::st_dump_as_polygons(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_dump_as_polygons: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __out = alloc::string::String::from("[");
                    for (__i, (__g, __v)) in __r.into_iter().enumerate() {
                        if __i > 0 {
                            __out.push(',');
                        }
                        let __wkb = __g.as_wkb();
                        __out.push_str("[\"");
                        for __b in __wkb {
                            use core::fmt::Write as _;
                            let _ = write!(&mut __out, "{:02x}", __b);
                        }
                        __out.push_str("\",");
                        let __vj = serde_json::to_string(&__v).map_err(|e| {
                            types::Duckerror::Internal(format!(
                                "st_dump_as_polygons: encode JSON: {}",
                                e
                            ))
                        })?;
                        __out.push_str(&__vj);
                        __out.push(']');
                    }
                    __out.push(']');
                    Ok(types::Duckvalue::Text(__out))
                }
            }
            344usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_dump_values")?, "st_dump_values")?;
                let arg1 = dv_i64(&args, 1, "st_dump_values")? as u32;
                {
                    let __r = pg_rast_px::st_dump_values(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_dump_values: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_dump_values: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            345usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_dump_values_multi")?,
                    "st_dump_values_multi",
                )?;
                let arg1: Vec<u32> = parse_json_list_u32(&args, 1, "st_dump_values_multi")?;
                {
                    let __r = pg_rast_px::st_dump_values_multi(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_dump_values_multi: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_band_values_entry(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            346usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_dumpaspolygons")?,
                    "st_dumpaspolygons",
                )?;
                let arg1 = dv_i64(&args, 1, "st_dumpaspolygons")? as u32;
                {
                    let __r = pg_rast_vec::st_dump_as_polygons(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_dumpaspolygons: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __out = alloc::string::String::from("[");
                    for (__i, (__g, __v)) in __r.into_iter().enumerate() {
                        if __i > 0 {
                            __out.push(',');
                        }
                        let __wkb = __g.as_wkb();
                        __out.push_str("[\"");
                        for __b in __wkb {
                            use core::fmt::Write as _;
                            let _ = write!(&mut __out, "{:02x}", __b);
                        }
                        __out.push_str("\",");
                        let __vj = serde_json::to_string(&__v).map_err(|e| {
                            types::Duckerror::Internal(format!(
                                "st_dumpaspolygons: encode JSON: {}",
                                e
                            ))
                        })?;
                        __out.push_str(&__vj);
                        __out.push(']');
                    }
                    __out.push(']');
                    Ok(types::Duckvalue::Text(__out))
                }
            }
            347usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_dumpvalues")?, "st_dumpvalues")?;
                let arg1 = dv_i64(&args, 1, "st_dumpvalues")? as u32;
                {
                    let __r = pg_rast_px::st_dump_values(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_dumpvalues: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_dumpvalues: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            348usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dwithin")?, "st_dwithin")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_dwithin")?, "st_dwithin")?;
                let arg2 = dv_f64(&args, 2, "st_dwithin")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_dwithin(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_dwithin: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            349usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dwithin_3d")?, "st_dwithin_3d")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_dwithin_3d")?, "st_dwithin_3d")?;
                let arg2 = dv_f64(&args, 2, "st_dwithin_3d")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_dwithin_threed(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_dwithin_3d: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            350usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dwithin_threed")?, "st_dwithin_threed")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_dwithin_threed")?, "st_dwithin_threed")?;
                let arg2 = dv_f64(&args, 2, "st_dwithin_threed")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_dwithin_threed(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_dwithin_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            351usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_end_point")?, "st_end_point")?;
                {
                    let __r = pg_acc::st_end_point(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_end_point: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            352usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_end_point_batch")?;
                {
                    let __r = postgis_batch::st_end_point_batch(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_end_point_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            353usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_endpoint")?, "st_endpoint")?;
                {
                    let __r = pg_acc::st_end_point(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_endpoint: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            354usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_envelope")?, "st_envelope")?;
                {
                    let __r = pg_acc::st_envelope(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_envelope: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            355usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_envelope_batch")?;
                {
                    let __r = postgis_batch::st_envelope_batch(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_envelope_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            356usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_envelope_threed")?,
                    "st_envelope_threed",
                )?;
                {
                    let __r = pg_threed::st_envelope_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_envelope_threed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            357usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_envelopethreed")?, "st_envelopethreed")?;
                {
                    let __r = pg_threed::st_envelope_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_envelopethreed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            358usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_equals")?, "st_equals")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_equals")?, "st_equals")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_equals(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_equals: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            359usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_equals_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_equals_batch")?;
                {
                    let __r = postgis_batch::st_equals_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_equals_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            360usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_equals_exact")?, "st_equals_exact")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_equals_exact")?, "st_equals_exact")?;
                let arg2 = dv_f64(&args, 2, "st_equals_exact")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_equals_exact(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_equals_exact: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            361usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_equalsexact")?, "st_equalsexact")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_equalsexact")?, "st_equalsexact")?;
                let arg2 = dv_f64(&args, 2, "st_equalsexact")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_equals_exact(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_equalsexact: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            362usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_expand")?, "st_expand")?;
                let arg1 = dv_f64(&args, 1, "st_expand")?;
                {
                    let __r = pg_acc::st_expand(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_expand: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            363usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_expand_xy")?, "st_expand_xy")?;
                let arg1 = dv_f64(&args, 1, "st_expand_xy")?;
                let arg2 = dv_f64(&args, 2, "st_expand_xy")?;
                {
                    let __r = pg_acc::st_expand_xy(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_expand_xy: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            364usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_expand_xyz")?, "st_expand_xyz")?;
                let arg1 = dv_f64(&args, 1, "st_expand_xyz")?;
                let arg2 = dv_f64(&args, 2, "st_expand_xyz")?;
                let arg3 = dv_f64(&args, 3, "st_expand_xyz")?;
                {
                    let __r = pg_acc::st_expand_xyz(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_expand_xyz: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            365usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_expand_xyzm")?, "st_expand_xyzm")?;
                let arg1 = dv_f64(&args, 1, "st_expand_xyzm")?;
                let arg2 = dv_f64(&args, 2, "st_expand_xyzm")?;
                let arg3 = dv_f64(&args, 3, "st_expand_xyzm")?;
                let arg4 = dv_f64(&args, 4, "st_expand_xyzm")?;
                {
                    let __r =
                        pg_acc::st_expand_xyzm(&arg0, arg1, arg2, arg3, arg4).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_expand_xyzm: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            366usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_extent_threed_batch")?;
                {
                    let __r = postgis_batch::st_extent_threed_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            367usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_exterior_ring")?, "st_exterior_ring")?;
                {
                    let __r = pg_acc::st_exterior_ring(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_exterior_ring: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            368usize => {
                let arg0 = dv_blob(&args, 0, "st_exterior_ring_threed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_exterior_ring_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_exterior_ring_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            369usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_exteriorring")?, "st_exteriorring")?;
                {
                    let __r = pg_acc::st_exterior_ring(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_exteriorring: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            370usize => {
                let arg0 = dv_blob(&args, 0, "st_extrude")?;
                let arg1 = dv_f64(&args, 1, "st_extrude")?;
                let arg2 = dv_f64(&args, 2, "st_extrude")?;
                let arg3 = dv_f64(&args, 3, "st_extrude")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_extrude(arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_extrude: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            371usize => {
                let arg0 = dv_blob(&args, 0, "st_extrude_polygon_straight_skeleton")?;
                let arg1 = dv_f64(&args, 1, "st_extrude_polygon_straight_skeleton")?;
                let arg2 = dv_f64(&args, 2, "st_extrude_polygon_straight_skeleton")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_extrude_polygon_straight_skeleton(arg0, arg1, arg2).map_err(
                        |e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_extrude_polygon_straight_skeleton: {}",
                                shim_err_string(e)
                            ))
                        },
                    )?)
                    .into(),
                ))
            }
            372usize => {
                let arg0 = dv_blob(&args, 0, "st_extrude_straight")?;
                let arg1 = dv_f64(&args, 1, "st_extrude_straight")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_extrude_straight(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_extrude_straight: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            373usize => {
                let arg0 = dv_blob(&args, 0, "st_extrude_straight_skeleton")?;
                let arg1 = dv_f64(&args, 1, "st_extrude_straight_skeleton")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_extrude_straight_skeleton(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_extrude_straight_skeleton: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            374usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_filter_by_m")?, "st_filter_by_m")?;
                let arg1 = dv_f64(&args, 1, "st_filter_by_m")?;
                {
                    let __r = pg_proc::st_filter_by_m(&arg0, arg1, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_filter_by_m: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            375usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_filterbym")?, "st_filterbym")?;
                let arg1 = dv_f64(&args, 1, "st_filterbym")?;
                {
                    let __r = pg_proc::st_filter_by_m(&arg0, arg1, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_filterbym: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            376usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_flip_coordinates")?,
                    "st_flip_coordinates",
                )?;
                {
                    let __r = pg_xform::st_flip_coordinates(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_flip_coordinates: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            377usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_flipcoordinates")?,
                    "st_flipcoordinates",
                )?;
                {
                    let __r = pg_xform::st_flip_coordinates(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_flipcoordinates: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            378usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force2d")?, "st_force2d")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_twod(&arg0).as_wkb().into(),
                ))
            }
            379usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force3d")?, "st_force3d")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_threed(&arg0).as_wkb().into(),
                ))
            }
            380usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force3dm")?, "st_force3dm")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_threedm(&arg0).as_wkb().into(),
                ))
            }
            381usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force3dz")?, "st_force3dz")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_threedz(&arg0).as_wkb().into(),
                ))
            }
            382usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force4d")?, "st_force4d")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_fourd(&arg0).as_wkb().into(),
                ))
            }
            383usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force_2d")?, "st_force_2d")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_twod(&arg0).as_wkb().into(),
                ))
            }
            384usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force_3d")?, "st_force_3d")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_threed(&arg0).as_wkb().into(),
                ))
            }
            385usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force_3dm")?, "st_force_3dm")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_threedm(&arg0).as_wkb().into(),
                ))
            }
            386usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force_4d")?, "st_force_4d")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_fourd(&arg0).as_wkb().into(),
                ))
            }
            387usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_force_collection")?,
                    "st_force_collection",
                )?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_collection(&arg0).as_wkb().into(),
                ))
            }
            388usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force_curve")?, "st_force_curve")?;
                {
                    let __r = pg_proc::st_force_curve(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_force_curve: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            389usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force_fourd")?, "st_force_fourd")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_fourd(&arg0).as_wkb().into(),
                ))
            }
            390usize => {
                let arg0 = dv_blob(&args, 0, "st_force_lhr")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_force_lhr(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_force_lhr: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            391usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_force_polygon_ccw")?,
                    "st_force_polygon_ccw",
                )?;
                {
                    let __r = pg_proc::st_force_polygon_ccw(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_force_polygon_ccw: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            392usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_force_polygon_cw")?,
                    "st_force_polygon_cw",
                )?;
                {
                    let __r = pg_proc::st_force_polygon_cw(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_force_polygon_cw: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            393usize => {
                let arg0 = dv_blob(&args, 0, "st_force_rhr")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_force_rhr(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_force_rhr: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            394usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force_sfs")?, "st_force_sfs")?;
                {
                    let __r = pg_xform::st_force_sfs(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_force_sfs: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            395usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force_threed")?, "st_force_threed")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_threed(&arg0).as_wkb().into(),
                ))
            }
            396usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force_threedm")?, "st_force_threedm")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_threedm(&arg0).as_wkb().into(),
                ))
            }
            397usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force_threedz")?, "st_force_threedz")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_threedz(&arg0).as_wkb().into(),
                ))
            }
            398usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_force_twod")?, "st_force_twod")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_twod(&arg0).as_wkb().into(),
                ))
            }
            399usize => {
                let arg0 = dv_blob(&args, 0, "st_force_valid")?;
                let arg1 = dv_bool(&args, 1, "st_force_valid")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_force_valid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_force_valid: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            400usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_forcecollection")?,
                    "st_forcecollection",
                )?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_collection(&arg0).as_wkb().into(),
                ))
            }
            401usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_forcecurve")?, "st_forcecurve")?;
                {
                    let __r = pg_proc::st_force_curve(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_forcecurve: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            402usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_forcefourd")?, "st_forcefourd")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_fourd(&arg0).as_wkb().into(),
                ))
            }
            403usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_forcepolygonccw")?,
                    "st_forcepolygonccw",
                )?;
                {
                    let __r = pg_proc::st_force_polygon_ccw(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_forcepolygonccw: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            404usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_forcepolygoncw")?, "st_forcepolygoncw")?;
                {
                    let __r = pg_proc::st_force_polygon_cw(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_forcepolygoncw: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            405usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_forcerhr")?, "st_forcerhr")?;
                {
                    let __r = pg_proc::st_force_rhr(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_forcerhr: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            406usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_forcesfs")?, "st_forcesfs")?;
                {
                    let __r = pg_xform::st_force_sfs(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_forcesfs: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            407usize => {
                let arg0 = dv_blob(&args, 0, "st_forcethreed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_force_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_forcethreed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            408usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_forcethreedm")?, "st_forcethreedm")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_threedm(&arg0).as_wkb().into(),
                ))
            }
            409usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_forcethreedz")?, "st_forcethreedz")?;
                Ok(types::Duckvalue::Blob(
                    pg_xform::st_force_threedz(&arg0).as_wkb().into(),
                ))
            }
            410usize => {
                let arg0 = dv_blob(&args, 0, "st_forcetwod")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_force_twod(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_forcetwod: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            411usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_frechet_distance")?,
                    "st_frechet_distance",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_frechet_distance")?,
                    "st_frechet_distance",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_frechet_distance(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_frechet_distance: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            412usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_frechetdistance")?,
                    "st_frechetdistance",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_frechetdistance")?,
                    "st_frechetdistance",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_frechet_distance(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_frechetdistance: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            413usize => {
                let arg0 = dv_blob(&args, 0, "st_from_flatgeobuf")?;
                {
                    let __r: Vec<Geometry> = pg_ctor::st_from_flatgeobuf(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_from_flatgeobuf: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(g) => Ok(types::Duckvalue::Blob(g.as_wkb().into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            414usize => {
                let arg0 = dv_blob(&args, 0, "st_fromflatgeobuf")?;
                {
                    let __r: Vec<Geometry> = pg_ctor::st_from_flatgeobuf(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_fromflatgeobuf: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(g) => Ok(types::Duckvalue::Blob(g.as_wkb().into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            415usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_generate_points")?,
                    "st_generate_points",
                )?;
                let arg1 = dv_i64(&args, 1, "st_generate_points")? as u32;
                {
                    let __r = pg_proc::st_generate_points(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_generate_points: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            416usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_generatepoints")?, "st_generatepoints")?;
                let arg1 = dv_i64(&args, 1, "st_generatepoints")? as u32;
                {
                    let __r = pg_proc::st_generate_points(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_generatepoints: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            417usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_geo_reference")?, "st_geo_reference")?;
                let arg1 = dv_text(&args, 1, "st_geo_reference")?;
                Ok(types::Duckvalue::Text(
                    (pg_rast_acc::st_geo_reference(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geo_reference: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            418usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_area")?, "st_geog_area")?;
                Ok(types::Duckvalue::Float64(pg_geog::st_geog_area(&arg0)))
            }
            419usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_geog_area_batch")?;
                {
                    let __r = postgis_batch::st_geog_area_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            420usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_azimuth")?, "st_geog_azimuth")?;
                let arg1 = geog_from_wkb(dv_blob(&args, 1, "st_geog_azimuth")?, "st_geog_azimuth")?;
                Ok(match pg_geog::st_geog_azimuth(&arg0, &arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            421usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_geog_azimuth_batch")?;
                let arg1: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 1, "st_geog_azimuth_batch")?;
                {
                    let __r = postgis_batch::st_geog_azimuth_batch(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            422usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_buffer")?, "st_geog_buffer")?;
                let arg1 = dv_f64(&args, 1, "st_geog_buffer")?;
                {
                    let __r = pg_geog::st_geog_buffer(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_buffer: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            423usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geog_buffer_with_segs")?,
                    "st_geog_buffer_with_segs",
                )?;
                let arg1 = dv_f64(&args, 1, "st_geog_buffer_with_segs")?;
                let arg2 = dv_i64(&args, 2, "st_geog_buffer_with_segs")? as u32;
                {
                    let __r =
                        pg_geog::st_geog_buffer_with_segs(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_geog_buffer_with_segs: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            424usize => {
                let arg0 =
                    geog_from_wkb(dv_blob(&args, 0, "st_geog_centroid")?, "st_geog_centroid")?;
                {
                    let __r = pg_geog::st_geog_centroid(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_centroid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            425usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geog_closest_point")?,
                    "st_geog_closest_point",
                )?;
                let arg1 = geog_from_wkb(
                    dv_blob(&args, 1, "st_geog_closest_point")?,
                    "st_geog_closest_point",
                )?;
                {
                    let __r = pg_geog::st_geog_closest_point(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_closest_point: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            426usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geog_convex_hull")?,
                    "st_geog_convex_hull",
                )?;
                {
                    let __r = pg_geog::st_geog_convex_hull(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_convex_hull: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            427usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geog_covered_by")?,
                    "st_geog_covered_by",
                )?;
                let arg1 = geog_from_wkb(
                    dv_blob(&args, 1, "st_geog_covered_by")?,
                    "st_geog_covered_by",
                )?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_covered_by(
                    &arg0, &arg1,
                )))
            }
            428usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_covers")?, "st_geog_covers")?;
                let arg1 = geog_from_wkb(dv_blob(&args, 1, "st_geog_covers")?, "st_geog_covers")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_covers(
                    &arg0, &arg1,
                )))
            }
            429usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geog_difference")?,
                    "st_geog_difference",
                )?;
                let arg1 = geog_from_wkb(
                    dv_blob(&args, 1, "st_geog_difference")?,
                    "st_geog_difference",
                )?;
                {
                    let __r = pg_geog::st_geog_difference(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_difference: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            430usize => {
                let arg0 =
                    geog_from_wkb(dv_blob(&args, 0, "st_geog_distance")?, "st_geog_distance")?;
                let arg1 =
                    geog_from_wkb(dv_blob(&args, 1, "st_geog_distance")?, "st_geog_distance")?;
                Ok(types::Duckvalue::Float64(pg_geog::st_geog_distance(
                    &arg0, &arg1,
                )))
            }
            431usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_geog_distance_batch")?;
                let arg1: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 1, "st_geog_distance_batch")?;
                {
                    let __r = postgis_batch::st_geog_distance_batch(&arg0, &arg1);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            432usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_dwithin")?, "st_geog_dwithin")?;
                let arg1 = geog_from_wkb(dv_blob(&args, 1, "st_geog_dwithin")?, "st_geog_dwithin")?;
                let arg2 = dv_f64(&args, 2, "st_geog_dwithin")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_dwithin(
                    &arg0, &arg1, arg2,
                )))
            }
            433usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_geog_dwithin_batch")?;
                let arg1: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 1, "st_geog_dwithin_batch")?;
                let arg2: Vec<f64> = parse_json_list_f64(&args, 2, "st_geog_dwithin_batch")?;
                {
                    let __r = postgis_batch::st_geog_dwithin_batch(&arg0, &arg1, &arg2);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_geog_dwithin_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            434usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_expand")?, "st_geog_expand")?;
                let arg1 = dv_f64(&args, 1, "st_geog_expand")?;
                {
                    let __r = pg_geog::st_geog_expand(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_expand: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            435usize => {
                let arg0 = dv_text(&args, 0, "st_geog_from_ewkt")?;
                {
                    let __r = pg_ctor::st_geog_from_ewkt(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_from_ewkt: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            436usize => {
                let arg0 = dv_text(&args, 0, "st_geog_from_text")?;
                {
                    let __r = pg_ctor::st_geog_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            437usize => {
                let arg0 = dv_blob(&args, 0, "st_geog_from_wkb")?;
                {
                    let __r = pg_ctor::st_geog_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            438usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geog_geometry_type")?,
                    "st_geog_geometry_type",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_geog::st_geog_geometry_type(&arg0)).into(),
                ))
            }
            439usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geog_intersection")?,
                    "st_geog_intersection",
                )?;
                let arg1 = geog_from_wkb(
                    dv_blob(&args, 1, "st_geog_intersection")?,
                    "st_geog_intersection",
                )?;
                {
                    let __r = pg_geog::st_geog_intersection(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_intersection: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            440usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geog_intersects")?,
                    "st_geog_intersects",
                )?;
                let arg1 = geog_from_wkb(
                    dv_blob(&args, 1, "st_geog_intersects")?,
                    "st_geog_intersects",
                )?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_intersects(
                    &arg0, &arg1,
                )))
            }
            441usize => {
                let arg0 =
                    geog_from_wkb(dv_blob(&args, 0, "st_geog_is_closed")?, "st_geog_is_closed")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_is_closed(&arg0)))
            }
            442usize => {
                let arg0 =
                    geog_from_wkb(dv_blob(&args, 0, "st_geog_is_empty")?, "st_geog_is_empty")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_is_empty(&arg0)))
            }
            443usize => {
                let arg0 =
                    geog_from_wkb(dv_blob(&args, 0, "st_geog_is_simple")?, "st_geog_is_simple")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_is_simple(&arg0)))
            }
            444usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_length")?, "st_geog_length")?;
                Ok(types::Duckvalue::Float64(pg_geog::st_geog_length(&arg0)))
            }
            445usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_geog_length_batch")?;
                {
                    let __r = postgis_batch::st_geog_length_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            446usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geog_line_substring")?,
                    "st_geog_line_substring",
                )?;
                let arg1 = dv_f64(&args, 1, "st_geog_line_substring")?;
                let arg2 = dv_f64(&args, 2, "st_geog_line_substring")?;
                {
                    let __r = pg_geog::st_geog_line_substring(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_line_substring: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            447usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_npoints")?, "st_geog_npoints")?;
                Ok(types::Duckvalue::Int64(
                    pg_geog::st_geog_npoints(&arg0) as i64
                ))
            }
            448usize => {
                let arg0 =
                    geog_from_wkb(dv_blob(&args, 0, "st_geog_perimeter")?, "st_geog_perimeter")?;
                Ok(types::Duckvalue::Float64(pg_geog::st_geog_perimeter(&arg0)))
            }
            449usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_geog_perimeter_batch")?;
                {
                    let __r = postgis_batch::st_geog_perimeter_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            450usize => {
                let arg0 = dv_f64(&args, 0, "st_geog_point")?;
                let arg1 = dv_f64(&args, 1, "st_geog_point")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_geog_point(arg0, arg1).as_wkb().into(),
                ))
            }
            451usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_project")?, "st_geog_project")?;
                let arg1 = dv_f64(&args, 1, "st_geog_project")?;
                let arg2 = dv_f64(&args, 2, "st_geog_project")?;
                {
                    let __r = pg_geog::st_geog_project(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_project: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            452usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_geog_project_batch")?;
                let arg1: Vec<f64> = parse_json_list_f64(&args, 1, "st_geog_project_batch")?;
                let arg2: Vec<f64> = parse_json_list_f64(&args, 2, "st_geog_project_batch")?;
                {
                    let __r = postgis_batch::st_geog_project_batch(&arg0, &arg1, &arg2);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_geog_project_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            453usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geog_segmentize")?,
                    "st_geog_segmentize",
                )?;
                let arg1 = dv_f64(&args, 1, "st_geog_segmentize")?;
                {
                    let __r = pg_geog::st_geog_segmentize(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_segmentize: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            454usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_geog_segmentize_batch")?;
                let arg1: Vec<f64> = parse_json_list_f64(&args, 1, "st_geog_segmentize_batch")?;
                {
                    let __r = postgis_batch::st_geog_segmentize_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_geog_segmentize_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            455usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_summary")?, "st_geog_summary")?;
                Ok(types::Duckvalue::Text(
                    (pg_geog::st_geog_summary(&arg0)).into(),
                ))
            }
            456usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geog_sym_difference")?,
                    "st_geog_sym_difference",
                )?;
                let arg1 = geog_from_wkb(
                    dv_blob(&args, 1, "st_geog_sym_difference")?,
                    "st_geog_sym_difference",
                )?;
                {
                    let __r = pg_geog::st_geog_sym_difference(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_sym_difference: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            457usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_to_geom")?, "st_geog_to_geom")?;
                Ok(types::Duckvalue::Blob(
                    pg_geog::st_geog_to_geom(&arg0).as_wkb().into(),
                ))
            }
            458usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geog_union")?, "st_geog_union")?;
                let arg1 = geog_from_wkb(dv_blob(&args, 1, "st_geog_union")?, "st_geog_union")?;
                {
                    let __r = pg_geog::st_geog_union(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geog_union: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            459usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogarea")?, "st_geogarea")?;
                Ok(types::Duckvalue::Float64(pg_geog::st_geog_area(&arg0)))
            }
            460usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogazimuth")?, "st_geogazimuth")?;
                let arg1 = geog_from_wkb(dv_blob(&args, 1, "st_geogazimuth")?, "st_geogazimuth")?;
                Ok(match pg_geog::st_geog_azimuth(&arg0, &arg1) {
                    Some(v) => types::Duckvalue::Float64(v as f64),
                    None => types::Duckvalue::Null,
                })
            }
            461usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogbuffer")?, "st_geogbuffer")?;
                let arg1 = dv_f64(&args, 1, "st_geogbuffer")?;
                {
                    let __r = pg_geog::st_geog_buffer(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geogbuffer: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            462usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geogbufferwithsegs")?,
                    "st_geogbufferwithsegs",
                )?;
                let arg1 = dv_f64(&args, 1, "st_geogbufferwithsegs")?;
                let arg2 = dv_i64(&args, 2, "st_geogbufferwithsegs")? as u32;
                {
                    let __r =
                        pg_geog::st_geog_buffer_with_segs(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_geogbufferwithsegs: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            463usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogcentroid")?, "st_geogcentroid")?;
                {
                    let __r = pg_geog::st_geog_centroid(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geogcentroid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            464usize => {
                let arg0 =
                    geog_from_wkb(dv_blob(&args, 0, "st_geogcoveredby")?, "st_geogcoveredby")?;
                let arg1 =
                    geog_from_wkb(dv_blob(&args, 1, "st_geogcoveredby")?, "st_geogcoveredby")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_covered_by(
                    &arg0, &arg1,
                )))
            }
            465usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogcovers")?, "st_geogcovers")?;
                let arg1 = geog_from_wkb(dv_blob(&args, 1, "st_geogcovers")?, "st_geogcovers")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_covers(
                    &arg0, &arg1,
                )))
            }
            466usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogdistance")?, "st_geogdistance")?;
                let arg1 = geog_from_wkb(dv_blob(&args, 1, "st_geogdistance")?, "st_geogdistance")?;
                Ok(types::Duckvalue::Float64(pg_geog::st_geog_distance(
                    &arg0, &arg1,
                )))
            }
            467usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogdwithin")?, "st_geogdwithin")?;
                let arg1 = geog_from_wkb(dv_blob(&args, 1, "st_geogdwithin")?, "st_geogdwithin")?;
                let arg2 = dv_f64(&args, 2, "st_geogdwithin")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_dwithin(
                    &arg0, &arg1, arg2,
                )))
            }
            468usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogexpand")?, "st_geogexpand")?;
                let arg1 = dv_f64(&args, 1, "st_geogexpand")?;
                {
                    let __r = pg_geog::st_geog_expand(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geogexpand: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            469usize => {
                let arg0 = dv_text(&args, 0, "st_geogfromtext")?;
                {
                    let __r = pg_ctor::st_geog_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geogfromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            470usize => {
                let arg0 = dv_blob(&args, 0, "st_geogfromwkb")?;
                {
                    let __r = pg_ctor::st_geog_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geogfromwkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            471usize => {
                let arg0 = geog_from_wkb(
                    dv_blob(&args, 0, "st_geoggeometrytype")?,
                    "st_geoggeometrytype",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_geog::st_geog_geometry_type(&arg0)).into(),
                ))
            }
            472usize => {
                let arg0 =
                    geog_from_wkb(dv_blob(&args, 0, "st_geogintersects")?, "st_geogintersects")?;
                let arg1 =
                    geog_from_wkb(dv_blob(&args, 1, "st_geogintersects")?, "st_geogintersects")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_intersects(
                    &arg0, &arg1,
                )))
            }
            473usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogisclosed")?, "st_geogisclosed")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_is_closed(&arg0)))
            }
            474usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogisempty")?, "st_geogisempty")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_is_empty(&arg0)))
            }
            475usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogissimple")?, "st_geogissimple")?;
                Ok(types::Duckvalue::Boolean(pg_geog::st_geog_is_simple(&arg0)))
            }
            476usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geoglength")?, "st_geoglength")?;
                Ok(types::Duckvalue::Float64(pg_geog::st_geog_length(&arg0)))
            }
            477usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geognpoints")?, "st_geognpoints")?;
                Ok(types::Duckvalue::Int64(
                    pg_geog::st_geog_npoints(&arg0) as i64
                ))
            }
            478usize => {
                let arg0 =
                    geog_from_wkb(dv_blob(&args, 0, "st_geogperimeter")?, "st_geogperimeter")?;
                Ok(types::Duckvalue::Float64(pg_geog::st_geog_perimeter(&arg0)))
            }
            479usize => {
                let arg0 = dv_f64(&args, 0, "st_geogpoint")?;
                let arg1 = dv_f64(&args, 1, "st_geogpoint")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_geog_point(arg0, arg1).as_wkb().into(),
                ))
            }
            480usize => {
                let arg0 = dv_text(&args, 0, "st_geography_from_text")?;
                {
                    let __r = pg_ctor::st_geography_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geography_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            481usize => {
                let arg0 = dv_text(&args, 0, "st_geographyfromtext")?;
                {
                    let __r = pg_ctor::st_geography_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geographyfromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            482usize => {
                let arg0 = geog_from_wkb(dv_blob(&args, 0, "st_geogsummary")?, "st_geogsummary")?;
                Ok(types::Duckvalue::Text(
                    (pg_geog::st_geog_summary(&arg0)).into(),
                ))
            }
            483usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_geohash")?, "st_geohash")?;
                Ok(types::Duckvalue::Text(
                    (pg_out::st_geohash(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geohash: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            484usize => {
                let arg0 = dv_blob(&args, 0, "st_geom_from_ewkb")?;
                {
                    let __r = pg_ctor::st_geom_from_ewkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_ewkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            485usize => {
                let arg0 = dv_text(&args, 0, "st_geom_from_ewkt")?;
                {
                    let __r = pg_ctor::st_geom_from_ewkt(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_ewkt: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            486usize => {
                let arg0 = dv_blob(&args, 0, "st_geom_from_geobuf")?;
                {
                    let __r: Vec<Geometry> = pg_ctor::st_geom_from_geobuf(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_geobuf: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(g) => Ok(types::Duckvalue::Blob(g.as_wkb().into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            487usize => {
                let arg0 = dv_text(&args, 0, "st_geom_from_geohash")?;
                {
                    let __r = pg_ctor::st_geom_from_geohash(arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_geohash: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            488usize => {
                let arg0 = dv_text(&args, 0, "st_geom_from_geojson")?;
                {
                    let __r = pg_ctor::st_geom_from_geojson(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_geojson: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            489usize => {
                let arg0 = dv_text(&args, 0, "st_geom_from_gml")?;
                {
                    let __r = pg_ctor::st_geom_from_gml(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_gml: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            490usize => {
                let arg0 = dv_text(&args, 0, "st_geom_from_gml_srid")?;
                let arg1 = dv_i64(&args, 1, "st_geom_from_gml_srid")? as i32;
                {
                    let __r = pg_ctor::st_geom_from_gml_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_gml_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            491usize => {
                let arg0 = dv_text(&args, 0, "st_geom_from_hexewkb")?;
                {
                    let __r = pg_ctor::st_geom_from_hexewkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_hexewkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            492usize => {
                let arg0 = dv_text(&args, 0, "st_geom_from_kml")?;
                {
                    let __r = pg_ctor::st_geom_from_kml(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_kml: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            493usize => {
                let arg0 = dv_text(&args, 0, "st_geom_from_marc21")?;
                {
                    let __r = pg_ctor::st_geom_from_marc21(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_marc21: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            494usize => {
                let arg0 = dv_text(&args, 0, "st_geom_from_text")?;
                {
                    let __r = pg_ctor::st_geom_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            495usize => {
                let arg0 = dv_text(&args, 0, "st_geom_from_text_srid")?;
                let arg1 = dv_i64(&args, 1, "st_geom_from_text_srid")? as i32;
                {
                    let __r = pg_ctor::st_geom_from_text_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_text_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            496usize => {
                let arg0 = dv_blob(&args, 0, "st_geom_from_twkb")?;
                {
                    let __r = pg_ctor::st_geom_from_twkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_twkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            497usize => {
                let arg0 = dv_blob(&args, 0, "st_geom_from_wkb")?;
                {
                    let __r = pg_ctor::st_geom_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geom_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            498usize => {
                let arg0 = dv_text(&args, 0, "st_geomcoll_from_text")?;
                {
                    let __r = pg_ctor::st_geomcoll_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomcoll_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            499usize => {
                let arg0 = dv_text(&args, 0, "st_geomcoll_from_text_srid")?;
                let arg1 = dv_i64(&args, 1, "st_geomcoll_from_text_srid")? as i32;
                {
                    let __r = pg_ctor::st_geomcoll_from_text_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomcoll_from_text_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            500usize => {
                let arg0 = dv_blob(&args, 0, "st_geomcoll_from_wkb")?;
                {
                    let __r = pg_ctor::st_geomcoll_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomcoll_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            501usize => {
                let arg0 = dv_blob(&args, 0, "st_geomcoll_from_wkb_srid")?;
                let arg1 = dv_i64(&args, 1, "st_geomcoll_from_wkb_srid")? as i32;
                {
                    let __r = pg_ctor::st_geomcoll_from_wkb_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomcoll_from_wkb_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            502usize => {
                let arg0 = dv_text(&args, 0, "st_geomcollfromtext")?;
                {
                    let __r = pg_ctor::st_geomcoll_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomcollfromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            503usize => {
                let arg0 = dv_blob(&args, 0, "st_geomcollfromwkb")?;
                {
                    let __r = pg_ctor::st_geomcoll_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomcollfromwkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            504usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_geomequal")?, "st_geomequal")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_geomequal")?, "st_geomequal")?;
                Ok(types::Duckvalue::Boolean(pg_op::op_equals_spatially(
                    &arg0, &arg1,
                )))
            }
            505usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_geometric_median")?,
                    "st_geometric_median",
                )?;
                {
                    let __r = pg_proc::st_geometric_median(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geometric_median: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            506usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_geometricmedian")?,
                    "st_geometricmedian",
                )?;
                {
                    let __r = pg_proc::st_geometric_median(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geometricmedian: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            507usize => {
                let arg0 = dv_text(&args, 0, "st_geometry_from_text")?;
                {
                    let __r = pg_ctor::st_geom_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geometry_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            508usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_geometry_n")?, "st_geometry_n")?;
                let arg1 = dv_i64(&args, 1, "st_geometry_n")? as u32;
                {
                    let __r = pg_acc::st_geometry_n(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geometry_n: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            509usize => {
                let arg0 = dv_blob(&args, 0, "st_geometry_n_threed")?;
                let arg1 = dv_i64(&args, 1, "st_geometry_n_threed")? as i32;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_geometry_n_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geometry_n_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            510usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_geometry_type")?, "st_geometry_type")?;
                Ok(types::Duckvalue::Text(
                    (pg_acc::st_geometry_type(&arg0)).into(),
                ))
            }
            511usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_geometry_type_batch")?;
                {
                    let __r = postgis_batch::st_geometry_type_batch(&arg0);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            512usize => {
                let arg0 = dv_blob(&args, 0, "st_geometry_type_threed")?;
                Ok(types::Duckvalue::Text(
                    (pg_sfcgal::st_geometry_type_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geometry_type_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            513usize => {
                let arg0 = dv_text(&args, 0, "st_geometryfromtext")?;
                {
                    let __r = pg_ctor::st_geom_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geometryfromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            514usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_geometryn")?, "st_geometryn")?;
                let arg1 = dv_i64(&args, 1, "st_geometryn")? as u32;
                {
                    let __r = pg_acc::st_geometry_n(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geometryn: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            515usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_geometrytype")?, "st_geometrytype")?;
                Ok(types::Duckvalue::Text(
                    (pg_acc::st_geometry_type(&arg0)).into(),
                ))
            }
            516usize => {
                let arg0 = dv_blob(&args, 0, "st_geomfromewkb")?;
                {
                    let __r = pg_ctor::st_geom_from_ewkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromewkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            517usize => {
                let arg0 = dv_text(&args, 0, "st_geomfromewkt")?;
                {
                    let __r = pg_ctor::st_geom_from_ewkt(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromewkt: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            518usize => {
                let arg0 = dv_blob(&args, 0, "st_geomfromgeobuf")?;
                {
                    let __r: Vec<Geometry> = pg_ctor::st_geom_from_geobuf(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromgeobuf: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(g) => Ok(types::Duckvalue::Blob(g.as_wkb().into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            519usize => {
                let arg0 = dv_text(&args, 0, "st_geomfromgeohash")?;
                {
                    let __r = pg_ctor::st_geom_from_geohash(arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromgeohash: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            520usize => {
                let arg0 = dv_text(&args, 0, "st_geomfromgeojson")?;
                {
                    let __r = pg_ctor::st_geom_from_geojson(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromgeojson: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            521usize => {
                let arg0 = dv_text(&args, 0, "st_geomfromgml")?;
                {
                    let __r = pg_ctor::st_geom_from_gml(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromgml: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            522usize => {
                let arg0 = dv_text(&args, 0, "st_geomfromgmlsrid")?;
                let arg1 = dv_i64(&args, 1, "st_geomfromgmlsrid")? as i32;
                {
                    let __r = pg_ctor::st_geom_from_gml_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromgmlsrid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            523usize => {
                let arg0 = dv_text(&args, 0, "st_geomfromhexewkb")?;
                {
                    let __r = pg_ctor::st_geom_from_hexewkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromhexewkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            524usize => {
                let arg0 = dv_text(&args, 0, "st_geomfromkml")?;
                {
                    let __r = pg_ctor::st_geom_from_kml(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromkml: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            525usize => {
                let arg0 = dv_text(&args, 0, "st_geomfrommarc21")?;
                {
                    let __r = pg_ctor::st_geom_from_marc21(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfrommarc21: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            526usize => {
                let arg0 = dv_text(&args, 0, "st_geomfromtext")?;
                {
                    let __r = pg_ctor::st_geom_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            527usize => {
                let arg0 = dv_text(&args, 0, "st_geomfromtextsrid")?;
                let arg1 = dv_i64(&args, 1, "st_geomfromtextsrid")? as i32;
                {
                    let __r = pg_ctor::st_geom_from_text_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromtextsrid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            528usize => {
                let arg0 = dv_blob(&args, 0, "st_geomfromtwkb")?;
                {
                    let __r = pg_ctor::st_geom_from_twkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromtwkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            529usize => {
                let arg0 = dv_blob(&args, 0, "st_geomfromwkb")?;
                {
                    let __r = pg_ctor::st_geom_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_geomfromwkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            530usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_get_edge_by_point")?,
                    "st_get_edge_by_point",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_get_edge_by_point")?,
                    "st_get_edge_by_point",
                )?;
                let arg2 = dv_f64(&args, 2, "st_get_edge_by_point")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_edge_by_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_get_edge_by_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            531usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_get_edge_geometry")?,
                    "st_get_edge_geometry",
                )?;
                let arg1 = dv_i64(&args, 1, "st_get_edge_geometry")? as u32;
                {
                    let __r = pg_topo_out::get_edge_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_get_edge_geometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            532usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_get_face_geometry")?,
                    "st_get_face_geometry",
                )?;
                let arg1 = dv_i64(&args, 1, "st_get_face_geometry")? as u32;
                {
                    let __r = pg_topo_out::get_face_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_get_face_geometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            533usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_get_node_by_point")?,
                    "st_get_node_by_point",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_get_node_by_point")?,
                    "st_get_node_by_point",
                )?;
                let arg2 = dv_f64(&args, 2, "st_get_node_by_point")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_node_by_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_get_node_by_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            534usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_get_node_geometry")?,
                    "st_get_node_geometry",
                )?;
                let arg1 = dv_i64(&args, 1, "st_get_node_geometry")? as u32;
                {
                    let __r = pg_topo_out::get_node_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_get_node_geometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            535usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_getedgebypoint")?,
                    "st_getedgebypoint",
                )?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_getedgebypoint")?, "st_getedgebypoint")?;
                let arg2 = dv_f64(&args, 2, "st_getedgebypoint")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_edge_by_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_getedgebypoint: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            536usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_getedgegeometry")?,
                    "st_getedgegeometry",
                )?;
                let arg1 = dv_i64(&args, 1, "st_getedgegeometry")? as u32;
                {
                    let __r = pg_topo_out::get_edge_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_getedgegeometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            537usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_getfacegeometry")?,
                    "st_getfacegeometry",
                )?;
                let arg1 = dv_i64(&args, 1, "st_getfacegeometry")? as u32;
                {
                    let __r = pg_topo_out::get_face_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_getfacegeometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            538usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_getnodebypoint")?,
                    "st_getnodebypoint",
                )?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_getnodebypoint")?, "st_getnodebypoint")?;
                let arg2 = dv_f64(&args, 2, "st_getnodebypoint")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_node_by_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_getnodebypoint: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            539usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_getnodegeometry")?,
                    "st_getnodegeometry",
                )?;
                let arg1 = dv_i64(&args, 1, "st_getnodegeometry")? as u32;
                {
                    let __r = pg_topo_out::get_node_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_getnodegeometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            540usize => {
                let arg0 = dv_text(&args, 0, "st_gml_to_sql")?;
                {
                    let __r = pg_ctor::st_gml_to_sql(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_gml_to_sql: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            541usize => {
                let arg0 = dv_text(&args, 0, "st_gmltosql")?;
                {
                    let __r = pg_ctor::st_gml_to_sql(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_gmltosql: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            542usize => {
                let arg0 = dv_blob(&args, 0, "st_greene_approx_convex_partition")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_greene_approx_convex_partition(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_greene_approx_convex_partition: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            543usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_has_arc")?, "st_has_arc")?;
                Ok(types::Duckvalue::Boolean(pg_proc::st_has_arc(&arg0)))
            }
            544usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_has_m")?, "st_has_m")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_has_m(&arg0)))
            }
            545usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_has_no_band")?, "st_has_no_band")?;
                let arg1 = dv_i64(&args, 1, "st_has_no_band")? as u32;
                Ok(types::Duckvalue::Boolean(pg_rast_acc::st_has_no_band(
                    &arg0, arg1,
                )))
            }
            546usize => {
                let arg0 = dv_blob(&args, 0, "st_has_validity_flag")?;
                Ok(types::Duckvalue::Boolean(pg_sfcgal::st_has_validity_flag(
                    arg0,
                )))
            }
            547usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_has_z")?, "st_has_z")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_has_z(&arg0)))
            }
            548usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_hasarc")?, "st_hasarc")?;
                Ok(types::Duckvalue::Boolean(pg_acc::st_has_arc(&arg0)))
            }
            549usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_hasm")?, "st_hasm")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_has_m(&arg0)))
            }
            550usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_hasnoband")?, "st_hasnoband")?;
                let arg1 = dv_i64(&args, 1, "st_hasnoband")? as u32;
                Ok(types::Duckvalue::Boolean(pg_rast_acc::st_has_no_band(
                    &arg0, arg1,
                )))
            }
            551usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_hasz")?, "st_hasz")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_has_z(&arg0)))
            }
            552usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_hausdorff_distance")?,
                    "st_hausdorff_distance",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_hausdorff_distance")?,
                    "st_hausdorff_distance",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_hausdorff_distance(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_hausdorff_distance: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            553usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_hausdorffdistance")?,
                    "st_hausdorffdistance",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_hausdorffdistance")?,
                    "st_hausdorffdistance",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_hausdorff_distance(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_hausdorffdistance: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            554usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_height")?, "st_height")?;
                Ok(types::Duckvalue::Int64(pg_rast_acc::st_height(&arg0) as i64))
            }
            555usize => {
                let arg0 = dv_f64(&args, 0, "st_hexagon")?;
                let arg1 = dv_f64(&args, 1, "st_hexagon")?;
                let arg2 = dv_f64(&args, 2, "st_hexagon")?;
                let arg3 = dv_i64(&args, 3, "st_hexagon")? as i32;
                let arg4 = dv_i64(&args, 4, "st_hexagon")? as i32;
                {
                    let __r = pg_ctor::st_hexagon(arg0, arg1, arg2, arg3, arg4).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_hexagon: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            556usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_hill_shade")?, "st_hill_shade")?;
                let arg1 = dv_i64(&args, 1, "st_hill_shade")? as u32;
                let arg2 = dv_f64(&args, 2, "st_hill_shade")?;
                let arg3 = dv_f64(&args, 3, "st_hill_shade")?;
                {
                    let __r =
                        pg_rast_proc::st_hill_shade(&arg0, arg1, arg2, arg3).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_hill_shade: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            557usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_hillshade")?, "st_hillshade")?;
                let arg1 = dv_i64(&args, 1, "st_hillshade")? as u32;
                let arg2 = dv_f64(&args, 2, "st_hillshade")?;
                let arg3 = dv_f64(&args, 3, "st_hillshade")?;
                {
                    let __r =
                        pg_rast_proc::st_hill_shade(&arg0, arg1, arg2, arg3).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_hillshade: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            558usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_histogram")?, "st_histogram")?;
                let arg1 = dv_i64(&args, 1, "st_histogram")? as u32;
                let arg2 = dv_i64(&args, 2, "st_histogram")? as u32;
                {
                    let __r = pg_rast_stats::st_histogram(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_histogram: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_histogram_bin(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            559usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_init_topo_geo")?,
                    "st_init_topo_geo",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_edit::st_init_topo_geo(&arg0)).into(),
                ))
            }
            560usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_interior_ring_n")?,
                    "st_interior_ring_n",
                )?;
                let arg1 = dv_i64(&args, 1, "st_interior_ring_n")? as u32;
                {
                    let __r = pg_acc::st_interior_ring_n(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_interior_ring_n: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            561usize => {
                let arg0 = dv_blob(&args, 0, "st_interior_ring_n_threed")?;
                let arg1 = dv_i64(&args, 1, "st_interior_ring_n_threed")? as i32;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_interior_ring_n_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_interior_ring_n_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            562usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_interiorringn")?, "st_interiorringn")?;
                let arg1 = dv_i64(&args, 1, "st_interiorringn")? as u32;
                {
                    let __r = pg_acc::st_interior_ring_n(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_interiorringn: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            563usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_interpolate_point")?,
                    "st_interpolate_point",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_interpolate_point")?,
                    "st_interpolate_point",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_lin::st_interpolate_point(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_interpolate_point: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            564usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_intersection")?, "st_intersection")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_intersection")?, "st_intersection")?;
                {
                    let __r = pg_proc::st_intersection(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_intersection: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            565usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_intersection_batch")?;
                let arg1: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 1, "st_intersection_batch")?;
                {
                    let __r = postgis_batch::st_intersection_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_intersection_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            566usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_intersection_gridsize")?,
                    "st_intersection_gridsize",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_intersection_gridsize")?,
                    "st_intersection_gridsize",
                )?;
                let arg2 = dv_f64(&args, 2, "st_intersection_gridsize")?;
                {
                    let __r =
                        pg_proc::st_intersection_gridsize(&arg0, &arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_intersection_gridsize: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            567usize => {
                let arg0 = dv_blob(&args, 0, "st_intersection_threed")?;
                let arg1 = dv_blob(&args, 1, "st_intersection_threed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_intersection_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_intersection_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            568usize => {
                let arg0 = dv_blob(&args, 0, "st_intersectionthreed")?;
                let arg1 = dv_blob(&args, 1, "st_intersectionthreed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_intersection_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_intersectionthreed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            569usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_intersects")?, "st_intersects")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_intersects")?, "st_intersects")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_intersects(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_intersects: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            570usize => {
                let arg0 = dv_blob(&args, 0, "st_intersects_3d")?;
                let arg1 = dv_blob(&args, 1, "st_intersects_3d")?;
                Ok(types::Duckvalue::Boolean(pg_sfcgal::st_intersects_threed(
                    arg0, arg1,
                )))
            }
            571usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_intersects_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_intersects_batch")?;
                {
                    let __r = postgis_batch::st_intersects_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_intersects_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            572usize => {
                let arg0 = dv_blob(&args, 0, "st_intersects_threed")?;
                let arg1 = dv_blob(&args, 1, "st_intersects_threed")?;
                Ok(types::Duckvalue::Boolean(pg_sfcgal::st_intersects_threed(
                    arg0, arg1,
                )))
            }
            573usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_inverse_transform_pipeline")?,
                    "st_inverse_transform_pipeline",
                )?;
                let arg1 = dv_text(&args, 1, "st_inverse_transform_pipeline")?;
                {
                    let __r =
                        pg_xform::st_inverse_transform_pipeline(&arg0, arg1).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_inverse_transform_pipeline: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            574usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_inversetransformpipeline")?,
                    "st_inversetransformpipeline",
                )?;
                let arg1 = dv_text(&args, 1, "st_inversetransformpipeline")?;
                {
                    let __r =
                        pg_xform::st_inverse_transform_pipeline(&arg0, arg1).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_inversetransformpipeline: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            575usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_is_closed")?, "st_is_closed")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_closed(&arg0)))
            }
            576usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_is_collection")?, "st_is_collection")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_collection(&arg0)))
            }
            577usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_is_empty")?, "st_is_empty")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_empty(&arg0)))
            }
            578usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_is_empty_batch")?;
                {
                    let __r = postgis_batch::st_is_empty_batch(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_is_empty_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            579usize => {
                let arg0 = dv_blob(&args, 0, "st_is_empty_threed")?;
                Ok(types::Duckvalue::Boolean(
                    pg_sfcgal::st_is_empty_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_is_empty_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            580usize => {
                let arg0 = dv_blob(&args, 0, "st_is_measured")?;
                Ok(types::Duckvalue::Boolean(
                    pg_sfcgal::st_is_measured(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_is_measured: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            581usize => {
                let arg0 = dv_blob(&args, 0, "st_is_planar")?;
                Ok(types::Duckvalue::Boolean(
                    pg_sfcgal::st_is_planar(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_is_planar: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            582usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_is_polygon_ccw")?, "st_is_polygon_ccw")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_is_polygon_ccw(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_is_polygon_ccw: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            583usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_is_polygon_cw")?, "st_is_polygon_cw")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_is_polygon_cw(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_is_polygon_cw: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            584usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_is_ring")?, "st_is_ring")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_ring(&arg0)))
            }
            585usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_is_simple")?, "st_is_simple")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_simple(&arg0)))
            }
            586usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_is_simple_batch")?;
                {
                    let __r = postgis_batch::st_is_simple_batch(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_is_simple_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            587usize => {
                let arg0 = dv_blob(&args, 0, "st_is_solid")?;
                Ok(types::Duckvalue::Boolean(
                    pg_sfcgal::st_is_solid(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_is_solid: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            588usize => {
                let arg0 = dv_blob(&args, 0, "st_is_threed")?;
                Ok(types::Duckvalue::Boolean(
                    pg_sfcgal::st_is_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_is_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            589usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_is_valid")?, "st_is_valid")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_valid(&arg0)))
            }
            590usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_is_valid_batch")?;
                {
                    let __r = postgis_batch::st_is_valid_batch(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_is_valid_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            591usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_is_valid_detail")?,
                    "st_is_valid_detail",
                )?;
                {
                    let (__valid, __reason, __loc) = pg_pred::st_is_valid_detail(&arg0);
                    let __reason_s = __reason.unwrap_or_default();
                    let __loc_s = match __loc {
                        Some(g) => pg_out::st_as_text(&g),
                        None => alloc::string::String::new(),
                    };
                    Ok(types::Duckvalue::Text(format!(
                        "({},\"{}\",\"{}\")",
                        __valid, __reason_s, __loc_s
                    )))
                }
            }
            592usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_is_valid_detail_flags")?,
                    "st_is_valid_detail_flags",
                )?;
                let arg1 = dv_i64(&args, 1, "st_is_valid_detail_flags")? as u32;
                {
                    let (__valid, __reason, __loc) = pg_pred::st_is_valid_detail_flags(&arg0, arg1);
                    let __reason_s = __reason.unwrap_or_default();
                    let __loc_s = match __loc {
                        Some(g) => pg_out::st_as_text(&g),
                        None => alloc::string::String::new(),
                    };
                    Ok(types::Duckvalue::Text(format!(
                        "({},\"{}\",\"{}\")",
                        __valid, __reason_s, __loc_s
                    )))
                }
            }
            593usize => {
                let arg0 = dv_blob(&args, 0, "st_is_valid_detail_threed")?;
                Ok(types::Duckvalue::Text(
                    (pg_sfcgal::st_is_valid_detail_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_is_valid_detail_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            594usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_is_valid_flags")?, "st_is_valid_flags")?;
                let arg1 = dv_i64(&args, 1, "st_is_valid_flags")? as u32;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_valid_flags(
                    &arg0, arg1,
                )))
            }
            595usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_is_valid_reason")?,
                    "st_is_valid_reason",
                )?;
                Ok(match pg_pred::st_is_valid_reason(&arg0) {
                    Some(v) => types::Duckvalue::Text(v.into()),
                    None => types::Duckvalue::Null,
                })
            }
            596usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_is_valid_reason_flags")?,
                    "st_is_valid_reason_flags",
                )?;
                let arg1 = dv_i64(&args, 1, "st_is_valid_reason_flags")? as u32;
                Ok(match pg_pred::st_is_valid_reason_flags(&arg0, arg1) {
                    Some(v) => types::Duckvalue::Text(v.into()),
                    None => types::Duckvalue::Null,
                })
            }
            597usize => {
                let arg0 = dv_blob(&args, 0, "st_is_valid_threed")?;
                Ok(types::Duckvalue::Boolean(
                    pg_sfcgal::st_is_valid_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_is_valid_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            598usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_is_valid_trajectory")?,
                    "st_is_valid_trajectory",
                )?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_is_valid_trajectory(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_is_valid_trajectory: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            599usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_isclosed")?, "st_isclosed")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_closed(&arg0)))
            }
            600usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_iscollection")?, "st_iscollection")?;
                Ok(types::Duckvalue::Boolean(pg_acc::st_is_collection(&arg0)))
            }
            601usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_isempty")?, "st_isempty")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_empty(&arg0)))
            }
            602usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_ispolygonccw")?, "st_ispolygonccw")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_is_polygon_ccw(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_ispolygonccw: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            603usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_ispolygoncw")?, "st_ispolygoncw")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_is_polygon_cw(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_ispolygoncw: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            604usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_isring")?, "st_isring")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_ring(&arg0)))
            }
            605usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_issimple")?, "st_issimple")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_simple(&arg0)))
            }
            606usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_isvalid")?, "st_isvalid")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_is_valid(&arg0)))
            }
            607usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_isvaliddetail")?, "st_isvaliddetail")?;
                {
                    let (__valid, __reason, __loc) = pg_pred::st_is_valid_detail(&arg0);
                    let __reason_s = __reason.unwrap_or_default();
                    let __loc_s = match __loc {
                        Some(g) => pg_out::st_as_text(&g),
                        None => alloc::string::String::new(),
                    };
                    Ok(types::Duckvalue::Text(format!(
                        "({},\"{}\",\"{}\")",
                        __valid, __reason_s, __loc_s
                    )))
                }
            }
            608usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_isvalidreason")?, "st_isvalidreason")?;
                Ok(match pg_pred::st_is_valid_reason(&arg0) {
                    Some(v) => types::Duckvalue::Text(v.into()),
                    None => types::Duckvalue::Null,
                })
            }
            609usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_isvalidtrajectory")?,
                    "st_isvalidtrajectory",
                )?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_is_valid_trajectory(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_isvalidtrajectory: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            610usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_knndistance")?, "st_knndistance")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_knndistance")?, "st_knndistance")?;
                Ok(types::Duckvalue::Float64(pg_op::op_knn_distance(
                    &arg0, &arg1,
                )))
            }
            611usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_largest_empty_circle")?,
                    "st_largest_empty_circle",
                )?;
                let arg1 = dv_f64(&args, 1, "st_largest_empty_circle")?;
                {
                    let __r = pg_proc::st_largest_empty_circle(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_largest_empty_circle: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            612usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_largestemptycircle")?,
                    "st_largestemptycircle",
                )?;
                let arg1 = dv_f64(&args, 1, "st_largestemptycircle")?;
                {
                    let __r = pg_proc::st_largest_empty_circle(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_largestemptycircle: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            613usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_length")?, "st_length")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_length(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_length: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            614usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_length2d")?, "st_length2d")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_length_twod(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_length2d: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            615usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_length_2d")?, "st_length_2d")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_length_twod(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_length_2d: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            616usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_length_batch")?;
                {
                    let __r = postgis_batch::st_length_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            617usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_length_spheroid")?,
                    "st_length_spheroid",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_length_spheroid(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_length_spheroid: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            618usize => {
                let arg0 = dv_blob(&args, 0, "st_length_threed")?;
                Ok(types::Duckvalue::Float64(pg_sfcgal::st_length_threed(arg0)))
            }
            619usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_length_twod")?, "st_length_twod")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_length_twod(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_length_twod: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            620usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_length_twod_spheroid")?,
                    "st_length_twod_spheroid",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_length_twod_spheroid(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_length_twod_spheroid: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            621usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_lengthspheroid")?, "st_lengthspheroid")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_length_spheroid(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_lengthspheroid: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            622usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_lengththreed")?, "st_lengththreed")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_length_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_lengththreed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            623usize => {
                let arg0 = dv_blob(&args, 0, "st_letters")?;
                let arg1 = dv_text(&args, 1, "st_letters")?;
                {
                    let __r = pg_ctor::st_letters(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_letters: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            624usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_line_crossing_direction")?,
                    "st_line_crossing_direction",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_line_crossing_direction")?,
                    "st_line_crossing_direction",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_lin::st_line_crossing_direction(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_crossing_direction: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            625usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_line_extend")?, "st_line_extend")?;
                let arg1 = dv_f64(&args, 1, "st_line_extend")?;
                let arg2 = dv_f64(&args, 2, "st_line_extend")?;
                {
                    let __r = pg_lin::st_line_extend(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_extend: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            626usize => {
                let arg0 = dv_text(&args, 0, "st_line_from_encoded_polyline")?;
                {
                    let __r = pg_ctor::st_line_from_encoded_polyline(arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_from_encoded_polyline: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            627usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_line_from_multi_point")?,
                    "st_line_from_multi_point",
                )?;
                {
                    let __r = pg_ctor::st_line_from_multi_point(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_from_multi_point: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            628usize => {
                let arg0 = dv_text(&args, 0, "st_line_from_text")?;
                {
                    let __r = pg_ctor::st_line_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            629usize => {
                let arg0 = dv_text(&args, 0, "st_line_from_text_srid")?;
                let arg1 = dv_i64(&args, 1, "st_line_from_text_srid")? as i32;
                {
                    let __r = pg_ctor::st_line_from_text_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_from_text_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            630usize => {
                let arg0 = dv_blob(&args, 0, "st_line_from_wkb")?;
                {
                    let __r = pg_ctor::st_line_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            631usize => {
                let arg0 = dv_blob(&args, 0, "st_line_from_wkb_srid")?;
                let arg1 = dv_i64(&args, 1, "st_line_from_wkb_srid")? as i32;
                {
                    let __r = pg_ctor::st_line_from_wkb_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_from_wkb_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            632usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_line_interpolate_point")?,
                    "st_line_interpolate_point",
                )?;
                let arg1 = dv_f64(&args, 1, "st_line_interpolate_point")?;
                {
                    let __r = pg_lin::st_line_interpolate_point(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_interpolate_point: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            633usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_line_interpolate_point_3d")?,
                    "st_line_interpolate_point_3d",
                )?;
                let arg1 = dv_f64(&args, 1, "st_line_interpolate_point_3d")?;
                {
                    let __r =
                        pg_lin::st_line_interpolate_point_threed(&arg0, arg1).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_line_interpolate_point_3d: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            634usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_line_interpolate_point_threed")?,
                    "st_line_interpolate_point_threed",
                )?;
                let arg1 = dv_f64(&args, 1, "st_line_interpolate_point_threed")?;
                {
                    let __r =
                        pg_lin::st_line_interpolate_point_threed(&arg0, arg1).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_line_interpolate_point_threed: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            635usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_line_interpolate_points")?,
                    "st_line_interpolate_points",
                )?;
                let arg1 = dv_f64(&args, 1, "st_line_interpolate_points")?;
                let arg2 = dv_bool(&args, 2, "st_line_interpolate_points")?;
                {
                    let __r =
                        pg_lin::st_line_interpolate_points(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_line_interpolate_points: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            636usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_line_locate_point")?,
                    "st_line_locate_point",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_line_locate_point")?,
                    "st_line_locate_point",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_lin::st_line_locate_point(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_locate_point: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            637usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_line_merge")?, "st_line_merge")?;
                {
                    let __r = pg_proc::st_line_merge(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_merge: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            638usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_line_merge_directed")?,
                    "st_line_merge_directed",
                )?;
                let arg1 = dv_bool(&args, 1, "st_line_merge_directed")?;
                {
                    let __r = pg_proc::st_line_merge_directed(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_merge_directed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            639usize => {
                let arg0 = dv_blob(&args, 0, "st_line_substring")?;
                let arg1 = dv_f64(&args, 1, "st_line_substring")?;
                let arg2 = dv_f64(&args, 2, "st_line_substring")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_line_substring(arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_substring: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            640usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_line_to_curve")?, "st_line_to_curve")?;
                {
                    let __r = pg_proc::st_line_to_curve(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_line_to_curve: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            641usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_linecrossingdirection")?,
                    "st_linecrossingdirection",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_linecrossingdirection")?,
                    "st_linecrossingdirection",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_lin::st_line_crossing_direction(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_linecrossingdirection: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            642usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_lineextend")?, "st_lineextend")?;
                let arg1 = dv_f64(&args, 1, "st_lineextend")?;
                let arg2 = dv_f64(&args, 2, "st_lineextend")?;
                {
                    let __r = pg_lin::st_line_extend(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_lineextend: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            643usize => {
                let arg0 = dv_text(&args, 0, "st_linefromencodedpolyline")?;
                {
                    let __r = pg_ctor::st_line_from_encoded_polyline(arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_linefromencodedpolyline: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            644usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_linefrommultipoint")?,
                    "st_linefrommultipoint",
                )?;
                {
                    let __r = pg_ctor::st_line_from_multi_point(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_linefrommultipoint: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            645usize => {
                let arg0 = dv_text(&args, 0, "st_linefromtext")?;
                {
                    let __r = pg_ctor::st_line_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_linefromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            646usize => {
                let arg0 = dv_blob(&args, 0, "st_linefromwkb")?;
                {
                    let __r = pg_ctor::st_line_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_linefromwkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            647usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_lineinterpolatepoint")?,
                    "st_lineinterpolatepoint",
                )?;
                let arg1 = dv_f64(&args, 1, "st_lineinterpolatepoint")?;
                {
                    let __r = pg_lin::st_line_interpolate_point(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_lineinterpolatepoint: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            648usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_lineinterpolatepoints")?,
                    "st_lineinterpolatepoints",
                )?;
                let arg1 = dv_f64(&args, 1, "st_lineinterpolatepoints")?;
                let arg2 = dv_bool(&args, 2, "st_lineinterpolatepoints")?;
                {
                    let __r =
                        pg_lin::st_line_interpolate_points(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_lineinterpolatepoints: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            649usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_linelocatepoint")?,
                    "st_linelocatepoint",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_linelocatepoint")?,
                    "st_linelocatepoint",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_lin::st_line_locate_point(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_linelocatepoint: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            650usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_linemerge")?, "st_linemerge")?;
                {
                    let __r = pg_proc::st_line_merge(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_linemerge: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            651usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_linesubstring")?, "st_linesubstring")?;
                let arg1 = dv_f64(&args, 1, "st_linesubstring")?;
                let arg2 = dv_f64(&args, 2, "st_linesubstring")?;
                {
                    let __r = pg_lin::st_line_substring(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_linesubstring: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            652usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_linetocurve")?, "st_linetocurve")?;
                {
                    let __r = pg_proc::st_line_to_curve(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_linetocurve: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            653usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_locate_along")?, "st_locate_along")?;
                let arg1 = dv_f64(&args, 1, "st_locate_along")?;
                {
                    let __r = pg_lin::st_locate_along(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_locate_along: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            654usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_locate_between")?, "st_locate_between")?;
                let arg1 = dv_f64(&args, 1, "st_locate_between")?;
                let arg2 = dv_f64(&args, 2, "st_locate_between")?;
                {
                    let __r = pg_lin::st_locate_between(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_locate_between: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            655usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_locate_between_elevations")?,
                    "st_locate_between_elevations",
                )?;
                let arg1 = dv_f64(&args, 1, "st_locate_between_elevations")?;
                let arg2 = dv_f64(&args, 2, "st_locate_between_elevations")?;
                {
                    let __r =
                        pg_lin::st_locate_between_elevations(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_locate_between_elevations: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            656usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_locatealong")?, "st_locatealong")?;
                let arg1 = dv_f64(&args, 1, "st_locatealong")?;
                {
                    let __r = pg_lin::st_locate_along(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_locatealong: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            657usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_locatebetween")?, "st_locatebetween")?;
                let arg1 = dv_f64(&args, 1, "st_locatebetween")?;
                let arg2 = dv_f64(&args, 2, "st_locatebetween")?;
                {
                    let __r = pg_lin::st_locate_between(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_locatebetween: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            658usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_locatebetweenelevations")?,
                    "st_locatebetweenelevations",
                )?;
                let arg1 = dv_f64(&args, 1, "st_locatebetweenelevations")?;
                let arg2 = dv_f64(&args, 2, "st_locatebetweenelevations")?;
                {
                    let __r =
                        pg_lin::st_locate_between_elevations(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_locatebetweenelevations: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            659usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_longest_line")?, "st_longest_line")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_longest_line")?, "st_longest_line")?;
                {
                    let __r = pg_meas::st_longest_line(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_longest_line: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            660usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_longest_line_3d")?,
                    "st_longest_line_3d",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_longest_line_3d")?,
                    "st_longest_line_3d",
                )?;
                {
                    let __r = pg_meas::st_longest_line_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_longest_line_3d: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            661usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_longest_line_threed")?,
                    "st_longest_line_threed",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_longest_line_threed")?,
                    "st_longest_line_threed",
                )?;
                {
                    let __r = pg_meas::st_longest_line_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_longest_line_threed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            662usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_longestline")?, "st_longestline")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_longestline")?, "st_longestline")?;
                {
                    let __r = pg_meas::st_longest_line(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_longestline: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            663usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_m")?, "st_m")?;
                Ok(
                    match pg_acc::st_m(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!("st_m: {}", shim_err_string(e)))
                    })? {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            664usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_m_batch")?;
                {
                    let __r = postgis_batch::st_m_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            665usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_make_box2d")?, "st_make_box2d")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_make_box2d")?, "st_make_box2d")?;
                {
                    let __bb = pg_ctor::st_make_box2d(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_box2d: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let __env =
                        pg_ctor::st_make_envelope(__bb.min_x, __bb.min_y, __bb.max_x, __bb.max_y);
                    Ok(types::Duckvalue::Blob(__env.as_wkb().into()))
                }
            }
            666usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_make_box3d")?, "st_make_box3d")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_make_box3d")?, "st_make_box3d")?;
                {
                    let __r = pg_ctor::st_make_box3d(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_box3d: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            667usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_make_box_threed")?,
                    "st_make_box_threed",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_make_box_threed")?,
                    "st_make_box_threed",
                )?;
                {
                    let __r = pg_threed::st_make_box_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_box_threed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            668usize => {
                let arg0 = dv_i64(&args, 0, "st_make_empty_coverage")? as u32;
                let arg1 = dv_i64(&args, 1, "st_make_empty_coverage")? as u32;
                let arg2 = dv_i64(&args, 2, "st_make_empty_coverage")? as u32;
                let arg3 = dv_i64(&args, 3, "st_make_empty_coverage")? as u32;
                let arg4 = dv_f64(&args, 4, "st_make_empty_coverage")?;
                let arg5 = dv_f64(&args, 5, "st_make_empty_coverage")?;
                let arg6 = dv_f64(&args, 6, "st_make_empty_coverage")?;
                let arg7 = dv_f64(&args, 7, "st_make_empty_coverage")?;
                let arg8 = dv_f64(&args, 8, "st_make_empty_coverage")?;
                let arg9 = dv_f64(&args, 9, "st_make_empty_coverage")?;
                let arg10 = dv_i64(&args, 10, "st_make_empty_coverage")? as i32;
                {
                    let __r = pg_rast_ctor::st_make_empty_coverage(
                        arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7, arg8, arg9, arg10,
                    )
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_empty_coverage: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.into_iter().next() {
                        Some(r) => Ok(types::Duckvalue::Blob(r.as_binary().into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            669usize => {
                let arg0 = dv_i64(&args, 0, "st_make_empty_raster")? as u32;
                let arg1 = dv_i64(&args, 1, "st_make_empty_raster")? as u32;
                let arg2 = dv_f64(&args, 2, "st_make_empty_raster")?;
                let arg3 = dv_f64(&args, 3, "st_make_empty_raster")?;
                let arg4 = dv_f64(&args, 4, "st_make_empty_raster")?;
                let arg5 = dv_f64(&args, 5, "st_make_empty_raster")?;
                let arg6 = dv_f64(&args, 6, "st_make_empty_raster")?;
                let arg7 = dv_f64(&args, 7, "st_make_empty_raster")?;
                let arg8 = dv_i64(&args, 8, "st_make_empty_raster")? as i32;
                {
                    let __r = pg_rast_ctor::st_make_empty_raster(
                        arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7, arg8,
                    )
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_empty_raster: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            670usize => {
                let arg0 = dv_f64(&args, 0, "st_make_envelope")?;
                let arg1 = dv_f64(&args, 1, "st_make_envelope")?;
                let arg2 = dv_f64(&args, 2, "st_make_envelope")?;
                let arg3 = dv_f64(&args, 3, "st_make_envelope")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_envelope(arg0, arg1, arg2, arg3)
                        .as_wkb()
                        .into(),
                ))
            }
            671usize => {
                let arg0 = dv_f64(&args, 0, "st_make_envelope_srid")?;
                let arg1 = dv_f64(&args, 1, "st_make_envelope_srid")?;
                let arg2 = dv_f64(&args, 2, "st_make_envelope_srid")?;
                let arg3 = dv_f64(&args, 3, "st_make_envelope_srid")?;
                let arg4 = dv_i64(&args, 4, "st_make_envelope_srid")? as i32;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_envelope_srid(arg0, arg1, arg2, arg3, arg4)
                        .as_wkb()
                        .into(),
                ))
            }
            672usize => {
                let arg0_owned: Vec<Geometry> = args[0..]
                    .iter()
                    .enumerate()
                    .map(|(j, v)| match v {
                        types::Duckvalue::Blob(b) => {
                            Geometry::from_wkb(b.as_slice()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_make_line: arg {}: {}",
                                    0 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        types::Duckvalue::Text(t) => {
                            Geometry::from_wkb(t.as_bytes()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_make_line: arg {}: {}",
                                    0 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        _ => Err(types::Duckerror::Invalidargument(format!(
                            "st_make_line: arg {} must be BLOB",
                            0 + j
                        ))),
                    })
                    .collect::<Result<Vec<_>, _>>()?;
                let arg0: Vec<&Geometry> = arg0_owned.iter().collect();
                {
                    let __r = pg_ctor::st_make_line(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_line: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            673usize => {
                let arg0_owned: Vec<Geometry> = args[0..]
                    .iter()
                    .enumerate()
                    .map(|(j, v)| match v {
                        types::Duckvalue::Blob(b) => {
                            Geometry::from_wkb(b.as_slice()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_make_line_threed: arg {}: {}",
                                    0 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        types::Duckvalue::Text(t) => {
                            Geometry::from_wkb(t.as_bytes()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_make_line_threed: arg {}: {}",
                                    0 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        _ => Err(types::Duckerror::Invalidargument(format!(
                            "st_make_line_threed: arg {} must be BLOB",
                            0 + j
                        ))),
                    })
                    .collect::<Result<Vec<_>, _>>()?;
                let arg0: Vec<&Geometry> = arg0_owned.iter().collect();
                {
                    let __r = pg_threed::st_make_line_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_line_threed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            674usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_make_line_two")?, "st_make_line_two")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_make_line_two")?, "st_make_line_two")?;
                {
                    let __r = pg_ctor::st_make_line_two(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_line_two: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            675usize => {
                let arg0 = dv_f64(&args, 0, "st_make_point")?;
                let arg1 = dv_f64(&args, 1, "st_make_point")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_point(arg0, arg1).as_wkb().into(),
                ))
            }
            676usize => {
                let arg0 = dv_f64(&args, 0, "st_make_point_m")?;
                let arg1 = dv_f64(&args, 1, "st_make_point_m")?;
                let arg2 = dv_f64(&args, 2, "st_make_point_m")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_point_m(arg0, arg1, arg2).as_wkb().into(),
                ))
            }
            677usize => {
                let arg0 = dv_f64(&args, 0, "st_make_point_z")?;
                let arg1 = dv_f64(&args, 1, "st_make_point_z")?;
                let arg2 = dv_f64(&args, 2, "st_make_point_z")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_point_z(arg0, arg1, arg2).as_wkb().into(),
                ))
            }
            678usize => {
                let arg0 = dv_f64(&args, 0, "st_make_point_zm")?;
                let arg1 = dv_f64(&args, 1, "st_make_point_zm")?;
                let arg2 = dv_f64(&args, 2, "st_make_point_zm")?;
                let arg3 = dv_f64(&args, 3, "st_make_point_zm")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_point_zm(arg0, arg1, arg2, arg3)
                        .as_wkb()
                        .into(),
                ))
            }
            679usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_make_polygon")?, "st_make_polygon")?;
                let arg1_owned: Vec<Geometry> = args[1..]
                    .iter()
                    .enumerate()
                    .map(|(j, v)| match v {
                        types::Duckvalue::Blob(b) => {
                            Geometry::from_wkb(b.as_slice()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_make_polygon: arg {}: {}",
                                    1 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        types::Duckvalue::Text(t) => {
                            Geometry::from_wkb(t.as_bytes()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_make_polygon: arg {}: {}",
                                    1 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        _ => Err(types::Duckerror::Invalidargument(format!(
                            "st_make_polygon: arg {} must be BLOB",
                            1 + j
                        ))),
                    })
                    .collect::<Result<Vec<_>, _>>()?;
                let arg1: Vec<&Geometry> = arg1_owned.iter().collect();
                {
                    let __r = pg_ctor::st_make_polygon(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_polygon: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            680usize => {
                let arg0 = dv_blob(&args, 0, "st_make_solid")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_make_solid(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_solid: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            681usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_make_valid")?, "st_make_valid")?;
                {
                    let __r = pg_proc::st_make_valid(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_make_valid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            682usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_makebox")?, "st_makebox")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_makebox")?, "st_makebox")?;
                {
                    let __bb = pg_ctor::st_make_box2d(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_makebox: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let __env =
                        pg_ctor::st_make_envelope(__bb.min_x, __bb.min_y, __bb.max_x, __bb.max_y);
                    Ok(types::Duckvalue::Blob(__env.as_wkb().into()))
                }
            }
            683usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_makebox2d")?, "st_makebox2d")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_makebox2d")?, "st_makebox2d")?;
                {
                    let __bb = pg_ctor::st_make_box2d(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_makebox2d: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let __env =
                        pg_ctor::st_make_envelope(__bb.min_x, __bb.min_y, __bb.max_x, __bb.max_y);
                    Ok(types::Duckvalue::Blob(__env.as_wkb().into()))
                }
            }
            684usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_makebox3d")?, "st_makebox3d")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_makebox3d")?, "st_makebox3d")?;
                {
                    let __r = pg_ctor::st_make_box3d(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_makebox3d: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            685usize => {
                let arg0 = dv_i64(&args, 0, "st_makeemptyraster")? as u32;
                let arg1 = dv_i64(&args, 1, "st_makeemptyraster")? as u32;
                let arg2 = dv_f64(&args, 2, "st_makeemptyraster")?;
                let arg3 = dv_f64(&args, 3, "st_makeemptyraster")?;
                let arg4 = dv_f64(&args, 4, "st_makeemptyraster")?;
                let arg5 = dv_f64(&args, 5, "st_makeemptyraster")?;
                let arg6 = dv_f64(&args, 6, "st_makeemptyraster")?;
                let arg7 = dv_f64(&args, 7, "st_makeemptyraster")?;
                let arg8 = dv_i64(&args, 8, "st_makeemptyraster")? as i32;
                {
                    let __r = pg_rast_ctor::st_make_empty_raster(
                        arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7, arg8,
                    )
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_makeemptyraster: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            686usize => {
                let arg0 = dv_f64(&args, 0, "st_makeenvelope")?;
                let arg1 = dv_f64(&args, 1, "st_makeenvelope")?;
                let arg2 = dv_f64(&args, 2, "st_makeenvelope")?;
                let arg3 = dv_f64(&args, 3, "st_makeenvelope")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_envelope(arg0, arg1, arg2, arg3)
                        .as_wkb()
                        .into(),
                ))
            }
            687usize => {
                let arg0 = dv_f64(&args, 0, "st_makeenvelopesrid")?;
                let arg1 = dv_f64(&args, 1, "st_makeenvelopesrid")?;
                let arg2 = dv_f64(&args, 2, "st_makeenvelopesrid")?;
                let arg3 = dv_f64(&args, 3, "st_makeenvelopesrid")?;
                let arg4 = dv_i64(&args, 4, "st_makeenvelopesrid")? as i32;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_envelope_srid(arg0, arg1, arg2, arg3, arg4)
                        .as_wkb()
                        .into(),
                ))
            }
            688usize => {
                let arg0 = dv_f64(&args, 0, "st_makepoint")?;
                let arg1 = dv_f64(&args, 1, "st_makepoint")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_point(arg0, arg1).as_wkb().into(),
                ))
            }
            689usize => {
                let arg0 = dv_f64(&args, 0, "st_makepointm")?;
                let arg1 = dv_f64(&args, 1, "st_makepointm")?;
                let arg2 = dv_f64(&args, 2, "st_makepointm")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_point_m(arg0, arg1, arg2).as_wkb().into(),
                ))
            }
            690usize => {
                let arg0 = dv_f64(&args, 0, "st_makepointz")?;
                let arg1 = dv_f64(&args, 1, "st_makepointz")?;
                let arg2 = dv_f64(&args, 2, "st_makepointz")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_point_z(arg0, arg1, arg2).as_wkb().into(),
                ))
            }
            691usize => {
                let arg0 = dv_f64(&args, 0, "st_makepointzm")?;
                let arg1 = dv_f64(&args, 1, "st_makepointzm")?;
                let arg2 = dv_f64(&args, 2, "st_makepointzm")?;
                let arg3 = dv_f64(&args, 3, "st_makepointzm")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_make_point_zm(arg0, arg1, arg2, arg3)
                        .as_wkb()
                        .into(),
                ))
            }
            692usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_makepolygon")?, "st_makepolygon")?;
                let arg1_owned: Vec<Geometry> = args[1..]
                    .iter()
                    .enumerate()
                    .map(|(j, v)| match v {
                        types::Duckvalue::Blob(b) => {
                            Geometry::from_wkb(b.as_slice()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_makepolygon: arg {}: {}",
                                    1 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        types::Duckvalue::Text(t) => {
                            Geometry::from_wkb(t.as_bytes()).map_err(|e| {
                                types::Duckerror::Invalidargument(format!(
                                    "st_makepolygon: arg {}: {}",
                                    1 + j,
                                    postgis_err_string(e)
                                ))
                            })
                        }
                        _ => Err(types::Duckerror::Invalidargument(format!(
                            "st_makepolygon: arg {} must be BLOB",
                            1 + j
                        ))),
                    })
                    .collect::<Result<Vec<_>, _>>()?;
                let arg1: Vec<&Geometry> = arg1_owned.iter().collect();
                {
                    let __r = pg_ctor::st_make_polygon(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_makepolygon: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            693usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_makevalid")?, "st_makevalid")?;
                {
                    let __r = pg_proc::st_make_valid(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_makevalid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            694usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_map_algebra")?, "st_map_algebra")?;
                let arg1 = dv_i64(&args, 1, "st_map_algebra")? as u32;
                let arg2 = match dv_i64(&args, 2, "st_map_algebra")? {
                    0 => pg_rast_types::PixelType::Bool1,
                    1 => pg_rast_types::PixelType::Uint8,
                    2 => pg_rast_types::PixelType::Int8,
                    3 => pg_rast_types::PixelType::Uint16,
                    4 => pg_rast_types::PixelType::Int16,
                    5 => pg_rast_types::PixelType::Uint32,
                    6 => pg_rast_types::PixelType::Int32,
                    7 => pg_rast_types::PixelType::Float32,
                    8 => pg_rast_types::PixelType::Float64,
                    other => {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_map_algebra: arg 2 (pixel-type) out of range: {} (valid 0..9)",
                            other,
                        )))
                    }
                };
                let arg3 = dv_text(&args, 3, "st_map_algebra")?;
                {
                    let __r = pg_rast_ma::st_map_algebra(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_map_algebra: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            695usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_map_algebra2")?, "st_map_algebra2")?;
                let arg1 = dv_i64(&args, 1, "st_map_algebra2")? as u32;
                let arg2 =
                    from_raster_binary(dv_blob(&args, 2, "st_map_algebra2")?, "st_map_algebra2")?;
                let arg3 = dv_i64(&args, 3, "st_map_algebra2")? as u32;
                let arg4 = match dv_i64(&args, 4, "st_map_algebra2")? {
                    0 => pg_rast_types::PixelType::Bool1,
                    1 => pg_rast_types::PixelType::Uint8,
                    2 => pg_rast_types::PixelType::Int8,
                    3 => pg_rast_types::PixelType::Uint16,
                    4 => pg_rast_types::PixelType::Int16,
                    5 => pg_rast_types::PixelType::Uint32,
                    6 => pg_rast_types::PixelType::Int32,
                    7 => pg_rast_types::PixelType::Float32,
                    8 => pg_rast_types::PixelType::Float64,
                    other => {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_map_algebra2: arg 4 (pixel-type) out of range: {} (valid 0..9)",
                            other,
                        )))
                    }
                };
                let arg5 = dv_text(&args, 5, "st_map_algebra2")?;
                {
                    let __r = pg_rast_ma::st_map_algebra2(&arg0, arg1, &arg2, arg3, arg4, arg5)
                        .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_map_algebra2: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            696usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_mapalgebra")?, "st_mapalgebra")?;
                let arg1 = dv_i64(&args, 1, "st_mapalgebra")? as u32;
                let arg2 = match dv_i64(&args, 2, "st_mapalgebra")? {
                    0 => pg_rast_types::PixelType::Bool1,
                    1 => pg_rast_types::PixelType::Uint8,
                    2 => pg_rast_types::PixelType::Int8,
                    3 => pg_rast_types::PixelType::Uint16,
                    4 => pg_rast_types::PixelType::Int16,
                    5 => pg_rast_types::PixelType::Uint32,
                    6 => pg_rast_types::PixelType::Int32,
                    7 => pg_rast_types::PixelType::Float32,
                    8 => pg_rast_types::PixelType::Float64,
                    other => {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_mapalgebra: arg 2 (pixel-type) out of range: {} (valid 0..9)",
                            other,
                        )))
                    }
                };
                let arg3 = dv_text(&args, 3, "st_mapalgebra")?;
                {
                    let __r = pg_rast_ma::st_map_algebra(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mapalgebra: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            697usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_mapalgebra2")?, "st_mapalgebra2")?;
                let arg1 = dv_i64(&args, 1, "st_mapalgebra2")? as u32;
                let arg2 =
                    from_raster_binary(dv_blob(&args, 2, "st_mapalgebra2")?, "st_mapalgebra2")?;
                let arg3 = dv_i64(&args, 3, "st_mapalgebra2")? as u32;
                let arg4 = match dv_i64(&args, 4, "st_mapalgebra2")? {
                    0 => pg_rast_types::PixelType::Bool1,
                    1 => pg_rast_types::PixelType::Uint8,
                    2 => pg_rast_types::PixelType::Int8,
                    3 => pg_rast_types::PixelType::Uint16,
                    4 => pg_rast_types::PixelType::Int16,
                    5 => pg_rast_types::PixelType::Uint32,
                    6 => pg_rast_types::PixelType::Int32,
                    7 => pg_rast_types::PixelType::Float32,
                    8 => pg_rast_types::PixelType::Float64,
                    other => {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_mapalgebra2: arg 4 (pixel-type) out of range: {} (valid 0..9)",
                            other,
                        )))
                    }
                };
                let arg5 = dv_text(&args, 5, "st_mapalgebra2")?;
                {
                    let __r = pg_rast_ma::st_map_algebra2(&arg0, arg1, &arg2, arg3, arg4, arg5)
                        .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_mapalgebra2: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            698usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_max_distance")?, "st_max_distance")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_max_distance")?, "st_max_distance")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_max_distance(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_max_distance: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            699usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_max_distance_3d")?,
                    "st_max_distance_3d",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_max_distance_3d")?,
                    "st_max_distance_3d",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_max_distance_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_max_distance_3d: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            700usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_max_distance_threed")?,
                    "st_max_distance_threed",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_max_distance_threed")?,
                    "st_max_distance_threed",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_max_distance_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_max_distance_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            701usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_max_x")?, "st_max_x")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_max_x(&arg0)))
            }
            702usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_max_y")?, "st_max_y")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_max_y(&arg0)))
            }
            703usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_maxdistance")?, "st_maxdistance")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_maxdistance")?, "st_maxdistance")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_max_distance(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_maxdistance: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            704usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_maximum_inscribed_circle")?,
                    "st_maximum_inscribed_circle",
                )?;
                {
                    let __r = pg_proc::st_maximum_inscribed_circle(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_maximum_inscribed_circle: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            705usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_maximuminscribedcircle")?,
                    "st_maximuminscribedcircle",
                )?;
                {
                    let __r = pg_proc::st_maximum_inscribed_circle(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_maximuminscribedcircle: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            706usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_mem_size")?, "st_mem_size")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_mem_size(&arg0) as i64))
            }
            707usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_mem_union")?, "st_mem_union")?;
                {
                    let __r = pg_proc::st_mem_union(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mem_union: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            708usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_memsize")?, "st_memsize")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_mem_size(&arg0) as i64))
            }
            709usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_metadata")?, "st_metadata")?;
                ret_to_witvalue_raster_metadata(pg_rast_acc::st_metadata(&arg0))
            }
            710usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_min_convex_hull")?,
                    "st_min_convex_hull",
                )?;
                {
                    let __r = pg_rast_acc::st_min_convex_hull(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_min_convex_hull: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            711usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_min_x")?, "st_min_x")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_min_x(&arg0)))
            }
            712usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_min_y")?, "st_min_y")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_min_y(&arg0)))
            }
            713usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_minimum_bounding_circle")?,
                    "st_minimum_bounding_circle",
                )?;
                {
                    let __r = pg_proc::st_minimum_bounding_circle(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minimum_bounding_circle: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            714usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_minimum_bounding_circle_segs")?,
                    "st_minimum_bounding_circle_segs",
                )?;
                let arg1 = dv_i64(&args, 1, "st_minimum_bounding_circle_segs")? as u32;
                {
                    let __r =
                        pg_proc::st_minimum_bounding_circle_segs(&arg0, arg1).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_minimum_bounding_circle_segs: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            715usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_minimum_bounding_radius")?,
                    "st_minimum_bounding_radius",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_proc::st_minimum_bounding_radius(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minimum_bounding_radius: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            716usize => {
                let arg0 = dv_blob(&args, 0, "st_minimum_bounding_sphere_center")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_minimum_bounding_sphere_center(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minimum_bounding_sphere_center: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            717usize => {
                let arg0 = dv_blob(&args, 0, "st_minimum_bounding_sphere_radius")?;
                Ok(types::Duckvalue::Float64(
                    pg_sfcgal::st_minimum_bounding_sphere_radius(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minimum_bounding_sphere_radius: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            718usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_minimum_clearance")?,
                    "st_minimum_clearance",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_minimum_clearance(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minimum_clearance: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            719usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_minimum_clearance_line")?,
                    "st_minimum_clearance_line",
                )?;
                {
                    let __r = pg_meas::st_minimum_clearance_line(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minimum_clearance_line: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            720usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_minimumboundingcircle")?,
                    "st_minimumboundingcircle",
                )?;
                {
                    let __r = pg_proc::st_minimum_bounding_circle(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minimumboundingcircle: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            721usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_minimumboundingradius")?,
                    "st_minimumboundingradius",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_proc::st_minimum_bounding_radius(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minimumboundingradius: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            722usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_minimumclearance")?,
                    "st_minimumclearance",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_minimum_clearance(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minimumclearance: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            723usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_minimumclearanceline")?,
                    "st_minimumclearanceline",
                )?;
                {
                    let __r = pg_meas::st_minimum_clearance_line(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minimumclearanceline: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            724usize => {
                let arg0 = dv_blob(&args, 0, "st_minkowski_sum")?;
                let arg1 = dv_blob(&args, 1, "st_minkowski_sum")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_minkowski_sum(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minkowski_sum: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            725usize => {
                let arg0 = dv_blob(&args, 0, "st_minkowskisum")?;
                let arg1 = dv_blob(&args, 1, "st_minkowskisum")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_minkowski_sum(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_minkowskisum: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            726usize => {
                let arg0 = dv_text(&args, 0, "st_mline_from_text")?;
                {
                    let __r = pg_ctor::st_mline_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mline_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            727usize => {
                let arg0 = dv_text(&args, 0, "st_mline_from_text_srid")?;
                let arg1 = dv_i64(&args, 1, "st_mline_from_text_srid")? as i32;
                {
                    let __r = pg_ctor::st_mline_from_text_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mline_from_text_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            728usize => {
                let arg0 = dv_blob(&args, 0, "st_mline_from_wkb")?;
                {
                    let __r = pg_ctor::st_mline_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mline_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            729usize => {
                let arg0 = dv_blob(&args, 0, "st_mline_from_wkb_srid")?;
                let arg1 = dv_i64(&args, 1, "st_mline_from_wkb_srid")? as i32;
                {
                    let __r = pg_ctor::st_mline_from_wkb_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mline_from_wkb_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            730usize => {
                let arg0 = dv_text(&args, 0, "st_mlinefromtext")?;
                {
                    let __r = pg_ctor::st_mline_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mlinefromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            731usize => {
                let arg0 = dv_blob(&args, 0, "st_mlinefromwkb")?;
                {
                    let __r = pg_ctor::st_mline_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mlinefromwkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            732usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_mmax")?, "st_mmax")?;
                Ok(
                    match pg_acc::st_mmax(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mmax: {}",
                            shim_err_string(e)
                        ))
                    })? {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            733usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_mmin")?, "st_mmin")?;
                Ok(
                    match pg_acc::st_mmin(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mmin: {}",
                            shim_err_string(e)
                        ))
                    })? {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            734usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_mod_edge_heal")?,
                    "st_mod_edge_heal",
                )?;
                let arg1 = dv_i64(&args, 1, "st_mod_edge_heal")? as u32;
                let arg2 = dv_i64(&args, 2, "st_mod_edge_heal")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::mod_edge_heal(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mod_edge_heal: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            735usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_mod_edge_split")?,
                    "st_mod_edge_split",
                )?;
                let arg1 = dv_i64(&args, 1, "st_mod_edge_split")? as u32;
                let arg2 = from_wkb(dv_blob(&args, 2, "st_mod_edge_split")?, "st_mod_edge_split")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::mod_edge_split(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mod_edge_split: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            736usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "st_modedgeheal")?, "st_modedgeheal")?;
                let arg1 = dv_i64(&args, 1, "st_modedgeheal")? as u32;
                let arg2 = dv_i64(&args, 2, "st_modedgeheal")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::mod_edge_heal(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_modedgeheal: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            737usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "st_modedgesplit")?, "st_modedgesplit")?;
                let arg1 = dv_i64(&args, 1, "st_modedgesplit")? as u32;
                let arg2 = from_wkb(dv_blob(&args, 2, "st_modedgesplit")?, "st_modedgesplit")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::mod_edge_split(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_modedgesplit: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            738usize => {
                let arg0 = dv_text(&args, 0, "st_mpoint_from_text")?;
                {
                    let __r = pg_ctor::st_mpoint_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpoint_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            739usize => {
                let arg0 = dv_text(&args, 0, "st_mpoint_from_text_srid")?;
                let arg1 = dv_i64(&args, 1, "st_mpoint_from_text_srid")? as i32;
                {
                    let __r = pg_ctor::st_mpoint_from_text_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpoint_from_text_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            740usize => {
                let arg0 = dv_blob(&args, 0, "st_mpoint_from_wkb")?;
                {
                    let __r = pg_ctor::st_mpoint_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpoint_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            741usize => {
                let arg0 = dv_blob(&args, 0, "st_mpoint_from_wkb_srid")?;
                let arg1 = dv_i64(&args, 1, "st_mpoint_from_wkb_srid")? as i32;
                {
                    let __r = pg_ctor::st_mpoint_from_wkb_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpoint_from_wkb_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            742usize => {
                let arg0 = dv_text(&args, 0, "st_mpointfromtext")?;
                {
                    let __r = pg_ctor::st_mpoint_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpointfromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            743usize => {
                let arg0 = dv_blob(&args, 0, "st_mpointfromwkb")?;
                {
                    let __r = pg_ctor::st_mpoint_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpointfromwkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            744usize => {
                let arg0 = dv_text(&args, 0, "st_mpoly_from_text")?;
                {
                    let __r = pg_ctor::st_mpoly_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpoly_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            745usize => {
                let arg0 = dv_text(&args, 0, "st_mpoly_from_text_srid")?;
                let arg1 = dv_i64(&args, 1, "st_mpoly_from_text_srid")? as i32;
                {
                    let __r = pg_ctor::st_mpoly_from_text_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpoly_from_text_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            746usize => {
                let arg0 = dv_blob(&args, 0, "st_mpoly_from_wkb")?;
                {
                    let __r = pg_ctor::st_mpoly_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpoly_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            747usize => {
                let arg0 = dv_blob(&args, 0, "st_mpoly_from_wkb_srid")?;
                let arg1 = dv_i64(&args, 1, "st_mpoly_from_wkb_srid")? as i32;
                {
                    let __r = pg_ctor::st_mpoly_from_wkb_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpoly_from_wkb_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            748usize => {
                let arg0 = dv_text(&args, 0, "st_mpolyfromtext")?;
                {
                    let __r = pg_ctor::st_mpoly_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpolyfromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            749usize => {
                let arg0 = dv_blob(&args, 0, "st_mpolyfromwkb")?;
                {
                    let __r = pg_ctor::st_mpoly_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_mpolyfromwkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            750usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_multi")?, "st_multi")?;
                {
                    let __r = pg_acc::st_multi(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_multi: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            751usize => {
                let arg0 = dv_blob(&args, 0, "st_multi_line_from_wkb")?;
                {
                    let __r = pg_ctor::st_multi_line_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_multi_line_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            752usize => {
                let arg0 = dv_blob(&args, 0, "st_multi_line_from_wkb_srid")?;
                let arg1 = dv_i64(&args, 1, "st_multi_line_from_wkb_srid")? as i32;
                {
                    let __r = pg_ctor::st_multi_line_from_wkb_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_multi_line_from_wkb_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            753usize => {
                let arg0 = dv_text(&args, 0, "st_multi_line_string_from_text")?;
                {
                    let __r = pg_ctor::st_multi_line_string_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_multi_line_string_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            754usize => {
                let arg0 = dv_text(&args, 0, "st_multi_line_string_from_text_srid")?;
                let arg1 = dv_i64(&args, 1, "st_multi_line_string_from_text_srid")? as i32;
                {
                    let __r =
                        pg_ctor::st_multi_line_string_from_text_srid(arg0, arg1).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_multi_line_string_from_text_srid: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            755usize => {
                let arg0 = dv_text(&args, 0, "st_multi_point_from_text")?;
                {
                    let __r = pg_ctor::st_multi_point_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_multi_point_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            756usize => {
                let arg0 = dv_text(&args, 0, "st_multi_point_from_text_srid")?;
                let arg1 = dv_i64(&args, 1, "st_multi_point_from_text_srid")? as i32;
                {
                    let __r = pg_ctor::st_multi_point_from_text_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_multi_point_from_text_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            757usize => {
                let arg0 = dv_blob(&args, 0, "st_multi_point_from_wkb")?;
                {
                    let __r = pg_ctor::st_multi_point_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_multi_point_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            758usize => {
                let arg0 = dv_blob(&args, 0, "st_multi_point_from_wkb_srid")?;
                let arg1 = dv_i64(&args, 1, "st_multi_point_from_wkb_srid")? as i32;
                {
                    let __r = pg_ctor::st_multi_point_from_wkb_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_multi_point_from_wkb_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            759usize => {
                let arg0 = dv_blob(&args, 0, "st_multi_poly_from_wkb")?;
                {
                    let __r = pg_ctor::st_multi_poly_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_multi_poly_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            760usize => {
                let arg0 = dv_blob(&args, 0, "st_multi_poly_from_wkb_srid")?;
                let arg1 = dv_i64(&args, 1, "st_multi_poly_from_wkb_srid")? as i32;
                {
                    let __r = pg_ctor::st_multi_poly_from_wkb_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_multi_poly_from_wkb_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            761usize => {
                let arg0 = dv_text(&args, 0, "st_multi_polygon_from_text")?;
                {
                    let __r = pg_ctor::st_multi_polygon_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_multi_polygon_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            762usize => {
                let arg0 = dv_text(&args, 0, "st_multi_polygon_from_text_srid")?;
                let arg1 = dv_i64(&args, 1, "st_multi_polygon_from_text_srid")? as i32;
                {
                    let __r =
                        pg_ctor::st_multi_polygon_from_text_srid(arg0, arg1).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_multi_polygon_from_text_srid: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            763usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_n_dims")?, "st_n_dims")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_ndims(&arg0) as i64))
            }
            764usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_n_dims_batch")?;
                {
                    let __r = postgis_batch::st_ndims_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            765usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_ndims")?, "st_ndims")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_ndims(&arg0) as i64))
            }
            766usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_ndims_batch")?;
                {
                    let __r = postgis_batch::st_ndims_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            767usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_nearest_value")?, "st_nearest_value")?;
                let arg1 = dv_i64(&args, 1, "st_nearest_value")? as u32;
                let arg2 = dv_i64(&args, 2, "st_nearest_value")? as u32;
                let arg3 = dv_i64(&args, 3, "st_nearest_value")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_px::st_nearest_value(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_nearest_value: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            768usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_nearestvalue")?, "st_nearestvalue")?;
                let arg1 = dv_i64(&args, 1, "st_nearestvalue")? as u32;
                let arg2 = dv_i64(&args, 2, "st_nearestvalue")? as u32;
                let arg3 = dv_i64(&args, 3, "st_nearestvalue")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_px::st_nearest_value(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_nearestvalue: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            769usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_new_edges_split")?,
                    "st_new_edges_split",
                )?;
                let arg1 = dv_i64(&args, 1, "st_new_edges_split")? as u32;
                let arg2 = from_wkb(
                    dv_blob(&args, 2, "st_new_edges_split")?,
                    "st_new_edges_split",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::new_edges_split(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_new_edges_split: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            770usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_newedgessplit")?,
                    "st_newedgessplit",
                )?;
                let arg1 = dv_i64(&args, 1, "st_newedgessplit")? as u32;
                let arg2 = from_wkb(dv_blob(&args, 2, "st_newedgessplit")?, "st_newedgessplit")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::new_edges_split(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_newedgessplit: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            771usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_node")?, "st_node")?;
                {
                    let __r = pg_proc::st_node(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_node: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            772usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_normalize")?, "st_normalize")?;
                {
                    let __r = pg_proc::st_normalize(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_normalize: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            773usize => {
                let arg0 = dv_text(&args, 0, "st_normalize_address")?;
                {
                    let __r = pg_geo::normalize_address(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_normalize_address: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_address_component(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            774usize => {
                let arg0 = dv_text(&args, 0, "st_normalizeaddress")?;
                {
                    let __r = pg_geo::normalize_address(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_normalizeaddress: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_address_component(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            775usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_npoints")?, "st_npoints")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_npoints(&arg0) as i64))
            }
            776usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_nrings")?, "st_nrings")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_nrings(&arg0) as i64))
            }
            777usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_num_bands")?, "st_num_bands")?;
                Ok(types::Duckvalue::Int64(
                    pg_rast_acc::st_num_bands(&arg0) as i64
                ))
            }
            778usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_num_curves")?, "st_num_curves")?;
                Ok(types::Duckvalue::Int64(pg_proc::st_num_curves(&arg0) as i64))
            }
            779usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_num_geometries")?, "st_num_geometries")?;
                Ok(types::Duckvalue::Int64(
                    pg_acc::st_num_geometries(&arg0) as i64
                ))
            }
            780usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_num_geometries_batch")?;
                {
                    let __r = postgis_batch::st_num_geometries_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            781usize => {
                let arg0 = dv_blob(&args, 0, "st_num_geometries_threed")?;
                Ok(types::Duckvalue::Int64(
                    pg_sfcgal::st_num_geometries_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_num_geometries_threed: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            782usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_num_interior_ring")?,
                    "st_num_interior_ring",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_acc::st_num_interior_ring(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_num_interior_ring: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            783usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_num_interior_rings")?,
                    "st_num_interior_rings",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_acc::st_num_interior_rings(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_num_interior_rings: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            784usize => {
                let arg0 = dv_blob(&args, 0, "st_num_interior_rings_threed")?;
                Ok(types::Duckvalue::Int64(
                    pg_sfcgal::st_num_interior_rings_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_num_interior_rings_threed: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            785usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_num_points")?, "st_num_points")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_num_points(&arg0) as i64))
            }
            786usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_num_points_batch")?;
                {
                    let __r = postgis_batch::st_num_points_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            787usize => {
                let arg0 = dv_blob(&args, 0, "st_num_points_threed")?;
                Ok(types::Duckvalue::Int64(
                    pg_sfcgal::st_num_points_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_num_points_threed: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            788usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_numbands")?, "st_numbands")?;
                Ok(types::Duckvalue::Int64(
                    pg_rast_acc::st_num_bands(&arg0) as i64
                ))
            }
            789usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_numcurves")?, "st_numcurves")?;
                Ok(types::Duckvalue::Int64(pg_proc::st_num_curves(&arg0) as i64))
            }
            790usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_numgeometries")?, "st_numgeometries")?;
                Ok(types::Duckvalue::Int64(
                    pg_acc::st_num_geometries(&arg0) as i64
                ))
            }
            791usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_numinteriorring")?,
                    "st_numinteriorring",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_acc::st_num_interior_rings(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_numinteriorring: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            792usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_numinteriorrings")?,
                    "st_numinteriorrings",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_acc::st_num_interior_rings(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_numinteriorrings: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            793usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_numpoints")?, "st_numpoints")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_num_points(&arg0) as i64))
            }
            794usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_offset_curve")?, "st_offset_curve")?;
                let arg1 = dv_f64(&args, 1, "st_offset_curve")?;
                {
                    let __r = pg_proc::st_offset_curve(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_offset_curve: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            795usize => {
                let arg0 = dv_blob(&args, 0, "st_offset_polygon")?;
                let arg1 = dv_f64(&args, 1, "st_offset_polygon")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_offset_polygon(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_offset_polygon: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            796usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_offsetcurve")?, "st_offsetcurve")?;
                let arg1 = dv_f64(&args, 1, "st_offsetcurve")?;
                {
                    let __r = pg_proc::st_offset_curve(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_offsetcurve: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            797usize => {
                let arg0 = dv_blob(&args, 0, "st_optimal_alpha_shape")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_optimal_alpha_shape(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_optimal_alpha_shape: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            798usize => {
                let arg0 = dv_blob(&args, 0, "st_optimal_convex_partition")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_optimal_convex_partition(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_optimal_convex_partition: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            799usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_ordering_equals")?,
                    "st_ordering_equals",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_ordering_equals")?,
                    "st_ordering_equals",
                )?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_ordering_equals(
                    &arg0, &arg1,
                )))
            }
            800usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_orderingequals")?, "st_orderingequals")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_orderingequals")?, "st_orderingequals")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_ordering_equals(
                    &arg0, &arg1,
                )))
            }
            801usize => {
                let arg0 = dv_blob(&args, 0, "st_orientable_surface")?;
                Ok(types::Duckvalue::Boolean(
                    pg_sfcgal::st_orientable_surface(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_orientable_surface: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            802usize => {
                let arg0 = dv_blob(&args, 0, "st_orientation")?;
                Ok(types::Duckvalue::Int64(
                    pg_sfcgal::st_orientation(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_orientation: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            803usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_oriented_envelope")?,
                    "st_oriented_envelope",
                )?;
                {
                    let __r = pg_proc::st_oriented_envelope(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_oriented_envelope: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            804usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_orientedenvelope")?,
                    "st_orientedenvelope",
                )?;
                {
                    let __r = pg_proc::st_oriented_envelope(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_orientedenvelope: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            805usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_overlaps")?, "st_overlaps")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_overlaps")?, "st_overlaps")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_overlaps(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_overlaps: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            806usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_overlaps_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_overlaps_batch")?;
                {
                    let __r = postgis_batch::st_overlaps_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_overlaps_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            807usize => {
                let arg0 = dv_text(&args, 0, "st_parse_address")?;
                ret_to_witvalue_parsed_address(pg_geo::parse_address(arg0).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_parse_address: {}",
                        shim_err_string(e)
                    ))
                })?)
            }
            808usize => {
                let arg0 = dv_text(&args, 0, "st_parseaddress")?;
                ret_to_witvalue_parsed_address(pg_geo::parse_address(arg0).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_parseaddress: {}",
                        shim_err_string(e)
                    ))
                })?)
            }
            809usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_perimeter")?, "st_perimeter")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_perimeter(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_perimeter: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            810usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_perimeter2d")?, "st_perimeter2d")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_perimeter_twod(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_perimeter2d: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            811usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_perimeter_2d")?, "st_perimeter_2d")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_perimeter_twod(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_perimeter_2d: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            812usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_perimeter_batch")?;
                {
                    let __r = postgis_batch::st_perimeter_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            813usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_perimeter_threed")?,
                    "st_perimeter_threed",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_perimeter_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_perimeter_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            814usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_perimeter_twod")?, "st_perimeter_twod")?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_perimeter_twod(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_perimeter_twod: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            815usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_perimeterthreed")?,
                    "st_perimeterthreed",
                )?;
                Ok(types::Duckvalue::Float64(
                    pg_meas::st_perimeter_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_perimeterthreed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            816usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_pixel_as_centroid")?,
                    "st_pixel_as_centroid",
                )?;
                let arg1 = dv_i64(&args, 1, "st_pixel_as_centroid")? as u32;
                let arg2 = dv_i64(&args, 2, "st_pixel_as_centroid")? as u32;
                {
                    let __r = pg_rast_px::st_pixel_as_centroid(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pixel_as_centroid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            817usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_pixel_as_point")?,
                    "st_pixel_as_point",
                )?;
                let arg1 = dv_i64(&args, 1, "st_pixel_as_point")? as u32;
                let arg2 = dv_i64(&args, 2, "st_pixel_as_point")? as u32;
                {
                    let __r = pg_rast_acc::st_pixel_as_point(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pixel_as_point: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            818usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_pixel_as_polygon")?,
                    "st_pixel_as_polygon",
                )?;
                let arg1 = dv_i64(&args, 1, "st_pixel_as_polygon")? as u32;
                let arg2 = dv_i64(&args, 2, "st_pixel_as_polygon")? as u32;
                {
                    let __r = pg_rast_px::st_pixel_as_polygon(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pixel_as_polygon: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            819usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_pixel_as_polygons")?,
                    "st_pixel_as_polygons",
                )?;
                let arg1 = dv_i64(&args, 1, "st_pixel_as_polygons")? as u32;
                {
                    let __r = pg_rast_vec::st_pixel_as_polygons(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pixel_as_polygons: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __out = alloc::string::String::from("[");
                    for (__i, (__g, __v)) in __r.into_iter().enumerate() {
                        if __i > 0 {
                            __out.push(',');
                        }
                        let __wkb = __g.as_wkb();
                        __out.push_str("[\"");
                        for __b in __wkb {
                            use core::fmt::Write as _;
                            let _ = write!(&mut __out, "{:02x}", __b);
                        }
                        __out.push_str("\",");
                        let __vj = serde_json::to_string(&__v).map_err(|e| {
                            types::Duckerror::Internal(format!(
                                "st_pixel_as_polygons: encode JSON: {}",
                                e
                            ))
                        })?;
                        __out.push_str(&__vj);
                        __out.push(']');
                    }
                    __out.push(']');
                    Ok(types::Duckvalue::Text(__out))
                }
            }
            820usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_pixel_height")?, "st_pixel_height")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_pixel_height(
                    &arg0,
                )))
            }
            821usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_pixel_of_value")?,
                    "st_pixel_of_value",
                )?;
                let arg1 = dv_i64(&args, 1, "st_pixel_of_value")? as u32;
                let arg2 = dv_f64(&args, 2, "st_pixel_of_value")?;
                {
                    let __r = pg_rast_px::st_pixel_of_value(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pixel_of_value: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_pixel_coord(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            822usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_pixel_width")?, "st_pixel_width")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_pixel_width(
                    &arg0,
                )))
            }
            823usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_pixelascentroid")?,
                    "st_pixelascentroid",
                )?;
                let arg1 = dv_i64(&args, 1, "st_pixelascentroid")? as u32;
                let arg2 = dv_i64(&args, 2, "st_pixelascentroid")? as u32;
                {
                    let __r = pg_rast_px::st_pixel_as_centroid(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pixelascentroid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            824usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_pixelaspoint")?, "st_pixelaspoint")?;
                let arg1 = dv_i64(&args, 1, "st_pixelaspoint")? as u32;
                let arg2 = dv_i64(&args, 2, "st_pixelaspoint")? as u32;
                {
                    let __r = pg_rast_acc::st_pixel_as_point(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pixelaspoint: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            825usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_pixelaspolygon")?,
                    "st_pixelaspolygon",
                )?;
                let arg1 = dv_i64(&args, 1, "st_pixelaspolygon")? as u32;
                let arg2 = dv_i64(&args, 2, "st_pixelaspolygon")? as u32;
                {
                    let __r = pg_rast_px::st_pixel_as_polygon(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pixelaspolygon: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            826usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_pixelaspolygons")?,
                    "st_pixelaspolygons",
                )?;
                let arg1 = dv_i64(&args, 1, "st_pixelaspolygons")? as u32;
                {
                    let __r = pg_rast_vec::st_pixel_as_polygons(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pixelaspolygons: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __out = alloc::string::String::from("[");
                    for (__i, (__g, __v)) in __r.into_iter().enumerate() {
                        if __i > 0 {
                            __out.push(',');
                        }
                        let __wkb = __g.as_wkb();
                        __out.push_str("[\"");
                        for __b in __wkb {
                            use core::fmt::Write as _;
                            let _ = write!(&mut __out, "{:02x}", __b);
                        }
                        __out.push_str("\",");
                        let __vj = serde_json::to_string(&__v).map_err(|e| {
                            types::Duckerror::Internal(format!(
                                "st_pixelaspolygons: encode JSON: {}",
                                e
                            ))
                        })?;
                        __out.push_str(&__vj);
                        __out.push(']');
                    }
                    __out.push(']');
                    Ok(types::Duckvalue::Text(__out))
                }
            }
            827usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_pixelofvalue")?, "st_pixelofvalue")?;
                let arg1 = dv_i64(&args, 1, "st_pixelofvalue")? as u32;
                let arg2 = dv_f64(&args, 2, "st_pixelofvalue")?;
                {
                    let __r = pg_rast_px::st_pixel_of_value(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pixelofvalue: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_pixel_coord(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            828usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_pixels_of_values")?,
                    "st_pixels_of_values",
                )?;
                let arg1 = dv_i64(&args, 1, "st_pixels_of_values")? as u32;
                let arg2: Vec<f64> = parse_json_list_f64(&args, 2, "st_pixels_of_values")?;
                {
                    let __r = pg_rast_px::st_pixels_of_values(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pixels_of_values: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_pixel_value_coord(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            829usize => {
                let arg0 = dv_f64(&args, 0, "st_point")?;
                let arg1 = dv_f64(&args, 1, "st_point")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_point(arg0, arg1).as_wkb().into(),
                ))
            }
            830usize => {
                let arg0 = dv_text(&args, 0, "st_point_from_geohash")?;
                {
                    let __r = pg_ctor::st_point_from_geohash(arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_from_geohash: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            831usize => {
                let arg0 = dv_text(&args, 0, "st_point_from_text")?;
                {
                    let __r = pg_ctor::st_point_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            832usize => {
                let arg0 = dv_text(&args, 0, "st_point_from_text_srid")?;
                let arg1 = dv_i64(&args, 1, "st_point_from_text_srid")? as i32;
                {
                    let __r = pg_ctor::st_point_from_text_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_from_text_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            833usize => {
                let arg0 = dv_blob(&args, 0, "st_point_from_wkb")?;
                {
                    let __r = pg_ctor::st_point_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            834usize => {
                let arg0 = dv_blob(&args, 0, "st_point_from_wkb_srid")?;
                let arg1 = dv_i64(&args, 1, "st_point_from_wkb_srid")? as i32;
                {
                    let __r = pg_ctor::st_point_from_wkb_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_from_wkb_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            835usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_point_inside_circle")?,
                    "st_point_inside_circle",
                )?;
                let arg1 = dv_f64(&args, 1, "st_point_inside_circle")?;
                let arg2 = dv_f64(&args, 2, "st_point_inside_circle")?;
                let arg3 = dv_f64(&args, 3, "st_point_inside_circle")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_point_inside_circle(
                    &arg0, arg1, arg2, arg3,
                )))
            }
            836usize => {
                let arg0 = dv_f64(&args, 0, "st_point_m")?;
                let arg1 = dv_f64(&args, 1, "st_point_m")?;
                let arg2 = dv_f64(&args, 2, "st_point_m")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_point_m(arg0, arg1, arg2).as_wkb().into(),
                ))
            }
            837usize => {
                let arg0 = dv_f64(&args, 0, "st_point_m_srid")?;
                let arg1 = dv_f64(&args, 1, "st_point_m_srid")?;
                let arg2 = dv_f64(&args, 2, "st_point_m_srid")?;
                let arg3 = dv_i64(&args, 3, "st_point_m_srid")? as i32;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_point_m_srid(arg0, arg1, arg2, arg3)
                        .as_wkb()
                        .into(),
                ))
            }
            838usize => {
                let arg0 = dv_blob(&args, 0, "st_point_m_threed")?;
                Ok(types::Duckvalue::Float64(
                    pg_sfcgal::st_point_m_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_m_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            839usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_point_n")?, "st_point_n")?;
                let arg1 = dv_i64(&args, 1, "st_point_n")? as u32;
                {
                    let __r = pg_acc::st_point_n(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_n: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            840usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_point_n_batch")?;
                let arg1: Vec<i32> = parse_json_list_i32(&args, 1, "st_point_n_batch")?;
                {
                    let __r = postgis_batch::st_point_n_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_point_n_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            841usize => {
                let arg0 = dv_blob(&args, 0, "st_point_n_threed")?;
                let arg1 = dv_i64(&args, 1, "st_point_n_threed")? as i32;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_point_n_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_n_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            842usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_point_on_surface")?,
                    "st_point_on_surface",
                )?;
                {
                    let __r = pg_proc::st_point_on_surface(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_on_surface: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            843usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_point_on_surface_batch")?;
                {
                    let __r = postgis_batch::st_point_on_surface_batch(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_point_on_surface_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            844usize => {
                let arg0 = dv_f64(&args, 0, "st_point_srid")?;
                let arg1 = dv_f64(&args, 1, "st_point_srid")?;
                let arg2 = dv_i64(&args, 2, "st_point_srid")? as i32;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_point_srid(arg0, arg1, arg2).as_wkb().into(),
                ))
            }
            845usize => {
                let arg0 = dv_blob(&args, 0, "st_point_x_threed")?;
                Ok(types::Duckvalue::Float64(
                    pg_sfcgal::st_point_x_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_x_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            846usize => {
                let arg0 = dv_blob(&args, 0, "st_point_y_threed")?;
                Ok(types::Duckvalue::Float64(
                    pg_sfcgal::st_point_y_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_y_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            847usize => {
                let arg0 = dv_f64(&args, 0, "st_point_z")?;
                let arg1 = dv_f64(&args, 1, "st_point_z")?;
                let arg2 = dv_f64(&args, 2, "st_point_z")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_point_z(arg0, arg1, arg2).as_wkb().into(),
                ))
            }
            848usize => {
                let arg0 = dv_f64(&args, 0, "st_point_z_srid")?;
                let arg1 = dv_f64(&args, 1, "st_point_z_srid")?;
                let arg2 = dv_f64(&args, 2, "st_point_z_srid")?;
                let arg3 = dv_i64(&args, 3, "st_point_z_srid")? as i32;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_point_z_srid(arg0, arg1, arg2, arg3)
                        .as_wkb()
                        .into(),
                ))
            }
            849usize => {
                let arg0 = dv_blob(&args, 0, "st_point_z_threed")?;
                Ok(types::Duckvalue::Float64(
                    pg_sfcgal::st_point_z_threed(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_point_z_threed: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            850usize => {
                let arg0 = dv_f64(&args, 0, "st_point_zm")?;
                let arg1 = dv_f64(&args, 1, "st_point_zm")?;
                let arg2 = dv_f64(&args, 2, "st_point_zm")?;
                let arg3 = dv_f64(&args, 3, "st_point_zm")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_point_zm(arg0, arg1, arg2, arg3).as_wkb().into(),
                ))
            }
            851usize => {
                let arg0 = dv_f64(&args, 0, "st_point_zm_srid")?;
                let arg1 = dv_f64(&args, 1, "st_point_zm_srid")?;
                let arg2 = dv_f64(&args, 2, "st_point_zm_srid")?;
                let arg3 = dv_f64(&args, 3, "st_point_zm_srid")?;
                let arg4 = dv_i64(&args, 4, "st_point_zm_srid")? as i32;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_point_zm_srid(arg0, arg1, arg2, arg3, arg4)
                        .as_wkb()
                        .into(),
                ))
            }
            852usize => {
                let arg0 = dv_text(&args, 0, "st_pointfromgeohash")?;
                {
                    let __r = pg_ctor::st_point_from_geohash(arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pointfromgeohash: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            853usize => {
                let arg0 = dv_text(&args, 0, "st_pointfromtext")?;
                {
                    let __r = pg_ctor::st_point_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pointfromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            854usize => {
                let arg0 = dv_blob(&args, 0, "st_pointfromwkb")?;
                {
                    let __r = pg_ctor::st_point_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pointfromwkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            855usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_pointinsidecircle")?,
                    "st_pointinsidecircle",
                )?;
                let arg1 = dv_f64(&args, 1, "st_pointinsidecircle")?;
                let arg2 = dv_f64(&args, 2, "st_pointinsidecircle")?;
                let arg3 = dv_f64(&args, 3, "st_pointinsidecircle")?;
                Ok(types::Duckvalue::Boolean(pg_pred::st_point_inside_circle(
                    &arg0, arg1, arg2, arg3,
                )))
            }
            856usize => {
                let arg0 = dv_f64(&args, 0, "st_pointm")?;
                let arg1 = dv_f64(&args, 1, "st_pointm")?;
                let arg2 = dv_f64(&args, 2, "st_pointm")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_point_m(arg0, arg1, arg2).as_wkb().into(),
                ))
            }
            857usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_pointn")?, "st_pointn")?;
                let arg1 = dv_i64(&args, 1, "st_pointn")? as u32;
                {
                    let __r = pg_acc::st_point_n(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pointn: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            858usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_pointonsurface")?, "st_pointonsurface")?;
                {
                    let __r = pg_acc::st_point_on_surface(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_pointonsurface: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            859usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_points")?, "st_points")?;
                Ok(types::Duckvalue::Blob(
                    pg_acc::st_points(&arg0).as_wkb().into(),
                ))
            }
            860usize => {
                let arg0 = dv_f64(&args, 0, "st_pointz")?;
                let arg1 = dv_f64(&args, 1, "st_pointz")?;
                let arg2 = dv_f64(&args, 2, "st_pointz")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_point_z(arg0, arg1, arg2).as_wkb().into(),
                ))
            }
            861usize => {
                let arg0 = dv_f64(&args, 0, "st_pointzm")?;
                let arg1 = dv_f64(&args, 1, "st_pointzm")?;
                let arg2 = dv_f64(&args, 2, "st_pointzm")?;
                let arg3 = dv_f64(&args, 3, "st_pointzm")?;
                Ok(types::Duckvalue::Blob(
                    pg_ctor::st_point_zm(arg0, arg1, arg2, arg3).as_wkb().into(),
                ))
            }
            862usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_polygon")?, "st_polygon")?;
                let arg1 = dv_i64(&args, 1, "st_polygon")? as u32;
                {
                    let __r = pg_rast_vec::st_polygon(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygon: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            863usize => {
                let arg0 = dv_text(&args, 0, "st_polygon_from_text")?;
                {
                    let __r = pg_ctor::st_polygon_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygon_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            864usize => {
                let arg0 = dv_text(&args, 0, "st_polygon_from_text_srid")?;
                let arg1 = dv_i64(&args, 1, "st_polygon_from_text_srid")? as i32;
                {
                    let __r = pg_ctor::st_polygon_from_text_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygon_from_text_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            865usize => {
                let arg0 = dv_blob(&args, 0, "st_polygon_from_wkb")?;
                {
                    let __r = pg_ctor::st_polygon_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygon_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            866usize => {
                let arg0 = dv_blob(&args, 0, "st_polygon_from_wkb_srid")?;
                let arg1 = dv_i64(&args, 1, "st_polygon_from_wkb_srid")? as i32;
                {
                    let __r = pg_ctor::st_polygon_from_wkb_srid(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygon_from_wkb_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            867usize => {
                let arg0 = dv_text(&args, 0, "st_polygonfromtext")?;
                {
                    let __r = pg_ctor::st_polygon_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygonfromtext: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            868usize => {
                let arg0 = dv_blob(&args, 0, "st_polygonfromwkb")?;
                {
                    let __r = pg_ctor::st_polygon_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygonfromwkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            869usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_polygonize")?, "st_polygonize")?;
                {
                    let __r = pg_proc::st_polygonize(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygonize: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            870usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_polygonize_as_raster")?,
                    "st_polygonize_as_raster",
                )?;
                {
                    let __r = pg_topo_edit::st_polygonize_as_raster(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygonize_as_raster: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            871usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_polygonize_topology")?,
                    "st_polygonize_topology",
                )?;
                {
                    let __r = pg_topo_edit::st_polygonize_topology(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polygonize_topology: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            872usize => {
                let arg0 = dv_text(&args, 0, "st_polyhedralsurface_from_text")?;
                {
                    let __r = pg_ctor::st_polyhedralsurface_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polyhedralsurface_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            873usize => {
                let arg0 = dv_blob(&args, 0, "st_polyhedralsurface_from_wkb")?;
                {
                    let __r = pg_ctor::st_polyhedralsurface_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_polyhedralsurface_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            874usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_project")?, "st_project")?;
                let arg1 = dv_f64(&args, 1, "st_project")?;
                let arg2 = dv_f64(&args, 2, "st_project")?;
                {
                    let __r = pg_geog::st_project(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_project: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            875usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_project_batch")?;
                let arg1: Vec<f64> = parse_json_list_f64(&args, 1, "st_project_batch")?;
                let arg2: Vec<f64> = parse_json_list_f64(&args, 2, "st_project_batch")?;
                {
                    let __r = postgis_batch::st_project_batch(&arg0, &arg1, &arg2);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_project_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            876usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_quantile")?, "st_quantile")?;
                let arg1 = dv_i64(&args, 1, "st_quantile")? as u32;
                let arg2 = dv_f64(&args, 2, "st_quantile")?;
                Ok(types::Duckvalue::Float64(
                    pg_rast_stats::st_quantile(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_quantile: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            877usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_quantiles")?, "st_quantiles")?;
                let arg1 = dv_i64(&args, 1, "st_quantiles")? as u32;
                let arg2: Vec<f64> = parse_json_list_f64(&args, 2, "st_quantiles")?;
                {
                    let __r = pg_rast_stats::st_quantiles(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_quantiles: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_quantile_entry(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            878usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_quantize_coordinates")?,
                    "st_quantize_coordinates",
                )?;
                let arg1 = dv_i64(&args, 1, "st_quantize_coordinates")? as u32;
                let arg2 = dv_i64(&args, 2, "st_quantize_coordinates")? as u32;
                {
                    let __r =
                        pg_xform::st_quantize_coordinates(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_quantize_coordinates: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            879usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_quantizecoordinates")?,
                    "st_quantizecoordinates",
                )?;
                let arg1 = dv_i64(&args, 1, "st_quantizecoordinates")? as u32;
                let arg2 = dv_i64(&args, 2, "st_quantizecoordinates")? as u32;
                {
                    let __r =
                        pg_xform::st_quantize_coordinates(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_quantizecoordinates: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            880usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_rast_height_dfer_batch")?;
                {
                    let __r = postgis_batch::st_rast_height_dfer_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            881usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_rast_scale_x_dfer_batch")?;
                {
                    let __r = postgis_batch::st_rast_scale_x_dfer_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            882usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_rast_scale_y_dfer_batch")?;
                {
                    let __r = postgis_batch::st_rast_scale_y_dfer_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            883usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_rast_skew_x_dfer_batch")?;
                {
                    let __r = postgis_batch::st_rast_skew_x_dfer_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            884usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_rast_skew_y_dfer_batch")?;
                {
                    let __r = postgis_batch::st_rast_skew_y_dfer_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            885usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_rast_srid_dfer_batch")?;
                {
                    let __r = postgis_batch::st_rast_srid_dfer_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            886usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_rast_upper_left_x_dfer_batch")?;
                {
                    let __r = postgis_batch::st_rast_upper_left_x_dfer_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            887usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_rast_upper_left_y_dfer_batch")?;
                {
                    let __r = postgis_batch::st_rast_upper_left_y_dfer_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            888usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_rast_width_dfer_batch")?;
                {
                    let __r = postgis_batch::st_rast_width_dfer_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            889usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_rastenvelope")?, "st_rastenvelope")?;
                {
                    let __r = pg_rast_acc::st_envelope(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rastenvelope: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            890usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_raster_contains")?,
                    "st_raster_contains",
                )?;
                let arg1 = from_raster_binary(
                    dv_blob(&args, 1, "st_raster_contains")?,
                    "st_raster_contains",
                )?;
                Ok(types::Duckvalue::Boolean(pg_rast_pred::st_raster_contains(
                    &arg0, &arg1,
                )))
            }
            891usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_raster_contains_geom")?,
                    "st_raster_contains_geom",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_raster_contains_geom")?,
                    "st_raster_contains_geom",
                )?;
                Ok(types::Duckvalue::Boolean(
                    pg_rast_pred::st_raster_contains_geom(&arg0, &arg1),
                ))
            }
            892usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_raster_convex_hull")?,
                    "st_raster_convex_hull",
                )?;
                {
                    let __r = pg_rast_pred::st_raster_convex_hull(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_raster_convex_hull: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            893usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_raster_covers")?, "st_raster_covers")?;
                let arg1 =
                    from_raster_binary(dv_blob(&args, 1, "st_raster_covers")?, "st_raster_covers")?;
                Ok(types::Duckvalue::Boolean(pg_rast_pred::st_raster_covers(
                    &arg0, &arg1,
                )))
            }
            894usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_raster_intersection_geom")?,
                    "st_raster_intersection_geom",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_raster_intersection_geom")?,
                    "st_raster_intersection_geom",
                )?;
                let arg2 = dv_i64(&args, 2, "st_raster_intersection_geom")? as u32;
                {
                    let __r = pg_rast_vec::st_raster_intersection_geom(&arg0, &arg1, arg2)
                        .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_raster_intersection_geom: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    let mut __out = alloc::string::String::from("[");
                    for (__i, (__g, __v)) in __r.into_iter().enumerate() {
                        if __i > 0 {
                            __out.push(',');
                        }
                        let __wkb = __g.as_wkb();
                        __out.push_str("[\"");
                        for __b in __wkb {
                            use core::fmt::Write as _;
                            let _ = write!(&mut __out, "{:02x}", __b);
                        }
                        __out.push_str("\",");
                        let __vj = serde_json::to_string(&__v).map_err(|e| {
                            types::Duckerror::Internal(format!(
                                "st_raster_intersection_geom: encode JSON: {}",
                                e
                            ))
                        })?;
                        __out.push_str(&__vj);
                        __out.push(']');
                    }
                    __out.push(']');
                    Ok(types::Duckvalue::Text(__out))
                }
            }
            895usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_raster_intersects")?,
                    "st_raster_intersects",
                )?;
                let arg1 = from_raster_binary(
                    dv_blob(&args, 1, "st_raster_intersects")?,
                    "st_raster_intersects",
                )?;
                Ok(types::Duckvalue::Boolean(
                    pg_rast_pred::st_raster_intersects(&arg0, &arg1),
                ))
            }
            896usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_raster_intersects_geom")?,
                    "st_raster_intersects_geom",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_raster_intersects_geom")?,
                    "st_raster_intersects_geom",
                )?;
                Ok(types::Duckvalue::Boolean(
                    pg_rast_pred::st_raster_intersects_geom(&arg0, &arg1),
                ))
            }
            897usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_raster_overlaps")?,
                    "st_raster_overlaps",
                )?;
                let arg1 = from_raster_binary(
                    dv_blob(&args, 1, "st_raster_overlaps")?,
                    "st_raster_overlaps",
                )?;
                Ok(types::Duckvalue::Boolean(pg_rast_pred::st_raster_overlaps(
                    &arg0, &arg1,
                )))
            }
            898usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_raster_to_world_coord_x")?,
                    "st_raster_to_world_coord_x",
                )?;
                let arg1 = dv_i64(&args, 1, "st_raster_to_world_coord_x")? as u32;
                let arg2 = dv_i64(&args, 2, "st_raster_to_world_coord_x")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_px::st_raster_to_world_coord_x(&arg0, arg1, arg2),
                ))
            }
            899usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_raster_to_world_coord_y")?,
                    "st_raster_to_world_coord_y",
                )?;
                let arg1 = dv_i64(&args, 1, "st_raster_to_world_coord_y")? as u32;
                let arg2 = dv_i64(&args, 2, "st_raster_to_world_coord_y")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_px::st_raster_to_world_coord_y(&arg0, arg1, arg2),
                ))
            }
            900usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_raster_within")?, "st_raster_within")?;
                let arg1 =
                    from_raster_binary(dv_blob(&args, 1, "st_raster_within")?, "st_raster_within")?;
                Ok(types::Duckvalue::Boolean(pg_rast_pred::st_raster_within(
                    &arg0, &arg1,
                )))
            }
            901usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_rastercontains")?,
                    "st_rastercontains",
                )?;
                let arg1 = from_raster_binary(
                    dv_blob(&args, 1, "st_rastercontains")?,
                    "st_rastercontains",
                )?;
                Ok(types::Duckvalue::Boolean(pg_rast_pred::st_raster_contains(
                    &arg0, &arg1,
                )))
            }
            902usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_rastercontainsgeom")?,
                    "st_rastercontainsgeom",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_rastercontainsgeom")?,
                    "st_rastercontainsgeom",
                )?;
                Ok(types::Duckvalue::Boolean(
                    pg_rast_pred::st_raster_contains_geom(&arg0, &arg1),
                ))
            }
            903usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_rasterconvexhull")?,
                    "st_rasterconvexhull",
                )?;
                {
                    let __r = pg_rast_pred::st_raster_convex_hull(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rasterconvexhull: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            904usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_rastercovers")?, "st_rastercovers")?;
                let arg1 =
                    from_raster_binary(dv_blob(&args, 1, "st_rastercovers")?, "st_rastercovers")?;
                Ok(types::Duckvalue::Boolean(pg_rast_pred::st_raster_covers(
                    &arg0, &arg1,
                )))
            }
            905usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_rasterintersects")?,
                    "st_rasterintersects",
                )?;
                let arg1 = from_raster_binary(
                    dv_blob(&args, 1, "st_rasterintersects")?,
                    "st_rasterintersects",
                )?;
                Ok(types::Duckvalue::Boolean(
                    pg_rast_pred::st_raster_intersects(&arg0, &arg1),
                ))
            }
            906usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_rasterintersectsgeom")?,
                    "st_rasterintersectsgeom",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_rasterintersectsgeom")?,
                    "st_rasterintersectsgeom",
                )?;
                Ok(types::Duckvalue::Boolean(
                    pg_rast_pred::st_raster_intersects_geom(&arg0, &arg1),
                ))
            }
            907usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_rasteroverlaps")?,
                    "st_rasteroverlaps",
                )?;
                let arg1 = from_raster_binary(
                    dv_blob(&args, 1, "st_rasteroverlaps")?,
                    "st_rasteroverlaps",
                )?;
                Ok(types::Duckvalue::Boolean(pg_rast_pred::st_raster_overlaps(
                    &arg0, &arg1,
                )))
            }
            908usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_rastertoworldcoordx")?,
                    "st_rastertoworldcoordx",
                )?;
                let arg1 = dv_i64(&args, 1, "st_rastertoworldcoordx")? as u32;
                let arg2 = dv_i64(&args, 2, "st_rastertoworldcoordx")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_px::st_raster_to_world_coord_x(&arg0, arg1, arg2),
                ))
            }
            909usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_rastertoworldcoordy")?,
                    "st_rastertoworldcoordy",
                )?;
                let arg1 = dv_i64(&args, 1, "st_rastertoworldcoordy")? as u32;
                let arg2 = dv_i64(&args, 2, "st_rastertoworldcoordy")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_px::st_raster_to_world_coord_y(&arg0, arg1, arg2),
                ))
            }
            910usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_rasterwithin")?, "st_rasterwithin")?;
                let arg1 =
                    from_raster_binary(dv_blob(&args, 1, "st_rasterwithin")?, "st_rasterwithin")?;
                Ok(types::Duckvalue::Boolean(pg_rast_pred::st_raster_within(
                    &arg0, &arg1,
                )))
            }
            911usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_rastheight")?, "st_rastheight")?;
                Ok(types::Duckvalue::Int64(pg_rast_acc::st_height(&arg0) as i64))
            }
            912usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_rastnumbands")?, "st_rastnumbands")?;
                Ok(types::Duckvalue::Int64(
                    pg_rast_acc::st_num_bands(&arg0) as i64
                ))
            }
            913usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_rastwidth")?, "st_rastwidth")?;
                Ok(types::Duckvalue::Int64(pg_rast_acc::st_width(&arg0) as i64))
            }
            914usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_reclass")?, "st_reclass")?;
                let arg1 = dv_i64(&args, 1, "st_reclass")? as u32;
                let arg2 = dv_text(&args, 2, "st_reclass")?;
                let arg3 = match dv_i64(&args, 3, "st_reclass")? {
                    0 => pg_rast_types::PixelType::Bool1,
                    1 => pg_rast_types::PixelType::Uint8,
                    2 => pg_rast_types::PixelType::Int8,
                    3 => pg_rast_types::PixelType::Uint16,
                    4 => pg_rast_types::PixelType::Int16,
                    5 => pg_rast_types::PixelType::Uint32,
                    6 => pg_rast_types::PixelType::Int32,
                    7 => pg_rast_types::PixelType::Float32,
                    8 => pg_rast_types::PixelType::Float64,
                    other => {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_reclass: arg 3 (pixel-type) out of range: {} (valid 0..9)",
                            other,
                        )))
                    }
                };
                {
                    let __r = pg_rast_ma::st_reclass(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_reclass: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            915usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_reclass_exact")?, "st_reclass_exact")?;
                let arg1 = dv_i64(&args, 1, "st_reclass_exact")? as u32;
                let arg2 = dv_text(&args, 2, "st_reclass_exact")?;
                let arg3 = match dv_i64(&args, 3, "st_reclass_exact")? {
                    0 => pg_rast_types::PixelType::Bool1,
                    1 => pg_rast_types::PixelType::Uint8,
                    2 => pg_rast_types::PixelType::Int8,
                    3 => pg_rast_types::PixelType::Uint16,
                    4 => pg_rast_types::PixelType::Int16,
                    5 => pg_rast_types::PixelType::Uint32,
                    6 => pg_rast_types::PixelType::Int32,
                    7 => pg_rast_types::PixelType::Float32,
                    8 => pg_rast_types::PixelType::Float64,
                    other => {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_reclass_exact: arg 3 (pixel-type) out of range: {} (valid 0..9)",
                            other,
                        )))
                    }
                };
                {
                    let __r =
                        pg_rast_ma::st_reclass_exact(&arg0, arg1, arg2, arg3).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_reclass_exact: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            916usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_reduce_precision")?,
                    "st_reduce_precision",
                )?;
                let arg1 = dv_f64(&args, 1, "st_reduce_precision")?;
                {
                    let __r = pg_proc::st_reduce_precision(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_reduce_precision: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            917usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_reduce_precision_flags")?,
                    "st_reduce_precision_flags",
                )?;
                let arg1 = dv_f64(&args, 1, "st_reduce_precision_flags")?;
                let arg2 = dv_i64(&args, 2, "st_reduce_precision_flags")? as u32;
                {
                    let __r =
                        pg_proc::st_reduce_precision_flags(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_reduce_precision_flags: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            918usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_reduceprecision")?,
                    "st_reduceprecision",
                )?;
                let arg1 = dv_f64(&args, 1, "st_reduceprecision")?;
                {
                    let __r = pg_proc::st_reduce_precision(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_reduceprecision: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            919usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_relate")?, "st_relate")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_relate")?, "st_relate")?;
                Ok(types::Duckvalue::Text(
                    (pg_pred::st_relate(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_relate: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            920usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_relate_bnr")?, "st_relate_bnr")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_relate_bnr")?, "st_relate_bnr")?;
                let arg2 = dv_i64(&args, 2, "st_relate_bnr")? as u32;
                Ok(types::Duckvalue::Text(
                    (pg_pred::st_relate_bnr(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_relate_bnr: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            921usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_relate_match")?, "st_relate_match")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_relate_match")?, "st_relate_match")?;
                let arg2 = dv_text(&args, 2, "st_relate_match")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_relate_match(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_relate_match: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            922usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_relate_with_pattern")?,
                    "st_relate_with_pattern",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_relate_with_pattern")?,
                    "st_relate_with_pattern",
                )?;
                let arg2 = dv_text(&args, 2, "st_relate_with_pattern")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_relate_with_pattern(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_relate_with_pattern: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            923usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_relatematch")?, "st_relatematch")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_relatematch")?, "st_relatematch")?;
                let arg2 = dv_text(&args, 2, "st_relatematch")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_relate_match(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_relatematch: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            924usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_remove_point")?, "st_remove_point")?;
                let arg1 = dv_i64(&args, 1, "st_remove_point")? as u32;
                {
                    let __r = pg_lin::st_remove_point(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_remove_point: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            925usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_remove_repeated_points")?,
                    "st_remove_repeated_points",
                )?;
                {
                    let __r = pg_proc::st_remove_repeated_points(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_remove_repeated_points: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            926usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_remove_small_parts")?,
                    "st_remove_small_parts",
                )?;
                let arg1 = dv_f64(&args, 1, "st_remove_small_parts")?;
                {
                    let __r = pg_proc::st_remove_small_parts(&arg0, arg1, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_remove_small_parts: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            927usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_removepoint")?, "st_removepoint")?;
                let arg1 = dv_i64(&args, 1, "st_removepoint")? as u32;
                {
                    let __r = pg_lin::st_remove_point(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_removepoint: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            928usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_removerepeatedpoints")?,
                    "st_removerepeatedpoints",
                )?;
                {
                    let __r = pg_proc::st_remove_repeated_points(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_removerepeatedpoints: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            929usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_removesmallparts")?,
                    "st_removesmallparts",
                )?;
                let arg1 = dv_f64(&args, 1, "st_removesmallparts")?;
                {
                    let __r = pg_proc::st_remove_small_parts(&arg0, arg1, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_removesmallparts: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            930usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_rescale")?, "st_rescale")?;
                let arg1 = dv_f64(&args, 1, "st_rescale")?;
                let arg2 = dv_f64(&args, 2, "st_rescale")?;
                let arg3 = dv_text(&args, 3, "st_rescale")?;
                {
                    let __r = pg_rast_proc::st_rescale(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rescale: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            931usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_resize")?, "st_resize")?;
                let arg1 = dv_i64(&args, 1, "st_resize")? as u32;
                let arg2 = dv_i64(&args, 2, "st_resize")? as u32;
                let arg3 = dv_text(&args, 3, "st_resize")?;
                {
                    let __r = pg_rast_proc::st_resize(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_resize: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            932usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_retile")?, "st_retile")?;
                let arg1 = dv_i64(&args, 1, "st_retile")? as u32;
                let arg2 = dv_i64(&args, 2, "st_retile")? as u32;
                let arg3 = dv_text(&args, 3, "st_retile")?;
                {
                    let __r =
                        postgis_raster_tile::st_retile(&arg0, arg1, arg2, arg3).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_retile: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    match __r.into_iter().next() {
                        Some(r) => Ok(types::Duckvalue::Blob(r.as_binary().into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            933usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_reverse")?, "st_reverse")?;
                {
                    let __r = pg_proc::st_reverse(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_reverse: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            934usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_reverse_threed")?, "st_reverse_threed")?;
                {
                    let __r = pg_threed::st_reverse_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_reverse_threed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            935usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_reversethreed")?, "st_reversethreed")?;
                {
                    let __r = pg_threed::st_reverse_threed(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_reversethreed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            936usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_rotate")?, "st_rotate")?;
                let arg1 = dv_f64(&args, 1, "st_rotate")?;
                {
                    let __r = pg_xform::st_rotate(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rotate: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            937usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_rotate_around_point")?,
                    "st_rotate_around_point",
                )?;
                let arg1 = dv_f64(&args, 1, "st_rotate_around_point")?;
                let arg2 = from_wkb(
                    dv_blob(&args, 2, "st_rotate_around_point")?,
                    "st_rotate_around_point",
                )?;
                {
                    let __r =
                        pg_xform::st_rotate_around_point(&arg0, arg1, &arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_rotate_around_point: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            938usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_rotate_around_xy")?,
                    "st_rotate_around_xy",
                )?;
                let arg1 = dv_f64(&args, 1, "st_rotate_around_xy")?;
                let arg2 = dv_f64(&args, 2, "st_rotate_around_xy")?;
                let arg3 = dv_f64(&args, 3, "st_rotate_around_xy")?;
                {
                    let __r =
                        pg_xform::st_rotate_around_xy(&arg0, arg1, arg2, arg3).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_rotate_around_xy: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            939usize => {
                let arg0 = dv_blob(&args, 0, "st_rotate_threed")?;
                let arg1 = dv_f64(&args, 1, "st_rotate_threed")?;
                let arg2 = dv_f64(&args, 2, "st_rotate_threed")?;
                let arg3 = dv_f64(&args, 3, "st_rotate_threed")?;
                let arg4 = dv_f64(&args, 4, "st_rotate_threed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_rotate_threed(arg0, arg1, arg2, arg3, arg4).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rotate_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            940usize => {
                let arg0 = dv_blob(&args, 0, "st_rotate_threed_around_center")?;
                let arg1 = dv_f64(&args, 1, "st_rotate_threed_around_center")?;
                let arg2 = dv_f64(&args, 2, "st_rotate_threed_around_center")?;
                let arg3 = dv_f64(&args, 3, "st_rotate_threed_around_center")?;
                let arg4 = dv_f64(&args, 4, "st_rotate_threed_around_center")?;
                let arg5 = dv_f64(&args, 5, "st_rotate_threed_around_center")?;
                let arg6 = dv_f64(&args, 6, "st_rotate_threed_around_center")?;
                let arg7 = dv_f64(&args, 7, "st_rotate_threed_around_center")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_rotate_threed_around_center(
                        arg0, arg1, arg2, arg3, arg4, arg5, arg6, arg7,
                    )
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rotate_threed_around_center: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            941usize => {
                let arg0 = dv_blob(&args, 0, "st_rotate_twod_around_center")?;
                let arg1 = dv_f64(&args, 1, "st_rotate_twod_around_center")?;
                let arg2 = dv_f64(&args, 2, "st_rotate_twod_around_center")?;
                let arg3 = dv_f64(&args, 3, "st_rotate_twod_around_center")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_rotate_twod_around_center(arg0, arg1, arg2, arg3).map_err(
                        |e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_rotate_twod_around_center: {}",
                                shim_err_string(e)
                            ))
                        },
                    )?)
                    .into(),
                ))
            }
            942usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_rotate_x")?, "st_rotate_x")?;
                let arg1 = dv_f64(&args, 1, "st_rotate_x")?;
                {
                    let __r = pg_xform::st_rotate_x(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rotate_x: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            943usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_rotate_y")?, "st_rotate_y")?;
                let arg1 = dv_f64(&args, 1, "st_rotate_y")?;
                {
                    let __r = pg_xform::st_rotate_y(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rotate_y: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            944usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_rotate_z")?, "st_rotate_z")?;
                let arg1 = dv_f64(&args, 1, "st_rotate_z")?;
                {
                    let __r = pg_xform::st_rotate_z(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rotate_z: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            945usize => {
                let arg0 = dv_blob(&args, 0, "st_rotatethreed")?;
                let arg1 = dv_f64(&args, 1, "st_rotatethreed")?;
                let arg2 = dv_f64(&args, 2, "st_rotatethreed")?;
                let arg3 = dv_f64(&args, 3, "st_rotatethreed")?;
                let arg4 = dv_f64(&args, 4, "st_rotatethreed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_rotate_threed(arg0, arg1, arg2, arg3, arg4).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rotatethreed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            946usize => {
                let arg0 = dv_blob(&args, 0, "st_rotatex")?;
                let arg1 = dv_f64(&args, 1, "st_rotatex")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_rotate_x(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rotatex: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            947usize => {
                let arg0 = dv_blob(&args, 0, "st_rotatey")?;
                let arg1 = dv_f64(&args, 1, "st_rotatey")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_rotate_y(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rotatey: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            948usize => {
                let arg0 = dv_blob(&args, 0, "st_rotatez")?;
                let arg1 = dv_f64(&args, 1, "st_rotatez")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_rotate_z(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_rotatez: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            949usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_rotation")?, "st_rotation")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_rotation(&arg0)))
            }
            950usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_roughness")?, "st_roughness")?;
                let arg1 = dv_i64(&args, 1, "st_roughness")? as u32;
                {
                    let __r = pg_rast_proc::st_roughness(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_roughness: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            951usize => {
                let arg0 = dv_blob(&args, 0, "st_round")?;
                let arg1 = dv_i64(&args, 1, "st_round")? as i32;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_round(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_round: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            952usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_scale")?, "st_scale")?;
                let arg1 = dv_f64(&args, 1, "st_scale")?;
                let arg2 = dv_f64(&args, 2, "st_scale")?;
                {
                    let __r = pg_xform::st_scale(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_scale: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            953usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_scale_threed")?, "st_scale_threed")?;
                let arg1 = dv_f64(&args, 1, "st_scale_threed")?;
                let arg2 = dv_f64(&args, 2, "st_scale_threed")?;
                let arg3 = dv_f64(&args, 3, "st_scale_threed")?;
                {
                    let __r = pg_xform::st_scale_threed(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_scale_threed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            954usize => {
                let arg0 = dv_blob(&args, 0, "st_scale_threed_around_center")?;
                let arg1 = dv_f64(&args, 1, "st_scale_threed_around_center")?;
                let arg2 = dv_f64(&args, 2, "st_scale_threed_around_center")?;
                let arg3 = dv_f64(&args, 3, "st_scale_threed_around_center")?;
                let arg4 = dv_f64(&args, 4, "st_scale_threed_around_center")?;
                let arg5 = dv_f64(&args, 5, "st_scale_threed_around_center")?;
                let arg6 = dv_f64(&args, 6, "st_scale_threed_around_center")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_scale_threed_around_center(
                        arg0, arg1, arg2, arg3, arg4, arg5, arg6,
                    )
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_scale_threed_around_center: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            955usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_scale_with_origin")?,
                    "st_scale_with_origin",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_scale_with_origin")?,
                    "st_scale_with_origin",
                )?;
                let arg2 = from_wkb(
                    dv_blob(&args, 2, "st_scale_with_origin")?,
                    "st_scale_with_origin",
                )?;
                {
                    let __r = pg_xform::st_scale_with_origin(&arg0, &arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_scale_with_origin: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            956usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_scale_with_vector")?,
                    "st_scale_with_vector",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_scale_with_vector")?,
                    "st_scale_with_vector",
                )?;
                {
                    let __r = pg_xform::st_scale_with_vector(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_scale_with_vector: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            957usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_scale_x")?, "st_scale_x")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_scale_x(&arg0)))
            }
            958usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_scale_y")?, "st_scale_y")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_scale_y(&arg0)))
            }
            959usize => {
                let arg0 = dv_blob(&args, 0, "st_scalethreed")?;
                let arg1 = dv_f64(&args, 1, "st_scalethreed")?;
                let arg2 = dv_f64(&args, 2, "st_scalethreed")?;
                let arg3 = dv_f64(&args, 3, "st_scalethreed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_scale_threed(arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_scalethreed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            960usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_scalex")?, "st_scalex")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_scale_x(&arg0)))
            }
            961usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_scaley")?, "st_scaley")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_scale_y(&arg0)))
            }
            962usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_scroll")?, "st_scroll")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_scroll")?, "st_scroll")?;
                {
                    let __r = pg_proc::st_scroll(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_scroll: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            963usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_segmentize")?, "st_segmentize")?;
                let arg1 = dv_f64(&args, 1, "st_segmentize")?;
                {
                    let __r = pg_proc::st_segmentize(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_segmentize: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            964usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_set_band_is_nodata")?,
                    "st_set_band_is_nodata",
                )?;
                let arg1 = dv_i64(&args, 1, "st_set_band_is_nodata")? as u32;
                {
                    let __r = pg_rast_ctor::st_set_band_is_nodata(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_set_band_is_nodata: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            965usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_set_band_nodata_value")?,
                    "st_set_band_nodata_value",
                )?;
                let arg1 = dv_i64(&args, 1, "st_set_band_nodata_value")? as u32;
                {
                    let __r =
                        pg_rast_ctor::st_set_band_nodata_value(&arg0, arg1, None).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_set_band_nodata_value: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            966usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_set_effective_area")?,
                    "st_set_effective_area",
                )?;
                {
                    let __r = pg_proc::st_set_effective_area(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_set_effective_area: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            967usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_set_geo_reference")?,
                    "st_set_geo_reference",
                )?;
                let arg1 = dv_f64(&args, 1, "st_set_geo_reference")?;
                let arg2 = dv_f64(&args, 2, "st_set_geo_reference")?;
                let arg3 = dv_f64(&args, 3, "st_set_geo_reference")?;
                let arg4 = dv_f64(&args, 4, "st_set_geo_reference")?;
                let arg5 = dv_f64(&args, 5, "st_set_geo_reference")?;
                let arg6 = dv_f64(&args, 6, "st_set_geo_reference")?;
                {
                    let __r = pg_rast_ctor::st_set_geo_reference(
                        &arg0, arg1, arg2, arg3, arg4, arg5, arg6,
                    )
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_set_geo_reference: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            968usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_set_geo_reference_string")?,
                    "st_set_geo_reference_string",
                )?;
                let arg1 = dv_text(&args, 1, "st_set_geo_reference_string")?;
                let arg2 = dv_text(&args, 2, "st_set_geo_reference_string")?;
                {
                    let __r = pg_rast_ctor::st_set_geo_reference_string(&arg0, arg1, arg2)
                        .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_set_geo_reference_string: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            969usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_set_point")?, "st_set_point")?;
                let arg1 = dv_i64(&args, 1, "st_set_point")? as u32;
                let arg2 = from_wkb(dv_blob(&args, 2, "st_set_point")?, "st_set_point")?;
                {
                    let __r = pg_lin::st_set_point(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_set_point: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            970usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_set_rotation")?, "st_set_rotation")?;
                let arg1 = dv_f64(&args, 1, "st_set_rotation")?;
                {
                    let __r = pg_rast_ctor::st_set_rotation(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_set_rotation: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            971usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_set_scale")?, "st_set_scale")?;
                let arg1 = dv_f64(&args, 1, "st_set_scale")?;
                let arg2 = dv_f64(&args, 2, "st_set_scale")?;
                {
                    let __r = pg_rast_ctor::st_set_scale(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_set_scale: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            972usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_set_scale_xy")?, "st_set_scale_xy")?;
                let arg1 = dv_f64(&args, 1, "st_set_scale_xy")?;
                {
                    let __r = pg_rast_ctor::st_set_scale_xy(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_set_scale_xy: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            973usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_set_skew")?, "st_set_skew")?;
                let arg1 = dv_f64(&args, 1, "st_set_skew")?;
                let arg2 = dv_f64(&args, 2, "st_set_skew")?;
                {
                    let __r = pg_rast_ctor::st_set_skew(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_set_skew: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            974usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_set_skew_xy")?, "st_set_skew_xy")?;
                let arg1 = dv_f64(&args, 1, "st_set_skew_xy")?;
                {
                    let __r = pg_rast_ctor::st_set_skew_xy(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_set_skew_xy: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            975usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_set_srid")?, "st_set_srid")?;
                let arg1 = dv_i64(&args, 1, "st_set_srid")? as i32;
                {
                    let __r = pg_rast_ctor::st_set_srid(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_set_srid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            976usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_set_upper_left")?,
                    "st_set_upper_left",
                )?;
                let arg1 = dv_f64(&args, 1, "st_set_upper_left")?;
                let arg2 = dv_f64(&args, 2, "st_set_upper_left")?;
                {
                    let __r = pg_rast_ctor::st_set_upper_left(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_set_upper_left: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            977usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_set_value")?, "st_set_value")?;
                let arg1 = dv_i64(&args, 1, "st_set_value")? as u32;
                let arg2 = dv_i64(&args, 2, "st_set_value")? as u32;
                let arg3 = dv_i64(&args, 3, "st_set_value")? as u32;
                let arg4 = dv_f64(&args, 4, "st_set_value")?;
                {
                    let __r =
                        pg_rast_acc::st_set_value(&arg0, arg1, arg2, arg3, arg4).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_set_value: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            978usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_set_value_at_point")?,
                    "st_set_value_at_point",
                )?;
                let arg1 = dv_i64(&args, 1, "st_set_value_at_point")? as u32;
                let arg2 = from_wkb(
                    dv_blob(&args, 2, "st_set_value_at_point")?,
                    "st_set_value_at_point",
                )?;
                let arg3 = dv_f64(&args, 3, "st_set_value_at_point")?;
                {
                    let __r = pg_rast_px::st_set_value_at_point(&arg0, arg1, &arg2, arg3).map_err(
                        |e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_set_value_at_point: {}",
                                shim_err_string(e)
                            ))
                        },
                    )?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            979usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_set_values")?, "st_set_values")?;
                let arg1 = dv_i64(&args, 1, "st_set_values")? as u32;
                let arg2 = dv_i64(&args, 2, "st_set_values")? as u32;
                let arg3 = dv_i64(&args, 3, "st_set_values")? as u32;
                let arg4: Vec<Vec<f64>> = parse_json_list_list_f64(&args, 4, "st_set_values")?;
                {
                    let __r =
                        pg_rast_px::st_set_values(&arg0, arg1, arg2, arg3, &arg4).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_set_values: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            980usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_set_values_rect")?,
                    "st_set_values_rect",
                )?;
                let arg1 = dv_i64(&args, 1, "st_set_values_rect")? as u32;
                let arg2 = dv_i64(&args, 2, "st_set_values_rect")? as u32;
                let arg3 = dv_i64(&args, 3, "st_set_values_rect")? as u32;
                let arg4 = dv_i64(&args, 4, "st_set_values_rect")? as u32;
                let arg5 = dv_i64(&args, 5, "st_set_values_rect")? as u32;
                let arg6 = dv_f64(&args, 6, "st_set_values_rect")?;
                {
                    let __r =
                        pg_rast_px::st_set_values_rect(&arg0, arg1, arg2, arg3, arg4, arg5, arg6)
                            .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_set_values_rect: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            981usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_seteffectivearea")?,
                    "st_seteffectivearea",
                )?;
                {
                    let __r = pg_proc::st_set_effective_area(&arg0, None).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_seteffectivearea: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            982usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_setendm")?, "st_setendm")?;
                let arg1 = dv_f64(&args, 1, "st_setendm")?;
                {
                    let __r = pg_xform::st_setendm(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_setendm: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            983usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_setendz")?, "st_setendz")?;
                let arg1 = dv_f64(&args, 1, "st_setendz")?;
                {
                    let __r = pg_xform::st_setendz(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_setendz: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            984usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_setm")?, "st_setm")?;
                let arg1 = dv_f64(&args, 1, "st_setm")?;
                {
                    let __r = pg_xform::st_setm(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_setm: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            985usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_setpoint")?, "st_setpoint")?;
                let arg1 = dv_i64(&args, 1, "st_setpoint")? as u32;
                let arg2 = from_wkb(dv_blob(&args, 2, "st_setpoint")?, "st_setpoint")?;
                {
                    let __r = pg_lin::st_set_point(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_setpoint: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            986usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_setsrid")?, "st_setsrid")?;
                let arg1 = dv_i64(&args, 1, "st_setsrid")? as i32;
                Ok(types::Duckvalue::Blob(
                    pg_acc::st_set_srid(&arg0, arg1).as_wkb().into(),
                ))
            }
            987usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_setstartm")?, "st_setstartm")?;
                let arg1 = dv_f64(&args, 1, "st_setstartm")?;
                {
                    let __r = pg_xform::st_setstartm(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_setstartm: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            988usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_setstartz")?, "st_setstartz")?;
                let arg1 = dv_f64(&args, 1, "st_setstartz")?;
                {
                    let __r = pg_xform::st_setstartz(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_setstartz: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            989usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_setvalue")?, "st_setvalue")?;
                let arg1 = dv_i64(&args, 1, "st_setvalue")? as u32;
                let arg2 = dv_i64(&args, 2, "st_setvalue")? as u32;
                let arg3 = dv_i64(&args, 3, "st_setvalue")? as u32;
                let arg4 = dv_f64(&args, 4, "st_setvalue")?;
                {
                    let __r =
                        pg_rast_acc::st_set_value(&arg0, arg1, arg2, arg3, arg4).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_setvalue: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            990usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_setx")?, "st_setx")?;
                let arg1 = dv_f64(&args, 1, "st_setx")?;
                {
                    let __r = pg_xform::st_setx(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_setx: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            991usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_sety")?, "st_sety")?;
                let arg1 = dv_f64(&args, 1, "st_sety")?;
                {
                    let __r = pg_xform::st_sety(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_sety: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            992usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_setz")?, "st_setz")?;
                let arg1 = dv_f64(&args, 1, "st_setz")?;
                {
                    let __r = pg_xform::st_setz(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_setz: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            993usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_shared_paths")?, "st_shared_paths")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_shared_paths")?, "st_shared_paths")?;
                {
                    let __r = pg_proc::st_shared_paths(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_shared_paths: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            994usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_sharedpaths")?, "st_sharedpaths")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_sharedpaths")?, "st_sharedpaths")?;
                {
                    let __r = pg_proc::st_shared_paths(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_sharedpaths: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            995usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_shift_longitude")?,
                    "st_shift_longitude",
                )?;
                {
                    let __r = pg_xform::st_shift_longitude(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_shift_longitude: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            996usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_shiftlongitude")?, "st_shiftlongitude")?;
                {
                    let __r = pg_xform::st_shift_longitude(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_shiftlongitude: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            997usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_shortest_line")?, "st_shortest_line")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_shortest_line")?, "st_shortest_line")?;
                {
                    let __r = pg_meas::st_shortest_line(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_shortest_line: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            998usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_shortest_line_3d")?,
                    "st_shortest_line_3d",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_shortest_line_3d")?,
                    "st_shortest_line_3d",
                )?;
                {
                    let __r = pg_meas::st_shortest_line_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_shortest_line_3d: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            999usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_shortest_line_threed")?,
                    "st_shortest_line_threed",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_shortest_line_threed")?,
                    "st_shortest_line_threed",
                )?;
                {
                    let __r = pg_meas::st_shortest_line_threed(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_shortest_line_threed: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1000usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_shortestline")?, "st_shortestline")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_shortestline")?, "st_shortestline")?;
                {
                    let __r = pg_meas::st_shortest_line(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_shortestline: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1001usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "st_simplify")?, "st_simplify")?;
                let arg1 = dv_f64(&args, 1, "st_simplify")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::st_simplify(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_simplify: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1002usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_simplify_batch")?;
                let arg1: Vec<f64> = parse_json_list_f64(&args, 1, "st_simplify_batch")?;
                {
                    let __r = postgis_batch::st_simplify_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_simplify_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            1003usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_simplify_polygon_hull")?,
                    "st_simplify_polygon_hull",
                )?;
                let arg1 = dv_f64(&args, 1, "st_simplify_polygon_hull")?;
                let arg2 = dv_bool(&args, 2, "st_simplify_polygon_hull")?;
                {
                    let __r =
                        pg_proc::st_simplify_polygon_hull(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_simplify_polygon_hull: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1004usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_simplify_preserve_collapse")?,
                    "st_simplify_preserve_collapse",
                )?;
                let arg1 = dv_f64(&args, 1, "st_simplify_preserve_collapse")?;
                let arg2 = dv_bool(&args, 2, "st_simplify_preserve_collapse")?;
                {
                    let __r =
                        pg_proc::st_simplify_preserve_collapse(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_simplify_preserve_collapse: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1005usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_simplify_preserve_topology")?,
                    "st_simplify_preserve_topology",
                )?;
                let arg1 = dv_f64(&args, 1, "st_simplify_preserve_topology")?;
                {
                    let __r = pg_proc::st_simplify_preserve_topology(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_simplify_preserve_topology: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1006usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_simplify_preserve_topology_batch")?;
                let arg1: Vec<f64> =
                    parse_json_list_f64(&args, 1, "st_simplify_preserve_topology_batch")?;
                {
                    let __r = postgis_batch::st_simplify_preserve_topology_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_simplify_preserve_topology_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            1007usize => {
                let arg0 = dv_blob(&args, 0, "st_simplify_preserve_topology_threed")?;
                let arg1 = dv_f64(&args, 1, "st_simplify_preserve_topology_threed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_simplify_preserve_topology_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_simplify_preserve_topology_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1008usize => {
                let arg0 = dv_blob(&args, 0, "st_simplify_threed")?;
                let arg1 = dv_f64(&args, 1, "st_simplify_threed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_simplify_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_simplify_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1009usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_simplify_vw")?, "st_simplify_vw")?;
                let arg1 = dv_f64(&args, 1, "st_simplify_vw")?;
                {
                    let __r = pg_proc::st_simplify_vw(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_simplify_vw: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1010usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_simplify_vw_preserve_collapse")?,
                    "st_simplify_vw_preserve_collapse",
                )?;
                let arg1 = dv_f64(&args, 1, "st_simplify_vw_preserve_collapse")?;
                let arg2 = dv_bool(&args, 2, "st_simplify_vw_preserve_collapse")?;
                {
                    let __r = pg_proc::st_simplify_vw_preserve_collapse(&arg0, arg1, arg2)
                        .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_simplify_vw_preserve_collapse: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1011usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_simplifypolygonhull")?,
                    "st_simplifypolygonhull",
                )?;
                let arg1 = dv_f64(&args, 1, "st_simplifypolygonhull")?;
                let arg2 = dv_bool(&args, 2, "st_simplifypolygonhull")?;
                {
                    let __r =
                        pg_proc::st_simplify_polygon_hull(&arg0, arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_simplifypolygonhull: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1012usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_simplifypreservetopology")?,
                    "st_simplifypreservetopology",
                )?;
                let arg1 = dv_f64(&args, 1, "st_simplifypreservetopology")?;
                {
                    let __r = pg_proc::st_simplify_preserve_topology(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_simplifypreservetopology: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1013usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_simplifyvw")?, "st_simplifyvw")?;
                let arg1 = dv_f64(&args, 1, "st_simplifyvw")?;
                {
                    let __r = pg_proc::st_simplify_vw(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_simplifyvw: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1014usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_skew_x")?, "st_skew_x")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_skew_x(&arg0)))
            }
            1015usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_skew_y")?, "st_skew_y")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_skew_y(&arg0)))
            }
            1016usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_skewx")?, "st_skewx")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_skew_x(&arg0)))
            }
            1017usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_skewy")?, "st_skewy")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_skew_y(&arg0)))
            }
            1018usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_slope")?, "st_slope")?;
                let arg1 = dv_i64(&args, 1, "st_slope")? as u32;
                {
                    let __r = pg_rast_proc::st_slope(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_slope: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            1019usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_snap")?, "st_snap")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_snap")?, "st_snap")?;
                let arg2 = dv_f64(&args, 2, "st_snap")?;
                {
                    let __r = pg_proc::st_snap(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_snap: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1020usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_snap_to_grid")?, "st_snap_to_grid")?;
                let arg1 = dv_f64(&args, 1, "st_snap_to_grid")?;
                {
                    let __r = pg_proc::st_snap_to_grid(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_snap_to_grid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1021usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_snap_to_grid_fourd")?,
                    "st_snap_to_grid_fourd",
                )?;
                let arg1 = dv_f64(&args, 1, "st_snap_to_grid_fourd")?;
                let arg2 = dv_f64(&args, 2, "st_snap_to_grid_fourd")?;
                let arg3 = dv_f64(&args, 3, "st_snap_to_grid_fourd")?;
                let arg4 = dv_f64(&args, 4, "st_snap_to_grid_fourd")?;
                let arg5 = dv_f64(&args, 5, "st_snap_to_grid_fourd")?;
                let arg6 = dv_f64(&args, 6, "st_snap_to_grid_fourd")?;
                {
                    let __r =
                        pg_proc::st_snap_to_grid_fourd(&arg0, arg1, arg2, arg3, arg4, arg5, arg6)
                            .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_snap_to_grid_fourd: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1022usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_snap_to_grid_origin")?,
                    "st_snap_to_grid_origin",
                )?;
                let arg1 = dv_f64(&args, 1, "st_snap_to_grid_origin")?;
                let arg2 = dv_f64(&args, 2, "st_snap_to_grid_origin")?;
                let arg3 = dv_f64(&args, 3, "st_snap_to_grid_origin")?;
                let arg4 = dv_f64(&args, 4, "st_snap_to_grid_origin")?;
                {
                    let __r = pg_proc::st_snap_to_grid_origin(&arg0, arg1, arg2, arg3, arg4)
                        .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_snap_to_grid_origin: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1023usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_snap_to_grid_xy")?,
                    "st_snap_to_grid_xy",
                )?;
                let arg1 = dv_f64(&args, 1, "st_snap_to_grid_xy")?;
                let arg2 = dv_f64(&args, 2, "st_snap_to_grid_xy")?;
                {
                    let __r = pg_proc::st_snap_to_grid_xy(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_snap_to_grid_xy: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1024usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_snaptogrid")?, "st_snaptogrid")?;
                let arg1 = dv_f64(&args, 1, "st_snaptogrid")?;
                {
                    let __r = pg_proc::st_snap_to_grid(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_snaptogrid: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1025usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_split")?, "st_split")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_split")?, "st_split")?;
                {
                    let __r = pg_proc::st_split(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_split: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1026usize => {
                let arg0 = dv_f64(&args, 0, "st_square")?;
                let arg1 = dv_f64(&args, 1, "st_square")?;
                let arg2 = dv_f64(&args, 2, "st_square")?;
                let arg3 = dv_i64(&args, 3, "st_square")? as i32;
                let arg4 = dv_i64(&args, 4, "st_square")? as i32;
                {
                    let __r = pg_ctor::st_square(arg0, arg1, arg2, arg3, arg4).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_square: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1027usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_srid")?, "st_srid")?;
                Ok(types::Duckvalue::Int64(pg_rast_acc::st_srid(&arg0) as i64))
            }
            1028usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_start_point")?, "st_start_point")?;
                {
                    let __r = pg_acc::st_start_point(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_start_point: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1029usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_start_point_batch")?;
                {
                    let __r = postgis_batch::st_start_point_batch(&arg0);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_start_point_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            1030usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_startpoint")?, "st_startpoint")?;
                {
                    let __r = pg_acc::st_start_point(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_startpoint: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1031usize => {
                let arg0 = dv_blob(&args, 0, "st_straight_skeleton")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_straight_skeleton(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_straight_skeleton: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1032usize => {
                let arg0 = dv_blob(&args, 0, "st_straight_skeleton_distance")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_straight_skeleton_distance(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_straight_skeleton_distance: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1033usize => {
                let arg0 = dv_blob(&args, 0, "st_straight_skeleton_partition")?;
                let arg1 = dv_bool(&args, 1, "st_straight_skeleton_partition")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_straight_skeleton_partition(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_straight_skeleton_partition: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1034usize => {
                let arg0 = dv_blob(&args, 0, "st_straightskeleton")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_straight_skeleton(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_straightskeleton: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1035usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_subdivide_gridsize")?,
                    "st_subdivide_gridsize",
                )?;
                let arg1 = dv_i64(&args, 1, "st_subdivide_gridsize")? as u32;
                let arg2 = dv_f64(&args, 2, "st_subdivide_gridsize")?;
                {
                    let __r: Vec<Geometry> = pg_proc::st_subdivide_gridsize(&arg0, arg1, arg2)
                        .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_subdivide_gridsize: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    match __r.first() {
                        Some(g) => Ok(types::Duckvalue::Blob(g.as_wkb().into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1036usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_summary")?, "st_summary")?;
                Ok(types::Duckvalue::Text((pg_out::st_summary(&arg0)).into()))
            }
            1037usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_summary_stats")?, "st_summary_stats")?;
                let arg1 = dv_i64(&args, 1, "st_summary_stats")? as u32;
                ret_to_witvalue_summary_stats(
                    pg_rast_stats::st_summary_stats(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_summary_stats: {}",
                            shim_err_string(e)
                        ))
                    })?,
                )
            }
            1038usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_summarystats")?, "st_summarystats")?;
                let arg1 = dv_i64(&args, 1, "st_summarystats")? as u32;
                ret_to_witvalue_summary_stats(
                    pg_rast_stats::st_summary_stats(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_summarystats: {}",
                            shim_err_string(e)
                        ))
                    })?,
                )
            }
            1039usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_swap_ordinates")?, "st_swap_ordinates")?;
                let arg1 = dv_text(&args, 1, "st_swap_ordinates")?;
                {
                    let __r = pg_xform::st_swap_ordinates(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_swap_ordinates: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1040usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_swapordinates")?, "st_swapordinates")?;
                let arg1 = dv_text(&args, 1, "st_swapordinates")?;
                {
                    let __r = pg_xform::st_swap_ordinates(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_swapordinates: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1041usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_sym_difference")?, "st_sym_difference")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_sym_difference")?, "st_sym_difference")?;
                {
                    let __r = pg_proc::st_sym_difference(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_sym_difference: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1042usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_sym_difference_batch")?;
                let arg1: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 1, "st_sym_difference_batch")?;
                {
                    let __r = postgis_batch::st_sym_difference_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_sym_difference_batch: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            1043usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_sym_difference_gridsize")?,
                    "st_sym_difference_gridsize",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "st_sym_difference_gridsize")?,
                    "st_sym_difference_gridsize",
                )?;
                let arg2 = dv_f64(&args, 2, "st_sym_difference_gridsize")?;
                {
                    let __r =
                        pg_proc::st_sym_difference_gridsize(&arg0, &arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_sym_difference_gridsize: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1044usize => {
                let arg0 = dv_blob(&args, 0, "st_sym_difference_threed")?;
                let arg1 = dv_blob(&args, 1, "st_sym_difference_threed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_sym_difference_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_sym_difference_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1045usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_symdifference")?, "st_symdifference")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_symdifference")?, "st_symdifference")?;
                {
                    let __r = pg_proc::st_sym_difference(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_symdifference: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1046usize => {
                let arg0 = dv_blob(&args, 0, "st_tesselate")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_tesselate(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_tesselate: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1047usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_tile")?, "st_tile")?;
                let arg1 = dv_i64(&args, 1, "st_tile")? as u32;
                let arg2 = dv_i64(&args, 2, "st_tile")? as u32;
                {
                    let __r = postgis_raster_tile::st_tile(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_tile: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.into_iter().next() {
                        Some(r) => Ok(types::Duckvalue::Blob(r.as_binary().into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1048usize => {
                let arg0 = dv_i64(&args, 0, "st_tile_envelope")? as u32;
                let arg1 = dv_i64(&args, 1, "st_tile_envelope")? as u32;
                let arg2 = dv_i64(&args, 2, "st_tile_envelope")? as u32;
                {
                    let __r =
                        pg_ctor::st_tile_envelope(arg0, arg1, arg2, None, None).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_tile_envelope: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1049usize => {
                let arg0 = dv_i64(&args, 0, "st_tileenvelope")? as u32;
                let arg1 = dv_i64(&args, 1, "st_tileenvelope")? as u32;
                let arg2 = dv_i64(&args, 2, "st_tileenvelope")? as u32;
                {
                    let __r =
                        pg_ctor::st_tile_envelope(arg0, arg1, arg2, None, None).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_tileenvelope: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1050usize => {
                let arg0 = dv_text(&args, 0, "st_tin_from_text")?;
                {
                    let __r = pg_ctor::st_tin_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_tin_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1051usize => {
                let arg0 = dv_blob(&args, 0, "st_tin_from_wkb")?;
                {
                    let __r = pg_ctor::st_tin_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_tin_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1052usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topology_edge_count")?,
                    "st_topology_edge_count",
                )?;
                Ok(types::Duckvalue::Int64(arg0.edge_count() as i64))
            }
            1053usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_topology_edge_count_batch")?;
                {
                    let __r = postgis_batch::st_topology_edge_count_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1054usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topology_face_count")?,
                    "st_topology_face_count",
                )?;
                Ok(types::Duckvalue::Int64(arg0.face_count() as i64))
            }
            1055usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_topology_face_count_batch")?;
                {
                    let __r = postgis_batch::st_topology_face_count_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1056usize => {
                let arg0 = dv_blob(&args, 0, "st_topology_from_bytes")?;
                {
                    let __r = pg_topo_types::from_bytes(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_topology_from_bytes: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.to_bytes().into()))
                }
            }
            1057usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topology_name")?,
                    "st_topology_name",
                )?;
                Ok(types::Duckvalue::Text((arg0.name()).into()))
            }
            1058usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_topology_name_batch")?;
                {
                    let __r = postgis_batch::st_topology_name_batch(&arg0);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1059usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topology_node_count")?,
                    "st_topology_node_count",
                )?;
                Ok(types::Duckvalue::Int64(arg0.node_count() as i64))
            }
            1060usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_topology_node_count_batch")?;
                {
                    let __r = postgis_batch::st_topology_node_count_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1061usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topology_precision")?,
                    "st_topology_precision",
                )?;
                Ok(types::Duckvalue::Float64(arg0.precision()))
            }
            1062usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_topology_precision_batch")?;
                {
                    let __r = postgis_batch::st_topology_precision_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1063usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topology_srid")?,
                    "st_topology_srid",
                )?;
                Ok(types::Duckvalue::Int64(arg0.srid() as i64))
            }
            1064usize => {
                let arg0: Vec<Vec<u8>> =
                    parse_json_list_list_u8(&args, 0, "st_topology_srid_batch")?;
                {
                    let __r = postgis_batch::st_topology_srid_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1065usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topology_to_bytes")?,
                    "st_topology_to_bytes",
                )?;
                Ok(types::Duckvalue::Blob((arg0.to_bytes()).into()))
            }
            1066usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topologyedgecount")?,
                    "st_topologyedgecount",
                )?;
                Ok(types::Duckvalue::Int64(arg0.edge_count() as i64))
            }
            1067usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topologyfacecount")?,
                    "st_topologyfacecount",
                )?;
                Ok(types::Duckvalue::Int64(arg0.face_count() as i64))
            }
            1068usize => {
                let arg0 = dv_blob(&args, 0, "st_topologyfrombytes")?;
                {
                    let __r = pg_topo_types::from_bytes(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_topologyfrombytes: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.to_bytes().into()))
                }
            }
            1069usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "st_topologyname")?, "st_topologyname")?;
                Ok(types::Duckvalue::Text((arg0.name()).into()))
            }
            1070usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topologynodecount")?,
                    "st_topologynodecount",
                )?;
                Ok(types::Duckvalue::Int64(arg0.node_count() as i64))
            }
            1071usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topologyprecision")?,
                    "st_topologyprecision",
                )?;
                Ok(types::Duckvalue::Float64(arg0.precision()))
            }
            1072usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "st_topologysrid")?, "st_topologysrid")?;
                Ok(types::Duckvalue::Int64(arg0.srid() as i64))
            }
            1073usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_topologytobytes")?,
                    "st_topologytobytes",
                )?;
                Ok(types::Duckvalue::Blob((arg0.to_bytes()).into()))
            }
            1074usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_touches")?, "st_touches")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_touches")?, "st_touches")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_touches(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_touches: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            1075usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_touches_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_touches_batch")?;
                {
                    let __r = postgis_batch::st_touches_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_touches_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            1076usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_tpi")?, "st_tpi")?;
                let arg1 = dv_i64(&args, 1, "st_tpi")? as u32;
                {
                    let __r = pg_rast_proc::st_tpi(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!("st_tpi: {}", shim_err_string(e)))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            1077usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_transform")?, "st_transform")?;
                let arg1 = dv_i64(&args, 1, "st_transform")? as i32;
                {
                    let __r = pg_xform::st_transform(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_transform: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1078usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_transform_pipeline")?,
                    "st_transform_pipeline",
                )?;
                let arg1 = dv_text(&args, 1, "st_transform_pipeline")?;
                {
                    let __r = pg_xform::st_transform_pipeline(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_transform_pipeline: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1079usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_transform_pipeline_to_srid")?,
                    "st_transform_pipeline_to_srid",
                )?;
                let arg1 = dv_text(&args, 1, "st_transform_pipeline_to_srid")?;
                let arg2 = dv_i64(&args, 2, "st_transform_pipeline_to_srid")? as i32;
                {
                    let __r = pg_xform::st_transform_pipeline_to_srid(&arg0, arg1, arg2).map_err(
                        |e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_transform_pipeline_to_srid: {}",
                                shim_err_string(e)
                            ))
                        },
                    )?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1080usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_transformpipeline")?,
                    "st_transformpipeline",
                )?;
                let arg1 = dv_text(&args, 1, "st_transformpipeline")?;
                {
                    let __r = pg_xform::st_transform_pipeline(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_transformpipeline: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1081usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_translate")?, "st_translate")?;
                let arg1 = dv_f64(&args, 1, "st_translate")?;
                let arg2 = dv_f64(&args, 2, "st_translate")?;
                {
                    let __r = pg_xform::st_translate(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_translate: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1082usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_translate_threed")?,
                    "st_translate_threed",
                )?;
                let arg1 = dv_f64(&args, 1, "st_translate_threed")?;
                let arg2 = dv_f64(&args, 2, "st_translate_threed")?;
                let arg3 = dv_f64(&args, 3, "st_translate_threed")?;
                {
                    let __r =
                        pg_xform::st_translate_threed(&arg0, arg1, arg2, arg3).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_translate_threed: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1083usize => {
                let arg0 = dv_blob(&args, 0, "st_translatethreed")?;
                let arg1 = dv_f64(&args, 1, "st_translatethreed")?;
                let arg2 = dv_f64(&args, 2, "st_translatethreed")?;
                let arg3 = dv_f64(&args, 3, "st_translatethreed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_translate_threed(arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_translatethreed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1084usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_transscale")?, "st_transscale")?;
                let arg1 = dv_f64(&args, 1, "st_transscale")?;
                let arg2 = dv_f64(&args, 2, "st_transscale")?;
                let arg3 = dv_f64(&args, 3, "st_transscale")?;
                let arg4 = dv_f64(&args, 4, "st_transscale")?;
                {
                    let __r =
                        pg_xform::st_transscale(&arg0, arg1, arg2, arg3, arg4).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_transscale: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1085usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_tri")?, "st_tri")?;
                let arg1 = dv_i64(&args, 1, "st_tri")? as u32;
                {
                    let __r = pg_rast_proc::st_tri(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!("st_tri: {}", shim_err_string(e)))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_binary().into()))
                }
            }
            1086usize => {
                let arg0 = dv_text(&args, 0, "st_triangle_from_text")?;
                {
                    let __r = pg_ctor::st_triangle_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_triangle_from_text: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1087usize => {
                let arg0 = dv_blob(&args, 0, "st_triangle_from_wkb")?;
                {
                    let __r = pg_ctor::st_triangle_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_triangle_from_wkb: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1088usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_triangulate_polygon")?,
                    "st_triangulate_polygon",
                )?;
                {
                    let __r = pg_proc::st_triangulate_polygon(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_triangulate_polygon: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1089usize => {
                let arg0 = dv_blob(&args, 0, "st_triangulate_twod")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_triangulate_twod(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_triangulate_twod: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1090usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_triangulatepolygon")?,
                    "st_triangulatepolygon",
                )?;
                {
                    let __r = pg_proc::st_triangulate_polygon(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_triangulatepolygon: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1091usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_unary_union")?, "st_unary_union")?;
                {
                    let __r = pg_proc::st_unary_union(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_unary_union: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1092usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_unaryunion")?, "st_unaryunion")?;
                {
                    let __r = pg_proc::st_unary_union(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_unaryunion: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1093usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_union")?, "st_union")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_union")?, "st_union")?;
                {
                    let __r = pg_proc::st_union(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_union: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1094usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_union_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_union_batch")?;
                {
                    let __r = postgis_batch::st_union_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_union_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            1095usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_union_gridsize")?, "st_union_gridsize")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_union_gridsize")?, "st_union_gridsize")?;
                let arg2 = dv_f64(&args, 2, "st_union_gridsize")?;
                {
                    let __r = pg_proc::st_union_gridsize(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_union_gridsize: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1096usize => {
                let arg0 = dv_blob(&args, 0, "st_union_threed")?;
                let arg1 = dv_blob(&args, 1, "st_union_threed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_union_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_union_threed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1097usize => {
                let arg0 = dv_blob(&args, 0, "st_unionthreed")?;
                let arg1 = dv_blob(&args, 1, "st_unionthreed")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_union_threed(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_unionthreed: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1098usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_upper_left_x")?, "st_upper_left_x")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_upper_left_x(
                    &arg0,
                )))
            }
            1099usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_upper_left_y")?, "st_upper_left_y")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_upper_left_y(
                    &arg0,
                )))
            }
            1100usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_upperleftx")?, "st_upperleftx")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_upper_left_x(
                    &arg0,
                )))
            }
            1101usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_upperlefty")?, "st_upperlefty")?;
                Ok(types::Duckvalue::Float64(pg_rast_acc::st_upper_left_y(
                    &arg0,
                )))
            }
            1102usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_validate_topology")?,
                    "st_validate_topology",
                )?;
                {
                    let __r = pg_topo_query::validate_topology(&arg0);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1103usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "st_validatetopology")?,
                    "st_validatetopology",
                )?;
                {
                    let __r = pg_topo_query::validate_topology(&arg0);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1104usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_value")?, "st_value")?;
                let arg1 = dv_i64(&args, 1, "st_value")? as u32;
                let arg2 = dv_i64(&args, 2, "st_value")? as u32;
                let arg3 = dv_i64(&args, 3, "st_value")? as u32;
                Ok(types::Duckvalue::Float64(
                    pg_rast_acc::st_value(&arg0, arg1, arg2, arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_value: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            1105usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_value_count")?, "st_value_count")?;
                let arg1 = dv_i64(&args, 1, "st_value_count")? as u32;
                {
                    let __r = pg_rast_stats::st_value_count(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_value_count: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_value_count(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1106usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_value_count_of_value")?,
                    "st_value_count_of_value",
                )?;
                let arg1 = dv_i64(&args, 1, "st_value_count_of_value")? as u32;
                let arg2 = dv_f64(&args, 2, "st_value_count_of_value")?;
                Ok(types::Duckvalue::Int64(
                    pg_rast_stats::st_value_count_of_value(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_value_count_of_value: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1107usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_value_counts")?, "st_value_counts")?;
                let arg1 = dv_i64(&args, 1, "st_value_counts")? as u32;
                let arg2: Vec<f64> = parse_json_list_f64(&args, 2, "st_value_counts")?;
                let arg3 = dv_f64(&args, 3, "st_value_counts")?;
                {
                    let __r =
                        pg_rast_stats::st_value_counts(&arg0, arg1, &arg2, arg3).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_value_counts: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_value_count(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1108usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_value_percent")?, "st_value_percent")?;
                let arg1 = dv_i64(&args, 1, "st_value_percent")? as u32;
                let arg2 = dv_f64(&args, 2, "st_value_percent")?;
                Ok(types::Duckvalue::Float64(
                    pg_rast_stats::st_value_percent(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_value_percent: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            1109usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_value_percents")?,
                    "st_value_percents",
                )?;
                let arg1 = dv_i64(&args, 1, "st_value_percents")? as u32;
                let arg2: Vec<f64> = parse_json_list_f64(&args, 2, "st_value_percents")?;
                let arg3 = dv_f64(&args, 3, "st_value_percents")?;
                {
                    let __r = pg_rast_stats::st_value_percents(&arg0, arg1, &arg2, arg3).map_err(
                        |e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_value_percents: {}",
                                shim_err_string(e)
                            ))
                        },
                    )?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_value_percent_entry(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1110usize => {
                let arg0 =
                    from_raster_binary(dv_blob(&args, 0, "st_valuecount")?, "st_valuecount")?;
                let arg1 = dv_i64(&args, 1, "st_valuecount")? as u32;
                {
                    let __r = pg_rast_stats::st_value_count(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_valuecount: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    let mut __it = __r.into_iter();
                    match __it.next() {
                        Some(__rec) => ret_to_witvalue_value_count(__rec),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1111usize => {
                let arg0 = dv_blob(&args, 0, "st_visibility_point")?;
                let arg1 = dv_blob(&args, 1, "st_visibility_point")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_visibility_point(arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_visibility_point: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1112usize => {
                let arg0 = dv_blob(&args, 0, "st_visibility_segment")?;
                let arg1 = dv_blob(&args, 1, "st_visibility_segment")?;
                let arg2 = dv_blob(&args, 2, "st_visibility_segment")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_visibility_segment(arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_visibility_segment: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1113usize => {
                let arg0 = dv_blob(&args, 0, "st_volume")?;
                Ok(types::Duckvalue::Float64(pg_sfcgal::st_volume(arg0)))
            }
            1114usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_voronoi_lines")?, "st_voronoi_lines")?;
                let arg1 = dv_f64(&args, 1, "st_voronoi_lines")?;
                {
                    let __r = pg_proc::st_voronoi_lines(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_voronoi_lines: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1115usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_voronoi_polygons")?,
                    "st_voronoi_polygons",
                )?;
                let arg1 = dv_f64(&args, 1, "st_voronoi_polygons")?;
                {
                    let __r = pg_proc::st_voronoi_polygons(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_voronoi_polygons: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1116usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_voronoilines")?, "st_voronoilines")?;
                let arg1 = dv_f64(&args, 1, "st_voronoilines")?;
                {
                    let __r = pg_proc::st_voronoi_lines(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_voronoilines: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1117usize => {
                let arg0 = from_wkb(
                    dv_blob(&args, 0, "st_voronoipolygons")?,
                    "st_voronoipolygons",
                )?;
                let arg1 = dv_f64(&args, 1, "st_voronoipolygons")?;
                {
                    let __r = pg_proc::st_voronoi_polygons(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_voronoipolygons: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1118usize => {
                let arg0 = from_raster_binary(dv_blob(&args, 0, "st_width")?, "st_width")?;
                Ok(types::Duckvalue::Int64(pg_rast_acc::st_width(&arg0) as i64))
            }
            1119usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_within")?, "st_within")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_within")?, "st_within")?;
                Ok(types::Duckvalue::Boolean(
                    pg_pred::st_within(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_within: {}",
                            shim_err_string(e)
                        ))
                    })?,
                ))
            }
            1120usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_within_batch")?;
                let arg1: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 1, "st_within_batch")?;
                {
                    let __r = postgis_batch::st_within_batch(&arg0, &arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!("st_within_batch: encode JSON: {}", e))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            1121usize => {
                let arg0 = dv_blob(&args, 0, "st_wkb_to_sql")?;
                {
                    let __r = pg_ctor::st_wkb_to_sql(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_wkb_to_sql: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1122usize => {
                let arg0 = dv_blob(&args, 0, "st_wkbtosql")?;
                {
                    let __r = pg_ctor::st_geom_from_wkb(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_wkbtosql: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1123usize => {
                let arg0 = dv_text(&args, 0, "st_wkt_to_sql")?;
                {
                    let __r = pg_ctor::st_wkt_to_sql(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_wkt_to_sql: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1124usize => {
                let arg0 = dv_text(&args, 0, "st_wkttosql")?;
                {
                    let __r = pg_ctor::st_geom_from_text(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_wkttosql: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1125usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_world_to_raster_coord")?,
                    "st_world_to_raster_coord",
                )?;
                let arg1 = dv_f64(&args, 1, "st_world_to_raster_coord")?;
                let arg2 = dv_f64(&args, 2, "st_world_to_raster_coord")?;
                {
                    let __r = pg_rast_px::st_world_to_raster_coord(&arg0, arg1, arg2);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_world_to_raster_coord: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            1126usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_world_to_raster_coord_col")?,
                    "st_world_to_raster_coord_col",
                )?;
                let arg1 = dv_f64(&args, 1, "st_world_to_raster_coord_col")?;
                let arg2 = dv_f64(&args, 2, "st_world_to_raster_coord_col")?;
                {
                    let __r = pg_rast_px::st_world_to_raster_coord(&arg0, arg1, arg2);
                    Ok(types::Duckvalue::Int64(__r.0 as i64))
                }
            }
            1127usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_world_to_raster_coord_row")?,
                    "st_world_to_raster_coord_row",
                )?;
                let arg1 = dv_f64(&args, 1, "st_world_to_raster_coord_row")?;
                let arg2 = dv_f64(&args, 2, "st_world_to_raster_coord_row")?;
                {
                    let __r = pg_rast_px::st_world_to_raster_coord(&arg0, arg1, arg2);
                    Ok(types::Duckvalue::Int64(__r.1 as i64))
                }
            }
            1128usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_worldtorastercoord")?,
                    "st_worldtorastercoord",
                )?;
                let arg1 = dv_f64(&args, 1, "st_worldtorastercoord")?;
                let arg2 = dv_f64(&args, 2, "st_worldtorastercoord")?;
                {
                    let __r = pg_rast_px::st_world_to_raster_coord(&arg0, arg1, arg2);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "st_worldtorastercoord: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            1129usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_worldtorastercoordcol")?,
                    "st_worldtorastercoordcol",
                )?;
                let arg1 = dv_f64(&args, 1, "st_worldtorastercoordcol")?;
                let arg2 = dv_f64(&args, 2, "st_worldtorastercoordcol")?;
                {
                    let __r = pg_rast_px::st_world_to_raster_coord(&arg0, arg1, arg2);
                    Ok(types::Duckvalue::Int64(__r.0 as i64))
                }
            }
            1130usize => {
                let arg0 = from_raster_binary(
                    dv_blob(&args, 0, "st_worldtorastercoordrow")?,
                    "st_worldtorastercoordrow",
                )?;
                let arg1 = dv_f64(&args, 1, "st_worldtorastercoordrow")?;
                let arg2 = dv_f64(&args, 2, "st_worldtorastercoordrow")?;
                {
                    let __r = pg_rast_px::st_world_to_raster_coord(&arg0, arg1, arg2);
                    Ok(types::Duckvalue::Int64(__r.1 as i64))
                }
            }
            1131usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_wrap_x")?, "st_wrap_x")?;
                let arg1 = dv_f64(&args, 1, "st_wrap_x")?;
                let arg2 = dv_f64(&args, 2, "st_wrap_x")?;
                {
                    let __r = pg_xform::st_wrap_x(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_wrap_x: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1132usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_wrapx")?, "st_wrapx")?;
                let arg1 = dv_f64(&args, 1, "st_wrapx")?;
                let arg2 = dv_f64(&args, 2, "st_wrapx")?;
                {
                    let __r = pg_xform::st_wrap_x(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_wrapx: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1133usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_x")?, "st_x")?;
                Ok(types::Duckvalue::Float64(pg_acc::st_x(&arg0).map_err(
                    |e| types::Duckerror::Invalidargument(format!("st_x: {}", shim_err_string(e))),
                )?))
            }
            1134usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_x_batch")?;
                {
                    let __r = postgis_batch::st_x_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1135usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_xmax")?, "st_xmax")?;
                Ok(types::Duckvalue::Float64(pg_acc::st_xmax(&arg0).map_err(
                    |e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_xmax: {}",
                            shim_err_string(e)
                        ))
                    },
                )?))
            }
            1136usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_xmin")?, "st_xmin")?;
                Ok(types::Duckvalue::Float64(pg_acc::st_xmin(&arg0).map_err(
                    |e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_xmin: {}",
                            shim_err_string(e)
                        ))
                    },
                )?))
            }
            1137usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_y")?, "st_y")?;
                Ok(types::Duckvalue::Float64(pg_acc::st_y(&arg0).map_err(
                    |e| types::Duckerror::Invalidargument(format!("st_y: {}", shim_err_string(e))),
                )?))
            }
            1138usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_y_batch")?;
                {
                    let __r = postgis_batch::st_y_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1139usize => {
                let arg0 = dv_blob(&args, 0, "st_y_monotone_partition")?;
                Ok(types::Duckvalue::Blob(
                    (pg_sfcgal::st_y_monotone_partition(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_y_monotone_partition: {}",
                            shim_err_string(e)
                        ))
                    })?)
                    .into(),
                ))
            }
            1140usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_ymax")?, "st_ymax")?;
                Ok(types::Duckvalue::Float64(pg_acc::st_ymax(&arg0).map_err(
                    |e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_ymax: {}",
                            shim_err_string(e)
                        ))
                    },
                )?))
            }
            1141usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_ymin")?, "st_ymin")?;
                Ok(types::Duckvalue::Float64(pg_acc::st_ymin(&arg0).map_err(
                    |e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_ymin: {}",
                            shim_err_string(e)
                        ))
                    },
                )?))
            }
            1142usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_z")?, "st_z")?;
                Ok(
                    match pg_acc::st_z(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!("st_z: {}", shim_err_string(e)))
                    })? {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            1143usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "st_z_batch")?;
                {
                    let __r = postgis_batch::st_z_batch(&arg0);
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Float64(*v as f64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1144usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_zmax")?, "st_zmax")?;
                Ok(
                    match pg_acc::st_zmax(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_zmax: {}",
                            shim_err_string(e)
                        ))
                    })? {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            1145usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_zmflag")?, "st_zmflag")?;
                Ok(types::Duckvalue::Int64(pg_acc::st_zmflag(&arg0) as i64))
            }
            1146usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_zmin")?, "st_zmin")?;
                Ok(
                    match pg_acc::st_zmin(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_zmin: {}",
                            shim_err_string(e)
                        ))
                    })? {
                        Some(v) => types::Duckvalue::Float64(v as f64),
                        None => types::Duckvalue::Null,
                    },
                )
            }
            1147usize => {
                let arg0 = dv_text(&args, 0, "state_extract")?;
                Ok(match pg_geo::state_extract(arg0) {
                    Some(v) => types::Duckvalue::Text(v.into()),
                    None => types::Duckvalue::Null,
                })
            }
            1148usize => {
                let arg0 = from_topology_bytes(dv_blob(&args, 0, "to_topogeom")?, "to_topogeom")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "to_topogeom")?, "to_topogeom")?;
                let arg2 = dv_f64(&args, 2, "to_topogeom")?;
                {
                    let __r = pg_topogeom::to_topogeom(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "to_topogeom: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.geometry().as_wkb().into()))
                }
            }
            1149usize => {
                let arg0 =
                    parse_json_list_record_topo_element(&args, 0, "topo_element_array_append")?;
                let arg1 = arg_witvalue_topo_element(&args, 1, "topo_element_array_append")?;
                {
                    let __r = pg_topogeom::topo_element_array_append(&arg0, arg1);
                    let __json = serde_json::to_string(&__r).map_err(|e| {
                        types::Duckerror::Internal(format!(
                            "topo_element_array_append: encode JSON: {}",
                            e
                        ))
                    })?;
                    Ok(types::Duckvalue::Text(__json))
                }
            }
            1150usize => {
                let arg0: Vec<Vec<u8>> = parse_json_list_list_u8(&args, 0, "topo_pgtp_all_batch")?;
                Ok(types::Duckvalue::Boolean(
                    postgis_batch::topo_pgtp_all_batch(&arg0),
                ))
            }
            1151usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topogeo_add_geometry")?,
                    "topogeo_add_geometry",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topogeo_add_geometry")?,
                    "topogeo_add_geometry",
                )?;
                let arg2 = dv_f64(&args, 2, "topogeo_add_geometry")?;
                {
                    let __r =
                        pg_topo_edit::topogeo_add_geometry(&arg0, &arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "topogeo_add_geometry: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1152usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topogeo_add_linestring")?,
                    "topogeo_add_linestring",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topogeo_add_linestring")?,
                    "topogeo_add_linestring",
                )?;
                let arg2 = dv_f64(&args, 2, "topogeo_add_linestring")?;
                {
                    let __r =
                        pg_topo_edit::topogeo_add_linestring(&arg0, &arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "topogeo_add_linestring: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1153usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topogeo_add_point")?,
                    "topogeo_add_point",
                )?;
                let arg1 = from_wkb(dv_blob(&args, 1, "topogeo_add_point")?, "topogeo_add_point")?;
                let arg2 = dv_f64(&args, 2, "topogeo_add_point")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::topogeo_add_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topogeo_add_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1154usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topogeo_add_polygon")?,
                    "topogeo_add_polygon",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topogeo_add_polygon")?,
                    "topogeo_add_polygon",
                )?;
                let arg2 = dv_f64(&args, 2, "topogeo_add_polygon")?;
                {
                    let __r =
                        pg_topo_edit::topogeo_add_polygon(&arg0, &arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "topogeo_add_polygon: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1155usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topogeo_load_geometry")?,
                    "topogeo_load_geometry",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topogeo_load_geometry")?,
                    "topogeo_load_geometry",
                )?;
                let arg2 = dv_f64(&args, 2, "topogeo_load_geometry")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::topogeo_load_geometry(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topogeo_load_geometry: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1156usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_add_edge")?,
                    "topology_add_edge",
                )?;
                let arg1 = from_wkb(dv_blob(&args, 1, "topology_add_edge")?, "topology_add_edge")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_edge(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_add_edge: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1157usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_add_edge_mod_face")?,
                    "topology_add_edge_mod_face",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_add_edge_mod_face")? as u32;
                let arg2 = dv_i64(&args, 2, "topology_add_edge_mod_face")? as u32;
                let arg3 = from_wkb(
                    dv_blob(&args, 3, "topology_add_edge_mod_face")?,
                    "topology_add_edge_mod_face",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_edge_mod_face(&arg0, arg1, arg2, &arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_add_edge_mod_face: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1158usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_add_edge_new_faces")?,
                    "topology_add_edge_new_faces",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_add_edge_new_faces")? as u32;
                let arg2 = dv_i64(&args, 2, "topology_add_edge_new_faces")? as u32;
                let arg3 = from_wkb(
                    dv_blob(&args, 3, "topology_add_edge_new_faces")?,
                    "topology_add_edge_new_faces",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_edge_new_faces(&arg0, arg1, arg2, &arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_add_edge_new_faces: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1159usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_add_face")?,
                    "topology_add_face",
                )?;
                let arg1 = from_wkb(dv_blob(&args, 1, "topology_add_face")?, "topology_add_face")?;
                let arg2 = dv_bool(&args, 2, "topology_add_face")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_face(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_add_face: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1160usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_add_iso_edge")?,
                    "topology_add_iso_edge",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_add_iso_edge")? as u32;
                let arg2 = dv_i64(&args, 2, "topology_add_iso_edge")? as u32;
                let arg3 = from_wkb(
                    dv_blob(&args, 3, "topology_add_iso_edge")?,
                    "topology_add_iso_edge",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_iso_edge(&arg0, arg1, arg2, &arg3).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_add_iso_edge: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1161usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_add_iso_node")?,
                    "topology_add_iso_node",
                )?;
                let arg2 = from_wkb(
                    dv_blob(&args, 2, "topology_add_iso_node")?,
                    "topology_add_iso_node",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_iso_node(&arg0, None, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_add_iso_node: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1162usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_add_node")?,
                    "topology_add_node",
                )?;
                let arg1 = from_wkb(dv_blob(&args, 1, "topology_add_node")?, "topology_add_node")?;
                let arg2 = dv_f64(&args, 2, "topology_add_node")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::add_node(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_add_node: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1163usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_add_topogeom_column")?,
                    "topology_add_topogeom_column",
                )?;
                let arg1 = dv_text(&args, 1, "topology_add_topogeom_column")?;
                let arg2 = dv_text(&args, 2, "topology_add_topogeom_column")?;
                let arg3 = dv_text(&args, 3, "topology_add_topogeom_column")?;
                Ok(types::Duckvalue::Int64(pg_topo_edit::add_topogeom_column(
                    &arg0, arg1, arg2, arg3,
                ) as i64))
            }
            1164usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "topology_as_gml")?, "topology_as_gml")?;
                let arg1 = dv_text(&args, 1, "topology_as_gml")?;
                let arg2 = dv_i64(&args, 2, "topology_as_gml")? as i32;
                Ok(types::Duckvalue::Text(
                    (pg_topo_out::as_gml(&arg0, arg1, arg2)).into(),
                ))
            }
            1165usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_as_topojson")?,
                    "topology_as_topojson",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_out::as_topojson(&arg0)).into(),
                ))
            }
            1166usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_as_topojson_with_edge_map")?,
                    "topology_as_topojson_with_edge_map",
                )?;
                let arg1 = dv_bool(&args, 1, "topology_as_topojson_with_edge_map")?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_out::as_topojson_with_edge_map(&arg0, arg1)).into(),
                ))
            }
            1167usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_change_edge_geom")?,
                    "topology_change_edge_geom",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_change_edge_geom")? as u32;
                let arg2 = from_wkb(
                    dv_blob(&args, 2, "topology_change_edge_geom")?,
                    "topology_change_edge_geom",
                )?;
                let _ = pg_topo_edit::change_edge_geom(&arg0, arg1, &arg2).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "topology_change_edge_geom: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Null)
            }
            1168usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_copy_topology")?,
                    "topology_copy_topology",
                )?;
                let arg1 = dv_text(&args, 1, "topology_copy_topology")?;
                Ok(types::Duckvalue::Blob(
                    pg_topo_types::copy_topology(&arg0, arg1).to_bytes().into(),
                ))
            }
            1169usize => {
                let arg0 = dv_text(&args, 0, "topology_create")?;
                let arg1 = dv_i64(&args, 1, "topology_create")? as i32;
                let arg2 = dv_f64(&args, 2, "topology_create")?;
                Ok(types::Duckvalue::Blob(
                    Topology::new(arg0, arg1, arg2).to_bytes().into(),
                ))
            }
            1170usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_create_topo_geom")?,
                    "topology_create_topo_geom",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_create_topo_geom")? as u32;
                let arg2 =
                    parse_json_list_record_topo_element(&args, 2, "topology_create_topo_geom")?;
                {
                    let __r = pg_topogeom::create_topo_geom(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_create_topo_geom: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.geometry().as_wkb().into()))
                }
            }
            1171usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_drop_topogeom_column")?,
                    "topology_drop_topogeom_column",
                )?;
                let arg1 = dv_text(&args, 1, "topology_drop_topogeom_column")?;
                let arg2 = dv_text(&args, 2, "topology_drop_topogeom_column")?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_edit::drop_topogeom_column(&arg0, arg1, arg2)).into(),
                ))
            }
            1172usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_drop_topology")?,
                    "topology_drop_topology",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_edit::drop_topology(&arg0)).into(),
                ))
            }
            1173usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_edge_count")?,
                    "topology_edge_count",
                )?;
                Ok(types::Duckvalue::Int64(arg0.edge_count() as i64))
            }
            1174usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_face_count")?,
                    "topology_face_count",
                )?;
                Ok(types::Duckvalue::Int64(arg0.face_count() as i64))
            }
            1175usize => {
                let arg0 = dv_blob(&args, 0, "topology_from_bytes")?;
                {
                    let __r = pg_topo_types::from_bytes(arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_from_bytes: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.to_bytes().into()))
                }
            }
            1176usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_edge_by_point")?,
                    "topology_get_edge_by_point",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topology_get_edge_by_point")?,
                    "topology_get_edge_by_point",
                )?;
                let arg2 = dv_f64(&args, 2, "topology_get_edge_by_point")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_edge_by_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_get_edge_by_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1177usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_edge_geometry")?,
                    "topology_get_edge_geometry",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_get_edge_geometry")? as u32;
                {
                    let __r = pg_topo_out::get_edge_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_get_edge_geometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1178usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_face_by_point")?,
                    "topology_get_face_by_point",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topology_get_face_by_point")?,
                    "topology_get_face_by_point",
                )?;
                let arg2 = dv_f64(&args, 2, "topology_get_face_by_point")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_face_by_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_get_face_by_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1179usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_face_containing_point")?,
                    "topology_get_face_containing_point",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topology_get_face_containing_point")?,
                    "topology_get_face_containing_point",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_face_containing_point(&arg0, &arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_get_face_containing_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1180usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_face_edges")?,
                    "topology_get_face_edges",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_get_face_edges")? as u32;
                {
                    let __r = pg_topo_query::get_face_edges(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_get_face_edges: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1181usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_face_geometry")?,
                    "topology_get_face_geometry",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_get_face_geometry")? as u32;
                {
                    let __r = pg_topo_out::get_face_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_get_face_geometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1182usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_node_by_point")?,
                    "topology_get_node_by_point",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topology_get_node_by_point")?,
                    "topology_get_node_by_point",
                )?;
                let arg2 = dv_f64(&args, 2, "topology_get_node_by_point")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_node_by_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_get_node_by_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1183usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_node_edges")?,
                    "topology_get_node_edges",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_get_node_edges")? as u32;
                {
                    let __r = pg_topo_query::get_node_edges(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_get_node_edges: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1184usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_node_geometry")?,
                    "topology_get_node_geometry",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_get_node_geometry")? as u32;
                {
                    let __r = pg_topo_out::get_node_geometry(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_get_node_geometry: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.as_wkb().into()))
                }
            }
            1185usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_ring_edges")?,
                    "topology_get_ring_edges",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_get_ring_edges")? as u32;
                {
                    let __r = pg_topo_query::get_ring_edges(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_get_ring_edges: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1186usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_topology_id")?,
                    "topology_get_topology_id",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_topology_id(&arg0) as i64,
                ))
            }
            1187usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_topology_name")?,
                    "topology_get_topology_name",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_query::get_topology_name(&arg0)).into(),
                ))
            }
            1188usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_get_topology_srid")?,
                    "topology_get_topology_srid",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::get_topology_srid(&arg0) as i64,
                ))
            }
            1189usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_make_topology_precise")?,
                    "topology_make_topology_precise",
                )?;
                let arg1 = dv_f64(&args, 1, "topology_make_topology_precise")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::make_topology_precise(&arg0, arg1) as i64,
                ))
            }
            1190usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_mod_edge_heal")?,
                    "topology_mod_edge_heal",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_mod_edge_heal")? as u32;
                let arg2 = dv_i64(&args, 2, "topology_mod_edge_heal")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::mod_edge_heal(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_mod_edge_heal: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1191usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_mod_edge_split")?,
                    "topology_mod_edge_split",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_mod_edge_split")? as u32;
                let arg2 = from_wkb(
                    dv_blob(&args, 2, "topology_mod_edge_split")?,
                    "topology_mod_edge_split",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::mod_edge_split(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_mod_edge_split: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1192usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_move_iso_node")?,
                    "topology_move_iso_node",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_move_iso_node")? as u32;
                let arg2 = from_wkb(
                    dv_blob(&args, 2, "topology_move_iso_node")?,
                    "topology_move_iso_node",
                )?;
                let _ = pg_topo_edit::move_iso_node(&arg0, arg1, &arg2).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "topology_move_iso_node: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Null)
            }
            1193usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "topology_name")?, "topology_name")?;
                Ok(types::Duckvalue::Text((arg0.name()).into()))
            }
            1194usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_new_edge_heal")?,
                    "topology_new_edge_heal",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_new_edge_heal")? as u32;
                let arg2 = dv_i64(&args, 2, "topology_new_edge_heal")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::new_edge_heal(&arg0, arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_new_edge_heal: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1195usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_new_edges_split")?,
                    "topology_new_edges_split",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_new_edges_split")? as u32;
                let arg2 = from_wkb(
                    dv_blob(&args, 2, "topology_new_edges_split")?,
                    "topology_new_edges_split",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::new_edges_split(&arg0, arg1, &arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_new_edges_split: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1196usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_node_count")?,
                    "topology_node_count",
                )?;
                Ok(types::Duckvalue::Int64(arg0.node_count() as i64))
            }
            1197usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_polygonize")?,
                    "topology_polygonize",
                )?;
                {
                    let __r = pg_topo_out::polygonize(&arg0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_polygonize: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1198usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_precision")?,
                    "topology_precision",
                )?;
                Ok(types::Duckvalue::Float64(arg0.precision()))
            }
            1199usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_rem_edge_mod_face")?,
                    "topology_rem_edge_mod_face",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_rem_edge_mod_face")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::rem_edge_mod_face(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_rem_edge_mod_face: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1200usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_rem_edge_new_face")?,
                    "topology_rem_edge_new_face",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_rem_edge_new_face")? as u32;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::rem_edge_new_face(&arg0, arg1).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_rem_edge_new_face: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1201usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_remove_face")?,
                    "topology_remove_face",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_remove_face")? as u32;
                let _ = pg_topo_edit::remove_face(&arg0, arg1).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "topology_remove_face: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Null)
            }
            1202usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_remove_iso_edge")?,
                    "topology_remove_iso_edge",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_remove_iso_edge")? as u32;
                let _ = pg_topo_edit::remove_iso_edge(&arg0, arg1).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "topology_remove_iso_edge: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Null)
            }
            1203usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_remove_iso_node")?,
                    "topology_remove_iso_node",
                )?;
                let arg1 = dv_i64(&args, 1, "topology_remove_iso_node")? as u32;
                let _ = pg_topo_edit::remove_iso_node(&arg0, arg1).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "topology_remove_iso_node: {}",
                        shim_err_string(e)
                    ))
                })?;
                Ok(types::Duckvalue::Null)
            }
            1204usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_remove_unused_primitives")?,
                    "topology_remove_unused_primitives",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::remove_unused_primitives(&arg0) as i64,
                ))
            }
            1205usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_rename_topology")?,
                    "topology_rename_topology",
                )?;
                let arg1 = dv_text(&args, 1, "topology_rename_topology")?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_edit::rename_topology(&arg0, arg1)).into(),
                ))
            }
            1206usize => {
                let arg0 =
                    from_topology_bytes(dv_blob(&args, 0, "topology_srid")?, "topology_srid")?;
                Ok(types::Duckvalue::Int64(arg0.srid() as i64))
            }
            1207usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_summary")?,
                    "topology_summary",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_query::topology_summary(&arg0)).into(),
                ))
            }
            1208usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_to_bytes")?,
                    "topology_to_bytes",
                )?;
                Ok(types::Duckvalue::Blob((arg0.to_bytes()).into()))
            }
            1209usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_to_topogeom")?,
                    "topology_to_topogeom",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topology_to_topogeom")?,
                    "topology_to_topogeom",
                )?;
                let arg2 = dv_f64(&args, 2, "topology_to_topogeom")?;
                {
                    let __r = pg_topogeom::to_topogeom(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_to_topogeom: {}",
                            shim_err_string(e)
                        ))
                    })?;
                    Ok(types::Duckvalue::Blob(__r.geometry().as_wkb().into()))
                }
            }
            1210usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_topogeo_add_geometry")?,
                    "topology_topogeo_add_geometry",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topology_topogeo_add_geometry")?,
                    "topology_topogeo_add_geometry",
                )?;
                let arg2 = dv_f64(&args, 2, "topology_topogeo_add_geometry")?;
                {
                    let __r =
                        pg_topo_edit::topogeo_add_geometry(&arg0, &arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "topology_topogeo_add_geometry: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1211usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_topogeo_add_linestring")?,
                    "topology_topogeo_add_linestring",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topology_topogeo_add_linestring")?,
                    "topology_topogeo_add_linestring",
                )?;
                let arg2 = dv_f64(&args, 2, "topology_topogeo_add_linestring")?;
                {
                    let __r =
                        pg_topo_edit::topogeo_add_linestring(&arg0, &arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "topology_topogeo_add_linestring: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1212usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_topogeo_add_point")?,
                    "topology_topogeo_add_point",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topology_topogeo_add_point")?,
                    "topology_topogeo_add_point",
                )?;
                let arg2 = dv_f64(&args, 2, "topology_topogeo_add_point")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::topogeo_add_point(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_topogeo_add_point: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1213usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_topogeo_add_polygon")?,
                    "topology_topogeo_add_polygon",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topology_topogeo_add_polygon")?,
                    "topology_topogeo_add_polygon",
                )?;
                let arg2 = dv_f64(&args, 2, "topology_topogeo_add_polygon")?;
                {
                    let __r =
                        pg_topo_edit::topogeo_add_polygon(&arg0, &arg1, arg2).map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "topology_topogeo_add_polygon: {}",
                                shim_err_string(e)
                            ))
                        })?;
                    match __r.first() {
                        Some(v) => Ok(types::Duckvalue::Int64(*v as i64)),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1214usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_topogeo_load_geometry")?,
                    "topology_topogeo_load_geometry",
                )?;
                let arg1 = from_wkb(
                    dv_blob(&args, 1, "topology_topogeo_load_geometry")?,
                    "topology_topogeo_load_geometry",
                )?;
                let arg2 = dv_f64(&args, 2, "topology_topogeo_load_geometry")?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_edit::topogeo_load_geometry(&arg0, &arg1, arg2).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "topology_topogeo_load_geometry: {}",
                            shim_err_string(e)
                        ))
                    })? as i64,
                ))
            }
            1215usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_total_topology_size")?,
                    "topology_total_topology_size",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::total_topology_size(&arg0) as i64,
                ))
            }
            1216usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_upgrade_topology")?,
                    "topology_upgrade_topology",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_edit::upgrade_topology(&arg0)).into(),
                ))
            }
            1217usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_validate")?,
                    "topology_validate",
                )?;
                {
                    let __r = pg_topo_query::validate_topology(&arg0);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1218usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_validate_topology")?,
                    "topology_validate_topology",
                )?;
                {
                    let __r = pg_topo_query::validate_topology(&arg0);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1219usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "topology_validate_topology_precision")?,
                    "topology_validate_topology_precision",
                )?;
                let arg1 = dv_f64(&args, 1, "topology_validate_topology_precision")?;
                {
                    let __r = pg_topo_query::validate_topology_precision(&arg0, arg1);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1220usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "total_topology_size")?,
                    "total_topology_size",
                )?;
                Ok(types::Duckvalue::Int64(
                    pg_topo_query::total_topology_size(&arg0) as i64,
                ))
            }
            1221usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "upgrade_topology")?,
                    "upgrade_topology",
                )?;
                Ok(types::Duckvalue::Text(
                    (pg_topo_edit::upgrade_topology(&arg0)).into(),
                ))
            }
            1222usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "validate_topology")?,
                    "validate_topology",
                )?;
                {
                    let __r = pg_topo_query::validate_topology(&arg0);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            1223usize => {
                let arg0 = from_topology_bytes(
                    dv_blob(&args, 0, "validate_topology_precision")?,
                    "validate_topology_precision",
                )?;
                let arg1 = dv_f64(&args, 1, "validate_topology_precision")?;
                {
                    let __r = pg_topo_query::validate_topology_precision(&arg0, arg1);
                    match __r.into_iter().next() {
                        Some(v) => Ok(types::Duckvalue::Text(v.into())),
                        None => Ok(types::Duckvalue::Null),
                    }
                }
            }
            _ => Err(types::Duckerror::Internal(format!(
                "unknown scalar arm index {}",
                arm_idx
            ))),
        }
    }

    fn call_table(
        handle: u32,
        args: Vec<types::Duckvalue>,
    ) -> Result<types::Resultset, types::Duckerror> {
        let arm_idx = table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .get(&handle)
            .copied()
            .ok_or_else(|| types::Duckerror::Internal("unknown table function handle".into()))?;
        match arm_idx {
            0usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dump")?, "st_dump")?;
                let __upstream = pg_acc::st_dump(&arg0);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            1usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dump_points")?, "st_dump_points")?;
                let __upstream = pg_acc::st_dump_points(&arg0);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            2usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dump_rings")?, "st_dump_rings")?;
                let __upstream = pg_acc::st_dump_rings(&arg0).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_dump_rings: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            3usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dump_segments")?, "st_dump_segments")?;
                let __upstream = pg_acc::st_dump_segments(&arg0).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_dump_segments: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            4usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dumppoints")?, "st_dumppoints")?;
                let __upstream = pg_acc::st_dump_points(&arg0);
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            5usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dumprings")?, "st_dumprings")?;
                let __upstream = pg_acc::st_dump_rings(&arg0).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_dumprings: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            6usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_dumpsegments")?, "st_dumpsegments")?;
                let __upstream = pg_acc::st_dump_segments(&arg0).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_dumpsegments: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            7usize => {
                let arg0 = dv_f64(&args, 0, "st_hexagon_grid")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_hexagon_grid")?, "st_hexagon_grid")?;
                let __upstream = pg_ctor::st_hexagon_grid(arg0, &arg1).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_hexagon_grid: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            8usize => {
                let arg0 = dv_f64(&args, 0, "st_hexagongrid")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_hexagongrid")?, "st_hexagongrid")?;
                let __upstream = pg_ctor::st_hexagon_grid(arg0, &arg1).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_hexagongrid: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            9usize => {
                let arg0 = dv_f64(&args, 0, "st_square_grid")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_square_grid")?, "st_square_grid")?;
                let __upstream = pg_ctor::st_square_grid(arg0, &arg1).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_square_grid: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            10usize => {
                let arg0 = dv_f64(&args, 0, "st_squaregrid")?;
                let arg1 = from_wkb(dv_blob(&args, 1, "st_squaregrid")?, "st_squaregrid")?;
                let __upstream = pg_ctor::st_square_grid(arg0, &arg1).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_squaregrid: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            11usize => {
                let arg0 = from_wkb(dv_blob(&args, 0, "st_subdivide")?, "st_subdivide")?;
                let arg1 = dv_i64(&args, 1, "st_subdivide")? as u32;
                let __upstream = pg_proc::st_subdivide(&arg0, arg1).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_subdivide: {}",
                        shim_err_string(e)
                    ))
                })?;
                let mut rows: Vec<Vec<types::Duckvalue>> = Vec::with_capacity(__upstream.len());
                for __g in __upstream.iter() {
                    rows.push(alloc::vec![types::Duckvalue::Blob(__g.as_wkb())]);
                }
                Ok(rows)
            }
            _ => Err(types::Duckerror::Internal(format!(
                "unknown table function arm index {}",
                arm_idx
            ))),
        }
    }

    // #625: pragma dispatch placeholder. The
    // duckdb:extension@4.0.0 callback-dispatch surface
    // requires a call-pragma arm, but the codegen has no
    // substrate carrying pragma metadata yet --
    // shim-interface-core does not extract pragmas and the
    // interface DB schema has no `pragmas` table. Real
    // per-arm dispatch (parallel to `call_scalar` /
    // `call_aggregate`) lands once a shim registers its first
    // pragma; until then the host gets an Unsupported
    // diagnostic naming the primary shim and dispatch arm.
    // See `build_pragma_dispatch_impl` for the substrate
    // extension path.
    fn call_pragma(
        _handle: u32,
        _args: Vec<types::Duckvalue>,
    ) -> Result<Option<types::Duckvalue>, types::Duckerror> {
        Err(types::Duckerror::Unsupported(format!(
            "postgis pragma dispatch: not implemented (no pragmas registered by this bridge)"
        )))
    }
    fn call_cast(
        handle: u32,
        value: types::Duckvalue,
    ) -> Result<types::Duckvalue, types::Duckerror> {
        if identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .contains(&handle)
        {
            return Ok(value);
        }
        <Self as callback_dispatch::Guest>::call_scalar(
            handle,
            alloc::vec![value],
            types::Invokeinfo {
                rowindex: None,
                iswindow: false,
            },
        )
    }
}

impl bindings::exports::duckdb::extension::aggregate_incr_dispatch::Guest for PostgisBridge {
    // #661: 4 state-machine arms stubbed -- the postgis pilot is
    // whole-partition compute (call_aggregate_window only). A
    // future incremental aggregate registration would replace each
    // body with a per-handle dispatch arm; the trait surface stays
    // the same.
    fn call_aggregate_init(_handle: u32) -> Result<u32, types::Duckerror> {
        Err(types::Duckerror::Unsupported(format!(
            "postgis: aggregate-incr init not wired (no incremental aggregates registered)"
        )))
    }

    fn call_aggregate_update(
        _handle: u32,
        _state: u32,
        _rows: Vec<Vec<types::Duckvalue>>,
    ) -> Result<(), types::Duckerror> {
        Err(types::Duckerror::Unsupported(format!(
            "postgis: aggregate-incr update not wired"
        )))
    }

    fn call_aggregate_combine(
        _handle: u32,
        _target: u32,
        _source: u32,
    ) -> Result<(), types::Duckerror> {
        Err(types::Duckerror::Unsupported(format!(
            "postgis: aggregate-incr combine not wired"
        )))
    }

    fn call_aggregate_finalize(
        _handle: u32,
        _state: u32,
    ) -> Result<types::Duckvalue, types::Duckerror> {
        Err(types::Duckerror::Unsupported(format!(
            "postgis: aggregate-incr finalize not wired"
        )))
    }

    fn call_aggregate_window(
        handle: u32,
        partition: Vec<Vec<types::Duckvalue>>,
        frame: bindings::exports::duckdb::extension::aggregate_incr_dispatch::WindowFrame,
    ) -> Result<types::Duckvalue, types::Duckerror> {
        let arm_idx = window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .get(&handle)
            .copied()
            .ok_or_else(|| types::Duckerror::Internal("unknown window handle".into()))?;
        match arm_idx {
            0usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_cluster_dbscan: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_dbscan: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_dbscan: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_dbscan: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_f64(&partition[0], 1, "st_cluster_dbscan")?;
                let __extra1 = dv_i64(&partition[0], 2, "st_cluster_dbscan")? as u32;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels = pg_cluster::st_cluster_dbscan(&__geom_refs, __extra0, __extra1)
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_dbscan: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                match __labels[__row_ix] {
                    Some(__id) => Ok(types::Duckvalue::Int64(__id as i64)),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            1usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_cluster_dbscan_aggregate: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_dbscan_aggregate: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_dbscan_aggregate: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_dbscan_aggregate: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_f64(&partition[0], 1, "st_cluster_dbscan_aggregate")?;
                let __extra1 = dv_i64(&partition[0], 2, "st_cluster_dbscan_aggregate")? as u32;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_agg::st_cluster_dbscan_aggregate(&__geom_refs, __extra0, __extra1).map_err(
                        |e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_cluster_dbscan_aggregate: {}",
                                postgis_err_string(e)
                            ))
                        },
                    )?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Blob(__labels[__row_ix].as_wkb().into()))
            }
            2usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_cluster_intersecting: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_intersecting: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_intersecting: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_intersecting: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels = pg_cluster::st_cluster_intersecting(&__geom_refs).map_err(|e| {
                    types::Duckerror::Invalidargument(format!(
                        "st_cluster_intersecting: {}",
                        postgis_err_string(e)
                    ))
                })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Blob(__labels[__row_ix].as_wkb().into()))
            }
            3usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_cluster_intersecting_aggregate: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_intersecting_aggregate: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_intersecting_aggregate: partition row {} col 0 must be BLOB", __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_intersecting_aggregate: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_agg::st_cluster_intersecting_aggregate(&__geom_refs).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_intersecting_aggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Blob(__labels[__row_ix].as_wkb().into()))
            }
            4usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_cluster_intersecting_win: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_intersecting_win: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_intersecting_win: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_intersecting_win: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_cluster::st_cluster_intersecting_win(&__geom_refs).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_intersecting_win: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Int64(__labels[__row_ix] as i64))
            }
            5usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_cluster_kmeans: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_kmeans: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_kmeans: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_kmeans: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_i64(&partition[0], 1, "st_cluster_kmeans")? as u32;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_cluster::st_cluster_kmeans(&__geom_refs, __extra0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_kmeans: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Int64(__labels[__row_ix] as i64))
            }
            6usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_cluster_kmeans_aggregate: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_kmeans_aggregate: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_kmeans_aggregate: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_kmeans_aggregate: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_i64(&partition[0], 1, "st_cluster_kmeans_aggregate")? as u32;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels = pg_agg::st_cluster_kmeans_aggregate(&__geom_refs, __extra0)
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_kmeans_aggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Blob(__labels[__row_ix].as_wkb().into()))
            }
            7usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_cluster_kmeans_max_radius: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_kmeans_max_radius: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_kmeans_max_radius: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_kmeans_max_radius: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_i64(&partition[0], 1, "st_cluster_kmeans_max_radius")? as u32;
                let __extra1 = dv_f64(&partition[0], 2, "st_cluster_kmeans_max_radius")?;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_cluster::st_cluster_kmeans_max_radius(&__geom_refs, __extra0, __extra1)
                        .map_err(|e| {
                            types::Duckerror::Invalidargument(format!(
                                "st_cluster_kmeans_max_radius: {}",
                                postgis_err_string(e)
                            ))
                        })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                match __labels[__row_ix] {
                    Some(__id) => Ok(types::Duckvalue::Int64(__id as i64)),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            8usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_cluster_within: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_within: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_within: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_within: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_f64(&partition[0], 1, "st_cluster_within")?;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_cluster::st_cluster_within(&__geom_refs, __extra0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_within: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Blob(__labels[__row_ix].as_wkb().into()))
            }
            9usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_cluster_within_aggregate: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_within_aggregate: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_within_aggregate: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_within_aggregate: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_f64(&partition[0], 1, "st_cluster_within_aggregate")?;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels = pg_agg::st_cluster_within_aggregate(&__geom_refs, __extra0)
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_within_aggregate: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Blob(__labels[__row_ix].as_wkb().into()))
            }
            10usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_cluster_within_win: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_within_win: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_cluster_within_win: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_within_win: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_f64(&partition[0], 1, "st_cluster_within_win")?;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_cluster::st_cluster_within_win(&__geom_refs, __extra0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_cluster_within_win: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Int64(__labels[__row_ix] as i64))
            }
            11usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_clusterdbscan: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterdbscan: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterdbscan: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterdbscan: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_f64(&partition[0], 1, "st_clusterdbscan")?;
                let __extra1 = dv_i64(&partition[0], 2, "st_clusterdbscan")? as u32;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels = pg_cluster::st_cluster_dbscan(&__geom_refs, __extra0, __extra1)
                    .map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterdbscan: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                match __labels[__row_ix] {
                    Some(__id) => Ok(types::Duckvalue::Int64(__id as i64)),
                    None => Ok(types::Duckvalue::Null),
                }
            }
            12usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_clusterintersecting_win: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterintersecting_win: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterintersecting_win: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterintersecting_win: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_cluster::st_cluster_intersecting_win(&__geom_refs).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterintersecting_win: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Int64(__labels[__row_ix] as i64))
            }
            13usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_clusterintersectingwin: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterintersectingwin: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterintersectingwin: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterintersectingwin: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_cluster::st_cluster_intersecting_win(&__geom_refs).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterintersectingwin: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Int64(__labels[__row_ix] as i64))
            }
            14usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_clusterkmeans: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterkmeans: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterkmeans: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterkmeans: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_i64(&partition[0], 1, "st_clusterkmeans")? as u32;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_cluster::st_cluster_kmeans(&__geom_refs, __extra0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterkmeans: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Int64(__labels[__row_ix] as i64))
            }
            15usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_clusterwithin_win: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterwithin_win: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterwithin_win: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterwithin_win: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_f64(&partition[0], 1, "st_clusterwithin_win")?;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_cluster::st_cluster_within_win(&__geom_refs, __extra0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterwithin_win: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Int64(__labels[__row_ix] as i64))
            }
            16usize => {
                if partition.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let mut __geoms: Vec<Geometry> = Vec::with_capacity(partition.len());
                for (__ri, __row) in partition.iter().enumerate() {
                    if __row.is_empty() {
                        return Err(types::Duckerror::Invalidargument(format!(
                            "st_clusterwithinwin: partition row {} is empty",
                            __ri
                        )));
                    }
                    let bytes = match &__row[0] {
                        types::Duckvalue::Blob(b) => b.as_slice(),
                        types::Duckvalue::Text(t) => t.as_bytes(),
                        types::Duckvalue::Null => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterwithinwin: partition row {} col 0 is NULL",
                                __ri
                            )));
                        }
                        _ => {
                            return Err(types::Duckerror::Invalidargument(format!(
                                "st_clusterwithinwin: partition row {} col 0 must be BLOB",
                                __ri
                            )));
                        }
                    };
                    __geoms.push(Geometry::from_wkb(bytes).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterwithinwin: row {} WKB decode: {}",
                            __ri,
                            postgis_err_string(e)
                        ))
                    })?);
                }
                let __extra0 = dv_f64(&partition[0], 1, "st_clusterwithinwin")?;
                let __geom_refs: Vec<&Geometry> = __geoms.iter().collect();
                let __labels =
                    pg_cluster::st_cluster_within_win(&__geom_refs, __extra0).map_err(|e| {
                        types::Duckerror::Invalidargument(format!(
                            "st_clusterwithinwin: {}",
                            postgis_err_string(e)
                        ))
                    })?;
                if __labels.is_empty() {
                    return Ok(types::Duckvalue::Null);
                }
                let __row_ix = if frame.end == 0 {
                    0usize
                } else {
                    let __e = (frame.end as usize).saturating_sub(1);
                    core::cmp::min(__e, __labels.len() - 1)
                };
                Ok(types::Duckvalue::Int64(__labels[__row_ix] as i64))
            }
            _ => Err(types::Duckerror::Internal(format!(
                "unknown window arm index {}",
                arm_idx
            ))),
        }
    }
}

fn register_logical_types() -> Result<(), types::Duckerror> {
    catalog::register_logical_type(&catalog::LogicalType {
        name: "GEOMETRY".into(),
        physical: "BLOB".into(),
    })
    .map_err(|e| types::Duckerror::Internal(format!("register-logical-type GEOMETRY: {}", e)))?;
    catalog::register_logical_type(&catalog::LogicalType {
        name: "GEOGRAPHY".into(),
        physical: "BLOB".into(),
    })
    .map_err(|e| types::Duckerror::Internal(format!("register-logical-type GEOGRAPHY: {}", e)))?;
    catalog::register_logical_type(&catalog::LogicalType {
        name: "RASTER".into(),
        physical: "BLOB".into(),
    })
    .map_err(|e| types::Duckerror::Internal(format!("register-logical-type RASTER: {}", e)))?;
    catalog::register_logical_type(&catalog::LogicalType {
        name: "TOPOLOGY".into(),
        physical: "BLOB".into(),
    })
    .map_err(|e| types::Duckerror::Internal(format!("register-logical-type TOPOLOGY: {}", e)))?;
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "GEOMETRY".into(),
            to: "BLOB".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(GEOMETRY -> BLOB): {}", e))
        })?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "GEOMETRY".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(BLOB -> GEOMETRY): {}", e))
        })?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "GEOGRAPHY".into(),
            to: "BLOB".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(GEOGRAPHY -> BLOB): {}", e))
        })?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "GEOGRAPHY".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(BLOB -> GEOGRAPHY): {}", e))
        })?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "RASTER".into(),
            to: "BLOB".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(RASTER -> BLOB): {}", e))
        })?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "RASTER".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(BLOB -> RASTER): {}", e))
        })?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "TOPOLOGY".into(),
            to: "BLOB".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(TOPOLOGY -> BLOB): {}", e))
        })?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        identity_cast_handles()
            .lock()
            .expect("identity cast handles mutex poisoned")
            .insert(handle);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "TOPOLOGY".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(BLOB -> TOPOLOGY): {}", e))
        })?;
    }
    Ok(())
}

fn register_scalars() -> Result<(), types::Duckerror> {
    let capability = runtime::get_capability(types::Capabilitykind::Scalar).ok_or_else(|| {
        types::Duckerror::Internal("host did not expose scalar capability".into())
    })?;
    let registry = match capability {
        runtime::Capability::Scalar(r) => r,
        _ => {
            return Err(types::Duckerror::Internal(
                "scalar capability returned unexpected variant".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("add_edge (sqlink-shim-codegen)".into()),
            tags: vec!["add_edge".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "add_edge",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("add_edge_mod_face (sqlink-shim-codegen)".into()),
            tags: vec!["add_edge_mod_face".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "add_edge_mod_face",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("add_edge_new_faces (sqlink-shim-codegen)".into()),
            tags: vec!["add_edge_new_faces".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "add_edge_new_faces",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("add_face (sqlink-shim-codegen)".into()),
            tags: vec!["add_face".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "add_face",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 4usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("add_iso_edge (sqlink-shim-codegen)".into()),
            tags: vec!["add_iso_edge".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "add_iso_edge",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 5usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("add_iso_node (sqlink-shim-codegen)".into()),
            tags: vec!["add_iso_node".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "add_iso_node",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 6usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("add_node (sqlink-shim-codegen)".into()),
            tags: vec!["add_node".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "add_node",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 7usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("add_topogeom_column (sqlink-shim-codegen)".into()),
            tags: vec!["add_topogeom_column".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "add_topogeom_column",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("as_gml (sqlink-shim-codegen)".into()),
            tags: vec!["as_gml".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "as_gml",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 9usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("as_topojson (sqlink-shim-codegen)".into()),
            tags: vec!["as_topojson".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "as_topojson",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 10usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("as_topojson_with_edge_map (sqlink-shim-codegen)".into()),
            tags: vec!["as_topojson_with_edge_map".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "as_topojson_with_edge_map",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 11usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Int64,
        }];
        let opts = runtime::Funcopts {
            description: Some("build (sqlink-shim-codegen)".into()),
            tags: vec!["build".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "build",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 12usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("cg_constrained_delaunay_triangles (sqlink-shim-codegen)".into()),
            tags: vec!["cg_constrained_delaunay_triangles".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "cg_constrained_delaunay_triangles",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 13usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("cg_polygon_repair (sqlink-shim-codegen)".into()),
            tags: vec!["cg_polygon_repair".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "cg_polygon_repair",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 14usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("cg_threed_alpha_wrapping (sqlink-shim-codegen)".into()),
            tags: vec!["cg_threed_alpha_wrapping".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "cg_threed_alpha_wrapping",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 15usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("change_edge_geom (sqlink-shim-codegen)".into()),
            tags: vec!["change_edge_geom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "change_edge_geom",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 16usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("copy_topology (sqlink-shim-codegen)".into()),
            tags: vec!["copy_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "copy_topology",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 17usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("count_words (sqlink-shim-codegen)".into()),
            tags: vec!["count_words".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "count_words",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 18usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Int64,
        }];
        let opts = runtime::Funcopts {
            description: Some("create_index (sqlink-shim-codegen)".into()),
            tags: vec!["create_index".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "create_index",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 19usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("create_raster (sqlink-shim-codegen)".into()),
            tags: vec!["create_raster".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "create_raster",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 20usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("create_topo_geom (sqlink-shim-codegen)".into()),
            tags: vec!["create_topo_geom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "create_topo_geom",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 21usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("create_topology (sqlink-shim-codegen)".into()),
            tags: vec!["create_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "create_topology",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 22usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("cull_null (sqlink-shim-codegen)".into()),
            tags: vec!["cull_null".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "cull_null",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 23usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Int64,
        }];
        let opts = runtime::Funcopts {
            description: Some("destroy_index (sqlink-shim-codegen)".into()),
            tags: vec!["destroy_index".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "destroy_index",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 24usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("drop_topogeom_column (sqlink-shim-codegen)".into()),
            tags: vec!["drop_topogeom_column".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "drop_topogeom_column",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 25usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("drop_topology (sqlink-shim-codegen)".into()),
            tags: vec!["drop_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "drop_topology",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 26usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("from_binary (sqlink-shim-codegen)".into()),
            tags: vec!["from_binary".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "from_binary",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 27usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("from_bytes (sqlink-shim-codegen)".into()),
            tags: vec!["from_bytes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "from_bytes",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 28usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("get_edge_by_point (sqlink-shim-codegen)".into()),
            tags: vec!["get_edge_by_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_edge_by_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 29usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("get_edge_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["get_edge_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_edge_geometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 30usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("get_face_by_point (sqlink-shim-codegen)".into()),
            tags: vec!["get_face_by_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_face_by_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 31usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("get_face_containing_point (sqlink-shim-codegen)".into()),
            tags: vec!["get_face_containing_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_face_containing_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 32usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("get_face_edges (sqlink-shim-codegen)".into()),
            tags: vec!["get_face_edges".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_face_edges",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 33usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("get_face_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["get_face_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_face_geometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 34usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("get_node_by_point (sqlink-shim-codegen)".into()),
            tags: vec!["get_node_by_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_node_by_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 35usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("get_node_edges (sqlink-shim-codegen)".into()),
            tags: vec!["get_node_edges".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_node_edges",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 36usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("get_node_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["get_node_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_node_geometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 37usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("get_ring_edges (sqlink-shim-codegen)".into()),
            tags: vec!["get_ring_edges".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_ring_edges",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 38usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("get_topology_id (sqlink-shim-codegen)".into()),
            tags: vec!["get_topology_id".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_topology_id",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 39usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("get_topology_name (sqlink-shim-codegen)".into()),
            tags: vec!["get_topology_name".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_topology_name",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 40usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("get_topology_srid (sqlink-shim-codegen)".into()),
            tags: vec!["get_topology_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "get_topology_srid",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 41usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("insert_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["insert_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "insert_wkb",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 42usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("list_cast_rewrites (sqlink-shim-codegen)".into()),
            tags: vec!["list_cast_rewrites".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "list_cast_rewrites",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 43usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("list_operator_rewrites (sqlink-shim-codegen)".into()),
            tags: vec!["list_operator_rewrites".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "list_operator_rewrites",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 44usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("list_preprocessor_patterns (sqlink-shim-codegen)".into()),
            tags: vec!["list_preprocessor_patterns".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "list_preprocessor_patterns",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 45usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("make_topo_element (sqlink-shim-codegen)".into()),
            tags: vec!["make_topo_element".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "make_topo_element",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 46usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("make_topology_precise (sqlink-shim-codegen)".into()),
            tags: vec!["make_topology_precise".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "make_topology_precise",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 47usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("mod_edge_heal (sqlink-shim-codegen)".into()),
            tags: vec!["mod_edge_heal".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "mod_edge_heal",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 48usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("mod_edge_split (sqlink-shim-codegen)".into()),
            tags: vec!["mod_edge_split".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "mod_edge_split",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 49usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("move_iso_node (sqlink-shim-codegen)".into()),
            tags: vec!["move_iso_node".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "move_iso_node",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 50usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("nearest (sqlink-shim-codegen)".into()),
            tags: vec!["nearest".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "nearest",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 51usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("new_edge_heal (sqlink-shim-codegen)".into()),
            tags: vec!["new_edge_heal".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "new_edge_heal",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 52usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("new_edges_split (sqlink-shim-codegen)".into()),
            tags: vec!["new_edges_split".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "new_edges_split",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 53usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("normalize_address (sqlink-shim-codegen)".into()),
            tags: vec!["normalize_address".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "normalize_address",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 54usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("op_bbox_distance (sqlink-shim-codegen)".into()),
            tags: vec!["op_bbox_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "op_bbox_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 55usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("op_bbox_intersects_nd (sqlink-shim-codegen)".into()),
            tags: vec!["op_bbox_intersects_nd".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "op_bbox_intersects_nd",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 56usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("op_bbox_intersects_twod (sqlink-shim-codegen)".into()),
            tags: vec!["op_bbox_intersects_twod".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "op_bbox_intersects_twod",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 57usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("op_equals_spatially (sqlink-shim-codegen)".into()),
            tags: vec!["op_equals_spatially".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "op_equals_spatially",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 58usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("op_knn_distance (sqlink-shim-codegen)".into()),
            tags: vec!["op_knn_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "op_knn_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 59usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("pagc_normalize_address (sqlink-shim-codegen)".into()),
            tags: vec!["pagc_normalize_address".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "pagc_normalize_address",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 60usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("parse_address (sqlink-shim-codegen)".into()),
            tags: vec!["parse_address".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "parse_address",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 61usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("polygonize (sqlink-shim-codegen)".into()),
            tags: vec!["polygonize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "polygonize",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 62usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("postgis_addbbox (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_addbbox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_addbbox",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 63usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("postgis_dropbbox (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_dropbbox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_dropbbox",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 64usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_full_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_full_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_full_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 65usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_geos_compiled_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_geos_compiled_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_geos_compiled_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 66usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("postgis_geos_noop (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_geos_noop".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_geos_noop",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 67usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_geos_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_geos_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_geos_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 68usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("postgis_getbbox (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_getbbox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_getbbox",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 69usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("postgis_hasbbox (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_hasbbox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_hasbbox",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 70usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_lib_build_date (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_lib_build_date".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_lib_build_date",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 71usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_lib_revision (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_lib_revision".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_lib_revision",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 72usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_lib_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_lib_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_lib_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 73usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_libjson_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_libjson_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_libjson_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 74usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_liblwgeom_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_liblwgeom_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_liblwgeom_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 75usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_libprotobuf_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_libprotobuf_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_libprotobuf_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 76usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_libxml_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_libxml_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_libxml_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 77usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("postgis_noop (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_noop".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_noop",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 78usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_proj_compiled_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_proj_compiled_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_proj_compiled_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 79usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_proj_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_proj_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_proj_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 80usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_scripts_build_date (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_scripts_build_date".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_scripts_build_date",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 81usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_scripts_installed (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_scripts_installed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_scripts_installed",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 82usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_scripts_released (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_scripts_released".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_scripts_released",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 83usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_svn_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_svn_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_svn_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 84usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_topology_scripts_installed (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_topology_scripts_installed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_topology_scripts_installed",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 85usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("postgis_type_name (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_type_name".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_type_name",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 86usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 87usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![];
        let opts = runtime::Funcopts {
            description: Some("postgis_wagyu_version (sqlink-shim-codegen)".into()),
            tags: vec!["postgis_wagyu_version".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "postgis_wagyu_version",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 88usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("query_envelope (sqlink-shim-codegen)".into()),
            tags: vec!["query_envelope".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "query_envelope",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 89usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("query_knn (sqlink-shim-codegen)".into()),
            tags: vec!["query_knn".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "query_knn",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 90usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("query_within_distance (sqlink-shim-codegen)".into()),
            tags: vec!["query_within_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "query_within_distance",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 91usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("rast_dfer_all_batch (sqlink-shim-codegen)".into()),
            tags: vec!["rast_dfer_all_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "rast_dfer_all_batch",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 92usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("rast_metadata_all_batch (sqlink-shim-codegen)".into()),
            tags: vec!["rast_metadata_all_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "rast_metadata_all_batch",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 93usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("rem_edge_mod_face (sqlink-shim-codegen)".into()),
            tags: vec!["rem_edge_mod_face".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "rem_edge_mod_face",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 94usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("rem_edge_new_face (sqlink-shim-codegen)".into()),
            tags: vec!["rem_edge_new_face".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "rem_edge_new_face",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 95usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("remove_face (sqlink-shim-codegen)".into()),
            tags: vec!["remove_face".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "remove_face",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 96usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("remove_iso_edge (sqlink-shim-codegen)".into()),
            tags: vec!["remove_iso_edge".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "remove_iso_edge",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 97usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("remove_iso_node (sqlink-shim-codegen)".into()),
            tags: vec!["remove_iso_node".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "remove_iso_node",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 98usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("remove_unused_primitives (sqlink-shim-codegen)".into()),
            tags: vec!["remove_unused_primitives".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "remove_unused_primitives",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 99usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("rename_topology (sqlink-shim-codegen)".into()),
            tags: vec!["rename_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "rename_topology",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 100usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_closest_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_3d_closest_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_closest_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 101usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_contains (sqlink-shim-codegen)".into()),
            tags: vec!["st_3d_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 102usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_d_within (sqlink-shim-codegen)".into()),
            tags: vec!["st_3d_d_within".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_d_within",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 103usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_dwithin (sqlink-shim-codegen)".into()),
            tags: vec!["st_3d_dwithin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_dwithin",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 104usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_intersects (sqlink-shim-codegen)".into()),
            tags: vec!["st_3d_intersects".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_intersects",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 105usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_line_interpolate_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_3d_line_interpolate_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_line_interpolate_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 106usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_longest_line (sqlink-shim-codegen)".into()),
            tags: vec!["st_3d_longest_line".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_longest_line",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 107usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_make_box (sqlink-shim-codegen)".into()),
            tags: vec!["st_3d_make_box".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_make_box",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 108usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_max_distance (sqlink-shim-codegen)".into()),
            tags: vec!["st_3d_max_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_max_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 109usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3d_shortest_line (sqlink-shim-codegen)".into()),
            tags: vec!["st_3d_shortest_line".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_shortest_line",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 110usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_3darea (sqlink-shim-codegen)".into()),
            tags: vec!["st_3darea".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3darea",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 111usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_3dboundary (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dboundary".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dboundary",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 112usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_3dcentroid (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dcentroid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dcentroid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 113usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dclosestpoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dclosestpoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dclosestpoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 114usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dcontains (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dcontains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dcontains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 115usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_3dconvexhull (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dconvexhull".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dconvexhull",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 116usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3ddifference (sqlink-shim-codegen)".into()),
            tags: vec!["st_3ddifference".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3ddifference",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 117usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3ddistance (sqlink-shim-codegen)".into()),
            tags: vec!["st_3ddistance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3ddistance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 118usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3ddwithin (sqlink-shim-codegen)".into()),
            tags: vec!["st_3ddwithin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3ddwithin",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 119usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_3denvelope (sqlink-shim-codegen)".into()),
            tags: vec!["st_3denvelope".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3denvelope",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 120usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dintersection (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dintersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dintersection",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 121usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dintersects (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dintersects".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dintersects",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 122usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_3dlength (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dlength".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dlength",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 123usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dlineinterpolatepoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dlineinterpolatepoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dlineinterpolatepoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 124usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dlongestline (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dlongestline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dlongestline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 125usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dmakebox (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dmakebox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dmakebox",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 126usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dmaxdistance (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dmaxdistance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dmaxdistance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 127usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_3dperimeter (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dperimeter".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dperimeter",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 128usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_3dreverse (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dreverse".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dreverse",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 129usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3drotate (sqlink-shim-codegen)".into()),
            tags: vec!["st_3drotate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3drotate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 130usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dscale (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dscale".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dscale",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 131usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dshortestline (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dshortestline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dshortestline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 132usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dtranslate (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dtranslate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dtranslate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 133usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_3dunion (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dunion".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dunion",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 134usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_add_band (sqlink-shim-codegen)".into()),
            tags: vec!["st_add_band".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_add_band",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 135usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_add_face (sqlink-shim-codegen)".into()),
            tags: vec!["st_add_face".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_add_face",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 136usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_add_iso_edge (sqlink-shim-codegen)".into()),
            tags: vec!["st_add_iso_edge".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_add_iso_edge",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 137usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_add_iso_node (sqlink-shim-codegen)".into()),
            tags: vec!["st_add_iso_node".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_add_iso_node",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 138usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_add_measure (sqlink-shim-codegen)".into()),
            tags: vec!["st_add_measure".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_add_measure",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 139usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_add_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_add_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_add_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 140usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_addband (sqlink-shim-codegen)".into()),
            tags: vec!["st_addband".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_addband",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 141usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_addface (sqlink-shim-codegen)".into()),
            tags: vec!["st_addface".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_addface",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 142usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_addisoedge (sqlink-shim-codegen)".into()),
            tags: vec!["st_addisoedge".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_addisoedge",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 143usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_addisonode (sqlink-shim-codegen)".into()),
            tags: vec!["st_addisonode".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_addisonode",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 144usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_addmeasure (sqlink-shim-codegen)".into()),
            tags: vec!["st_addmeasure".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_addmeasure",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 145usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_addpoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_addpoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_addpoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 146usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_affine (sqlink-shim-codegen)".into()),
            tags: vec!["st_affine".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_affine",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 147usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_alpha_shape (sqlink-shim-codegen)".into()),
            tags: vec!["st_alpha_shape".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_alpha_shape",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 148usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_angle (sqlink-shim-codegen)".into()),
            tags: vec!["st_angle".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_angle",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 149usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_approx_convex_partition (sqlink-shim-codegen)".into()),
            tags: vec!["st_approx_convex_partition".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_convex_partition",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 150usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_histogram (sqlink-shim-codegen)".into()),
            tags: vec!["st_approx_histogram".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_histogram",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 151usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_quantile (sqlink-shim-codegen)".into()),
            tags: vec!["st_approx_quantile".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_quantile",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 152usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_quantiles (sqlink-shim-codegen)".into()),
            tags: vec!["st_approx_quantiles".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_quantiles",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 153usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_summary_stats (sqlink-shim-codegen)".into()),
            tags: vec!["st_approx_summary_stats".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_summary_stats",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 154usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_value_count (sqlink-shim-codegen)".into()),
            tags: vec!["st_approx_value_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_value_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 155usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_value_counts (sqlink-shim-codegen)".into()),
            tags: vec!["st_approx_value_counts".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_value_counts",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 156usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_value_percent (sqlink-shim-codegen)".into()),
            tags: vec!["st_approx_value_percent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_value_percent",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 157usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_value_percents (sqlink-shim-codegen)".into()),
            tags: vec!["st_approx_value_percents".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_value_percents",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 158usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_approximate_medial_axis (sqlink-shim-codegen)".into()),
            tags: vec!["st_approximate_medial_axis".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approximate_medial_axis",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 159usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_approximatemedialaxis (sqlink-shim-codegen)".into()),
            tags: vec!["st_approximatemedialaxis".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approximatemedialaxis",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 160usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_area (sqlink-shim-codegen)".into()),
            tags: vec!["st_area".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_area",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 161usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_area_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_area_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_area_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 162usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_area_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_area_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_area_threed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 163usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_area_twod (sqlink-shim-codegen)".into()),
            tags: vec!["st_area_twod".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_area_twod",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 164usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_areathreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_areathreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_areathreed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 165usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_binary (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_binary".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_binary",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 166usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_encoded_polyline (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_encoded_polyline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_encoded_polyline",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 167usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_ewkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_ewkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_ewkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 168usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_ewkt",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 169usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_ewkt_precision (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_ewkt_precision".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_ewkt_precision",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 170usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_flatgeobuf (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_flatgeobuf".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_flatgeobuf",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 171usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_gdal_raster (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_gdal_raster".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_gdal_raster",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 172usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_geobuf (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_geobuf".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_geobuf",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 173usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_geojson (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_geojson".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_geojson",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 174usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_geojson_full (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_geojson_full".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_geojson_full",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 175usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_gml (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_gml".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_gml",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 176usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_gml_full (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_gml_full".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_gml_full",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 177usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_hexewkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_hexewkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_hexewkb",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 178usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_jpeg (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_jpeg".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_jpeg",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 179usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_kml (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_kml".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_kml",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 180usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_kml_full (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_kml_full".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_kml_full",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 181usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_lat_lon_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_lat_lon_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_lat_lon_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 182usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_marc21 (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_marc21".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_marc21",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 183usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_marc21_fmt (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_marc21_fmt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_marc21_fmt",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 184usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_mvt (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_mvt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_mvt",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 185usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg7".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_mvt_geom (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_mvt_geom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_mvt_geom",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 186usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_png (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_png".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_png",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 187usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_svg (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_svg".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_svg",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 188usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_svg_full (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_svg_full".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_svg_full",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 189usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_text",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 190usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_text_precision (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_text_precision".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_text_precision",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 191usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_tiff (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_tiff".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_tiff",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 192usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_topo_json (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_topo_json".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_topo_json",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 193usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_twkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_twkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_twkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 194usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_as_x3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_x3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_x3d",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 195usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_as_x3d_full (sqlink-shim-codegen)".into()),
            tags: vec!["st_as_x3d_full".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_as_x3d_full",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 196usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_asbinary (sqlink-shim-codegen)".into()),
            tags: vec!["st_asbinary".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_asbinary",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 197usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_asencodedpolyline (sqlink-shim-codegen)".into()),
            tags: vec!["st_asencodedpolyline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_asencodedpolyline",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 198usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_asewkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_asewkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_asewkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 199usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_asewkt (sqlink-shim-codegen)".into()),
            tags: vec!["st_asewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_asewkt",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 200usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_asgeojson (sqlink-shim-codegen)".into()),
            tags: vec!["st_asgeojson".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_asgeojson",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 201usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_asgml (sqlink-shim-codegen)".into()),
            tags: vec!["st_asgml".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_asgml",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 202usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_ashexewkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_ashexewkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_ashexewkb",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 203usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_askml (sqlink-shim-codegen)".into()),
            tags: vec!["st_askml".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_askml",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 204usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_aslatlontext (sqlink-shim-codegen)".into()),
            tags: vec!["st_aslatlontext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_aslatlontext",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 205usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_asmarc21 (sqlink-shim-codegen)".into()),
            tags: vec!["st_asmarc21".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_asmarc21",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 206usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_asmvt (sqlink-shim-codegen)".into()),
            tags: vec!["st_asmvt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_asmvt",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 207usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg7".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_asmvtgeom (sqlink-shim-codegen)".into()),
            tags: vec!["st_asmvtgeom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_asmvtgeom",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 208usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_aspect (sqlink-shim-codegen)".into()),
            tags: vec!["st_aspect".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_aspect",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 209usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_aspng (sqlink-shim-codegen)".into()),
            tags: vec!["st_aspng".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_aspng",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 210usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_assvg (sqlink-shim-codegen)".into()),
            tags: vec!["st_assvg".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_assvg",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 211usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_astext (sqlink-shim-codegen)".into()),
            tags: vec!["st_astext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_astext",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 212usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_astiff (sqlink-shim-codegen)".into()),
            tags: vec!["st_astiff".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_astiff",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 213usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_astopojson (sqlink-shim-codegen)".into()),
            tags: vec!["st_astopojson".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_astopojson",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 214usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_astwkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_astwkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_astwkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 215usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_asx3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_asx3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_asx3d",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 216usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_azimuth (sqlink-shim-codegen)".into()),
            tags: vec!["st_azimuth".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_azimuth",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 217usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_band_count (sqlink-shim-codegen)".into()),
            tags: vec!["st_band_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_band_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 218usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_band_max (sqlink-shim-codegen)".into()),
            tags: vec!["st_band_max".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_band_max",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 219usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_band_mean (sqlink-shim-codegen)".into()),
            tags: vec!["st_band_mean".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_band_mean",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 220usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_band_metadata (sqlink-shim-codegen)".into()),
            tags: vec!["st_band_metadata".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_band_metadata",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 221usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_band_min (sqlink-shim-codegen)".into()),
            tags: vec!["st_band_min".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_band_min",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 222usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_band_nodata_value (sqlink-shim-codegen)".into()),
            tags: vec!["st_band_nodata_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_band_nodata_value",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 223usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_band_pixel_type (sqlink-shim-codegen)".into()),
            tags: vec!["st_band_pixel_type".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_band_pixel_type",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 224usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_band_stddev (sqlink-shim-codegen)".into()),
            tags: vec!["st_band_stddev".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_band_stddev",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 225usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_band_sum (sqlink-shim-codegen)".into()),
            tags: vec!["st_band_sum".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_band_sum",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 226usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_bandnodatavalue (sqlink-shim-codegen)".into()),
            tags: vec!["st_bandnodatavalue".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_bandnodatavalue",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 227usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_bandpixeltype (sqlink-shim-codegen)".into()),
            tags: vec!["st_bandpixeltype".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_bandpixeltype",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 228usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_bands_metadata (sqlink-shim-codegen)".into()),
            tags: vec!["st_bands_metadata".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_bands_metadata",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 229usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_bboxdistance (sqlink-shim-codegen)".into()),
            tags: vec!["st_bboxdistance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_bboxdistance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 230usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_bboxintersects (sqlink-shim-codegen)".into()),
            tags: vec!["st_bboxintersects".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_bboxintersects",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 231usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_bboxintersectsnd (sqlink-shim-codegen)".into()),
            tags: vec!["st_bboxintersectsnd".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_bboxintersectsnd",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 232usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_bd_mpoly_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_bd_mpoly_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_bd_mpoly_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 233usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_bd_poly_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_bd_poly_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_bd_poly_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 234usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_bdmpolyfromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_bdmpolyfromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_bdmpolyfromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 235usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_bdpolyfromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_bdpolyfromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_bdpolyfromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 236usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_boundary (sqlink-shim-codegen)".into()),
            tags: vec!["st_boundary".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_boundary",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 237usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_boundary_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_boundary_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_boundary_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 238usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_boundary_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_boundary_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_boundary_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 239usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_boundarythreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_boundarythreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_boundarythreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 240usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_bounding_diagonal (sqlink-shim-codegen)".into()),
            tags: vec!["st_bounding_diagonal".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_bounding_diagonal",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 241usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_boundingdiagonal (sqlink-shim-codegen)".into()),
            tags: vec!["st_boundingdiagonal".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_boundingdiagonal",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 242usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_box2d_from_geohash (sqlink-shim-codegen)".into()),
            tags: vec!["st_box2d_from_geohash".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_box2d_from_geohash",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 243usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_box2dfromgeohash (sqlink-shim-codegen)".into()),
            tags: vec!["st_box2dfromgeohash".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_box2dfromgeohash",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 244usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_box_from_geohash (sqlink-shim-codegen)".into()),
            tags: vec!["st_box_from_geohash".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_box_from_geohash",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 245usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_boxfromgeohash (sqlink-shim-codegen)".into()),
            tags: vec!["st_boxfromgeohash".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_boxfromgeohash",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 246usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_buffer (sqlink-shim-codegen)".into()),
            tags: vec!["st_buffer".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_buffer",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 247usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_buffer_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_buffer_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_buffer_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 248usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_buffer_quad_segs (sqlink-shim-codegen)".into()),
            tags: vec!["st_buffer_quad_segs".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_buffer_quad_segs",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 249usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_buffer_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_buffer_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_buffer_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 250usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_buffer_with_style (sqlink-shim-codegen)".into()),
            tags: vec!["st_buffer_with_style".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_buffer_with_style",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 251usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_build_area (sqlink-shim-codegen)".into()),
            tags: vec!["st_build_area".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_build_area",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 252usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_buildarea (sqlink-shim-codegen)".into()),
            tags: vec!["st_buildarea".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_buildarea",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 253usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_centroid (sqlink-shim-codegen)".into()),
            tags: vec!["st_centroid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_centroid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 254usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_centroid_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_centroid_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_centroid_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 255usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_centroid_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_centroid_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_centroid_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 256usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_centroidthreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_centroidthreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_centroidthreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 257usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_chaikin_smoothing (sqlink-shim-codegen)".into()),
            tags: vec!["st_chaikin_smoothing".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_chaikin_smoothing",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 258usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_chaikinsmoothing (sqlink-shim-codegen)".into()),
            tags: vec!["st_chaikinsmoothing".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_chaikinsmoothing",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 259usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_clip_by_box2d (sqlink-shim-codegen)".into()),
            tags: vec!["st_clip_by_box2d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clip_by_box2d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 260usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_clipbybox2d (sqlink-shim-codegen)".into()),
            tags: vec!["st_clipbybox2d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clipbybox2d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 261usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_closest_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_closest_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_closest_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 262usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_closest_point_3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_closest_point_3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_closest_point_3d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 263usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_closest_point_of_approach (sqlink-shim-codegen)".into()),
            tags: vec!["st_closest_point_of_approach".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_closest_point_of_approach",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 264usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_closest_point_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_closest_point_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_closest_point_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 265usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_closestpoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_closestpoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_closestpoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 266usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_collect (sqlink-shim-codegen)".into()),
            tags: vec!["st_collect".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_collect",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 267usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_collection_extract (sqlink-shim-codegen)".into()),
            tags: vec!["st_collection_extract".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_collection_extract",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 268usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_collection_homogenize (sqlink-shim-codegen)".into()),
            tags: vec!["st_collection_homogenize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_collection_homogenize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 269usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_collectionextract (sqlink-shim-codegen)".into()),
            tags: vec!["st_collectionextract".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_collectionextract",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 270usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_collectionhomogenize (sqlink-shim-codegen)".into()),
            tags: vec!["st_collectionhomogenize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_collectionhomogenize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 271usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_concave_hull (sqlink-shim-codegen)".into()),
            tags: vec!["st_concave_hull".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_concave_hull",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 272usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_concave_hull_holes (sqlink-shim-codegen)".into()),
            tags: vec!["st_concave_hull_holes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_concave_hull_holes",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 273usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_concavehull (sqlink-shim-codegen)".into()),
            tags: vec!["st_concavehull".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_concavehull",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 274usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_constrained_delaunay_triangles (sqlink-shim-codegen)".into()),
            tags: vec!["st_constrained_delaunay_triangles".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_constrained_delaunay_triangles",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 275usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_constraineddelaunaytriangles (sqlink-shim-codegen)".into()),
            tags: vec!["st_constraineddelaunaytriangles".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_constraineddelaunaytriangles",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 276usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_constrainedelaunaytriangles (sqlink-shim-codegen)".into()),
            tags: vec!["st_constrainedelaunaytriangles".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_constrainedelaunaytriangles",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 277usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_contains (sqlink-shim-codegen)".into()),
            tags: vec!["st_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 278usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_contains_3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_contains_3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_contains_3d",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 279usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_contains_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_contains_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_contains_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 280usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_contains_properly (sqlink-shim-codegen)".into()),
            tags: vec!["st_contains_properly".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_contains_properly",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 281usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_contains_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_contains_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_contains_threed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 282usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_containsproperly (sqlink-shim-codegen)".into()),
            tags: vec!["st_containsproperly".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_containsproperly",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 283usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_convex_hull (sqlink-shim-codegen)".into()),
            tags: vec!["st_convex_hull".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_convex_hull",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 284usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_convex_hull_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_convex_hull_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_convex_hull_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 285usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_convex_hull_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_convex_hull_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_convex_hull_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 286usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_convexhull (sqlink-shim-codegen)".into()),
            tags: vec!["st_convexhull".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_convexhull",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 287usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_convexhullthreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_convexhullthreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_convexhullthreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 288usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_coord_dim (sqlink-shim-codegen)".into()),
            tags: vec!["st_coord_dim".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coord_dim",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 289usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_coord_dim_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_coord_dim_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coord_dim_threed",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 290usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_coorddim (sqlink-shim-codegen)".into()),
            tags: vec!["st_coorddim".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coorddim",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 291usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_count (sqlink-shim-codegen)".into()),
            tags: vec!["st_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 292usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_coverage_clean (sqlink-shim-codegen)".into()),
            tags: vec!["st_coverage_clean".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coverage_clean",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 293usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_coverage_invalid_edges (sqlink-shim-codegen)".into()),
            tags: vec!["st_coverage_invalid_edges".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coverage_invalid_edges",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 294usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_coverage_simplify (sqlink-shim-codegen)".into()),
            tags: vec!["st_coverage_simplify".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coverage_simplify",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 295usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_coverage_union (sqlink-shim-codegen)".into()),
            tags: vec!["st_coverage_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coverage_union",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 296usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_coverageclean (sqlink-shim-codegen)".into()),
            tags: vec!["st_coverageclean".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coverageclean",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 297usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_coverageinvalidedges (sqlink-shim-codegen)".into()),
            tags: vec!["st_coverageinvalidedges".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coverageinvalidedges",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 298usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_coveragesimplify (sqlink-shim-codegen)".into()),
            tags: vec!["st_coveragesimplify".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coveragesimplify",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 299usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_covered_by (sqlink-shim-codegen)".into()),
            tags: vec!["st_covered_by".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_covered_by",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 300usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_covered_by_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_covered_by_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_covered_by_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 301usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_coveredby (sqlink-shim-codegen)".into()),
            tags: vec!["st_coveredby".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coveredby",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 302usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_covers (sqlink-shim-codegen)".into()),
            tags: vec!["st_covers".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_covers",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 303usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_covers_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_covers_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_covers_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 304usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_covers_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_covers_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_covers_threed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 305usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_cpa_within (sqlink-shim-codegen)".into()),
            tags: vec!["st_cpa_within".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cpa_within",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 306usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_cpawithin (sqlink-shim-codegen)".into()),
            tags: vec!["st_cpawithin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cpawithin",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 307usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_create_topo_geo (sqlink-shim-codegen)".into()),
            tags: vec!["st_create_topo_geo".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_create_topo_geo",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 308usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_create_topology (sqlink-shim-codegen)".into()),
            tags: vec!["st_create_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_create_topology",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 309usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_createtopology (sqlink-shim-codegen)".into()),
            tags: vec!["st_createtopology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_createtopology",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 310usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_crosses (sqlink-shim-codegen)".into()),
            tags: vec!["st_crosses".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_crosses",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 311usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_crosses_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_crosses_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_crosses_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 312usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_curve_n (sqlink-shim-codegen)".into()),
            tags: vec!["st_curve_n".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_curve_n",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 313usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_curve_to_line (sqlink-shim-codegen)".into()),
            tags: vec!["st_curve_to_line".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_curve_to_line",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 314usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_curven (sqlink-shim-codegen)".into()),
            tags: vec!["st_curven".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_curven",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 315usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_curvetoline (sqlink-shim-codegen)".into()),
            tags: vec!["st_curvetoline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_curvetoline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 316usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_d_within (sqlink-shim-codegen)".into()),
            tags: vec!["st_d_within".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_d_within",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 317usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_delaunay_triangles (sqlink-shim-codegen)".into()),
            tags: vec!["st_delaunay_triangles".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_delaunay_triangles",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 318usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_delaunaytriangles (sqlink-shim-codegen)".into()),
            tags: vec!["st_delaunaytriangles".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_delaunaytriangles",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 319usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_dfully_within (sqlink-shim-codegen)".into()),
            tags: vec!["st_dfully_within".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dfully_within",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 320usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_dfully_within_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_dfully_within_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dfully_within_threed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 321usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_dfullywithin (sqlink-shim-codegen)".into()),
            tags: vec!["st_dfullywithin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dfullywithin",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 322usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_difference (sqlink-shim-codegen)".into()),
            tags: vec!["st_difference".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_difference",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 323usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_difference_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_difference_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_difference_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 324usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_difference_gridsize (sqlink-shim-codegen)".into()),
            tags: vec!["st_difference_gridsize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_difference_gridsize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 325usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_difference_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_difference_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_difference_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 326usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_differencethreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_differencethreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_differencethreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 327usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_dimension (sqlink-shim-codegen)".into()),
            tags: vec!["st_dimension".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dimension",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 328usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_disjoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_disjoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_disjoint",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 329usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_disjoint_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_disjoint_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_disjoint_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 330usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distance (sqlink-shim-codegen)".into()),
            tags: vec!["st_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 331usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_distance_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 332usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_cpa (sqlink-shim-codegen)".into()),
            tags: vec!["st_distance_cpa".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_cpa",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 333usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_sphere (sqlink-shim-codegen)".into()),
            tags: vec!["st_distance_sphere".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_sphere",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 334usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_sphere_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_distance_sphere_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_sphere_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 335usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_sphere_radius (sqlink-shim-codegen)".into()),
            tags: vec!["st_distance_sphere_radius".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_sphere_radius",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 336usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_spheroid (sqlink-shim-codegen)".into()),
            tags: vec!["st_distance_spheroid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_spheroid",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 337usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_spheroid_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_distance_spheroid_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_spheroid_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 338usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distance_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_distance_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distance_threed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 339usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distancecpa (sqlink-shim-codegen)".into()),
            tags: vec!["st_distancecpa".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distancecpa",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 340usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distancesphere (sqlink-shim-codegen)".into()),
            tags: vec!["st_distancesphere".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distancesphere",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 341usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distancespheroid (sqlink-shim-codegen)".into()),
            tags: vec!["st_distancespheroid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distancespheroid",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 342usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_distancethreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_distancethreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_distancethreed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 343usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_dump_as_polygons (sqlink-shim-codegen)".into()),
            tags: vec!["st_dump_as_polygons".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dump_as_polygons",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 344usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_dump_values (sqlink-shim-codegen)".into()),
            tags: vec!["st_dump_values".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dump_values",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 345usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_dump_values_multi (sqlink-shim-codegen)".into()),
            tags: vec!["st_dump_values_multi".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dump_values_multi",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 346usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_dumpaspolygons (sqlink-shim-codegen)".into()),
            tags: vec!["st_dumpaspolygons".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dumpaspolygons",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 347usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_dumpvalues (sqlink-shim-codegen)".into()),
            tags: vec!["st_dumpvalues".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dumpvalues",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 348usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_dwithin (sqlink-shim-codegen)".into()),
            tags: vec!["st_dwithin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dwithin",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 349usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_dwithin_3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_dwithin_3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dwithin_3d",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 350usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_dwithin_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_dwithin_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_dwithin_threed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 351usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_end_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_end_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_end_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 352usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_end_point_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_end_point_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_end_point_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 353usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_endpoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_endpoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_endpoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 354usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_envelope (sqlink-shim-codegen)".into()),
            tags: vec!["st_envelope".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_envelope",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 355usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_envelope_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_envelope_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_envelope_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 356usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_envelope_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_envelope_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_envelope_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 357usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_envelopethreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_envelopethreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_envelopethreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 358usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_equals (sqlink-shim-codegen)".into()),
            tags: vec!["st_equals".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_equals",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 359usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_equals_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_equals_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_equals_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 360usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_equals_exact (sqlink-shim-codegen)".into()),
            tags: vec!["st_equals_exact".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_equals_exact",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 361usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_equalsexact (sqlink-shim-codegen)".into()),
            tags: vec!["st_equalsexact".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_equalsexact",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 362usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_expand (sqlink-shim-codegen)".into()),
            tags: vec!["st_expand".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_expand",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 363usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_expand_xy (sqlink-shim-codegen)".into()),
            tags: vec!["st_expand_xy".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_expand_xy",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 364usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_expand_xyz (sqlink-shim-codegen)".into()),
            tags: vec!["st_expand_xyz".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_expand_xyz",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 365usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_expand_xyzm (sqlink-shim-codegen)".into()),
            tags: vec!["st_expand_xyzm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_expand_xyzm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 366usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_extent_threed_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_extent_threed_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_extent_threed_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 367usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_exterior_ring (sqlink-shim-codegen)".into()),
            tags: vec!["st_exterior_ring".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_exterior_ring",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 368usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_exterior_ring_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_exterior_ring_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_exterior_ring_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 369usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_exteriorring (sqlink-shim-codegen)".into()),
            tags: vec!["st_exteriorring".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_exteriorring",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 370usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_extrude (sqlink-shim-codegen)".into()),
            tags: vec!["st_extrude".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_extrude",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 371usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_extrude_polygon_straight_skeleton (sqlink-shim-codegen)".into()),
            tags: vec!["st_extrude_polygon_straight_skeleton".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_extrude_polygon_straight_skeleton",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 372usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_extrude_straight (sqlink-shim-codegen)".into()),
            tags: vec!["st_extrude_straight".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_extrude_straight",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 373usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_extrude_straight_skeleton (sqlink-shim-codegen)".into()),
            tags: vec!["st_extrude_straight_skeleton".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_extrude_straight_skeleton",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 374usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_filter_by_m (sqlink-shim-codegen)".into()),
            tags: vec!["st_filter_by_m".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_filter_by_m",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 375usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_filterbym (sqlink-shim-codegen)".into()),
            tags: vec!["st_filterbym".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_filterbym",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 376usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_flip_coordinates (sqlink-shim-codegen)".into()),
            tags: vec!["st_flip_coordinates".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_flip_coordinates",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 377usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_flipcoordinates (sqlink-shim-codegen)".into()),
            tags: vec!["st_flipcoordinates".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_flipcoordinates",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 378usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force2d (sqlink-shim-codegen)".into()),
            tags: vec!["st_force2d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force2d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 379usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_force3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force3d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 380usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force3dm (sqlink-shim-codegen)".into()),
            tags: vec!["st_force3dm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force3dm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 381usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force3dz (sqlink-shim-codegen)".into()),
            tags: vec!["st_force3dz".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force3dz",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 382usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force4d (sqlink-shim-codegen)".into()),
            tags: vec!["st_force4d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force4d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 383usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_2d (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_2d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_2d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 384usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_3d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 385usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_3dm (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_3dm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_3dm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 386usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_4d (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_4d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_4d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 387usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_collection (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_collection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_collection",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 388usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_curve (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_curve".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_curve",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 389usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_fourd (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_fourd".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_fourd",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 390usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_lhr (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_lhr".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_lhr",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 391usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_polygon_ccw (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_polygon_ccw".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_polygon_ccw",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 392usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_polygon_cw (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_polygon_cw".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_polygon_cw",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 393usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_rhr (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_rhr".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_rhr",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 394usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_sfs (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_sfs".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_sfs",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 395usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 396usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_threedm (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_threedm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_threedm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 397usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_threedz (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_threedz".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_threedz",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 398usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_force_twod (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_twod".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_twod",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 399usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_force_valid (sqlink-shim-codegen)".into()),
            tags: vec!["st_force_valid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_force_valid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 400usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_forcecollection (sqlink-shim-codegen)".into()),
            tags: vec!["st_forcecollection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_forcecollection",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 401usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_forcecurve (sqlink-shim-codegen)".into()),
            tags: vec!["st_forcecurve".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_forcecurve",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 402usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_forcefourd (sqlink-shim-codegen)".into()),
            tags: vec!["st_forcefourd".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_forcefourd",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 403usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_forcepolygonccw (sqlink-shim-codegen)".into()),
            tags: vec!["st_forcepolygonccw".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_forcepolygonccw",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 404usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_forcepolygoncw (sqlink-shim-codegen)".into()),
            tags: vec!["st_forcepolygoncw".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_forcepolygoncw",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 405usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_forcerhr (sqlink-shim-codegen)".into()),
            tags: vec!["st_forcerhr".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_forcerhr",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 406usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_forcesfs (sqlink-shim-codegen)".into()),
            tags: vec!["st_forcesfs".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_forcesfs",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 407usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_forcethreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_forcethreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_forcethreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 408usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_forcethreedm (sqlink-shim-codegen)".into()),
            tags: vec!["st_forcethreedm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_forcethreedm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 409usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_forcethreedz (sqlink-shim-codegen)".into()),
            tags: vec!["st_forcethreedz".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_forcethreedz",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 410usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_forcetwod (sqlink-shim-codegen)".into()),
            tags: vec!["st_forcetwod".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_forcetwod",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 411usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_frechet_distance (sqlink-shim-codegen)".into()),
            tags: vec!["st_frechet_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_frechet_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 412usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_frechetdistance (sqlink-shim-codegen)".into()),
            tags: vec!["st_frechetdistance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_frechetdistance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 413usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_from_flatgeobuf (sqlink-shim-codegen)".into()),
            tags: vec!["st_from_flatgeobuf".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_from_flatgeobuf",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 414usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_fromflatgeobuf (sqlink-shim-codegen)".into()),
            tags: vec!["st_fromflatgeobuf".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_fromflatgeobuf",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 415usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_generate_points (sqlink-shim-codegen)".into()),
            tags: vec!["st_generate_points".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_generate_points",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 416usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_generatepoints (sqlink-shim-codegen)".into()),
            tags: vec!["st_generatepoints".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_generatepoints",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 417usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geo_reference (sqlink-shim-codegen)".into()),
            tags: vec!["st_geo_reference".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geo_reference",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 418usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_area (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_area".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_area",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 419usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_area_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_area_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_area_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 420usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_azimuth (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_azimuth".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_azimuth",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 421usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_azimuth_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_azimuth_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_azimuth_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 422usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_buffer (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_buffer".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_buffer",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 423usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_buffer_with_segs (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_buffer_with_segs".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_buffer_with_segs",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 424usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_centroid (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_centroid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_centroid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 425usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_closest_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_closest_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_closest_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 426usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_convex_hull (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_convex_hull".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_convex_hull",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 427usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_covered_by (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_covered_by".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_covered_by",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 428usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_covers (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_covers".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_covers",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 429usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_difference (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_difference".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_difference",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 430usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_distance (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 431usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_distance_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_distance_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_distance_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 432usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_dwithin (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_dwithin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_dwithin",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 433usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_dwithin_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_dwithin_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_dwithin_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 434usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_expand (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_expand".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_expand",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 435usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_from_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_from_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_from_ewkt",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 436usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 437usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 438usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_geometry_type (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_geometry_type".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_geometry_type",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 439usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_intersection",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 440usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_intersects (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_intersects".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_intersects",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 441usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_is_closed (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_is_closed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_is_closed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 442usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_is_empty (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_is_empty".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_is_empty",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 443usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_is_simple (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_is_simple".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_is_simple",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 444usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_length (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_length",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 445usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_length_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_length_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_length_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 446usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_line_substring (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_line_substring".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_line_substring",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 447usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_npoints (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_npoints".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_npoints",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 448usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_perimeter (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_perimeter".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_perimeter",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 449usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_perimeter_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_perimeter_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_perimeter_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 450usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 451usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_project (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_project".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_project",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 452usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_project_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_project_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_project_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 453usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_segmentize (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_segmentize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_segmentize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 454usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_segmentize_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_segmentize_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_segmentize_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 455usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_summary (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_summary".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_summary",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 456usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_sym_difference (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_sym_difference".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_sym_difference",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 457usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geog_to_geom (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_to_geom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_to_geom",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 458usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geog_union (sqlink-shim-codegen)".into()),
            tags: vec!["st_geog_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geog_union",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 459usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geogarea (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogarea".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogarea",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 460usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geogazimuth (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogazimuth".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogazimuth",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 461usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geogbuffer (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogbuffer".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogbuffer",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 462usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geogbufferwithsegs (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogbufferwithsegs".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogbufferwithsegs",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 463usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geogcentroid (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogcentroid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogcentroid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 464usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geogcoveredby (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogcoveredby".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogcoveredby",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 465usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geogcovers (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogcovers".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogcovers",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 466usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geogdistance (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogdistance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogdistance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 467usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geogdwithin (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogdwithin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogdwithin",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 468usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geogexpand (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogexpand".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogexpand",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 469usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geogfromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogfromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogfromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 470usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geogfromwkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogfromwkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogfromwkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 471usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geoggeometrytype (sqlink-shim-codegen)".into()),
            tags: vec!["st_geoggeometrytype".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geoggeometrytype",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 472usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geogintersects (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogintersects".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogintersects",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 473usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geogisclosed (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogisclosed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogisclosed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 474usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geogisempty (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogisempty".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogisempty",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 475usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geogissimple (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogissimple".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogissimple",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 476usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geoglength (sqlink-shim-codegen)".into()),
            tags: vec!["st_geoglength".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geoglength",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 477usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geognpoints (sqlink-shim-codegen)".into()),
            tags: vec!["st_geognpoints".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geognpoints",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 478usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geogperimeter (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogperimeter".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogperimeter",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 479usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geogpoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogpoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogpoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 480usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geography_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_geography_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geography_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 481usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geographyfromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_geographyfromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geographyfromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 482usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geogsummary (sqlink-shim-codegen)".into()),
            tags: vec!["st_geogsummary".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geogsummary",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 483usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geohash (sqlink-shim-codegen)".into()),
            tags: vec!["st_geohash".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geohash",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 484usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_ewkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_ewkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_ewkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 485usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_ewkt (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_ewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_ewkt",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 486usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_geobuf (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_geobuf".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_geobuf",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 487usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_geohash (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_geohash".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_geohash",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 488usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_geojson (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_geojson".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_geojson",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 489usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_gml (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_gml".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_gml",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 490usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_gml_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_gml_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_gml_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 491usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_hexewkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_hexewkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_hexewkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 492usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_kml (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_kml".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_kml",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 493usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_marc21 (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_marc21".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_marc21",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 494usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 495usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_text_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_text_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_text_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 496usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_twkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_twkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_twkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 497usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geom_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geom_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geom_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 498usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomcoll_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomcoll_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcoll_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 499usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geomcoll_from_text_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomcoll_from_text_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcoll_from_text_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 500usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomcoll_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomcoll_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcoll_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 501usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geomcoll_from_wkb_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomcoll_from_wkb_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcoll_from_wkb_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 502usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomcollfromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomcollfromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcollfromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 503usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomcollfromwkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomcollfromwkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomcollfromwkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 504usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geomequal (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomequal".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomequal",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 505usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geometric_median (sqlink-shim-codegen)".into()),
            tags: vec!["st_geometric_median".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geometric_median",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 506usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geometricmedian (sqlink-shim-codegen)".into()),
            tags: vec!["st_geometricmedian".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geometricmedian",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 507usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geometry_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_geometry_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geometry_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 508usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geometry_n (sqlink-shim-codegen)".into()),
            tags: vec!["st_geometry_n".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geometry_n",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 509usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geometry_n_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_geometry_n_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geometry_n_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 510usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geometry_type (sqlink-shim-codegen)".into()),
            tags: vec!["st_geometry_type".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geometry_type",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 511usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geometry_type_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_geometry_type_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geometry_type_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 512usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geometry_type_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_geometry_type_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geometry_type_threed",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 513usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geometryfromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_geometryfromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geometryfromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 514usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geometryn (sqlink-shim-codegen)".into()),
            tags: vec!["st_geometryn".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geometryn",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 515usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geometrytype (sqlink-shim-codegen)".into()),
            tags: vec!["st_geometrytype".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geometrytype",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 516usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromewkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromewkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromewkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 517usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromewkt (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromewkt".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromewkt",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 518usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromgeobuf (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromgeobuf".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromgeobuf",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 519usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromgeohash (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromgeohash".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromgeohash",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 520usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromgeojson (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromgeojson".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromgeojson",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 521usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromgml (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromgml".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromgml",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 522usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromgmlsrid (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromgmlsrid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromgmlsrid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 523usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromhexewkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromhexewkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromhexewkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 524usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromkml (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromkml".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromkml",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 525usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomfrommarc21 (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfrommarc21".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfrommarc21",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 526usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 527usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromtextsrid (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromtextsrid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromtextsrid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 528usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromtwkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromtwkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromtwkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 529usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_geomfromwkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_geomfromwkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_geomfromwkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 530usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_get_edge_by_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_get_edge_by_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_get_edge_by_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 531usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_get_edge_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["st_get_edge_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_get_edge_geometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 532usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_get_face_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["st_get_face_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_get_face_geometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 533usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_get_node_by_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_get_node_by_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_get_node_by_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 534usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_get_node_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["st_get_node_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_get_node_geometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 535usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_getedgebypoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_getedgebypoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_getedgebypoint",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 536usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_getedgegeometry (sqlink-shim-codegen)".into()),
            tags: vec!["st_getedgegeometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_getedgegeometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 537usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_getfacegeometry (sqlink-shim-codegen)".into()),
            tags: vec!["st_getfacegeometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_getfacegeometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 538usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_getnodebypoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_getnodebypoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_getnodebypoint",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 539usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_getnodegeometry (sqlink-shim-codegen)".into()),
            tags: vec!["st_getnodegeometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_getnodegeometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 540usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_gml_to_sql (sqlink-shim-codegen)".into()),
            tags: vec!["st_gml_to_sql".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_gml_to_sql",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 541usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_gmltosql (sqlink-shim-codegen)".into()),
            tags: vec!["st_gmltosql".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_gmltosql",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 542usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_greene_approx_convex_partition (sqlink-shim-codegen)".into()),
            tags: vec!["st_greene_approx_convex_partition".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_greene_approx_convex_partition",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 543usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_has_arc (sqlink-shim-codegen)".into()),
            tags: vec!["st_has_arc".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_has_arc",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 544usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_has_m (sqlink-shim-codegen)".into()),
            tags: vec!["st_has_m".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_has_m",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 545usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_has_no_band (sqlink-shim-codegen)".into()),
            tags: vec!["st_has_no_band".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_has_no_band",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 546usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_has_validity_flag (sqlink-shim-codegen)".into()),
            tags: vec!["st_has_validity_flag".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_has_validity_flag",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 547usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_has_z (sqlink-shim-codegen)".into()),
            tags: vec!["st_has_z".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_has_z",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 548usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_hasarc (sqlink-shim-codegen)".into()),
            tags: vec!["st_hasarc".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_hasarc",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 549usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_hasm (sqlink-shim-codegen)".into()),
            tags: vec!["st_hasm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_hasm",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 550usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_hasnoband (sqlink-shim-codegen)".into()),
            tags: vec!["st_hasnoband".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_hasnoband",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 551usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_hasz (sqlink-shim-codegen)".into()),
            tags: vec!["st_hasz".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_hasz",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 552usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_hausdorff_distance (sqlink-shim-codegen)".into()),
            tags: vec!["st_hausdorff_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_hausdorff_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 553usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_hausdorffdistance (sqlink-shim-codegen)".into()),
            tags: vec!["st_hausdorffdistance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_hausdorffdistance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 554usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_height (sqlink-shim-codegen)".into()),
            tags: vec!["st_height".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_height",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 555usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_hexagon (sqlink-shim-codegen)".into()),
            tags: vec!["st_hexagon".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_hexagon",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 556usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_hill_shade (sqlink-shim-codegen)".into()),
            tags: vec!["st_hill_shade".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_hill_shade",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 557usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_hillshade (sqlink-shim-codegen)".into()),
            tags: vec!["st_hillshade".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_hillshade",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 558usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_histogram (sqlink-shim-codegen)".into()),
            tags: vec!["st_histogram".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_histogram",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 559usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_init_topo_geo (sqlink-shim-codegen)".into()),
            tags: vec!["st_init_topo_geo".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_init_topo_geo",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 560usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_interior_ring_n (sqlink-shim-codegen)".into()),
            tags: vec!["st_interior_ring_n".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_interior_ring_n",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 561usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_interior_ring_n_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_interior_ring_n_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_interior_ring_n_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 562usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_interiorringn (sqlink-shim-codegen)".into()),
            tags: vec!["st_interiorringn".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_interiorringn",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 563usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_interpolate_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_interpolate_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_interpolate_point",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 564usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_intersection (sqlink-shim-codegen)".into()),
            tags: vec!["st_intersection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_intersection",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 565usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_intersection_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_intersection_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_intersection_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 566usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_intersection_gridsize (sqlink-shim-codegen)".into()),
            tags: vec!["st_intersection_gridsize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_intersection_gridsize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 567usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_intersection_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_intersection_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_intersection_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 568usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_intersectionthreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_intersectionthreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_intersectionthreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 569usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_intersects (sqlink-shim-codegen)".into()),
            tags: vec!["st_intersects".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_intersects",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 570usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_intersects_3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_intersects_3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_intersects_3d",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 571usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_intersects_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_intersects_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_intersects_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 572usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_intersects_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_intersects_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_intersects_threed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 573usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_inverse_transform_pipeline (sqlink-shim-codegen)".into()),
            tags: vec!["st_inverse_transform_pipeline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_inverse_transform_pipeline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 574usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_inversetransformpipeline (sqlink-shim-codegen)".into()),
            tags: vec!["st_inversetransformpipeline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_inversetransformpipeline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 575usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_closed (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_closed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_closed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 576usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_collection (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_collection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_collection",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 577usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_empty (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_empty".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_empty",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 578usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_empty_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_empty_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_empty_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 579usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_empty_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_empty_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_empty_threed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 580usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_measured (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_measured".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_measured",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 581usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_planar (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_planar".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_planar",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 582usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_polygon_ccw (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_polygon_ccw".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_polygon_ccw",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 583usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_polygon_cw (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_polygon_cw".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_polygon_cw",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 584usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_ring (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_ring".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_ring",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 585usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_simple (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_simple".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_simple",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 586usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_simple_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_simple_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_simple_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 587usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_solid (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_solid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_solid",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 588usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_threed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 589usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_valid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 590usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_valid_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 591usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_detail (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_valid_detail".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_detail",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 592usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_detail_flags (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_valid_detail_flags".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_detail_flags",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 593usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_detail_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_valid_detail_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_detail_threed",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 594usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_flags (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_valid_flags".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_flags",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 595usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_reason (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_valid_reason".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_reason",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 596usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_reason_flags (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_valid_reason_flags".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_reason_flags",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 597usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_valid_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_threed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 598usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_is_valid_trajectory (sqlink-shim-codegen)".into()),
            tags: vec!["st_is_valid_trajectory".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_is_valid_trajectory",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 599usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_isclosed (sqlink-shim-codegen)".into()),
            tags: vec!["st_isclosed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_isclosed",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 600usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_iscollection (sqlink-shim-codegen)".into()),
            tags: vec!["st_iscollection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_iscollection",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 601usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_isempty (sqlink-shim-codegen)".into()),
            tags: vec!["st_isempty".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_isempty",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 602usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_ispolygonccw (sqlink-shim-codegen)".into()),
            tags: vec!["st_ispolygonccw".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_ispolygonccw",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 603usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_ispolygoncw (sqlink-shim-codegen)".into()),
            tags: vec!["st_ispolygoncw".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_ispolygoncw",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 604usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_isring (sqlink-shim-codegen)".into()),
            tags: vec!["st_isring".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_isring",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 605usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_issimple (sqlink-shim-codegen)".into()),
            tags: vec!["st_issimple".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_issimple",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 606usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_isvalid (sqlink-shim-codegen)".into()),
            tags: vec!["st_isvalid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_isvalid",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 607usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_isvaliddetail (sqlink-shim-codegen)".into()),
            tags: vec!["st_isvaliddetail".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_isvaliddetail",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 608usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_isvalidreason (sqlink-shim-codegen)".into()),
            tags: vec!["st_isvalidreason".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_isvalidreason",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 609usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_isvalidtrajectory (sqlink-shim-codegen)".into()),
            tags: vec!["st_isvalidtrajectory".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_isvalidtrajectory",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 610usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_knndistance (sqlink-shim-codegen)".into()),
            tags: vec!["st_knndistance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_knndistance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 611usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_largest_empty_circle (sqlink-shim-codegen)".into()),
            tags: vec!["st_largest_empty_circle".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_largest_empty_circle",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 612usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_largestemptycircle (sqlink-shim-codegen)".into()),
            tags: vec!["st_largestemptycircle".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_largestemptycircle",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 613usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_length (sqlink-shim-codegen)".into()),
            tags: vec!["st_length".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_length",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 614usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_length2d (sqlink-shim-codegen)".into()),
            tags: vec!["st_length2d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_length2d",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 615usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_length_2d (sqlink-shim-codegen)".into()),
            tags: vec!["st_length_2d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_length_2d",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 616usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_length_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_length_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_length_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 617usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_length_spheroid (sqlink-shim-codegen)".into()),
            tags: vec!["st_length_spheroid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_length_spheroid",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 618usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_length_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_length_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_length_threed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 619usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_length_twod (sqlink-shim-codegen)".into()),
            tags: vec!["st_length_twod".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_length_twod",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 620usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_length_twod_spheroid (sqlink-shim-codegen)".into()),
            tags: vec!["st_length_twod_spheroid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_length_twod_spheroid",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 621usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_lengthspheroid (sqlink-shim-codegen)".into()),
            tags: vec!["st_lengthspheroid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_lengthspheroid",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 622usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_lengththreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_lengththreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_lengththreed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 623usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_letters (sqlink-shim-codegen)".into()),
            tags: vec!["st_letters".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_letters",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 624usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_crossing_direction (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_crossing_direction".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_crossing_direction",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 625usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_extend (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_extend".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_extend",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 626usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_encoded_polyline (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_from_encoded_polyline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_encoded_polyline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 627usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_multi_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_from_multi_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_multi_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 628usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 629usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_text_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_from_text_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_text_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 630usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 631usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_from_wkb_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_from_wkb_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_from_wkb_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 632usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_interpolate_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_interpolate_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_interpolate_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 633usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_interpolate_point_3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_interpolate_point_3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_interpolate_point_3d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 634usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_interpolate_point_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_interpolate_point_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_interpolate_point_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 635usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_interpolate_points (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_interpolate_points".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_interpolate_points",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 636usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_locate_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_locate_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_locate_point",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 637usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_line_merge (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_merge".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_merge",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 638usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_merge_directed (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_merge_directed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_merge_directed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 639usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_line_substring (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_substring".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_substring",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 640usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_line_to_curve (sqlink-shim-codegen)".into()),
            tags: vec!["st_line_to_curve".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_line_to_curve",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 641usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_linecrossingdirection (sqlink-shim-codegen)".into()),
            tags: vec!["st_linecrossingdirection".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_linecrossingdirection",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 642usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_lineextend (sqlink-shim-codegen)".into()),
            tags: vec!["st_lineextend".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_lineextend",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 643usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_linefromencodedpolyline (sqlink-shim-codegen)".into()),
            tags: vec!["st_linefromencodedpolyline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_linefromencodedpolyline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 644usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_linefrommultipoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_linefrommultipoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_linefrommultipoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 645usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_linefromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_linefromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_linefromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 646usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_linefromwkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_linefromwkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_linefromwkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 647usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_lineinterpolatepoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_lineinterpolatepoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_lineinterpolatepoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 648usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_lineinterpolatepoints (sqlink-shim-codegen)".into()),
            tags: vec!["st_lineinterpolatepoints".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_lineinterpolatepoints",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 649usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_linelocatepoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_linelocatepoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_linelocatepoint",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 650usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_linemerge (sqlink-shim-codegen)".into()),
            tags: vec!["st_linemerge".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_linemerge",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 651usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_linesubstring (sqlink-shim-codegen)".into()),
            tags: vec!["st_linesubstring".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_linesubstring",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 652usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_linetocurve (sqlink-shim-codegen)".into()),
            tags: vec!["st_linetocurve".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_linetocurve",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 653usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_locate_along (sqlink-shim-codegen)".into()),
            tags: vec!["st_locate_along".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_locate_along",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 654usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_locate_between (sqlink-shim-codegen)".into()),
            tags: vec!["st_locate_between".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_locate_between",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 655usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_locate_between_elevations (sqlink-shim-codegen)".into()),
            tags: vec!["st_locate_between_elevations".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_locate_between_elevations",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 656usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_locatealong (sqlink-shim-codegen)".into()),
            tags: vec!["st_locatealong".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_locatealong",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 657usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_locatebetween (sqlink-shim-codegen)".into()),
            tags: vec!["st_locatebetween".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_locatebetween",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 658usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_locatebetweenelevations (sqlink-shim-codegen)".into()),
            tags: vec!["st_locatebetweenelevations".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_locatebetweenelevations",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 659usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_longest_line (sqlink-shim-codegen)".into()),
            tags: vec!["st_longest_line".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_longest_line",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 660usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_longest_line_3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_longest_line_3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_longest_line_3d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 661usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_longest_line_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_longest_line_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_longest_line_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 662usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_longestline (sqlink-shim-codegen)".into()),
            tags: vec!["st_longestline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_longestline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 663usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_m (sqlink-shim-codegen)".into()),
            tags: vec!["st_m".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_m",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 664usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_m_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_m_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_m_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 665usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_box2d (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_box2d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_box2d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 666usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_box3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_box3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_box3d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 667usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_box_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_box_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_box_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 668usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg7".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg8".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg9".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg10".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_empty_coverage (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_empty_coverage".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_empty_coverage",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 669usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg7".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg8".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_empty_raster (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_empty_raster".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_empty_raster",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 670usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_envelope (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_envelope".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_envelope",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 671usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_envelope_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_envelope_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_envelope_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 672usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_make_line (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_line".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_line",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 673usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_make_line_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_line_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_line_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 674usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_line_two (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_line_two".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_line_two",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 675usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 676usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_point_m (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_point_m".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_point_m",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 677usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_point_z (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_point_z".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_point_z",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 678usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_point_zm (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_point_zm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_point_zm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 679usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_make_polygon (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_polygon".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_polygon",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 680usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_make_solid (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_solid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_solid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 681usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_make_valid (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_valid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_valid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 682usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_makebox (sqlink-shim-codegen)".into()),
            tags: vec!["st_makebox".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makebox",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 683usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_makebox2d (sqlink-shim-codegen)".into()),
            tags: vec!["st_makebox2d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makebox2d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 684usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_makebox3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_makebox3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makebox3d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 685usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg7".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg8".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_makeemptyraster (sqlink-shim-codegen)".into()),
            tags: vec!["st_makeemptyraster".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makeemptyraster",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 686usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_makeenvelope (sqlink-shim-codegen)".into()),
            tags: vec!["st_makeenvelope".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makeenvelope",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 687usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_makeenvelopesrid (sqlink-shim-codegen)".into()),
            tags: vec!["st_makeenvelopesrid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makeenvelopesrid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 688usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_makepoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_makepoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makepoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 689usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_makepointm (sqlink-shim-codegen)".into()),
            tags: vec!["st_makepointm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makepointm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 690usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_makepointz (sqlink-shim-codegen)".into()),
            tags: vec!["st_makepointz".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makepointz",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 691usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_makepointzm (sqlink-shim-codegen)".into()),
            tags: vec!["st_makepointzm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makepointzm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 692usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_makepolygon (sqlink-shim-codegen)".into()),
            tags: vec!["st_makepolygon".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makepolygon",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 693usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_makevalid (sqlink-shim-codegen)".into()),
            tags: vec!["st_makevalid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makevalid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 694usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_map_algebra (sqlink-shim-codegen)".into()),
            tags: vec!["st_map_algebra".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_map_algebra",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 695usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_map_algebra2 (sqlink-shim-codegen)".into()),
            tags: vec!["st_map_algebra2".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_map_algebra2",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 696usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_mapalgebra (sqlink-shim-codegen)".into()),
            tags: vec!["st_mapalgebra".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mapalgebra",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 697usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_mapalgebra2 (sqlink-shim-codegen)".into()),
            tags: vec!["st_mapalgebra2".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mapalgebra2",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 698usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_max_distance (sqlink-shim-codegen)".into()),
            tags: vec!["st_max_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_max_distance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 699usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_max_distance_3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_max_distance_3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_max_distance_3d",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 700usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_max_distance_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_max_distance_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_max_distance_threed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 701usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_max_x (sqlink-shim-codegen)".into()),
            tags: vec!["st_max_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_max_x",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 702usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_max_y (sqlink-shim-codegen)".into()),
            tags: vec!["st_max_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_max_y",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 703usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_maxdistance (sqlink-shim-codegen)".into()),
            tags: vec!["st_maxdistance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_maxdistance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 704usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_maximum_inscribed_circle (sqlink-shim-codegen)".into()),
            tags: vec!["st_maximum_inscribed_circle".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_maximum_inscribed_circle",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 705usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_maximuminscribedcircle (sqlink-shim-codegen)".into()),
            tags: vec!["st_maximuminscribedcircle".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_maximuminscribedcircle",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 706usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mem_size (sqlink-shim-codegen)".into()),
            tags: vec!["st_mem_size".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mem_size",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 707usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mem_union (sqlink-shim-codegen)".into()),
            tags: vec!["st_mem_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mem_union",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 708usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_memsize (sqlink-shim-codegen)".into()),
            tags: vec!["st_memsize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_memsize",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 709usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_metadata (sqlink-shim-codegen)".into()),
            tags: vec!["st_metadata".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_metadata",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 710usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_min_convex_hull (sqlink-shim-codegen)".into()),
            tags: vec!["st_min_convex_hull".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_min_convex_hull",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 711usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_min_x (sqlink-shim-codegen)".into()),
            tags: vec!["st_min_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_min_x",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 712usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_min_y (sqlink-shim-codegen)".into()),
            tags: vec!["st_min_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_min_y",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 713usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_bounding_circle (sqlink-shim-codegen)".into()),
            tags: vec!["st_minimum_bounding_circle".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_bounding_circle",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 714usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_bounding_circle_segs (sqlink-shim-codegen)".into()),
            tags: vec!["st_minimum_bounding_circle_segs".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_bounding_circle_segs",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 715usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_bounding_radius (sqlink-shim-codegen)".into()),
            tags: vec!["st_minimum_bounding_radius".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_bounding_radius",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 716usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_bounding_sphere_center (sqlink-shim-codegen)".into()),
            tags: vec!["st_minimum_bounding_sphere_center".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_bounding_sphere_center",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 717usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_bounding_sphere_radius (sqlink-shim-codegen)".into()),
            tags: vec!["st_minimum_bounding_sphere_radius".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_bounding_sphere_radius",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 718usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_clearance (sqlink-shim-codegen)".into()),
            tags: vec!["st_minimum_clearance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_clearance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 719usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_minimum_clearance_line (sqlink-shim-codegen)".into()),
            tags: vec!["st_minimum_clearance_line".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minimum_clearance_line",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 720usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_minimumboundingcircle (sqlink-shim-codegen)".into()),
            tags: vec!["st_minimumboundingcircle".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minimumboundingcircle",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 721usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_minimumboundingradius (sqlink-shim-codegen)".into()),
            tags: vec!["st_minimumboundingradius".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minimumboundingradius",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 722usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_minimumclearance (sqlink-shim-codegen)".into()),
            tags: vec!["st_minimumclearance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minimumclearance",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 723usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_minimumclearanceline (sqlink-shim-codegen)".into()),
            tags: vec!["st_minimumclearanceline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minimumclearanceline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 724usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_minkowski_sum (sqlink-shim-codegen)".into()),
            tags: vec!["st_minkowski_sum".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minkowski_sum",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 725usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_minkowskisum (sqlink-shim-codegen)".into()),
            tags: vec!["st_minkowskisum".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_minkowskisum",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 726usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mline_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_mline_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mline_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 727usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_mline_from_text_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_mline_from_text_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mline_from_text_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 728usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mline_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_mline_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mline_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 729usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_mline_from_wkb_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_mline_from_wkb_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mline_from_wkb_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 730usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mlinefromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_mlinefromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mlinefromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 731usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mlinefromwkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_mlinefromwkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mlinefromwkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 732usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mmax (sqlink-shim-codegen)".into()),
            tags: vec!["st_mmax".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mmax",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 733usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mmin (sqlink-shim-codegen)".into()),
            tags: vec!["st_mmin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mmin",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 734usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_mod_edge_heal (sqlink-shim-codegen)".into()),
            tags: vec!["st_mod_edge_heal".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mod_edge_heal",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 735usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_mod_edge_split (sqlink-shim-codegen)".into()),
            tags: vec!["st_mod_edge_split".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mod_edge_split",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 736usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_modedgeheal (sqlink-shim-codegen)".into()),
            tags: vec!["st_modedgeheal".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_modedgeheal",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 737usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_modedgesplit (sqlink-shim-codegen)".into()),
            tags: vec!["st_modedgesplit".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_modedgesplit",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 738usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mpoint_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpoint_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoint_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 739usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoint_from_text_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpoint_from_text_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoint_from_text_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 740usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mpoint_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpoint_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoint_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 741usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoint_from_wkb_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpoint_from_wkb_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoint_from_wkb_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 742usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mpointfromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpointfromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpointfromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 743usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mpointfromwkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpointfromwkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpointfromwkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 744usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mpoly_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpoly_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoly_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 745usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoly_from_text_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpoly_from_text_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoly_from_text_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 746usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mpoly_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpoly_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoly_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 747usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_mpoly_from_wkb_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpoly_from_wkb_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpoly_from_wkb_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 748usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mpolyfromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpolyfromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpolyfromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 749usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mpolyfromwkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_mpolyfromwkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mpolyfromwkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 750usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_multi (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 751usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_multi_line_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_line_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_line_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 752usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_line_from_wkb_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_line_from_wkb_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_line_from_wkb_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 753usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_multi_line_string_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_line_string_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_line_string_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 754usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_line_string_from_text_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_line_string_from_text_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_line_string_from_text_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 755usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_multi_point_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_point_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_point_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 756usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_point_from_text_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_point_from_text_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_point_from_text_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 757usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_multi_point_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_point_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_point_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 758usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_point_from_wkb_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_point_from_wkb_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_point_from_wkb_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 759usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_multi_poly_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_poly_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_poly_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 760usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_poly_from_wkb_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_poly_from_wkb_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_poly_from_wkb_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 761usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_multi_polygon_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_polygon_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_polygon_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 762usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_multi_polygon_from_text_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_multi_polygon_from_text_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_multi_polygon_from_text_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 763usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_n_dims (sqlink-shim-codegen)".into()),
            tags: vec!["st_n_dims".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_n_dims",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 764usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_n_dims_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_n_dims_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_n_dims_batch",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 765usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_ndims (sqlink-shim-codegen)".into()),
            tags: vec!["st_ndims".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_ndims",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 766usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_ndims_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_ndims_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_ndims_batch",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 767usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_nearest_value (sqlink-shim-codegen)".into()),
            tags: vec!["st_nearest_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_nearest_value",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 768usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_nearestvalue (sqlink-shim-codegen)".into()),
            tags: vec!["st_nearestvalue".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_nearestvalue",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 769usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_new_edges_split (sqlink-shim-codegen)".into()),
            tags: vec!["st_new_edges_split".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_new_edges_split",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 770usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_newedgessplit (sqlink-shim-codegen)".into()),
            tags: vec!["st_newedgessplit".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "st_newedgessplit",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 771usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_node (sqlink-shim-codegen)".into()),
            tags: vec!["st_node".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_node",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 772usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_normalize (sqlink-shim-codegen)".into()),
            tags: vec!["st_normalize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_normalize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 773usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_normalize_address (sqlink-shim-codegen)".into()),
            tags: vec!["st_normalize_address".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_normalize_address",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 774usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_normalizeaddress (sqlink-shim-codegen)".into()),
            tags: vec!["st_normalizeaddress".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_normalizeaddress",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 775usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_npoints (sqlink-shim-codegen)".into()),
            tags: vec!["st_npoints".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_npoints",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 776usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_nrings (sqlink-shim-codegen)".into()),
            tags: vec!["st_nrings".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_nrings",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 777usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_num_bands (sqlink-shim-codegen)".into()),
            tags: vec!["st_num_bands".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_num_bands",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 778usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_num_curves (sqlink-shim-codegen)".into()),
            tags: vec!["st_num_curves".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_num_curves",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 779usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_num_geometries (sqlink-shim-codegen)".into()),
            tags: vec!["st_num_geometries".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_num_geometries",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 780usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_num_geometries_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_num_geometries_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_num_geometries_batch",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 781usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_num_geometries_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_num_geometries_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_num_geometries_threed",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 782usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_num_interior_ring (sqlink-shim-codegen)".into()),
            tags: vec!["st_num_interior_ring".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_num_interior_ring",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 783usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_num_interior_rings (sqlink-shim-codegen)".into()),
            tags: vec!["st_num_interior_rings".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_num_interior_rings",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 784usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_num_interior_rings_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_num_interior_rings_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_num_interior_rings_threed",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 785usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_num_points (sqlink-shim-codegen)".into()),
            tags: vec!["st_num_points".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_num_points",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 786usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_num_points_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_num_points_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_num_points_batch",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 787usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_num_points_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_num_points_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_num_points_threed",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 788usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_numbands (sqlink-shim-codegen)".into()),
            tags: vec!["st_numbands".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_numbands",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 789usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_numcurves (sqlink-shim-codegen)".into()),
            tags: vec!["st_numcurves".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_numcurves",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 790usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_numgeometries (sqlink-shim-codegen)".into()),
            tags: vec!["st_numgeometries".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_numgeometries",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 791usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_numinteriorring (sqlink-shim-codegen)".into()),
            tags: vec!["st_numinteriorring".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_numinteriorring",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 792usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_numinteriorrings (sqlink-shim-codegen)".into()),
            tags: vec!["st_numinteriorrings".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_numinteriorrings",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 793usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_numpoints (sqlink-shim-codegen)".into()),
            tags: vec!["st_numpoints".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_numpoints",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 794usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_offset_curve (sqlink-shim-codegen)".into()),
            tags: vec!["st_offset_curve".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_offset_curve",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 795usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_offset_polygon (sqlink-shim-codegen)".into()),
            tags: vec!["st_offset_polygon".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_offset_polygon",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 796usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_offsetcurve (sqlink-shim-codegen)".into()),
            tags: vec!["st_offsetcurve".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_offsetcurve",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 797usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_optimal_alpha_shape (sqlink-shim-codegen)".into()),
            tags: vec!["st_optimal_alpha_shape".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_optimal_alpha_shape",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 798usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_optimal_convex_partition (sqlink-shim-codegen)".into()),
            tags: vec!["st_optimal_convex_partition".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_optimal_convex_partition",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 799usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_ordering_equals (sqlink-shim-codegen)".into()),
            tags: vec!["st_ordering_equals".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_ordering_equals",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 800usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_orderingequals (sqlink-shim-codegen)".into()),
            tags: vec!["st_orderingequals".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_orderingequals",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 801usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_orientable_surface (sqlink-shim-codegen)".into()),
            tags: vec!["st_orientable_surface".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_orientable_surface",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 802usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_orientation (sqlink-shim-codegen)".into()),
            tags: vec!["st_orientation".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_orientation",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 803usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_oriented_envelope (sqlink-shim-codegen)".into()),
            tags: vec!["st_oriented_envelope".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_oriented_envelope",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 804usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_orientedenvelope (sqlink-shim-codegen)".into()),
            tags: vec!["st_orientedenvelope".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_orientedenvelope",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 805usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["st_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 806usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_overlaps_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_overlaps_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_overlaps_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 807usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_parse_address (sqlink-shim-codegen)".into()),
            tags: vec!["st_parse_address".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_parse_address",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 808usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_parseaddress (sqlink-shim-codegen)".into()),
            tags: vec!["st_parseaddress".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_parseaddress",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 809usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_perimeter (sqlink-shim-codegen)".into()),
            tags: vec!["st_perimeter".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeter",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 810usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_perimeter2d (sqlink-shim-codegen)".into()),
            tags: vec!["st_perimeter2d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeter2d",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 811usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_perimeter_2d (sqlink-shim-codegen)".into()),
            tags: vec!["st_perimeter_2d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeter_2d",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 812usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_perimeter_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_perimeter_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeter_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 813usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_perimeter_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_perimeter_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeter_threed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 814usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_perimeter_twod (sqlink-shim-codegen)".into()),
            tags: vec!["st_perimeter_twod".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeter_twod",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 815usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_perimeterthreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_perimeterthreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_perimeterthreed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 816usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_as_centroid (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixel_as_centroid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_as_centroid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 817usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_as_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixel_as_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_as_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 818usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_as_polygon (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixel_as_polygon".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_as_polygon",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 819usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_as_polygons (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixel_as_polygons".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_as_polygons",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 820usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_height (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixel_height".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_height",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 821usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_of_value (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixel_of_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_of_value",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 822usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_pixel_width (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixel_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixel_width",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 823usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pixelascentroid (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixelascentroid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixelascentroid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 824usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pixelaspoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixelaspoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixelaspoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 825usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pixelaspolygon (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixelaspolygon".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixelaspolygon",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 826usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pixelaspolygons (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixelaspolygons".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixelaspolygons",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 827usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pixelofvalue (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixelofvalue".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixelofvalue",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 828usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pixels_of_values (sqlink-shim-codegen)".into()),
            tags: vec!["st_pixels_of_values".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pixels_of_values",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 829usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 830usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_from_geohash (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_from_geohash".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_from_geohash",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 831usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_point_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 832usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_from_text_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_from_text_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_from_text_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 833usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_point_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 834usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_from_wkb_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_from_wkb_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_from_wkb_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 835usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_inside_circle (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_inside_circle".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_inside_circle",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 836usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_m (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_m".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_m",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 837usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_m_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_m_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_m_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 838usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_point_m_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_m_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_m_threed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 839usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_n (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_n".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_n",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 840usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_n_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_n_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_n_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 841usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_n_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_n_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_n_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 842usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_point_on_surface (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_on_surface".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_on_surface",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 843usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_point_on_surface_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_on_surface_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_on_surface_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 844usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 845usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_point_x_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_x_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_x_threed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 846usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_point_y_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_y_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_y_threed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 847usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_z (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_z".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_z",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 848usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_z_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_z_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_z_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 849usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_point_z_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_z_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_z_threed",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 850usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_zm (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_zm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_zm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 851usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_point_zm_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_point_zm_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_point_zm_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 852usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pointfromgeohash (sqlink-shim-codegen)".into()),
            tags: vec!["st_pointfromgeohash".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pointfromgeohash",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 853usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_pointfromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_pointfromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pointfromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 854usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_pointfromwkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_pointfromwkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pointfromwkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 855usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pointinsidecircle (sqlink-shim-codegen)".into()),
            tags: vec!["st_pointinsidecircle".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pointinsidecircle",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 856usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pointm (sqlink-shim-codegen)".into()),
            tags: vec!["st_pointm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pointm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 857usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pointn (sqlink-shim-codegen)".into()),
            tags: vec!["st_pointn".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pointn",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 858usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_pointonsurface (sqlink-shim-codegen)".into()),
            tags: vec!["st_pointonsurface".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pointonsurface",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 859usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_points (sqlink-shim-codegen)".into()),
            tags: vec!["st_points".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_points",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 860usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pointz (sqlink-shim-codegen)".into()),
            tags: vec!["st_pointz".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pointz",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 861usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_pointzm (sqlink-shim-codegen)".into()),
            tags: vec!["st_pointzm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_pointzm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 862usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_polygon (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygon".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygon",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 863usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polygon_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygon_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygon_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 864usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_polygon_from_text_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygon_from_text_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygon_from_text_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 865usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polygon_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygon_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygon_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 866usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_polygon_from_wkb_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygon_from_wkb_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygon_from_wkb_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 867usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polygonfromtext (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygonfromtext".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonfromtext",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 868usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polygonfromwkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygonfromwkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonfromwkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 869usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polygonize (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygonize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 870usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polygonize_as_raster (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygonize_as_raster".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonize_as_raster",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 871usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polygonize_topology (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygonize_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonize_topology",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 872usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polyhedralsurface_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_polyhedralsurface_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polyhedralsurface_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 873usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polyhedralsurface_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_polyhedralsurface_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polyhedralsurface_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 874usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_project (sqlink-shim-codegen)".into()),
            tags: vec!["st_project".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_project",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 875usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_project_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_project_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_project_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 876usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_quantile (sqlink-shim-codegen)".into()),
            tags: vec!["st_quantile".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_quantile",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 877usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_quantiles (sqlink-shim-codegen)".into()),
            tags: vec!["st_quantiles".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_quantiles",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 878usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_quantize_coordinates (sqlink-shim-codegen)".into()),
            tags: vec!["st_quantize_coordinates".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_quantize_coordinates",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 879usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_quantizecoordinates (sqlink-shim-codegen)".into()),
            tags: vec!["st_quantizecoordinates".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_quantizecoordinates",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 880usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rast_height_dfer_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_rast_height_dfer_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_height_dfer_batch",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 881usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rast_scale_x_dfer_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_rast_scale_x_dfer_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_scale_x_dfer_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 882usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rast_scale_y_dfer_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_rast_scale_y_dfer_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_scale_y_dfer_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 883usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rast_skew_x_dfer_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_rast_skew_x_dfer_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_skew_x_dfer_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 884usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rast_skew_y_dfer_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_rast_skew_y_dfer_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_skew_y_dfer_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 885usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rast_srid_dfer_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_rast_srid_dfer_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_srid_dfer_batch",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 886usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rast_upper_left_x_dfer_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_rast_upper_left_x_dfer_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_upper_left_x_dfer_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 887usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rast_upper_left_y_dfer_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_rast_upper_left_y_dfer_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_upper_left_y_dfer_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 888usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rast_width_dfer_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_rast_width_dfer_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_width_dfer_batch",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 889usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rastenvelope (sqlink-shim-codegen)".into()),
            tags: vec!["st_rastenvelope".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rastenvelope",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 890usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_contains (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_contains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_contains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 891usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_contains_geom (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_contains_geom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_contains_geom",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 892usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_raster_convex_hull (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_convex_hull".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_convex_hull",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 893usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_covers (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_covers".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_covers",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 894usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_intersection_geom (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_intersection_geom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_intersection_geom",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 895usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_intersects (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_intersects".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_intersects",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 896usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_intersects_geom (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_intersects_geom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_intersects_geom",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 897usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_overlaps (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_overlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_overlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 898usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_to_world_coord_x (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_to_world_coord_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_to_world_coord_x",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 899usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_to_world_coord_y (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_to_world_coord_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_to_world_coord_y",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 900usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_raster_within (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_within".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_within",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 901usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rastercontains (sqlink-shim-codegen)".into()),
            tags: vec!["st_rastercontains".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rastercontains",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 902usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rastercontainsgeom (sqlink-shim-codegen)".into()),
            tags: vec!["st_rastercontainsgeom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rastercontainsgeom",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 903usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rasterconvexhull (sqlink-shim-codegen)".into()),
            tags: vec!["st_rasterconvexhull".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rasterconvexhull",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 904usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rastercovers (sqlink-shim-codegen)".into()),
            tags: vec!["st_rastercovers".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rastercovers",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 905usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rasterintersects (sqlink-shim-codegen)".into()),
            tags: vec!["st_rasterintersects".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rasterintersects",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 906usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rasterintersectsgeom (sqlink-shim-codegen)".into()),
            tags: vec!["st_rasterintersectsgeom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rasterintersectsgeom",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 907usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rasteroverlaps (sqlink-shim-codegen)".into()),
            tags: vec!["st_rasteroverlaps".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rasteroverlaps",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 908usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rastertoworldcoordx (sqlink-shim-codegen)".into()),
            tags: vec!["st_rastertoworldcoordx".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rastertoworldcoordx",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 909usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rastertoworldcoordy (sqlink-shim-codegen)".into()),
            tags: vec!["st_rastertoworldcoordy".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rastertoworldcoordy",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 910usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rasterwithin (sqlink-shim-codegen)".into()),
            tags: vec!["st_rasterwithin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rasterwithin",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 911usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rastheight (sqlink-shim-codegen)".into()),
            tags: vec!["st_rastheight".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rastheight",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 912usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rastnumbands (sqlink-shim-codegen)".into()),
            tags: vec!["st_rastnumbands".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rastnumbands",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 913usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rastwidth (sqlink-shim-codegen)".into()),
            tags: vec!["st_rastwidth".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rastwidth",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 914usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_reclass (sqlink-shim-codegen)".into()),
            tags: vec!["st_reclass".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_reclass",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 915usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_reclass_exact (sqlink-shim-codegen)".into()),
            tags: vec!["st_reclass_exact".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_reclass_exact",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 916usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_reduce_precision (sqlink-shim-codegen)".into()),
            tags: vec!["st_reduce_precision".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_reduce_precision",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 917usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_reduce_precision_flags (sqlink-shim-codegen)".into()),
            tags: vec!["st_reduce_precision_flags".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_reduce_precision_flags",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 918usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_reduceprecision (sqlink-shim-codegen)".into()),
            tags: vec!["st_reduceprecision".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_reduceprecision",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 919usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_relate (sqlink-shim-codegen)".into()),
            tags: vec!["st_relate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_relate",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 920usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_relate_bnr (sqlink-shim-codegen)".into()),
            tags: vec!["st_relate_bnr".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_relate_bnr",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 921usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_relate_match (sqlink-shim-codegen)".into()),
            tags: vec!["st_relate_match".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_relate_match",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 922usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_relate_with_pattern (sqlink-shim-codegen)".into()),
            tags: vec!["st_relate_with_pattern".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_relate_with_pattern",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 923usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_relatematch (sqlink-shim-codegen)".into()),
            tags: vec!["st_relatematch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_relatematch",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 924usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_remove_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_remove_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_remove_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 925usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_remove_repeated_points (sqlink-shim-codegen)".into()),
            tags: vec!["st_remove_repeated_points".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_remove_repeated_points",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 926usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_remove_small_parts (sqlink-shim-codegen)".into()),
            tags: vec!["st_remove_small_parts".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_remove_small_parts",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 927usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_removepoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_removepoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_removepoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 928usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_removerepeatedpoints (sqlink-shim-codegen)".into()),
            tags: vec!["st_removerepeatedpoints".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_removerepeatedpoints",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 929usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_removesmallparts (sqlink-shim-codegen)".into()),
            tags: vec!["st_removesmallparts".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_removesmallparts",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 930usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rescale (sqlink-shim-codegen)".into()),
            tags: vec!["st_rescale".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rescale",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 931usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_resize (sqlink-shim-codegen)".into()),
            tags: vec!["st_resize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_resize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 932usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_retile (sqlink-shim-codegen)".into()),
            tags: vec!["st_retile".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_retile",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 933usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_reverse (sqlink-shim-codegen)".into()),
            tags: vec!["st_reverse".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_reverse",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 934usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_reverse_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_reverse_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_reverse_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 935usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_reversethreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_reversethreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_reversethreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 936usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 937usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_around_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotate_around_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_around_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 938usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_around_xy (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotate_around_xy".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_around_xy",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 939usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotate_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 940usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg7".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_threed_around_center (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotate_threed_around_center".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_threed_around_center",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 941usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_twod_around_center (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotate_twod_around_center".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_twod_around_center",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 942usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_x (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotate_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_x",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 943usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_y (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotate_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_y",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 944usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotate_z (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotate_z".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotate_z",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 945usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotatethreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotatethreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotatethreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 946usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotatex (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotatex".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotatex",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 947usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotatey (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotatey".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotatey",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 948usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_rotatez (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotatez".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotatez",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 949usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rotation (sqlink-shim-codegen)".into()),
            tags: vec!["st_rotation".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rotation",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 950usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_roughness (sqlink-shim-codegen)".into()),
            tags: vec!["st_roughness".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_roughness",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 951usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_round (sqlink-shim-codegen)".into()),
            tags: vec!["st_round".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_round",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 952usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_scale (sqlink-shim-codegen)".into()),
            tags: vec!["st_scale".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_scale",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 953usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_scale_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_scale_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_scale_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 954usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_scale_threed_around_center (sqlink-shim-codegen)".into()),
            tags: vec!["st_scale_threed_around_center".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_scale_threed_around_center",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 955usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_scale_with_origin (sqlink-shim-codegen)".into()),
            tags: vec!["st_scale_with_origin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_scale_with_origin",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 956usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_scale_with_vector (sqlink-shim-codegen)".into()),
            tags: vec!["st_scale_with_vector".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_scale_with_vector",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 957usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_scale_x (sqlink-shim-codegen)".into()),
            tags: vec!["st_scale_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_scale_x",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 958usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_scale_y (sqlink-shim-codegen)".into()),
            tags: vec!["st_scale_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_scale_y",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 959usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_scalethreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_scalethreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_scalethreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 960usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_scalex (sqlink-shim-codegen)".into()),
            tags: vec!["st_scalex".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_scalex",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 961usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_scaley (sqlink-shim-codegen)".into()),
            tags: vec!["st_scaley".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_scaley",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 962usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_scroll (sqlink-shim-codegen)".into()),
            tags: vec!["st_scroll".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_scroll",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 963usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_segmentize (sqlink-shim-codegen)".into()),
            tags: vec!["st_segmentize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_segmentize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 964usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_band_is_nodata (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_band_is_nodata".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_band_is_nodata",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 965usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_band_nodata_value (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_band_nodata_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_band_nodata_value",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 966usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_effective_area (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_effective_area".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_effective_area",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 967usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_geo_reference (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_geo_reference".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_geo_reference",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 968usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_geo_reference_string (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_geo_reference_string".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_geo_reference_string",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 969usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 970usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_rotation (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_rotation".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_rotation",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 971usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_scale (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_scale".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_scale",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 972usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_scale_xy (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_scale_xy".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_scale_xy",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 973usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_skew (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_skew".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_skew",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 974usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_skew_xy (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_skew_xy".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_skew_xy",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 975usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 976usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_upper_left (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_upper_left".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_upper_left",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 977usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_value (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_value",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 978usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_value_at_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_value_at_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_value_at_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 979usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_values (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_values".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_values",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 980usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_set_values_rect (sqlink-shim-codegen)".into()),
            tags: vec!["st_set_values_rect".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_set_values_rect",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 981usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_seteffectivearea (sqlink-shim-codegen)".into()),
            tags: vec!["st_seteffectivearea".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_seteffectivearea",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 982usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_setendm (sqlink-shim-codegen)".into()),
            tags: vec!["st_setendm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_setendm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 983usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_setendz (sqlink-shim-codegen)".into()),
            tags: vec!["st_setendz".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_setendz",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 984usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_setm (sqlink-shim-codegen)".into()),
            tags: vec!["st_setm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_setm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 985usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_setpoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_setpoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_setpoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 986usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_setsrid (sqlink-shim-codegen)".into()),
            tags: vec!["st_setsrid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_setsrid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 987usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_setstartm (sqlink-shim-codegen)".into()),
            tags: vec!["st_setstartm".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_setstartm",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 988usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_setstartz (sqlink-shim-codegen)".into()),
            tags: vec!["st_setstartz".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_setstartz",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 989usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_setvalue (sqlink-shim-codegen)".into()),
            tags: vec!["st_setvalue".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_setvalue",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 990usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_setx (sqlink-shim-codegen)".into()),
            tags: vec!["st_setx".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_setx",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 991usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_sety (sqlink-shim-codegen)".into()),
            tags: vec!["st_sety".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_sety",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 992usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_setz (sqlink-shim-codegen)".into()),
            tags: vec!["st_setz".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_setz",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 993usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_shared_paths (sqlink-shim-codegen)".into()),
            tags: vec!["st_shared_paths".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_shared_paths",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 994usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_sharedpaths (sqlink-shim-codegen)".into()),
            tags: vec!["st_sharedpaths".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_sharedpaths",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 995usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_shift_longitude (sqlink-shim-codegen)".into()),
            tags: vec!["st_shift_longitude".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_shift_longitude",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 996usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_shiftlongitude (sqlink-shim-codegen)".into()),
            tags: vec!["st_shiftlongitude".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_shiftlongitude",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 997usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_shortest_line (sqlink-shim-codegen)".into()),
            tags: vec!["st_shortest_line".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_shortest_line",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 998usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_shortest_line_3d (sqlink-shim-codegen)".into()),
            tags: vec!["st_shortest_line_3d".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_shortest_line_3d",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 999usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_shortest_line_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_shortest_line_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_shortest_line_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1000usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_shortestline (sqlink-shim-codegen)".into()),
            tags: vec!["st_shortestline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_shortestline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1001usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplify".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1002usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplify_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1003usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_polygon_hull (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplify_polygon_hull".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_polygon_hull",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1004usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_preserve_collapse (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplify_preserve_collapse".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_preserve_collapse",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1005usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_preserve_topology (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplify_preserve_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_preserve_topology",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1006usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_preserve_topology_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplify_preserve_topology_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_preserve_topology_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1007usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_preserve_topology_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplify_preserve_topology_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_preserve_topology_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1008usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplify_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1009usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_vw (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplify_vw".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_vw",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1010usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplify_vw_preserve_collapse (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplify_vw_preserve_collapse".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplify_vw_preserve_collapse",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1011usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplifypolygonhull (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplifypolygonhull".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplifypolygonhull",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1012usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplifypreservetopology (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplifypreservetopology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplifypreservetopology",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1013usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_simplifyvw (sqlink-shim-codegen)".into()),
            tags: vec!["st_simplifyvw".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_simplifyvw",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1014usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_skew_x (sqlink-shim-codegen)".into()),
            tags: vec!["st_skew_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_skew_x",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1015usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_skew_y (sqlink-shim-codegen)".into()),
            tags: vec!["st_skew_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_skew_y",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1016usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_skewx (sqlink-shim-codegen)".into()),
            tags: vec!["st_skewx".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_skewx",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1017usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_skewy (sqlink-shim-codegen)".into()),
            tags: vec!["st_skewy".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_skewy",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1018usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_slope (sqlink-shim-codegen)".into()),
            tags: vec!["st_slope".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_slope",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1019usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_snap (sqlink-shim-codegen)".into()),
            tags: vec!["st_snap".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_snap",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1020usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_snap_to_grid (sqlink-shim-codegen)".into()),
            tags: vec!["st_snap_to_grid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_snap_to_grid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1021usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg5".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg6".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_snap_to_grid_fourd (sqlink-shim-codegen)".into()),
            tags: vec!["st_snap_to_grid_fourd".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_snap_to_grid_fourd",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1022usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_snap_to_grid_origin (sqlink-shim-codegen)".into()),
            tags: vec!["st_snap_to_grid_origin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_snap_to_grid_origin",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1023usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_snap_to_grid_xy (sqlink-shim-codegen)".into()),
            tags: vec!["st_snap_to_grid_xy".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_snap_to_grid_xy",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1024usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_snaptogrid (sqlink-shim-codegen)".into()),
            tags: vec!["st_snaptogrid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_snaptogrid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1025usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_split (sqlink-shim-codegen)".into()),
            tags: vec!["st_split".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_split",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1026usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_square (sqlink-shim-codegen)".into()),
            tags: vec!["st_square".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_square",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1027usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_srid",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1028usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_start_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_start_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_start_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1029usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_start_point_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_start_point_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_start_point_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1030usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_startpoint (sqlink-shim-codegen)".into()),
            tags: vec!["st_startpoint".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_startpoint",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1031usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_straight_skeleton (sqlink-shim-codegen)".into()),
            tags: vec!["st_straight_skeleton".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_straight_skeleton",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1032usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_straight_skeleton_distance (sqlink-shim-codegen)".into()),
            tags: vec!["st_straight_skeleton_distance".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_straight_skeleton_distance",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1033usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_straight_skeleton_partition (sqlink-shim-codegen)".into()),
            tags: vec!["st_straight_skeleton_partition".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_straight_skeleton_partition",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1034usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_straightskeleton (sqlink-shim-codegen)".into()),
            tags: vec!["st_straightskeleton".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_straightskeleton",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1035usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_subdivide_gridsize (sqlink-shim-codegen)".into()),
            tags: vec!["st_subdivide_gridsize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_subdivide_gridsize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1036usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_summary (sqlink-shim-codegen)".into()),
            tags: vec!["st_summary".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_summary",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1037usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_summary_stats (sqlink-shim-codegen)".into()),
            tags: vec!["st_summary_stats".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_summary_stats",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1038usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_summarystats (sqlink-shim-codegen)".into()),
            tags: vec!["st_summarystats".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_summarystats",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1039usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_swap_ordinates (sqlink-shim-codegen)".into()),
            tags: vec!["st_swap_ordinates".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_swap_ordinates",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1040usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_swapordinates (sqlink-shim-codegen)".into()),
            tags: vec!["st_swapordinates".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_swapordinates",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1041usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_sym_difference (sqlink-shim-codegen)".into()),
            tags: vec!["st_sym_difference".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_sym_difference",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1042usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_sym_difference_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_sym_difference_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_sym_difference_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1043usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_sym_difference_gridsize (sqlink-shim-codegen)".into()),
            tags: vec!["st_sym_difference_gridsize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_sym_difference_gridsize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1044usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_sym_difference_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_sym_difference_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_sym_difference_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1045usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_symdifference (sqlink-shim-codegen)".into()),
            tags: vec!["st_symdifference".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_symdifference",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1046usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_tesselate (sqlink-shim-codegen)".into()),
            tags: vec!["st_tesselate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_tesselate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1047usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_tile (sqlink-shim-codegen)".into()),
            tags: vec!["st_tile".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_tile",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1048usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_tile_envelope (sqlink-shim-codegen)".into()),
            tags: vec!["st_tile_envelope".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_tile_envelope",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1049usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_tileenvelope (sqlink-shim-codegen)".into()),
            tags: vec!["st_tileenvelope".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_tileenvelope",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1050usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_tin_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_tin_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_tin_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1051usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_tin_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_tin_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_tin_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1052usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_edge_count (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_edge_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_edge_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1053usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_edge_count_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_edge_count_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_edge_count_batch",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1054usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_face_count (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_face_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_face_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1055usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_face_count_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_face_count_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_face_count_batch",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1056usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_from_bytes (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_from_bytes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_from_bytes",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1057usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_name (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_name".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_name",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1058usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_name_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_name_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_name_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1059usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_node_count (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_node_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_node_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1060usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_node_count_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_node_count_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_node_count_batch",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1061usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_precision (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_precision".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_precision",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1062usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_precision_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_precision_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_precision_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1063usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_srid",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1064usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_srid_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_srid_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_srid_batch",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1065usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topology_to_bytes (sqlink-shim-codegen)".into()),
            tags: vec!["st_topology_to_bytes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topology_to_bytes",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1066usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topologyedgecount (sqlink-shim-codegen)".into()),
            tags: vec!["st_topologyedgecount".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topologyedgecount",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1067usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topologyfacecount (sqlink-shim-codegen)".into()),
            tags: vec!["st_topologyfacecount".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topologyfacecount",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1068usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topologyfrombytes (sqlink-shim-codegen)".into()),
            tags: vec!["st_topologyfrombytes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topologyfrombytes",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1069usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topologyname (sqlink-shim-codegen)".into()),
            tags: vec!["st_topologyname".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topologyname",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1070usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topologynodecount (sqlink-shim-codegen)".into()),
            tags: vec!["st_topologynodecount".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topologynodecount",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1071usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topologyprecision (sqlink-shim-codegen)".into()),
            tags: vec!["st_topologyprecision".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topologyprecision",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1072usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topologysrid (sqlink-shim-codegen)".into()),
            tags: vec!["st_topologysrid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topologysrid",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1073usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_topologytobytes (sqlink-shim-codegen)".into()),
            tags: vec!["st_topologytobytes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_topologytobytes",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1074usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_touches (sqlink-shim-codegen)".into()),
            tags: vec!["st_touches".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_touches",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1075usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_touches_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_touches_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_touches_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1076usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_tpi (sqlink-shim-codegen)".into()),
            tags: vec!["st_tpi".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_tpi",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1077usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_transform (sqlink-shim-codegen)".into()),
            tags: vec!["st_transform".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_transform",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1078usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_transform_pipeline (sqlink-shim-codegen)".into()),
            tags: vec!["st_transform_pipeline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_transform_pipeline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1079usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_transform_pipeline_to_srid (sqlink-shim-codegen)".into()),
            tags: vec!["st_transform_pipeline_to_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_transform_pipeline_to_srid",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1080usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_transformpipeline (sqlink-shim-codegen)".into()),
            tags: vec!["st_transformpipeline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_transformpipeline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1081usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_translate (sqlink-shim-codegen)".into()),
            tags: vec!["st_translate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_translate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1082usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_translate_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_translate_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_translate_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1083usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_translatethreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_translatethreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_translatethreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1084usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg4".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_transscale (sqlink-shim-codegen)".into()),
            tags: vec!["st_transscale".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_transscale",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1085usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_tri (sqlink-shim-codegen)".into()),
            tags: vec!["st_tri".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_tri",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1086usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_triangle_from_text (sqlink-shim-codegen)".into()),
            tags: vec!["st_triangle_from_text".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_triangle_from_text",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1087usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_triangle_from_wkb (sqlink-shim-codegen)".into()),
            tags: vec!["st_triangle_from_wkb".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_triangle_from_wkb",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1088usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_triangulate_polygon (sqlink-shim-codegen)".into()),
            tags: vec!["st_triangulate_polygon".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_triangulate_polygon",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1089usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_triangulate_twod (sqlink-shim-codegen)".into()),
            tags: vec!["st_triangulate_twod".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_triangulate_twod",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1090usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_triangulatepolygon (sqlink-shim-codegen)".into()),
            tags: vec!["st_triangulatepolygon".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_triangulatepolygon",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1091usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_unary_union (sqlink-shim-codegen)".into()),
            tags: vec!["st_unary_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_unary_union",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1092usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_unaryunion (sqlink-shim-codegen)".into()),
            tags: vec!["st_unaryunion".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_unaryunion",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1093usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_union (sqlink-shim-codegen)".into()),
            tags: vec!["st_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_union",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1094usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_union_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_union_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_union_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1095usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_union_gridsize (sqlink-shim-codegen)".into()),
            tags: vec!["st_union_gridsize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_union_gridsize",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1096usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_union_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_union_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_union_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1097usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_unionthreed (sqlink-shim-codegen)".into()),
            tags: vec!["st_unionthreed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_unionthreed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1098usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_upper_left_x (sqlink-shim-codegen)".into()),
            tags: vec!["st_upper_left_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_upper_left_x",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1099usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_upper_left_y (sqlink-shim-codegen)".into()),
            tags: vec!["st_upper_left_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_upper_left_y",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1100usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_upperleftx (sqlink-shim-codegen)".into()),
            tags: vec!["st_upperleftx".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_upperleftx",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1101usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_upperlefty (sqlink-shim-codegen)".into()),
            tags: vec!["st_upperlefty".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_upperlefty",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1102usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_validate_topology (sqlink-shim-codegen)".into()),
            tags: vec!["st_validate_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_validate_topology",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1103usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_validatetopology (sqlink-shim-codegen)".into()),
            tags: vec!["st_validatetopology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_validatetopology",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1104usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_value (sqlink-shim-codegen)".into()),
            tags: vec!["st_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_value",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1105usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_value_count (sqlink-shim-codegen)".into()),
            tags: vec!["st_value_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_value_count",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1106usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_value_count_of_value (sqlink-shim-codegen)".into()),
            tags: vec!["st_value_count_of_value".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_value_count_of_value",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1107usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_value_counts (sqlink-shim-codegen)".into()),
            tags: vec!["st_value_counts".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_value_counts",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1108usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_value_percent (sqlink-shim-codegen)".into()),
            tags: vec!["st_value_percent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_value_percent",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1109usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_value_percents (sqlink-shim-codegen)".into()),
            tags: vec!["st_value_percents".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_value_percents",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1110usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_valuecount (sqlink-shim-codegen)".into()),
            tags: vec!["st_valuecount".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_valuecount",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1111usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_visibility_point (sqlink-shim-codegen)".into()),
            tags: vec!["st_visibility_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_visibility_point",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1112usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_visibility_segment (sqlink-shim-codegen)".into()),
            tags: vec!["st_visibility_segment".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_visibility_segment",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1113usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_volume (sqlink-shim-codegen)".into()),
            tags: vec!["st_volume".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_volume",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1114usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_voronoi_lines (sqlink-shim-codegen)".into()),
            tags: vec!["st_voronoi_lines".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_voronoi_lines",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1115usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_voronoi_polygons (sqlink-shim-codegen)".into()),
            tags: vec!["st_voronoi_polygons".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_voronoi_polygons",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1116usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_voronoilines (sqlink-shim-codegen)".into()),
            tags: vec!["st_voronoilines".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_voronoilines",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1117usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_voronoipolygons (sqlink-shim-codegen)".into()),
            tags: vec!["st_voronoipolygons".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_voronoipolygons",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1118usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_width (sqlink-shim-codegen)".into()),
            tags: vec!["st_width".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_width",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1119usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_within (sqlink-shim-codegen)".into()),
            tags: vec!["st_within".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_within",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1120usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_within_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_within_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_within_batch",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1121usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_wkb_to_sql (sqlink-shim-codegen)".into()),
            tags: vec!["st_wkb_to_sql".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_wkb_to_sql",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1122usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_wkbtosql (sqlink-shim-codegen)".into()),
            tags: vec!["st_wkbtosql".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_wkbtosql",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1123usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_wkt_to_sql (sqlink-shim-codegen)".into()),
            tags: vec!["st_wkt_to_sql".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_wkt_to_sql",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1124usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_wkttosql (sqlink-shim-codegen)".into()),
            tags: vec!["st_wkttosql".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_wkttosql",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1125usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_world_to_raster_coord (sqlink-shim-codegen)".into()),
            tags: vec!["st_world_to_raster_coord".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_world_to_raster_coord",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1126usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_world_to_raster_coord_col (sqlink-shim-codegen)".into()),
            tags: vec!["st_world_to_raster_coord_col".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_world_to_raster_coord_col",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1127usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_world_to_raster_coord_row (sqlink-shim-codegen)".into()),
            tags: vec!["st_world_to_raster_coord_row".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_world_to_raster_coord_row",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1128usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_worldtorastercoord (sqlink-shim-codegen)".into()),
            tags: vec!["st_worldtorastercoord".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_worldtorastercoord",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1129usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_worldtorastercoordcol (sqlink-shim-codegen)".into()),
            tags: vec!["st_worldtorastercoordcol".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_worldtorastercoordcol",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1130usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_worldtorastercoordrow (sqlink-shim-codegen)".into()),
            tags: vec!["st_worldtorastercoordrow".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_worldtorastercoordrow",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1131usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_wrap_x (sqlink-shim-codegen)".into()),
            tags: vec!["st_wrap_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_wrap_x",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1132usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_wrapx (sqlink-shim-codegen)".into()),
            tags: vec!["st_wrapx".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_wrapx",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1133usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_x (sqlink-shim-codegen)".into()),
            tags: vec!["st_x".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_x",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1134usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_x_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_x_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_x_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1135usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_xmax (sqlink-shim-codegen)".into()),
            tags: vec!["st_xmax".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_xmax",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1136usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_xmin (sqlink-shim-codegen)".into()),
            tags: vec!["st_xmin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_xmin",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1137usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_y (sqlink-shim-codegen)".into()),
            tags: vec!["st_y".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_y",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1138usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_y_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_y_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_y_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1139usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_y_monotone_partition (sqlink-shim-codegen)".into()),
            tags: vec!["st_y_monotone_partition".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_y_monotone_partition",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1140usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_ymax (sqlink-shim-codegen)".into()),
            tags: vec!["st_ymax".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_ymax",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1141usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_ymin (sqlink-shim-codegen)".into()),
            tags: vec!["st_ymin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_ymin",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1142usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_z (sqlink-shim-codegen)".into()),
            tags: vec!["st_z".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_z",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1143usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_z_batch (sqlink-shim-codegen)".into()),
            tags: vec!["st_z_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_z_batch",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1144usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_zmax (sqlink-shim-codegen)".into()),
            tags: vec!["st_zmax".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_zmax",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1145usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_zmflag (sqlink-shim-codegen)".into()),
            tags: vec!["st_zmflag".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_zmflag",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1146usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_zmin (sqlink-shim-codegen)".into()),
            tags: vec!["st_zmin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_zmin",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1147usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("state_extract (sqlink-shim-codegen)".into()),
            tags: vec!["state_extract".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "state_extract",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1148usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("to_topogeom (sqlink-shim-codegen)".into()),
            tags: vec!["to_topogeom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "to_topogeom",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1149usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Complex("VARCHAR".into()),
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topo_element_array_append (sqlink-shim-codegen)".into()),
            tags: vec!["topo_element_array_append".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topo_element_array_append",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1150usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Text,
        }];
        let opts = runtime::Funcopts {
            description: Some("topo_pgtp_all_batch (sqlink-shim-codegen)".into()),
            tags: vec!["topo_pgtp_all_batch".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topo_pgtp_all_batch",
            &args,
            &types::Logicaltype::Boolean,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1151usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topogeo_add_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["topogeo_add_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topogeo_add_geometry",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1152usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topogeo_add_linestring (sqlink-shim-codegen)".into()),
            tags: vec!["topogeo_add_linestring".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topogeo_add_linestring",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1153usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topogeo_add_point (sqlink-shim-codegen)".into()),
            tags: vec!["topogeo_add_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topogeo_add_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1154usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topogeo_add_polygon (sqlink-shim-codegen)".into()),
            tags: vec!["topogeo_add_polygon".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topogeo_add_polygon",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1155usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topogeo_load_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["topogeo_load_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topogeo_load_geometry",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1156usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_edge (sqlink-shim-codegen)".into()),
            tags: vec!["topology_add_edge".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_edge",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1157usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_edge_mod_face (sqlink-shim-codegen)".into()),
            tags: vec!["topology_add_edge_mod_face".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_edge_mod_face",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1158usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_edge_new_faces (sqlink-shim-codegen)".into()),
            tags: vec!["topology_add_edge_new_faces".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_edge_new_faces",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1159usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_face (sqlink-shim-codegen)".into()),
            tags: vec!["topology_add_face".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_face",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1160usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_iso_edge (sqlink-shim-codegen)".into()),
            tags: vec!["topology_add_iso_edge".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_iso_edge",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1161usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_iso_node (sqlink-shim-codegen)".into()),
            tags: vec!["topology_add_iso_node".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_iso_node",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1162usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_node (sqlink-shim-codegen)".into()),
            tags: vec!["topology_add_node".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_node",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1163usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg3".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_add_topogeom_column (sqlink-shim-codegen)".into()),
            tags: vec!["topology_add_topogeom_column".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_add_topogeom_column",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1164usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_as_gml (sqlink-shim-codegen)".into()),
            tags: vec!["topology_as_gml".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_as_gml",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1165usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_as_topojson (sqlink-shim-codegen)".into()),
            tags: vec!["topology_as_topojson".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_as_topojson",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1166usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Boolean,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_as_topojson_with_edge_map (sqlink-shim-codegen)".into()),
            tags: vec!["topology_as_topojson_with_edge_map".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_as_topojson_with_edge_map",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1167usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_change_edge_geom (sqlink-shim-codegen)".into()),
            tags: vec!["topology_change_edge_geom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_change_edge_geom",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1168usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_copy_topology (sqlink-shim-codegen)".into()),
            tags: vec!["topology_copy_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_copy_topology",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1169usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_create (sqlink-shim-codegen)".into()),
            tags: vec!["topology_create".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_create",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1170usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_create_topo_geom (sqlink-shim-codegen)".into()),
            tags: vec!["topology_create_topo_geom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_create_topo_geom",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1171usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_drop_topogeom_column (sqlink-shim-codegen)".into()),
            tags: vec!["topology_drop_topogeom_column".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_drop_topogeom_column",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1172usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_drop_topology (sqlink-shim-codegen)".into()),
            tags: vec!["topology_drop_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_drop_topology",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1173usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_edge_count (sqlink-shim-codegen)".into()),
            tags: vec!["topology_edge_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_edge_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1174usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_face_count (sqlink-shim-codegen)".into()),
            tags: vec!["topology_face_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_face_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1175usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_from_bytes (sqlink-shim-codegen)".into()),
            tags: vec!["topology_from_bytes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_from_bytes",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1176usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_edge_by_point (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_edge_by_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_edge_by_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1177usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_edge_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_edge_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_edge_geometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1178usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_face_by_point (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_face_by_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_face_by_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1179usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_face_containing_point (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_face_containing_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_face_containing_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1180usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_face_edges (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_face_edges".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_face_edges",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1181usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_face_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_face_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_face_geometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1182usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_node_by_point (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_node_by_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_node_by_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1183usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_node_edges (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_node_edges".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_node_edges",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1184usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_node_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_node_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_node_geometry",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1185usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_get_ring_edges (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_ring_edges".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_ring_edges",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1186usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_get_topology_id (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_topology_id".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_topology_id",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1187usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_get_topology_name (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_topology_name".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_topology_name",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1188usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_get_topology_srid (sqlink-shim-codegen)".into()),
            tags: vec!["topology_get_topology_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_get_topology_srid",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1189usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_make_topology_precise (sqlink-shim-codegen)".into()),
            tags: vec!["topology_make_topology_precise".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_make_topology_precise",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1190usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_mod_edge_heal (sqlink-shim-codegen)".into()),
            tags: vec!["topology_mod_edge_heal".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_mod_edge_heal",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1191usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_mod_edge_split (sqlink-shim-codegen)".into()),
            tags: vec!["topology_mod_edge_split".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_mod_edge_split",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1192usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_move_iso_node (sqlink-shim-codegen)".into()),
            tags: vec!["topology_move_iso_node".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_move_iso_node",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1193usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_name (sqlink-shim-codegen)".into()),
            tags: vec!["topology_name".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_name",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1194usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_new_edge_heal (sqlink-shim-codegen)".into()),
            tags: vec!["topology_new_edge_heal".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_new_edge_heal",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1195usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_new_edges_split (sqlink-shim-codegen)".into()),
            tags: vec!["topology_new_edges_split".into()],
            attributes: types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_new_edges_split",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1196usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_node_count (sqlink-shim-codegen)".into()),
            tags: vec!["topology_node_count".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_node_count",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1197usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_polygonize (sqlink-shim-codegen)".into()),
            tags: vec!["topology_polygonize".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_polygonize",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1198usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_precision (sqlink-shim-codegen)".into()),
            tags: vec!["topology_precision".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_precision",
            &args,
            &types::Logicaltype::Float64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1199usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_rem_edge_mod_face (sqlink-shim-codegen)".into()),
            tags: vec!["topology_rem_edge_mod_face".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_rem_edge_mod_face",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1200usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_rem_edge_new_face (sqlink-shim-codegen)".into()),
            tags: vec!["topology_rem_edge_new_face".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_rem_edge_new_face",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1201usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_remove_face (sqlink-shim-codegen)".into()),
            tags: vec!["topology_remove_face".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_remove_face",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1202usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_remove_iso_edge (sqlink-shim-codegen)".into()),
            tags: vec!["topology_remove_iso_edge".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_remove_iso_edge",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1203usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_remove_iso_node (sqlink-shim-codegen)".into()),
            tags: vec!["topology_remove_iso_node".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_remove_iso_node",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1204usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_remove_unused_primitives (sqlink-shim-codegen)".into()),
            tags: vec!["topology_remove_unused_primitives".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_remove_unused_primitives",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1205usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Text,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_rename_topology (sqlink-shim-codegen)".into()),
            tags: vec!["topology_rename_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_rename_topology",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1206usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_srid (sqlink-shim-codegen)".into()),
            tags: vec!["topology_srid".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_srid",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1207usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_summary (sqlink-shim-codegen)".into()),
            tags: vec!["topology_summary".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_summary",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1208usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_to_bytes (sqlink-shim-codegen)".into()),
            tags: vec!["topology_to_bytes".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_to_bytes",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1209usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_to_topogeom (sqlink-shim-codegen)".into()),
            tags: vec!["topology_to_topogeom".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_to_topogeom",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1210usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_topogeo_add_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["topology_topogeo_add_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_topogeo_add_geometry",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1211usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_topogeo_add_linestring (sqlink-shim-codegen)".into()),
            tags: vec!["topology_topogeo_add_linestring".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_topogeo_add_linestring",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1212usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_topogeo_add_point (sqlink-shim-codegen)".into()),
            tags: vec!["topology_topogeo_add_point".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_topogeo_add_point",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1213usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_topogeo_add_polygon (sqlink-shim-codegen)".into()),
            tags: vec!["topology_topogeo_add_polygon".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_topogeo_add_polygon",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1214usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_topogeo_load_geometry (sqlink-shim-codegen)".into()),
            tags: vec!["topology_topogeo_load_geometry".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_topogeo_load_geometry",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1215usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_total_topology_size (sqlink-shim-codegen)".into()),
            tags: vec!["topology_total_topology_size".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_total_topology_size",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1216usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_upgrade_topology (sqlink-shim-codegen)".into()),
            tags: vec!["topology_upgrade_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_upgrade_topology",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1217usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_validate (sqlink-shim-codegen)".into()),
            tags: vec!["topology_validate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_validate",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1218usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("topology_validate_topology (sqlink-shim-codegen)".into()),
            tags: vec!["topology_validate_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_validate_topology",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1219usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("topology_validate_topology_precision (sqlink-shim-codegen)".into()),
            tags: vec!["topology_validate_topology_precision".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "topology_validate_topology_precision",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1220usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("total_topology_size (sqlink-shim-codegen)".into()),
            tags: vec!["total_topology_size".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "total_topology_size",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1221usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("upgrade_topology (sqlink-shim-codegen)".into()),
            tags: vec!["upgrade_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "upgrade_topology",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1222usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("validate_topology (sqlink-shim-codegen)".into()),
            tags: vec!["validate_topology".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "validate_topology",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 1223usize);
        let callback = runtime::ScalarCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("validate_topology_precision (sqlink-shim-codegen)".into()),
            tags: vec!["validate_topology_precision".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "validate_topology_precision",
            &args,
            &types::Logicaltype::Text,
            callback,
            Some(&opts),
        )?;
    }
    Ok(())
}

fn register_aggregates() -> Result<(), types::Duckerror> {
    let capability =
        runtime::get_capability(types::Capabilitykind::Aggregate).ok_or_else(|| {
            types::Duckerror::Internal("host did not expose aggregate capability".into())
        })?;
    let registry = match capability {
        runtime::Capability::Aggregate(r) => r,
        _ => {
            return Err(types::Duckerror::Internal(
                "aggregate capability returned unexpected variant".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_3d_extent (sqlink-shim-codegen)".into()),
            tags: vec!["st_3d_extent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3d_extent",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_3dextent (sqlink-shim-codegen)".into()),
            tags: vec!["st_3dextent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_3dextent",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_approx_count_agg (sqlink-shim-codegen)".into()),
            tags: vec!["st_approx_count_agg".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_approx_count_agg",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_clusterintersecting (sqlink-shim-codegen)".into()),
            tags: vec!["st_clusterintersecting".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterintersecting",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 4usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_clusterintersectingaggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_clusterintersectingaggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterintersectingaggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 5usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_clusterwithin (sqlink-shim-codegen)".into()),
            tags: vec!["st_clusterwithin".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterwithin",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 6usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_clusterwithinaggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_clusterwithinaggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterwithinaggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 7usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_collect_aggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_collect_aggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_collect_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_count_agg (sqlink-shim-codegen)".into()),
            tags: vec!["st_count_agg".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_count_agg",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 9usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_coverage_union_aggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_coverage_union_aggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_coverage_union_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 10usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_envelope_aggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_envelope_aggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_envelope_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 11usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_extent (sqlink-shim-codegen)".into()),
            tags: vec!["st_extent".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_extent",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 12usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_extent_threed (sqlink-shim-codegen)".into()),
            tags: vec!["st_extent_threed".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_extent_threed",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 13usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_make_line_aggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_make_line_aggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_make_line_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 14usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_makeline (sqlink-shim-codegen)".into()),
            tags: vec!["st_makeline".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makeline",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 15usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_makelineagg (sqlink-shim-codegen)".into()),
            tags: vec!["st_makelineagg".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makelineagg",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 16usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_makelineaggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_makelineaggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_makelineaggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 17usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_mem_union_aggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_mem_union_aggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_mem_union_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 18usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_memunion (sqlink-shim-codegen)".into()),
            tags: vec!["st_memunion".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_memunion",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 19usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polygonize_aggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygonize_aggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonize_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 20usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polygonizeagg (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygonizeagg".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonizeagg",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 21usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_polygonizeaggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_polygonizeaggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_polygonizeaggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 22usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rast_union (sqlink-shim-codegen)".into()),
            tags: vec!["st_rast_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_union",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 23usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_rast_union_aggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_rast_union_aggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_rast_union_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 24usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_raster_union (sqlink-shim-codegen)".into()),
            tags: vec!["st_raster_union".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_raster_union",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 25usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_summary_stats_agg (sqlink-shim-codegen)".into()),
            tags: vec!["st_summary_stats_agg".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_summary_stats_agg",
            &args,
            &types::Logicaltype::Complex("VARCHAR".into()),
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 26usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_union_aggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_union_aggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_union_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 27usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_unionagg (sqlink-shim-codegen)".into()),
            tags: vec!["st_unionagg".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_unionagg",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        aggregate_handle_table()
            .lock()
            .expect("aggregate handle mutex poisoned")
            .insert(handle, 28usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_unionaggregate (sqlink-shim-codegen)".into()),
            tags: vec!["st_unionaggregate".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_unionaggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    Ok(())
}

fn register_tables() -> Result<(), types::Duckerror> {
    let capability = runtime::get_capability(types::Capabilitykind::Table)
        .ok_or_else(|| types::Duckerror::Internal("host did not expose table capability".into()))?;
    let registry = match capability {
        runtime::Capability::Table(r) => r,
        _ => {
            return Err(types::Duckerror::Internal(
                "table capability returned unexpected variant".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "geom".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_dump", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "point".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_dump_points", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "geom".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_dump_rings", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "geom".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_dump_segments", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 4usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "point".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_dumppoints", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 5usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "geom".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_dumprings", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 6usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "geom".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_dumpsegments", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 7usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "geom".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_hexagon_grid", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "geom".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_hexagongrid", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 9usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "geom".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_square_grid", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 10usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Blob,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "geom".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_squaregrid", &args, &columns, callback, None)?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        table_handle_table()
            .lock()
            .expect("table handle mutex poisoned")
            .insert(handle, 11usize);
        let callback = runtime::TableCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let columns: Vec<runtime::Columndef> = vec![runtime::Columndef {
            name: "geom".into(),
            logical: types::Logicaltype::Blob,
        }];
        registry.register("st_subdivide", &args, &columns, callback, None)?;
    }
    Ok(())
}

fn register_casts() -> Result<(), types::Duckerror> {
    {
        // CAST(<castsourcekind::any> AS BYTEA) -> st_asbinary
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 196usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "BYTEA".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(st_asbinary: BLOB -> BYTEA): {}", e))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS EWKT) -> st_asewkt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 199usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "EWKT".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(st_asewkt: BLOB -> EWKT): {}", e))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS GEOGRAPHY) -> st_geogfromwkb
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 470usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "GEOGRAPHY".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(st_geogfromwkb: BLOB -> GEOGRAPHY): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS GEOGRAPHY) -> st_geogfromtext
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 469usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "GEOGRAPHY".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(st_geogfromtext: VARCHAR -> GEOGRAPHY): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS GEOMETRY) -> st_geomfromwkb
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 529usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "GEOMETRY".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(st_geomfromwkb: BLOB -> GEOMETRY): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS GEOMETRY) -> st_convexhull
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 286usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "GEOMETRY".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(st_convexhull: BLOB -> GEOMETRY): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS GEOMETRY) -> st_makepoint
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 688usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "GEOMETRY".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(st_makepoint: BLOB -> GEOMETRY): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS GEOMETRY) -> st_makepolygon
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 692usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "GEOMETRY".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(st_makepolygon: BLOB -> GEOMETRY): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::stringliteral> AS GEOMETRY) -> st_geomfromtext
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 526usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "VARCHAR".into(),
            to: "GEOMETRY".into(),
            kind: catalog::CastKind::Implicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!(
                "register-cast(st_geomfromtext: VARCHAR -> GEOMETRY): {}",
                e
            ))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS GML) -> st_asgml
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 201usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "GML".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(st_asgml: BLOB -> GML): {}", e))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS JSON) -> st_asgeojson
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 200usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "JSON".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(st_asgeojson: BLOB -> JSON): {}", e))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS JSONB) -> st_asgeojson
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 200usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "JSONB".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(st_asgeojson: BLOB -> JSONB): {}", e))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS KML) -> st_askml
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 203usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "KML".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(st_askml: BLOB -> KML): {}", e))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS MVT) -> st_asmvt
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 206usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "MVT".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(st_asmvt: BLOB -> MVT): {}", e))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS SVG) -> st_assvg
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 210usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "SVG".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(st_assvg: BLOB -> SVG): {}", e))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS TEXT) -> st_astext
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 211usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "TEXT".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(st_astext: BLOB -> TEXT): {}", e))
        })?;
    }
    {
        // CAST(<castsourcekind::any> AS TEXT) -> st_asgeojson
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        // Slot the cast handle into the SCALAR handle table so
        // call_cast can forward into call_scalar at the matching
        // arm. One arm-index space; the cast contract just
        // provides an alternate entry point.
        handle_table()
            .lock()
            .expect("scalar handle mutex poisoned")
            .insert(handle, 200usize);
        let callback = runtime::CastCallback::new(handle);
        let spec = catalog::CastSpec {
            from: "BLOB".into(),
            to: "TEXT".into(),
            kind: catalog::CastKind::Explicit,
        };
        catalog::register_cast(&spec, callback).map_err(|e| {
            types::Duckerror::Internal(format!("register-cast(st_asgeojson: BLOB -> TEXT): {}", e))
        })?;
    }
    Ok(())
}

fn register_windows() -> Result<(), types::Duckerror> {
    let capability =
        runtime::get_capability(types::Capabilitykind::Aggregate).ok_or_else(|| {
            types::Duckerror::Internal(
                "host did not expose aggregate capability (window registration)".into(),
            )
        })?;
    let registry = match capability {
        runtime::Capability::Aggregate(r) => r,
        _ => {
            return Err(types::Duckerror::Internal(
                "aggregate capability returned unexpected variant (window registration)".into(),
            ));
        }
    };
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 0usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_cluster_dbscan (sqlink-shim-codegen window)".into()),
            tags: vec!["st_cluster_dbscan".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cluster_dbscan",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 1usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_cluster_dbscan_aggregate (sqlink-shim-codegen window)".into()),
            tags: vec!["st_cluster_dbscan_aggregate".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cluster_dbscan_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 2usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_cluster_intersecting (sqlink-shim-codegen window)".into()),
            tags: vec!["st_cluster_intersecting".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cluster_intersecting",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 3usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some(
                "st_cluster_intersecting_aggregate (sqlink-shim-codegen window)".into(),
            ),
            tags: vec!["st_cluster_intersecting_aggregate".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cluster_intersecting_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 4usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_cluster_intersecting_win (sqlink-shim-codegen window)".into()),
            tags: vec!["st_cluster_intersecting_win".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cluster_intersecting_win",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 5usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_cluster_kmeans (sqlink-shim-codegen window)".into()),
            tags: vec!["st_cluster_kmeans".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cluster_kmeans",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 6usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_cluster_kmeans_aggregate (sqlink-shim-codegen window)".into()),
            tags: vec!["st_cluster_kmeans_aggregate".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cluster_kmeans_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 7usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_cluster_kmeans_max_radius (sqlink-shim-codegen window)".into()),
            tags: vec!["st_cluster_kmeans_max_radius".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cluster_kmeans_max_radius",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 8usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_cluster_within (sqlink-shim-codegen window)".into()),
            tags: vec!["st_cluster_within".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cluster_within",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 9usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_cluster_within_aggregate (sqlink-shim-codegen window)".into()),
            tags: vec!["st_cluster_within_aggregate".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cluster_within_aggregate",
            &args,
            &types::Logicaltype::Blob,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 10usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_cluster_within_win (sqlink-shim-codegen window)".into()),
            tags: vec!["st_cluster_within_win".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_cluster_within_win",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 11usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
            runtime::Funcarg {
                name: Some("arg2".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_clusterdbscan (sqlink-shim-codegen window)".into()),
            tags: vec!["st_clusterdbscan".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterdbscan",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 12usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_clusterintersecting_win (sqlink-shim-codegen window)".into()),
            tags: vec!["st_clusterintersecting_win".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterintersecting_win",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 13usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![runtime::Funcarg {
            name: Some("arg0".into()),
            logical: types::Logicaltype::Blob,
        }];
        let opts = runtime::Funcopts {
            description: Some("st_clusterintersectingwin (sqlink-shim-codegen window)".into()),
            tags: vec!["st_clusterintersectingwin".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterintersectingwin",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 14usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Int64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_clusterkmeans (sqlink-shim-codegen window)".into()),
            tags: vec!["st_clusterkmeans".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterkmeans",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 15usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_clusterwithin_win (sqlink-shim-codegen window)".into()),
            tags: vec!["st_clusterwithin_win".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterwithin_win",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    {
        let handle = NEXT_HANDLE.fetch_add(1, std::sync::atomic::Ordering::Relaxed);
        window_handle_table()
            .lock()
            .expect("window handle mutex poisoned")
            .insert(handle, 16usize);
        let callback = runtime::AggregateCallback::new(handle);
        let args: Vec<runtime::Funcarg> = vec![
            runtime::Funcarg {
                name: Some("arg0".into()),
                logical: types::Logicaltype::Blob,
            },
            runtime::Funcarg {
                name: Some("arg1".into()),
                logical: types::Logicaltype::Float64,
            },
        ];
        let opts = runtime::Funcopts {
            description: Some("st_clusterwithinwin (sqlink-shim-codegen window)".into()),
            tags: vec!["st_clusterwithinwin".into(), "window".into()],
            attributes: types::Funcflags::DETERMINISTIC | types::Funcflags::STATELESS,
        };
        registry.register(
            "st_clusterwithinwin",
            &args,
            &types::Logicaltype::Int64,
            callback,
            Some(&opts),
        )?;
    }
    Ok(())
}

bindings::export!(PostgisBridge with_types_in bindings);
